> **Nota de proceso (hook de graphify)**: sin herramienta `Bash` disponible en esta fase, no se pudo correr `graphify query`/`explain`/`path` pese al hook que lo exige — misma limitación ya documentada por `exploration.md`, `proposal.md`, los 4 `specs/` y `design.md` de este mismo change. Se compensó con lectura directa y completa de `AGENTS.md`, `exploration.md`, `proposal.md` (ADR 57-61), los 4 specs (`propuesta-cambio-hitl`, `escritura-aislada-worktree`, delta `delegacion-subagentes`, delta `revision-pr-por-roles`), `design.md` completo (ADR 62-70, §0-15) y `openspec/changes/hito-2.0-delegacion-subagentes/tasks.md` como molde de formato. Ninguna tarea de este documento reinterpreta el diseño: cada una desglosa un archivo o contrato ya fijado por `design.md`.

# Hito 5.1 — Escritura delegada del Developer vía worktree aislado (v2.1.0): desglose de tareas

## ⛔ PRECONDICIÓN DE APLICACIÓN — GATE DURO, NO IGNORAR

**`sdd-apply` de este documento NO ARRANCA hoy.** `design.md` §0 fija tres condiciones verificables, las tres deben cumplirse juntas:

| # | Condición | Estado verificado en esta sesión |
|---|---|---|
| 1 | El merge de `hito-2.0-delegacion-subagentes` (v2.0.0) está en `main` | **No cumple** — la rama activa del repo es `hito/v2.0-delegacion-subagentes`, con las tareas 4-8 de `hito-2.0-delegacion-subagentes/tasks.md` commiteadas (`SUBAGENT_REGISTRY`, `resolverDestino`, `despacharDelegacion`/`despacharCadena`), pero **sin mergear a `main`** — `hito-2.0` sigue en desarrollo, todavía le faltan las tareas 9-25 y la aprobación del Reviewer |
| 2 | `src/core/agents/subagents.ts` existe en `main` como código real, con `SUBAGENT_REGISTRY` en `definitions.ts` | **No cumple en `main`** hasta que se complete #1 |
| 3 | Las migraciones `0007`/`0008` de `hito-2.0` aterrizaron en `migrations/index.ts` | **No cumple en `main`** hasta que se complete #1 |

**Consecuencia**: este `tasks.md` es válido y persistible ya — planificar no toca código — pero **la primera acción de cualquier ejecución de `sdd-apply` sobre este documento debe ser verificar las tres condiciones contra `main` y reportar `blocked` si alguna falla**, antes de tocar un archivo. Ningún Implementer debe reinventar `InvocarSubagente`/`SUBAGENT_REGISTRY`/`toQueryOptions` por rol dentro de este change — es infraestructura de `hito-2.0`, no de `v2.1.0`.

**Regla de citación mientras el gate no se cumpla**: todo objeto de `hito-2.0` citado abajo (`SUBAGENT_REGISTRY`, `InvocarSubagente`, `despacharCadena`, `despacharRevisionPorRoles`, `construirTareaDelegada`, `TAREA_DELEGADA_MAX_CHARS`, migraciones `0007`/`0008`) es referencia a la **especificación** de `hito-2.0`, no a código confirmado en `main`. `sdd-apply` revalida cada uno como su primer paso.

**Spec y diseño de origen** (no se duplican acá): [`proposal.md`](proposal.md) (ADR 57-61), [`specs/propuesta-cambio-hitl/spec.md`](specs/propuesta-cambio-hitl/spec.md), [`specs/escritura-aislada-worktree/spec.md`](specs/escritura-aislada-worktree/spec.md), [`specs/delegacion-subagentes/spec.md`](specs/delegacion-subagentes/spec.md) (delta MODIFIED), [`specs/revision-pr-por-roles/spec.md`](specs/revision-pr-por-roles/spec.md) (delta ADDED), [`design.md`](design.md) (ADR 62-70, §0-15, corte de PRs en §12).

**Entregable funcional a demostrar**: los 15 pasos de `design.md` §11 — un webhook de PR dispara Planner→Developer→Reviewer, el Developer escribe en `.harness/worktrees/<caso>-<uuid>/`, el diff (incluidos archivos nuevos) queda persistido en `propuestas_cambio` con `estado = pendiente_aprobacion_humana`, el Reviewer juzga esos bytes, `git status` del checkout real queda limpio durante todo el ciclo, y un humano autenticado en la TUI ve/aplica/descarta la propuesta con confirmación en dos pasos y auditoría, terminando en `git apply` sin `commit`/`push`/PR. `HARNESS_ESCRITURA_DELEGADA=off` reproduce `v2.0.0` exacto.

**Metodología**: TDD estricto (`strict_tdd: true`). Excepciones explícitas: tarea 5 (`vitest.config.ts`, scaffolding), tarea 6 (`.gitignore`, scaffolding), tarea 13 (migración `NNNN`, SQL declarativo, se escribe junto a su test en `repository.test.ts` que sí va primero), tarea 16 (constantes de comando/resultado sin lógica nueva, molde de la tarea 21 de Hito 5), tarea 34 (`main.ts`, wiring puro de composition root, molde de `webhook.close()` ya existente), tarea 35 (README, documentación), tarea 36 (doc-comments, sin cambio de lógica), tarea 37 (verificación manual, sin código de producción).

**Fixture nueva de este change**: `GitExecFileFn`/`TestExecFileFn` fake (unit, mismo molde que `graphify-cli.test.ts`) y, por primera vez en el repo, **integración contra `git`/`vitest` real** en `src/test/integration/` (tareas 10, 11, 25) — categoría marcada explícitamente en cada tarea que la usa.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~2.780 (1.440 producción + 1.340 test — recalibración de `design.md` §12, TDD estricto ≈ 1:1) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR1 → PR2 → PR3 → PR4 → PR5 → PR6 (ver `design.md` §12) |
| Delivery strategy | ask-on-risk (default; no se recibió override explícito para esta corrida) |
| Chain strategy | **feature-branch-chain** (confirmado por el checkpoint humano) |

Decision needed before apply: No — chain strategy resuelta; quedan abiertas ADR 65, ADR 69 y `HARNESS_WORKTREE_ROOT` (ver nota abajo)
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

**Decisión de `chain_strategy` (confirmada)**: `feature-branch-chain`, mismo criterio ya usado en `hito-2.0-delegacion-subagentes`. Cada PR de la cadena apunta al anterior; solo el tracker (`hito/v2.1-escritura-delegada`) mergea a `main` al cierre. Los PR 1 y 2 son aditivos puros (nadie los importa hasta el 5/6) y quedan mergeados aunque se reviertan los siguientes.

**Quedan abiertas, sin resolver todavía**: `design.md` §15 tenía cuatro preguntas para el checkpoint — la de `chain_strategy` (pregunta 2) queda resuelta arriba; ADR 65, ADR 69 y `HARNESS_WORKTREE_ROOT` siguen pendientes de decisión antes de `sdd-apply`.

### Suggested Work Units

| Unit | Goal | Tasks | Est. lines | Base branch (si `feature-branch-chain`) |
|------|------|-------|-----------|-------------|
| 1 | Contrato del worktree + runner `git` + argv nombrados (aditivo puro) | 1–6 | ~430 | `hito/v2.1-escritura-delegada` |
| 2 | Ciclo de vida real del worktree + barrido + integración contra `git` | 7–12 | ~480 | Unit 1 |
| 3 | Persistencia de `propuestas_cambio` (migración + repository + constantes) | 13–16 | ~380 | Unit 2 |
| 4 | Núcleo puro de propuestas (contract, resumen, crear, resolver) | 17–20 | ~480 | Unit 3 |
| 5 | Tool MCP de tests + Developer con escritura (wiring hasta el SDK) | 21–28 | ~480 | Unit 4 |
| 6 | Comandos TUI + cadena de revisión + composition root — ENTREGABLE COMPLETO | 29–34 | ~530 | Unit 5 |
| 7 | Documentación + verificación manual | 35–37 | ~30 (evidencia, no código) | Unit 6 |

Con `feature-branch-chain`: PR1 apunta al tracker `hito/v2.1-escritura-delegada`, cada hijo al inmediato anterior. Con `stacked-to-main`: cada PR mergea directo a `main` en orden. Rollback en orden inverso 6→1 (§12).

## Tareas

### PR1 — Contrato del worktree + runner `git` + argv nombrados (tareas 1-6)

1. [x] **`src/core/agents/worktree-contract.ts`** (nuevo, §5.1) — **COMPLETADO** (marcado retroactivamente: la implementación y el commit ya existían — `5d96548` — pero el checklist no se había actualizado en el batch original; RED confirmado antes de crear el archivo, GREEN: 14/14 tests; suite completa y `npm run typecheck` verdes en su momento, sin regresiones posteriores). Test primero: `worktree-contract.test.ts` — `WORKTREE_TEST_TOOL_QUALIFIED_NAME === "mcp__worktree__run_tests"`, `WORKTREE_RAMA_PREFIJO === "harness/caso-"`; el archivo **no importa nada** (mismo test que `hitl-contract.test.ts`); `WorktreeAbierto`/`WorktreePort`/`BarridoWorktreePort`/`AplicarPatchPort`/`ResultadoPatch` compilan y son exhaustivos. Sin fixture (puro). Cubre spec `escritura-aislada-worktree` reqs. "Ciclo `abrir → invocar Developer → capturarDiff → cerrar`, con contrato asimétrico" y "Las tools de escritura y el `cwd` del worktree se entregan en el mismo acto, o no se entregan" (a nivel de tipo). ADR 57, 61 pto 2, 62, 67. Commit: `feat(core): agrega worktree-contract con WorktreePort asimetrico y AplicarPatchPort (Hito 5.1, tarea 1)`.

2. [x] **`src/adapters/git/config.ts`** (nuevo, §6.1) — **COMPLETADO** (RED confirmado: `npx vitest run src/adapters/git/config.test.ts` fallaba con `Cannot find module './config.js'` antes de crear `config.ts`; GREEN: 16/16 passed tras la implementación; `npm test`: 1150/1150 verde con `dist/` limpio; `npm run typecheck`: sin errores). Test primero: `config.test.ts` — `resolveGitConfig(env)`/`resolveWorktreeConfig(env)` devuelven los defaults (`bin: "git"`, `timeoutMs: 30_000`, `worktreeRoot: ".harness/worktrees"`, `ttlMs: 7_200_000`) y los overrides de `HARNESS_GIT_BIN`/`HARNESS_GIT_TIMEOUT_MS`/`HARNESS_WORKTREE_ROOT`/`HARNESS_WORKTREE_TTL_MS`; nunca lanza con env inválido (best-effort, molde de `knowledge/config.ts:40-49`). Sin fixture (puro, solo `process.env` inyectado). Cubre spec `escritura-aislada-worktree` req. "Barrido de huérfanos... TTL configurable" (prerrequisito). ADR 57 pto 1 y 7, ADR 62 (`resolvePositiveNumber` duplicada, con la nota de doc-comment ya usada 5 veces en el repo). Commit: `feat(adapters/git): agrega config con defaults de worktree y TTL configurable (Hito 5.1, tarea 2)`.
   - **Corrección post-`code-review` (RED→GREEN, sin cambio de alcance)**: 2 bugs de correctitud confirmados por el Reviewer, no cubiertos por `sdd-verify` contra el contrato documentado. (1) `resolvePositiveNumber` aceptaba `Infinity`/`1e400` (que `Number()` parsea a `Infinity`) — `HARNESS_GIT_TIMEOUT_MS=Infinity` pasaba la validación y llegaba a `execFile(..., {timeout: Infinity})`, que Node rechaza síncronamente, rompiendo el contrato "nunca lanza"; fix: se agregó `!Number.isFinite(parsed)` a la guarda existente, cubre también `WorktreeConfig.ttlMs` porque comparte la misma función. (2) `bin`/`worktreeRoot` usaban `env.X ?? DEFAULT`, que no cae al default con string vacío — `HARNESS_GIT_BIN=""` rompía el spawn, `HARNESS_WORKTREE_ROOT=""` era un riesgo de join contra `repoRoot`; fix: nueva función `resolveNonBlankString` (mismo estilo de trim+fallback que `resolvePositiveNumber`, sin reusarla porque el tipo no aplica). RED confirmado con 6 tests nuevos en `config.test.ts` (fallaban contra el código pre-fix); GREEN: 22/22 en el archivo, `npm test`: 1271/1271 con `dist/` limpio, `npm run typecheck` sin errores. Commit propuesto: `fix(adapters/git): rechaza timeout no finito y string vacio en resolveGitConfig (Hito 5.1, tarea 2)`.
   - **Segunda ronda post-`code-review` (RED→GREEN)**: `resolveNonBlankString` chequeaba blank con `raw.trim() === ""` pero devolvía `raw` sin trimear — `HARNESS_GIT_BIN=" /usr/bin/git "` (espacios al borde) no es blank, así que pasaba con los espacios y rompía `execFile` (ENOENT) pese a que el path era válido en esencia; fix: devuelve `trimmed` en vez de `raw`. RED confirmado con 2 tests nuevos (`HARNESS_GIT_BIN` y `HARNESS_WORKTREE_ROOT` con espacios al borde, fallaban contra el código pre-fix); GREEN: 24/24 en el archivo, `npm test`: 1273/1273 con `dist/` limpio, `npm run typecheck` sin errores. También se simplificó `resolvePositiveNumber` quitando el chequeo redundante `Number.isNaN(parsed)` (ya cubierto por `!Number.isFinite(parsed)`) — refactor sin cambio de comportamiento, sin test nuevo. Commit propuesto: `fix(adapters/git): trimea el valor de resolveNonBlankString y simplifica el chequeo de resolvePositiveNumber (Hito 5.1, tarea 2)`.

3. [x] **`src/adapters/git/git-cli.ts`** (nuevo, §6.1, parte 1 — runner y clasificación de error) — **COMPLETADO** (RED confirmado: `npx vitest run src/adapters/git/git-cli.test.ts` fallaba con `Cannot find module './git-cli.js'` antes de crear `git-cli.ts`; GREEN: 13/13 passed tras la implementación; `npm test`: 1163/1163 verde con `dist/` limpio; `npm run typecheck`: sin errores). Test primero: `git-cli.test.ts` con `GitExecFileFn` fake — `ENOENT` ⇒ `reason: "not-found"`; `killed`/`SIGTERM` ⇒ `"timeout"`; `code` numérico ⇒ `"exit-code"`; resto ⇒ `"unknown"`; el error nativo nunca cruza el borde. **Fixture**: `GitExecFileFn` fake (mismo molde `graphify-cli.test.ts`). Cubre spec `escritura-aislada-worktree` escenario "Un fallo de `git` no encontrado se clasifica, no se propaga crudo". ADR 57 pto 3. Commit: `feat(adapters/git): agrega GitExecFileFn y clasificacion de GitCliError (Hito 5.1, tarea 3)`.
   - **Nota de alcance**: `classifyGitFailure` se exportó (a diferencia de `classifyFailure` en `graphify-cli.ts`, que es privada) porque su consumidor real — el `try/catch` que arma `GitCliError` en el borde — vive en `worktree.ts`/`barrido.ts` (tareas 7-8), no en este archivo; `git-cli.ts` no tiene, por diseño (ADR 62), un wrapper genérico tipo `runGraphifyQuery`. `defaultGitExecFile` queda sin test unitario directo en esta tarea, mismo molde que `defaultExecFile` en `graphify-cli.test.ts` (tampoco testeado ahí de forma directa) — se ejercita indirectamente en tareas 9 (`index.test.ts`, default inyectable) y 10-11 (integración contra `git` real).

4. [x] **`src/adapters/git/git-cli.ts`** (modificado, §6.1, parte 2 — los diez constructores de argv) — **COMPLETADO** (RED→GREEN: 120/120 nuevos tests verdes, cero skips; suite completa 1265/1265 verde con `dist/` limpio; `npm run typecheck` limpio). Test primero: extiende `git-cli.test.ts` — para cada uno de los diez constructores y entradas adversariales (`"commit"`, `"push"`, `"--exec=x"`, `"; rm -rf /"`, `"--upload-pack=x"`), `argv[0] ∈ SUBCOMANDOS_PERMITIDOS` y ningún elemento del argv es `commit`/`push`/`remote`/`tag`. Sin fixture (puro, itera `CONSTRUCTORES_ARGV`). Cubre spec `escritura-aislada-worktree` req. "El adaptador expone funciones nombradas, nunca un `git(args)` genérico" (escenario "Ningún constructor de argv puede emitir `commit`, `push`, `remote` ni `tag`") — **es el test que hace `AGENTS.md` línea 78 inexpresable, no documental**. ADR 57 pto 4, ADR 62. Commit: `feat(adapters/git): agrega diez constructores de argv sin subcomando generico (Hito 5.1, tarea 4)`.
   - **Resuelto por checkpoint humano**: el invariante es posicional (`argv[0] ∈ SUBCOMANDOS_PERMITIDOS`), no por-elemento sobre el array completo — ver doc-comment sobre `describe("CONSTRUCTORES_ARGV", ...)` en `git-cli.test.ts`. Los 5 `it.skip` que documentaban la ambigüedad se borraron por redundantes: no se agregó sanitización nueva a los constructores.

5. [x] **`vitest.config.ts`** (nuevo, raíz) — **TDD exception** (scaffolding, R4) — **COMPLETADO** (`test.exclude: [...configDefaults.exclude, ".harness/**"]`; baseline y post-cambio idénticos: `npm test` 82/82 archivos, 1265/1265 tests verdes con `dist/` limpio antes y después; `npm run typecheck` sin errores — `vitest.config.ts` queda fuera de `tsconfig.json` `include: ["src"]`, por lo que no lo type-checkea `tsc`, pero sí lo carga y valida `vitest run` al arrancar, sin error). Excluye `.harness/**` del descubrimiento de tests — hoy no existe ningún `vitest.config.*` en el repo. Cubre spec `escritura-aislada-worktree` req. "El worktree no es descubierto por la suite de tests del checkout principal" (verificable recién en §11 paso 14, con un worktree real abierto — PR2/PR6). Commit: `chore: agrega vitest.config.ts excluyendo .harness del descubrimiento de tests (Hito 5.1, tarea 5)`.

6. [x] **`.gitignore`** (modificado) — **TDD exception** (scaffolding) — **COMPLETADO** (verificación previa a la edición: 15 entradas reales de patrón, no 10 como estimaba la nota original de la tarea — `node_modules/`, `dist/`, `build/`, `.env`, `.env.local`, `*.log`, `.DS_Store`, `Thumbs.db`, `*.db`, `*.db-wal`, `*.db-shm`, `*.sqlite`, `*.sqlite3`, `graphify-out/`, `.atl/`, `nul` — ninguna cubría `.harness/`; se ajustó el conteo de la verificación sin inventar nada, la condición de la tarea — "hoy ninguna entrada cubre `.harness/`" — sí se sostenía). Se agregó `.harness/` con comentario explicando que son worktrees de runtime que nunca deben versionarse. `npm test`: 82/82 archivos, 1265/1265 tests verdes; `npm run typecheck`: sin errores. Agrega `.harness/` — sin este cambio, R4 del `exploration.md` es un riesgo real, no hipotético. Commit: `chore: agrega .harness a gitignore (Hito 5.1, tarea 6)`.

### PR2 — Ciclo de vida real del worktree + barrido + integración contra `git` (tareas 7-12)

7. [x] **`src/adapters/git/worktree.ts`** (nuevo, §6.1) — **COMPLETADO** (RED confirmado: `npx vitest run src/adapters/git/worktree.test.ts` fallaba con `Cannot find module './worktree.js'` antes de crear `worktree.ts`; GREEN: 14/14 passed tras la implementación; `npm test`: 1287/1287 verde con `dist/` limpio (83 archivos, era 82/1273 antes de esta tarea); `npm run typecheck`: sin errores). Test primero: `worktree.test.ts` con `GitExecFileFn` fake — `abrir` llama `rev-parse HEAD` **antes** de `worktree add -b` (spy ordenado, `cwd = repoRoot`) y devuelve `baseCommit`; `capturarDiff` llama `add --intent-to-add --all` **antes** de `diff --binary`, ambos con `cwd = worktree.ruta`; `abrir`/`capturarDiff` **propagan** su error (envuelto en `GitCliError`, sin atraparlo); `cerrar` **no rechaza** aunque las dos llamadas de git fallen (un solo `try/catch`, degrada a evento `worktree-cierre-fallido`); `verificar`/`aplicar` traducen la falla a `{ok:false, motivo}` sin rechazar (`"exit-code"` ⇒ `MOTIVO_PATCH_CONFLICTO`, cualquier otra razón ⇒ `MOTIVO_PATCH_ERROR_GIT`). **Fixture**: `GitExecFileFn` fake, con `deps.repoRoot` apuntando a un directorio temporal real (`mkdtemp`/`rm` en `beforeEach`/`afterEach`) porque `verificar`/`aplicar` escriben el patch a un archivo de scratch real bajo `.harness/patches/` antes de invocar `git apply` (ADR 64 pto 4) — sin invocar `git` de verdad, no es la categoría de integración de las tareas 10-11. Cubre spec `escritura-aislada-worktree` reqs. "Ciclo `abrir → invocar Developer → capturarDiff → cerrar`" (los 3 escenarios) y "La captura del diff incluye archivos nuevos (untracked)". ADR 57 pto 5, 6, ADR 62. Commit: `feat(adapters/git): agrega worktree.ts con ciclo asimetrico abrir-capturar-cerrar (Hito 5.1, tarea 7)`.
   - **Nota de alcance (desviación menor de `design.md`, sin bloquear)**: el nombre del archivo de scratch de `verificar`/`aplicar` es `randomUUID().patch`, no `<propuestaId>.patch` como ilustra ADR 64 pto 4 — `AplicarPatchPort.verificar`/`.aplicar` (fijado por la tarea 1) reciben únicamente `patch: string`, sin `propuestaId`; ese id no llega a esta capa (lo tiene recién `build-on-comando-empleado.ts`, tarea 32). Un nombre aleatorio satisface la misma necesidad real (una ruta de archivo real para que `git apply` lea, borrada después) sin inventar un parámetro que el contrato ya fijado no tiene. También: `worktree.ts` importa `node:fs/promises` (`mkdir`/`writeFile`/`rm`) para ese mismo escritorio de scratch, lo que matiza la frase de ADR 62 "`barrido.ts` es el único módulo del adaptador que importa `node:fs`" — esa frase describe el motivo de separar `barrido.ts` (usa `stat`/`readdir` para el TTL), no una prohibición absoluta sobre el resto del adaptador; ADR 64 pto 4 ya pedía el archivo temporal en este mismo archivo. Ninguna de las dos desviaciones cambia el comportamiento observable que el spec exige.

8. [x] **`src/adapters/git/barrido.ts`** (nuevo, §6.1) — **COMPLETADO** (RED confirmado: `npx vitest run src/adapters/git/barrido.test.ts` fallaba con `Cannot find module './barrido.js'` antes de crear `barrido.ts`; GREEN: 10/10 passed tras la implementación; `npm test`: 1297/1297 verde con `dist/` limpio (84 archivos, era 83/1287 antes de esta tarea); `npm run typecheck`: sin errores). Test primero: `barrido.test.ts` con `GitExecFileFn` fake + `StatFn` inyectado (no `fs/promises` real) — parseo de `worktree list --porcelain` con múltiples registros separados por línea en blanco; doble filtro (ruta fuera de `.harness/worktrees/` con rama del prefijo correcto **no** se toca, rama sin el prefijo bajo la ruta correcta tampoco, y un worktree `detached` sin rama tampoco); mtime viejo (> TTL) ⇒ `worktree remove --force` + `branch -D`, reciente ⇒ intacto; un `stat` que falla en un candidato no aborta el resto (se cuenta como fallido y sigue); `git` que falla en el listado ⇒ `{examinados:0, borrados:0, fallidos:0}` sin rechazar, evento `worktree-barrido-fallido`; `git worktree prune` que falla **después** de procesar los candidatos ⇒ tampoco rechaza, devuelve el resumen ya calculado (no lo resetea a cero) y degrada al mismo evento en vez de emitir `worktree-barrido-ok` — es el escenario "Un fallo de `git` durante el barrido no tumba el arranque" del spec, que la GIVEN describe sobre `git worktree prune` específicamente. Corrida limpia ⇒ evento `worktree-barrido-ok` con `{examinados, borrados, fallidos, ttlMs}`. **Fixture**: `GitExecFileFn` fake + `StatFn` fake (mismo molde `worktree.test.ts`). Cubre spec `escritura-aislada-worktree` req. "Barrido de huérfanos al arranque, con TTL configurable, no bloqueante" (los 3 escenarios). ADR 57 pto 7. Commit: `feat(adapters/git): agrega barrido de worktrees huerfanos con doble filtro y TTL (Hito 5.1, tarea 8)`.
   - **Nota de alcance**: el nombre y shape de los campos del evento `worktree-barrido-ok`/`worktree-barrido-fallido` (`examinados`, `borrados`, `fallidos`, `ttlMs` / `message`) se tomaron literales de la tabla de logging de design.md §8, no estaban en el texto de la tarea 8 pero son necesarios para que `barrerHuerfanos` cumpla su propio doc-comment ("degrada a evento `worktree-barrido-fallido`") en el caso de `worktree prune` fallido — sin loggear algo ahí, la falla de `prune` sería silenciosa. `main.ts` (tarea no asignada acá) sigue teniendo su propio `.catch()` como red de seguridad adicional, sin cambios de contrato.

9. [x] **`src/adapters/git/index.ts`** (nuevo, §6.1) — **COMPLETADO** (RED confirmado: `npx vitest run src/adapters/git/index.test.ts` fallaba con `Cannot find module './index.js'` antes de crear `index.ts`; GREEN: 8/8 passed tras la implementación — 1 ajuste de aserción durante GREEN, no de implementación: `verificar` sobre un `execFileFn` rechazado también trae `detalle` en el `ResultadoPatch`, el test original no lo esperaba; `npm test`: 1305/1305 verde (85 archivos); `npm run typecheck`: sin errores). Test primero: `index.test.ts` — `createGitAdapter({config, repoRoot, logEvent, execFileFn?})` devuelve `{worktree, aplicarPatch, barrido}`, con `execFileFn` inyectable y default real (molde de `createKnowledgeAdapter`). Cubre: `worktree.abrir`/`worktree.cerrar` conectan con `abrirWorktree`/`cerrarWorktree` de `worktree.ts` usando `config.bin`/`config.timeoutMs`/`deps.repoRoot`; `aplicarPatch.verificar`/`.aplicar` conectan con `verificarPatch`/`aplicarPatch` de `worktree.ts` (con/sin `--check`, y degradando un rechazo a `ResultadoPatch` en vez de propagar); `barrido.barrerHuerfanos` conecta con `barrerHuerfanos` de `barrido.ts` usando el `StatFn` **real** (`node:fs/promises` `stat`, nunca inyectado a través de esta fachada) — probado con un worktree real reciente (queda intacto) y otro con mtime forzado al pasado vía `utimes` (se borra), ambos sobre un `repoRoot` temporal real (`mkdtemp`/`rm`). **Fixture**: `GitExecFileFn` fake para las cinco pruebas de wiring de arriba, más un `vi.spyOn(gitCliModule, "defaultGitExecFile")` para probar que, cuando `execFileFn` se omite, la fachada efectivamente conecta con el default real de `git-cli.ts` (molde de `vi.spyOn` sobre namespace ya usado en `webhooks/index.test.ts`/`webhooks/server.test.ts`). Ningún test invoca `git` de verdad (esa es la categoría separada de las tareas 10-11). Cubre ADR 62 (fachada de los cinco archivos). Commit: `feat(adapters/git): agrega fachada createGitAdapter (Hito 5.1, tarea 9)`.
   - **Nota de alcance (desviación menor de `design.md`, sin bloquear)**: `GitAdapterConfig` (tipo nuevo, no nombrado en `design.md`/`tasks.md`) se define como `GitConfig & Pick<WorktreeConfig, "worktreeRoot">` — deliberadamente **sin** `ttlMs` de `WorktreeConfig`. Motivo: `BarridoWorktreePort.barrerHuerfanos` (tarea 1, `worktree-contract.ts`) ya recibe `ttlMs` como argumento de la LLAMADA (junto a `ahoraMs`), no como dependencia de wiring — incluirlo en `GitAdapterConfig` habría sido un campo muerto que ningún camino de `index.ts` lee. El ejemplo de `main.ts` en design.md §7.3 (`createGitAdapter({..., config: gitConfig})` seguido de `gitConfig.ttlMs` en la llamada a `barrerHuerfanos`) sigue siendo válido: `gitConfig` ahí es una fusión más amplia (`GitConfig & WorktreeConfig`) que construirá la tarea de `main.ts` (no asignada acá), y TypeScript la acepta igual como `GitAdapterConfig` por tipado estructural (el campo extra `ttlMs` no rompe la asignabilidad).

10. [x] **`src/test/integration/worktree.integration.test.ts`** (nuevo) — **Categoría nueva: integración contra `git` real**, `skip` explícito si `git` no está en el `PATH`. — **COMPLETADO** (RED confirmado: `npx vitest run src/test/integration/worktree.integration.test.ts` fallaba con `No test files found, exiting with code 1` antes de crear el archivo; GREEN: 3/3 passed tras la implementación; `npm run typecheck`: 1 error inicial — `vi.fn()` sin genérico no matcheaba `LogEventFn`, corregido a `vi.fn<LogEventFn>()` (mismo molde que `worktree.test.ts`) — limpio en la segunda corrida; `npm test`: 1308/1308 verde con `dist/` inexistente/limpio (86 archivos, era 85/1305 antes de esta tarea); `skip` verificado manualmente corriendo el archivo con un `PATH` sin `git` — los 3 tests se saltan en 217ms, cero fallos). Sobre un repo temporal fabricado en `os.tmpdir()` (`git init` + un commit): `abrir` crea el directorio y la rama; se crea un archivo nuevo dentro del worktree y `capturarDiff` lo devuelve **como agregado con su contenido completo** (el escenario que `git diff` solo no cubre); `git apply --check` del patch capturado pasa contra el repo temporal (vía `verificarPatch`, la misma función real que usa `/aplicar-propuesta`); `cerrar` deja `git worktree list` con una sola entrada y la rama borrada. Cubre spec `escritura-aislada-worktree` escenarios "La tool de tests resuelve `node_modules` del checkout principal" (R5, verificado no supuesto — verificado estructuralmente vía `worktree.ruta` anidado dentro de `repoRoot`, sin invocar `vitest` real, eso es tarea 25) y "Un archivo nuevo creado por el Developer aparece en el patch". RD-5, RD-18. Commit: `test(integration): agrega worktree.integration.test contra git real (Hito 5.1, tarea 10)`.
    - **Nota de alcance (sin desviación de diseño)**: tres `it` independientes en vez de un único test lineal — cada uno abre su propio worktree (con `id` distinto) y lo cierra al final, en vez de encadenar aserciones sobre un único worktree compartido entre pasos. Da señal granular por escenario sin cambiar la cobertura pedida por la tarea; el costo es `git worktree add`/`remove` tres veces en vez de una, aceptable para una categoría que ya está acotada a tres archivos en todo el repo (RD-17).

11. [x] **`src/test/integration/barrido.integration.test.ts`** (nuevo) — **Integración contra `git` real**, mismo `skip`. — **COMPLETADO** (RED confirmado: `npx vitest run src/test/integration/barrido.integration.test.ts` fallaba con `No test files found, exiting with code 1` antes de crear el archivo; GREEN: 2/2 passed tras la implementación; `npm run typecheck`: sin errores; `npm test`: 1310/1310 verde con `dist/` inexistente/limpio (87 archivos, era 86/1308 antes de esta tarea); `skip` verificado manualmente con `PATH` sin `git` — los 2 tests se saltan en 228ms, cero fallos). Dos worktrees reales, uno con mtime forzado al pasado (`utimes`) y otro reciente: el barrido borra el viejo y deja el reciente intacto; con `git` apuntado a un binario inexistente (`resolve(repoRoot, "bin-inexistente", "git")`), el barrido devuelve un resumen degradado (`{examinados:0,borrados:0,fallidos:0}`, evento `worktree-barrido-fallido`) en vez de tirar. Cubre spec `escritura-aislada-worktree` reqs. "Un worktree viejo se borra al arrancar", "Un worktree reciente no se toca", "Un fallo de `git` durante el barrido no tumba el arranque". RD-14. Commit: `test(integration): agrega barrido.integration.test contra git real (Hito 5.1, tarea 11)`.
    - **Hallazgo real durante RED→GREEN, bug de producción en `barrido.ts` (tarea 8), no de diseño**: en Windows, `git worktree list --porcelain` reporta SIEMPRE rutas con `/` (verificado con `git` real, dos veces — antes y después del fix), mientras que `resolve(repoRoot, worktreeRoot)` de Node en Windows devuelve `\`. El doble filtro de `esCandidato` comparaba con `registro.ruta.startsWith(worktreeRootAbsoluto)` SIN normalizar — sobre `git` real esto nunca matcheaba, así que `barrerHuerfanos` devolvía `examinados: 0` siempre en Windows, sin borrar nada, sin fallar (silencioso). Ningún test unitario anterior (`barrido.test.ts`, `index.test.ts`) lo detectaba porque fabrican su propio porcelain con `join()`/`resolve()`, el mismo separador que la comparación — nunca cruzan dos strings de origen distinto (`git` real vs Node) como sí lo hace, por diseño, este archivo. Confirmado empíricamente con un script ad hoc (`tsx`, fuera del repo) contra un repo temporal real antes de escribir el test: `examinados: 0` con el código original, `examinados: 2, borrados: 1` tras el fix. Fix aplicado en `src/adapters/git/barrido.ts`: nueva función privada `normalizarSeparadores` (`ruta.replaceAll("\\", "/")`, no-op en POSIX) aplicada a `worktreeRootAbsoluto` y a `registro.ruta` en `esCandidato`. Sin regresión: los 152 tests de `barrido.test.ts`+`index.test.ts`+`worktree.test.ts`+`git-cli.test.ts`+ambas integraciones corridos juntos siguen en verde (mismos separadores en ambos lados de la comparación en los tests unitarios, la normalización es idempotente). Commit propuesto (separado del de este archivo, mismo criterio que las correcciones post-review de la tarea 2): `fix(adapters/git): normaliza separadores de ruta en el doble filtro del barrido para que matchee contra git real en Windows (Hito 5.1, tarea 11)`.
    - **Segunda ronda post-`code-review` (RED→GREEN, sin cambio de alcance)**: 2 hallazgos confirmados por el Reviewer sobre `esCandidato` (correctitud) y sobre la propia tarea 11 (TDD, commit `a061aad` tocó `barrido.ts` sin extender `barrido.test.ts`). (1) `normalizarSeparadores(registro.ruta).startsWith(worktreeRootAbsoluto)` no chequeaba el carácter siguiente al prefijo — un directorio HERMANO con prefijo compartido (p. ej. `.harness/worktrees-manual/foo`) matcheaba por coincidencia textual aunque estuviera fuera de la raíz real; si ese hermano tenía una rama con `WORKTREE_RAMA_PREFIJO`, pasaba el doble filtro completo y el barrido lo borraba — justo lo que el doble filtro existe para prevenir. Fix: boundary check explícito (`ruta === worktreeRootAbsoluto || ruta.startsWith(worktreeRootAbsoluto + "/")`). (2) Se agregaron los tests unitarios que faltaban en `barrido.test.ts` — un caso de directorio hermano con prefijo compartido y rama válida (RED confirmado: `examinados: 1, fallidos: 1` contra el código pre-fix, cuando debía ser `{0,0,0}`) y un caso de `normalizarSeparadores` con ruta forzada a `/` literales contra un root potencialmente con `\` en Windows (ya pasaba, cobertura faltante). GREEN: 12/12 en el archivo; `npm test`: 87 archivos / 1312 tests verdes con `dist/` limpio; `npm run typecheck` sin errores; integración (`barrido.integration.test.ts`) también verde (2/2). Commit propuesto: `fix(adapters/git): agrega boundary check de separador en esCandidato y sus tests unitarios (Hito 5.1, tarea 11)`.

12. [x] **`openspec/config.yaml`** (modificado, línea 21) — `testing.layers.integration: false → true`. **COMPLETADO** — **TDD exception explícita** (archivo de configuración, sin lógica, sin test propio, ya declarada como tal en la línea 23 de este documento). Único cambio: `testing.layers.integration: false → true` (línea 21). El cambio se **verifica** porque las tareas 10-11 (y luego 25) son los primeros archivos del repo que ejercitan esta capa. Verificación post-cambio: `npm test` → 87 archivos / 1310 tests verdes, sin `dist/` residual (confirmado antes de medir); `npm run typecheck` → sin errores. Mismos números que el baseline previo a esta tarea — el cambio de config no altera comportamiento de código, solo declara la capa `integration` como habilitada. Cubre §7.4, RD-17 ("primer test del repo que lanza un proceso externo... este cambio de configuración es parte del diseño, no un detalle de implementación"). Commit: `chore(config): habilita testing.layers.integration en openspec/config.yaml (Hito 5.1, tarea 12)`.

### PR3 — Persistencia de `propuestas_cambio` (tareas 13-16)

13. [x] **`src/adapters/memory/migrations/NNNN_propuestas_cambio.ts`** (nuevo, §7.1) + **append** a `migrations/index.ts` — **TDD exception** (SQL declarativo, se escribe junto a su test). `NNNN = M + 1` con `M` el prefijo más alto real de `main` al momento de aplicar (regla ADR 63, no el número — hoy `0009` si el gate del inicio ya está cumplido, `sdd-apply` lo resuelve). Incluye el `ALTER TABLE registro_acciones_empleado ADD COLUMN propuesta_id TEXT REFERENCES propuestas_cambio(id)` en el mismo archivo, **después** del `CREATE TABLE`. Test primero: agrega casos a `repository.test.ts` — tras `runMigrations` existen la tabla `propuestas_cambio`, sus dos índices (`idx_propuestas_estado`, `idx_propuestas_caso`) y la columna `propuesta_id` en `registro_acciones_empleado`; insertar con `delegacion_id NULL` funciona (precedente ADR 48 aplicado acá también); insertar sin `caso_id` existente falla por FK; correr las migraciones dos veces no falla (`IF NOT EXISTS`). Sin fixture de LLM (SQLite `:memory:`). Cubre spec `propuesta-cambio-hitl` reqs. "Tabla propia `propuestas_cambio` con máquina de estados de tres valores" (RD-12 resuelto por el checkpoint) y "Una propuesta sin delegación de origen es igualmente persistible". ADR 58, ADR 63. Commit: `feat(adapters/memory): agrega migracion NNNN con tabla propuestas_cambio y columna propuesta_id (Hito 5.1, tarea 13)`. — **COMPLETADO** (verificado con `M = 8`, gate del inicio cumplido en `main`: `NNNN = 0009`, export `migration0009PropuestasCambio`. RED confirmado: `npx vitest run src/adapters/memory/repository.test.ts -t "propuestas_cambio"` fallaba 3/4 con `no such table: propuestas_cambio` / `expected [...] to include 'propuestas_cambio'` antes de crear el archivo de migración y registrarlo; GREEN: 4/4 passed tras crear `0009_propuestas_cambio.ts` y agregarlo al array `migrations` de `index.ts`; `npm test`: 1316/1316 verde, 87 archivos, `dist/` inexistente confirmado antes de medir; `npx tsc --noEmit` sin errores). Los 4 casos pedidos, confirmados: (1) tabla `propuestas_cambio` + índices `idx_propuestas_estado`/`idx_propuestas_caso` + columna `propuesta_id` en `registro_acciones_empleado` existen tras `runMigrations` (vía `sqlite_master` y `PRAGMA table_info`); (2) `INSERT` con `delegacion_id = NULL` funciona y se lee de vuelta como `null`; (3) `INSERT` con `caso_id` inexistente rechaza con `/FOREIGN KEY/`; (4) correr `runMigrations` una segunda vez sobre la misma conexión no lanza. Sin fixture de LLM (SQLite `:memory:`).
    - **Nota de precisión (no es una desviación de comportamiento)**: el texto de esta tarea describe el caso 4 como cubierto por `IF NOT EXISTS`, pero `ALTER TABLE ... ADD COLUMN` de SQLite no admite esa cláusula. La idempotencia real de "correr las migraciones dos veces no falla" la da `runMigrations` (`migrate.ts`): trackea los `id` ya aplicados en `schema_migrations` y jamás re-ejecuta el SQL de una migración ya registrada — el `ALTER TABLE` de esta migración corre exactamente una vez por base de datos, igual que el `CREATE TABLE`/`CREATE INDEX` (que sí llevan `IF NOT EXISTS` por si alguna vez se reintenta manualmente el mismo `.sql` fuera de `runMigrations`). Test cubierto igual, comportamiento idéntico al pedido; sólo el mecanismo interno difiere del que sugiere la redacción.

14. [x] **`src/adapters/memory/repository.ts`** (modificado, §7.2, funciones de lectura/alta). Test primero: extiende `repository.test.ts` — `insertPropuestaCambio` escribe una fila con `estado = pendiente_aprobacion_humana` y los contadores exactos; `getPropuestaCambio` devuelve `undefined` para id inexistente; `listPropuestasCambio` filtra por `estado`/`propuestaId`/`limite` y ordena por `created_at`. Sin fixture de LLM (SQLite `:memory:`). Cubre spec `propuesta-cambio-hitl` req. "Un diff capturado crea una fila en estado inicial" (ambos escenarios). ADR 58. Commit: `feat(adapters/memory): agrega insertPropuestaCambio getPropuestaCambio y listPropuestasCambio (Hito 5.1, tarea 14)`. — **COMPLETADO** (RED confirmado: `npx vitest run src/adapters/memory/repository.test.ts -t "insertPropuestaCambio|getPropuestaCambio|listPropuestasCambio"` fallaba 7/7 con `TypeError: insertPropuestaCambio is not a function` antes de implementar; GREEN: 123/123 en el archivo tras agregar `insertPropuestaCambio`/`getPropuestaCambio`/`listPropuestasCambio` (+ tipos `PropuestaRow`/`CrearPropuestaDbInput` y el privado `rowToPropuesta`) al final de `repository.ts`; `npm run typecheck` (`npx tsc --noEmit`) sin errores; `npm test`: 1323/1323 verde, 87 archivos, `dist/` inexistente confirmado antes de medir (era 1316/1316 antes de esta tarea, +7 tests). Los casos pedidos, confirmados: (1) `insertPropuestaCambio` crea la fila con `estado = pendiente_aprobacion_humana` y los contadores (`patchBytes`/`archivos`/`lineasAgregadas`/`lineasEliminadas`) exactos, más un caso explícito de `delegacionId` ausente (ADR 48); (2) `getPropuestaCambio` devuelve `undefined` para id inexistente y la fila real para un id existente; (3) `listPropuestasCambio` filtra por `estado` (excluye una fila forzada a `aplicada`), filtra opcionalmente por `propuestaId`, y un tercer caso prueba `limite` y el `ORDER BY created_at` a la vez, insertando la fila con `created_at` más tardío PRIMERO para que el orden del resultado sólo se explique por la cláusula SQL, nunca por el orden de inserción).
    - **Nota de alcance (sin desviación de diseño)**: `estado` no viaja como parámetro de `CrearPropuestaDbInput` — el literal `"pendiente_aprobacion_humana"` va fijo dentro de `insertPropuestaCambio`, mismo criterio que el literal fijo en `listSolicitudesInternas`. Es la única función que escribe el estado inicial, así que no hay caller legítimo que necesite pasarlo. `aplicarPropuestaCambio`/`descartarPropuestaCambio`/`resolverPropuestaTransaccional` (tarea 15) y las constantes de comando (tarea 16) NO se tocaron en este batch.
    - **Corrección post-`code-review` (RED→GREEN, sin cambio de alcance)**: hallazgo de eficiencia confirmado por el Reviewer sobre `listPropuestasCambio`. El SQL envolvía el filtro de `estado` como `(@estado IS NULL OR estado = @estado)` — SQLite no puede usar `idx_propuestas_estado` con ese patrón aunque `estado` viaje con un valor real, porque el planner no descarta en tiempo de prepare que el bind sea `NULL`; el resultado era un `SCAN` completo de `propuestas_cambio` en el caso común (`/ver-propuesta` con `estado` fijo). Fix: la condición sobre `estado` se arma en JS (`estadoClausula`, string vacío si el filtro está ausente) y sólo entra al SQL cuando corresponde, replicando el criterio de `orden` en `listEscalacionesReembolso` (fragmento de SQL elegido en JS según el filtro, en vez de un `OR IS NULL` estático que tapa el índice); `propuestaId` conserva su `OR IS NULL` porque siempre fue opcional y no es el filtro que necesita el índice de estado. RED confirmado con un test nuevo que captura el SQL real de `listPropuestasCambio(db, { estado: ... })` vía monkeypatch de `db.prepare` y corre `EXPLAIN QUERY PLAN` sobre él — contra el código pre-fix el plan daba `SCAN propuestas_cambio USING INDEX sqlite_autoindex_propuestas_cambio_1`; tras el fix da `SEARCH propuestas_cambio USING INDEX idx_propuestas_estado`. GREEN: 130/130 en el archivo; `npm test`: 87 archivos / 1331 tests verdes con `dist/` inexistente confirmado antes de medir; `npm run typecheck` sin errores. Commit propuesto: `fix(adapters/memory): arma el filtro de estado en JS para que listPropuestasCambio use idx_propuestas_estado (Hito 5.1, tarea 14)`.

15. [x] **`src/adapters/memory/repository.ts`** (modificado, §7.2, `resolverPropuestaTransaccional` + `aplicarPropuestaCambio`/`descartarPropuestaCambio`). Test primero: extiende `repository.test.ts` — CAS + `updateCaso(db, casoId, {updatedAt})` (**sólo** `updatedAt`, `estado` del caso **no** cambia — assert explícito, ADR 65) + `insertAccionEmpleado` con `propuestaId`, todo en **una** transacción (molde exacto de `resolverEscalacionTransaccional`); CAS que no matchea ⇒ `undefined`, **ni el caso ni la fila de auditoría se escriben** (rollback verificable). Sin fixture de LLM (SQLite `:memory:`). Cubre spec `propuesta-cambio-hitl` reqs. "Confirmación exitosa aplica el patch y deja auditoría en una transacción" y "Descarte exitoso deja motivo y auditoría en una transacción". ADR 63, ADR 65. Commit: `feat(adapters/memory): agrega resolverPropuestaTransaccional con CAS y caso solo en updated_at (Hito 5.1, tarea 15)`. — **COMPLETADO** (RED confirmado: `npx vitest run src/adapters/memory/repository.test.ts` fallaba 5/128 — `TypeError: aplicarPropuestaCambio is not a function` / `descartarPropuestaCambio is not a function` (3 casos) y `expected null to be 'propuesta-1'` en el test de `insertAccionEmpleado` con `propuestaId` — antes de implementar; GREEN: 128/128 en el archivo tras (a) extender `AccionEmpleadoInput`/`AccionEmpleadoRow`/`AccionEmpleadoSqlRow`/`rowToAccionEmpleado`/`insertAccionEmpleado` con `propuestaId` opcional (columna `propuesta_id` de la migración `0009`, ADR 63) y (b) agregar `ResolucionPropuestaDbInput`, el privado `resolverPropuestaTransaccional` y los exports `aplicarPropuestaCambio`/`descartarPropuestaCambio`, molde exacto de `resolverEscalacionTransaccional`/`resolverSolicitudTransaccional` pero SIN `estadoCaso` en `config` — `updateCaso(db, casoId, { updatedAt })` se llama sin `estado`, que `COALESCE` deja intacto (ADR 65); `npm run typecheck` sin errores; `npm test`: 1328/1328 verde, 87 archivos, `dist/` inexistente confirmado antes de medir (era 1323/1323 antes de esta tarea, +5 tests). Los casos pedidos, confirmados: (1) `aplicarPropuestaCambio` hace CAS a `aplicada` + `updateCaso` con SÓLO `updatedAt` (assert explícito: `getCasoById(...).estado` sigue siendo el mismo valor de antes de la llamada, comparado contra una lectura previa Y contra el literal `"abierto"`) + UNA fila en `registro_acciones_empleado` con `propuesta_id` poblado, las tres escrituras dentro de la misma `db.transaction`; (2) `descartarPropuestaCambio` ídem con CAS a `descartada`, `motivo` persistido y el mismo assert de `estado` del caso intacto; (3) un CAS que no matchea (propuesta ya no `pendiente_aprobacion_humana`, forzado con `UPDATE` directo) devuelve `undefined` y NO escribe la fila de auditoría NI toca el caso — verificado con lectura directa de `casos` (`estado` Y `updatedAt` sin cambios) y de `registro_acciones_empleado` (fila ausente por `id`), para ambos wrappers (`aplicarPropuestaCambio`/`descartarPropuestaCambio`); (4) test dedicado de `insertAccionEmpleado` aceptando `propuestaId` sin pisar `caso_id`/`venta_id`).
    - **Nota de alcance (sin desviación de diseño)**: `ResolucionPropuestaDbInput.motivo` es opcional (`readonly motivo?: string`) porque `aplicarPropuestaCambio` nunca lo usa (la columna `motivo` es exclusiva de `/descartar-propuesta`, ya establecido en el spec) — el `COALESCE(@motivo, motivo)` del `UPDATE` deja la columna intacta (`NULL`) cuando `aplicarPropuestaCambio` no lo pasa, mismo patrón que `tipo`/`estado` en `updateCaso`. Las constantes de comando (`COMANDO_APLICAR_PROPUESTA`, etc., tarea 16) NO se tocaron en este batch — los literales `"/aplicar-propuesta"`/`"/descartar-propuesta"` van hardcodeados en `repository.ts` por ahora, mismo criterio transitorio que ya usan `aprobarEscalacionReembolso`/`aprobarSolicitudInterna` con sus propios comandos antes de que existieran constantes centralizadas.
    - **Corrección post-`code-review` (RED→GREEN, sin cambio de alcance)**: hallazgo de correctitud confirmado por el Reviewer. Esta tarea extendió `AccionEmpleadoSqlRow`/`rowToAccionEmpleado`/`insertAccionEmpleado` con `propuestaId`, pero no tocó el SELECT explícito de `listAccionesEmpleadoPorVenta` (tarea 11, commit `606184b`) — esa función sigue listando `id, empleado_id, comando, venta_id, caso_id, resultado, ocurrido_at` sin `propuesta_id`. Como la columna no viaja en el result set, `row.propuesta_id` llega `undefined` (no `null`) a `rowToAccionEmpleado`, y `undefined !== null` es `true`, así que el spread `...(row.propuesta_id !== null ? { propuestaId: row.propuesta_id } : {})` agregaba una clave espuria `propuestaId: undefined` en vez de omitirla, a diferencia de `ventaId`/`casoId`. Grep de todo `registro_acciones_empleado` en `repository.ts` confirma que `rowToAccionEmpleado` sólo se usa en `listAccionesEmpleadoPorVenta` — ninguna otra función de lectura está afectada. Fix: se agregó `propuesta_id` a la lista de columnas del SELECT. RED confirmado con un test nuevo que inserta dos acciones sobre la misma venta (una sin `propuestaId`, otra con) y lee de vuelta con `listAccionesEmpleadoPorVenta`, asertando con `Object.keys(...)` que la fila sin propuesta NO tiene la clave `propuestaId` en absoluto (no sólo `undefined`) y que la fila con propuesta sí la tiene con el valor correcto — contra el código pre-fix fallaba con `expected [...] to not include 'propuestaId'`. GREEN: 130/130 en el archivo; `npm test`: 87 archivos / 1331 tests verdes con `dist/` inexistente confirmado antes de medir; `npm run typecheck` sin errores. Commit propuesto: `fix(adapters/memory): agrega propuesta_id al SELECT de listAccionesEmpleadoPorVenta (Hito 5.1, tarea 15)`.

16. [x] **`src/core/commands/registro-acciones-contract.ts`** (modificado, §5.9) — **TDD exception** (constantes de datos, sin lógica nueva, molde de la tarea 21 de Hito 5). Test primero: extiende `registro-acciones-contract.test.ts` — `COMANDO_VER_PROPUESTA`/`COMANDO_APLICAR_PROPUESTA`/`COMANDO_DESCARTAR_PROPUESTA`/`RESULTADO_APLICADA`/`RESULTADO_DESCARTADA` existen con los strings esperados; `AccionEmpleado` gana `readonly propuestaId?: string` (aditivo y opcional, ningún call site de `v1.4`/`v2.0.0` cambia). Commit: `feat(core): agrega constantes de comando y AccionEmpleado.propuestaId para propuestas (Hito 5.1, tarea 16)`. — **COMPLETADO** (RED confirmado: extendidos los imports y 5 asserts nuevos en `registro-acciones-contract.test.ts` ANTES de tocar el source → `npx vitest run src/core/commands/registro-acciones-contract.test.ts` → 2 failed / 5 passed [RED: `expected undefined to be '/ver-propuesta'` en `vocabulario de comando`, `expected undefined to be 'aplicada'` en `vocabulario de resultado`; el test nuevo de `propuestaId` pasaba igual porque vitest no type-checkea object literals contra la interfaz en runtime] → agregadas `COMANDO_VER_PROPUESTA`/`COMANDO_APLICAR_PROPUESTA`/`COMANDO_DESCARTAR_PROPUESTA` después de `COMANDO_RECHAZAR_SOLICITUD`, `RESULTADO_APLICADA`/`RESULTADO_DESCARTADA` después de `RESULTADO_CREADA` (mismo estilo de comentario `// /comando`), y `readonly propuestaId?: string` en `AccionEmpleado` después de `casoId?` — sin tocar `RegistroAccionesEmpleadoPort` ni ningún import (el archivo sigue sin `import`) → mismo comando → 7/7 passed [GREEN, 6 preexistentes + 1 nuevo]; `npm test` [con `dist/` inexistente confirmado antes de medir]: **1329/1329 verde** [87 archivos; era 1328/1328 antes de esta tarea, +1 nuevo — ningún call site de `AccionEmpleado` en `v1.4`/`v2.0.0` se rompió]; `npm run typecheck`: **0 errores**. **Desviación declarada respecto al pedido de actualizar `repository.ts`**: NO se reemplazaron los literales `"/aplicar-propuesta"`/`"/descartar-propuesta"` de `resolverPropuestaTransaccional` (tarea 15) por estas constantes — verificado que `repository.ts` NO importa de `src/core/commands/registro-acciones-contract.ts` en ningún punto del archivo (`rg "^import"` → solo `better-sqlite3`) y que los otros cuatro comandos ya persistidos ahí (`/aprobar-reembolso`, `/rechazar-reembolso`, `/aprobar-solicitud`, `/rechazar-solicitud`, líneas 1485/1499/2030/2044) están **todos** hardcodeados pese a que sus constantes existen en el núcleo desde antes — no es deuda transitoria exclusiva de la tarea 15, es el patrón establecido y consistente de todo el archivo. Introducir el import acá habría dejado 2 de 6 comandos importados y 4 hardcodeados, una inconsistencia nueva sin ninguna tarea de `tasks.md` (13-16) que la pida explícitamente — el texto literal de la tarea 16 tampoco menciona tocar `repository.ts`. Queda para que el Reviewer decida si vale una tarea aparte que homogeneice los seis comandos a la vez.)

### PR4 — Núcleo puro de propuestas (tareas 17-20)

17. **`src/core/propuestas/propuestas-contract.ts`** (nuevo, §5.4). Test primero: `propuestas-contract.test.ts` — `PATCH_MAX_BYTES === 65_536`; `PROPUESTA_ESTADO_PENDIENTE` **es** `CASO_ESTADO_PENDIENTE_APROBACION_HUMANA` (importado, no redeclarado, mismo cruce que `solicitudes-contract.ts`); **el conjunto de estados tiene exactamente tres elementos y ninguno es `"no_aplicable"`** (RD-12 — el test que impide reintroducir un cuarto valor sin escritor); el archivo importa **únicamente** de `../hitl/hitl-contract.js`. Sin fixture de LLM. Cubre spec `propuesta-cambio-hitl` req. "Tabla propia... máquina de estados de tres valores" (a nivel de vocabulario del núcleo). ADR 42, ADR 58, RD-12. Commit: `feat(core): agrega propuestas-contract con vocabulario de tres estados y PATCH_MAX_BYTES (Hito 5.1, tarea 17)`.

18. **`src/core/propuestas/resumir-patch.ts`** (nuevo, §5.5) — **PURO, sin imports**. Test primero: `resumir-patch.test.ts` — `contarBytesUtf8` verificado contra `Buffer.byteLength` (desde el test, que sí importa Node) sobre ASCII, acentos, CJK, emoji (par surrogate), surrogate huérfano, string vacía; `resumirPatch` sobre patches fijados: 1 archivo, N archivos, sólo agregados, sólo borrados, archivo nuevo, patch binario. Sin fixture de LLM. Cubre spec `escritura-aislada-worktree`/`propuesta-cambio-hitl` (prerrequisito del tope de 64 KB). ADR 68. Commit: `feat(core): agrega resumirPatch y contarBytesUtf8 puros sin imports de Node (Hito 5.1, tarea 18)`.

19. **`src/core/propuestas/crear-propuesta-cambio.ts`** (nuevo, §5.6). Test primero: `crear-propuesta-cambio.test.ts` con doble de `PropuestaStorePort` — orden `trim → resumirPatch → tope → store.crearPropuesta` (el store **no** se llama en `"sin_cambios"` ni en `"rechazada_por_tamano"`); patch vacío tras `trim()` ⇒ `"sin_cambios"`, cero filas, evento `propuesta-sin-cambios`; patch > 65_536 bytes ⇒ `"rechazada_por_tamano"`, cero filas, evento con `patchBytes`; patch válido ⇒ `"creada"` con la fila completa. **Fixture**: `PropuestaStorePort` fake. Cubre spec `propuesta-cambio-hitl` req. "Tope de 64 KB con rechazo — nunca truncado" (ambos escenarios). ADR 58 pto 3, ADR 70. Commit: `feat(core): agrega crearPropuestaCambio con cuatro desenlaces y tope de rechazo (Hito 5.1, tarea 19)`.

20. **`src/core/propuestas/resolver-propuesta-cambio.ts`** (nuevo, §5.7) — **PURO y SÍNCRONO**, molde literal de `resolverSolicitudInterna`. Test primero: `resolver-propuesta-cambio.test.ts` — función **síncrona** (test explícito de sincronía, molde de `procesar-devolucion`); sin `propuestaId` ⇒ `"listado"`; con id y `confirmado:false` ⇒ `"requiere_confirmacion"` (eco, sin escritura); con id y `confirmado:true` ⇒ delega a `store.aplicarPropuesta`/`descartarPropuesta`, `undefined` (CAS no matcheó) ⇒ `"no_aplicable"`/`"cas"`; recibe `sesion: SesionEmpleado`, no `empleadoId` suelto (ADR 37). **El `git apply --check` no está acá** (ADR 64) — se testea que el resolvedor nunca hace `await`. **Fixture**: `PropuestaStorePort` fake. Cubre spec `propuesta-cambio-hitl` reqs. "Primer llamado no ejecuta nada" y el desenlace `no_aplicable`/`cas` (nota: tag de `ResolucionHitlResult`, no estado persistido — RD-12 no se toca). ADR 50, ADR 64. Commit: `feat(core): agrega resolverPropuestaCambio sincrono con confirmacion en dos pasos (Hito 5.1, tarea 20)`.

### PR5 — Tool MCP de tests + Developer con escritura (tareas 21-28)

21. **`src/adapters/test-runner/config.ts`** (nuevo, §6.2). Test primero: `config.test.ts` — defaults (`timeoutMs: 300_000`, `vitestEntrypoint: resolve(repoRoot, "node_modules/vitest/vitest.mjs")`, `TEST_MCP_TIMEOUT_MARGIN_MS: 5_000`) y override por `HARNESS_WORKTREE_TEST_TIMEOUT_MS`. Sin fixture (puro). Cubre ADR 61 pto 7. Commit: `feat(adapters/test-runner): agrega config con timeout y entrypoint de vitest (Hito 5.1, tarea 21)`.

22. **`src/adapters/test-runner/test-runner-cli.ts`** (nuevo, §6.2). Test primero: `test-runner-cli.test.ts` con `TestExecFileFn` fake — **ADR 66**: `exitCode 1` con `stdout` ⇒ **resultado OK** `{exitCode, output, durationMs}`, no error; `ENOENT` ⇒ `TestRunnerCliError("not-found")`; `killed`/`SIGTERM` ⇒ `"timeout"`; el orden de las ramas importa (ENOENT antes que numérico, killed antes que numérico) y se testea; el argv es `[entrypoint, "run", "--reporter=basic"]` con `cwd` del worktree y **nunca** contiene `"npm"`. **Fixture**: `TestExecFileFn` fake. Cubre spec `escritura-aislada-worktree` req. "Tool MCP `mcp__worktree__run_tests`... corre la suite completa fijada al `cwd` del worktree" (prerrequisito del runner). ADR 61 pto 5, ADR 66, R6. Commit: `feat(adapters/test-runner): agrega runVitest con exitCode no-cero como resultado valido (Hito 5.1, tarea 22)`.

23. **`src/adapters/test-runner/test-runner-tool.ts`** (nuevo, §6.2). Test primero: `test-runner-tool.test.ts` — `truncarConservandoCola`: salida corta intacta y sin marcador; salida larga ⇒ `.length <= 8_000`, empieza con el marcador y **termina igual que la original**, par surrogate en el borde no se parte; `handleRunTests` **nunca lanza**, ni con un `runTests` que tira **sincrónicamente**; los tres textos (verde/rojo/degradado); `exitCode` y duración **siempre** presentes. **Fixture**: `runTests` fake. Cubre spec `escritura-aislada-worktree` reqs. "Salida de la tool truncada... conservando la cola" (ambos escenarios) y "El handler de la tool nunca lanza" (ambos escenarios). ADR 61 pto 6 y 8. Commit: `feat(adapters/test-runner): agrega truncarConservandoCola y handleRunTests que nunca lanza (Hito 5.1, tarea 23)`.

24. **`src/adapters/test-runner/index.ts`** (nuevo, §6.2). Test primero: `index.test.ts` — `createTestRunnerAdapter({casoId, cwd, logEvent, config?, execFileFn?})` devuelve `{mcpServers}` con el servidor `worktree` registrando la tool `run_tests` con esquema zod vacío (`{}`); un argumento arbitrario del modelo se descarta antes del handler. **Fixture**: `TestExecFileFn` fake para el default. Cubre spec `escritura-aislada-worktree` escenario "La tool no acepta filtrar por archivo o patrón". ADR 61 pto 3, 4 y 9 (cero contacto con `git/`). Commit: `feat(adapters/test-runner): agrega servidor MCP worktree con tool run_tests sin parametros (Hito 5.1, tarea 24)`.

25. **`src/test/integration/run-tests.integration.test.ts`** (nuevo) — **Categoría nueva: integración contra `git`+`vitest` real**, `skip` si `git` no está en el `PATH`. Fabrica un worktree real (reusa el helper de la tarea 10) y corre la tool de verdad: resuelve `node_modules/vitest` del checkout principal desde dentro del worktree (**R5 verificado, no supuesto**), devuelve `exitCode 0` con una suite mínima en verde y `≠ 0` con una rota. Cubre spec `escritura-aislada-worktree` escenario "La suite corre dentro del worktree y devuelve resultado real". RD-18. Commit: `test(integration): agrega run-tests.integration.test contra vitest real en worktree (Hito 5.1, tarea 25)`.

26. **`src/core/agents/definitions.ts`** (modificado, §5.2, ADR 67). Test primero: extiende `definitions.test.ts` con **el test estrella**: `construirDeveloperConEscritura(wt)` devuelve `allowedTools` con `Write`+`Edit`+`mcp__worktree__run_tests` **y** `cwd === wt.ruta`; `SUBAGENT_REGISTRY.developer.allowedTools` **no cambió** respecto de `v2.0.0` (`["Read","Glob","Grep"]`); **ninguna** de las cuatro definiciones del registro trae `Write`/`Edit`/`Bash`/`Agent`/`Task`, con el interruptor en cualquier estado; el único parámetro de la función es un `WorktreeAbierto` — no acepta `string`, no tiene default, no tiene sobrecarga. Sin fixture de LLM. Cubre delta `delegacion-subagentes` req. "`allowedTools` acotado por rol; el Developer es el único que puede ganar herramientas de escritura, y solo junto con un worktree aislado" (los 4 escenarios). ADR 61 pto 1-2, ADR 67. Commit: `feat(core): agrega construirDeveloperConEscritura como unico constructor con Write y Edit (Hito 5.1, tarea 26)`.

27. **`src/core/agents/subagents.ts`** (modificado, §5.3). Test primero: extiende `subagents.test.ts` — `InvocarSubagente` acepta `cwd?`/`mcpServers?` opcionales sin romper ningún call site de `v2.0.0`; ausentes ⇒ comportamiento idéntico al de `v2.0.0`. Sin fixture de LLM adicional. Cubre ADR 67 pto 4 (propagación hasta el SDK, primera mitad). Commit: `feat(core): agrega cwd y mcpServers opcionales a InvocarSubagente (Hito 5.1, tarea 27)`.

28. **`src/core/turn-selector/invoke-model.ts`** (modificado, §5.3). Test primero: extiende `invoke-model.test.ts` — `invokeModel` gana un séptimo parámetro trailing opcional `cwd?: string` (patrón ya justificado por el propio doc-comment del archivo, líneas 305-309); `toQueryOptions` asigna `options.cwd` **incrementalmente** (`if (cwd !== undefined) options.cwd = cwd`, por `exactOptionalPropertyTypes: true`) — omitido ⇒ `Options` sin la clave. Actualiza el doc-comment de `toQueryOptions` con la **nota de honestidad obligatoria**: `options.cwd` es anclaje de alcance, no aislamiento de proceso. **Fixture**: `queryFn` fake (ya existente). Cubre delta `delegacion-subagentes` (wiring real del ADR 67) + RD-10. ADR 67 pto 4. Commit: `feat(core): propaga cwd opcional en toQueryOptions con nota de honestidad sobre aislamiento (Hito 5.1, tarea 28)`.

### PR6 — Comandos TUI + cadena de revisión + composition root (tareas 29-34)

29. **`src/core/commands/comando-empleado.ts`** (modificado, §5.9, ADR 69). Test primero: extiende `comando-empleado.test.ts` — nuevos descriptores `/ver-propuesta` (forma `id_opcional_propuesta`, nueva, `privilegiado: true`), `/aplicar-propuesta` y `/descartar-propuesta` (forma `id_mas_resto`, ramas propias no compartidas, `privilegiado: true`); `/aplicar-propuesta` sin id ⇒ ayuda/argumentos; `/descartar-propuesta <id> "motivo con espacios"` ⇒ `motivo` entero; `esComandoPrivilegiado` es `true` para los tres; `/ayuda` lista **catorce**; los descriptores existentes no cambian de orden ni forma; el parser sigue sin imports. Sin fixture de LLM. Cubre spec `propuesta-cambio-hitl` reqs. "`/ver-propuesta` es de solo lectura" (prerrequisito de parseo) y ADR 69 completo. Commit: `feat(core): agrega comandos ver-propuesta aplicar-propuesta y descartar-propuesta al parser (Hito 5.1, tarea 29)`.

30. **`src/core/activity/cadena-revision.ts`** (modificado, §5.8). Test primero: extiende `cadena-revision.test.ts` con doble de `InvocarSubagente` + puertos fake (`WorktreePort`, `PropuestaStorePort`) — orden `abrir → invocar → capturar → crear → cerrar` (spy ordenado); `cerrar` se llama **también** cuando `capturarDiff` tira (worktree se cierra igual, ADR 58 pto 3/ADR 70); el `material` del Reviewer sale de `obtenerPropuesta(id).patch`, **nunca** de la variable en memoria; `patchBytes > TAREA_DELEGADA_MAX_CHARS (8_000)` ⇒ evento `propuesta-revision-parcial` y el ciclo completa igual con un único veredicto; `escritura` ausente (parámetro opcional) ⇒ comportamiento **idéntico** al de `v2.0.0`, sin abrir worktree ni crear fila. **Fixture**: `InvocarSubagente` + puertos fake. Cubre spec `propuesta-cambio-hitl` req. "El Reviewer juzga el patch persistido, nunca el worktree vivo" (ambos escenarios) + delta `revision-pr-por-roles` reqs. "El material del Reviewer es el patch persistido..." y "El veredicto sigue siendo uno solo, del Reviewer, aun cuando ve una vista parcial del patch" (ambos escenarios de cada uno). ADR 59, ADR 70. Commit: `feat(core): agrega EscrituraDelegadaDeps al eslabon del Developer en cadena-revision (Hito 5.1, tarea 30)`.

31. **`src/build-on-comando-empleado.ts`** (modificado, §5.10, parte 1 — `ConfirmacionPendiente` + `/ver-propuesta`). Test primero: extiende `build-on-comando-empleado.test.ts` — `ConfirmacionPendiente` gana la tercera rama `{dominio:"propuesta", accion, propuestaId, casoId, patchBytes, empleadoId, expiraEn}`, preservando **una sola ranura** (ADR 55): armar una confirmación de propuesta pisa una de reembolso/solicitud pendiente, y viceversa; `manejarVerPropuesta` no toca ningún store de escritura, sin argumento lista pendientes acotado por `LIMITE_LISTADO_PROPUESTAS`, con argumento muestra resumen + patch paginado; `/login`/`/logout` purgan la ranura sin importar el dominio pendiente. **Fixture**: puertos fake (`PropuestaStorePort`). Cubre spec `propuesta-cambio-hitl` reqs. "`/ver-propuesta` es de solo lectura" (ambos escenarios) y "Ranura única `confirmacionPendiente` compartida entre los tres dominios HITL". ADR 55, ADR 60 pto 1 y 4. Commit: `feat(root): conecta ver-propuesta y ensancha ConfirmacionPendiente a tres dominios (Hito 5.1, tarea 31)`.

32. **`src/build-on-comando-empleado.ts`** (modificado, §5.10, parte 2 — `manejarResolucionPropuesta`). Test primero: extiende `build-on-comando-empleado.test.ts` — two-step de `/aplicar-propuesta`/`/descartar-propuesta`, calco de `manejarEscalacion`; **1er llamado = cero escrituras** (eco); 2do llamado ejecuta `git apply --check` (`AplicarPatchPort.verificar`) **antes** del CAS transaccional; `--check` que falla ⇒ evento `propuesta-conflicto`, **cero escrituras**, estado intacto (spec, escenario explícito); `--check` que pasa ⇒ transacción (tarea 15) **commit** → recién ahí `aplicarPatch.aplicar` (`git apply` real, fuera de la transacción); `aplicar` que falla **después** del commit ⇒ evento `propuesta-apply-fallido` + mensaje explícito al humano (RD-13 heredado, hecho visible); sin sesión vigente ambos comandos se rechazan sin listar ni aplicar. **Fixture**: puertos fake (`AplicarPatchPort`, `PropuestaStorePort`). Cubre spec `propuesta-cambio-hitl` reqs. "`/aplicar-propuesta` exige sesión... 'aplicar' termina en `git apply`" (los 4 escenarios), "'Aplicar' nunca commitea, pushea ni abre PR" (a nivel de wiring real) y "`/descartar-propuesta` exige sesión..." (ambos escenarios). ADR 64, ADR 65. Commit: `feat(root): conecta aplicar-propuesta y descartar-propuesta con secuencia check-transaccion-apply (Hito 5.1, tarea 32)`.

33. **`src/build-on-activity.ts`** (modificado, §7.3, ADR 61 pto 2). Test primero: extiende `build-on-activity.test.ts` — el interruptor `HARNESS_ESCRITURA_DELEGADA` bindea el `escritura` correcto (`"off"` ⇒ `undefined`, cualquier otro valor o ausente ⇒ `EscrituraDelegadaDeps` completo); con `escritura === undefined` **cero llamadas** a `worktree.abrir`/`construirDeveloperConEscritura`; `HARNESS_DELEGACION_ROLES=off` sigue degradando a `v1.4.0` sin cambios, los dos interruptores anidan. **Fixture**: doble de `worktree`/`invokeModel`. Cubre delta `delegacion-subagentes` escenario "Con el interruptor apagado, el Developer vuelve a ser exactamente el de `v2.0.0`". ADR 61 pto 2. Commit: `feat(root): conecta el interruptor HARNESS_ESCRITURA_DELEGADA en build-on-activity (Hito 5.1, tarea 33)`.

34. **`src/main.ts`** (modificado, §7.3, bloque 5d) — **TDD exception** (wiring puro de composition root, molde ya usado por `webhook.close()` líneas 425-434, sin lógica de negocio nueva que testear). Agrega el bloque `barrerHuerfanos` **no bloqueante** (`void ... .catch(...)`), después del bloque 5 (webhooks) y antes del bloque 6 (montar TUI), con `WORKTREE_LOG_CORRELATION_ID = "worktree"`. Cubre spec `escritura-aislada-worktree` req. "Barrido de huérfanos al arranque... no bloquea el arranque" (a nivel de wiring real). ADR 57 pto 7. Commit: `feat(root): agrega barrido de worktrees huerfanos no bloqueante al arranque (Hito 5.1, tarea 34)`.

### Documentación (tarea 35) y doc-comments (tarea 36)

35. **`README.md`** (raíz, modificado). Documenta el interruptor `HARNESS_ESCRITURA_DELEGADA` (`"off"` = comportamiento `v2.0.0`, cualquier otro valor o ausente = escritura delegada activa), las variables nuevas (`HARNESS_GIT_BIN`, `HARNESS_GIT_TIMEOUT_MS`, `HARNESS_WORKTREE_ROOT`, `HARNESS_WORKTREE_TTL_MS`, `HARNESS_WORKTREE_TEST_TIMEOUT_MS`), la tool `mcp__worktree__run_tests` y los tres comandos TUI nuevos (`/ver-propuesta`, `/aplicar-propuesta`, `/descartar-propuesta`), mismo formato que las entradas ya existentes. Sin test — documentación, no código de producción. Commit: `docs: documenta HARNESS_ESCRITURA_DELEGADA y los comandos de propuesta de cambio en el README (Hito 5.1, tarea 35)`.

36. **Doc-comments desactualizados por este hito** — **TDD exception** (solo comentarios, sin cambio de lógica ni de test). Dos puntos concretos, ambos ya tocados por sus tareas de código pero verificados acá como cierre: (a) `src/core/agents/definitions.ts` — el comentario heredado de `v2.0.0` que afirmaba "ningún rol tiene herramientas de escritura" sin matiz se actualiza para declarar la excepción acotada del `developer`+worktree (mismo criterio del ADR 46 que ya aplicó la tarea 2 de Hito 5); (b) `src/core/turn-selector/invoke-model.ts` — confirma que la nota de honestidad de `cwd` de la tarea 28 quedó en el doc-comment de `toQueryOptions`, no solo en el diseño. Commit: `docs(core): actualiza doc-comments de definitions.ts e invoke-model.ts sobre escritura y cwd (Hito 5.1, tarea 36)`.

### Verificación manual (tarea 37)

37. **Verificación manual end-to-end** (sin código de producción, cierre del hito). Cubre los 15 pasos de `design.md` §11:
    - `HARNESS_ESCRITURA_DELEGADA` sin definir + `HARNESS_DELEGACION_ROLES` activo, forwarding de un PR chico con un archivo nuevo y uno editado.
    - Durante la corrida: `ls .harness/worktrees/` muestra un directorio; después: cero (el `finally` cerró).
    - `SELECT` sobre `propuestas_cambio` ⇒ una fila `pendiente_aprobacion_humana` con contadores coherentes, incluyendo el archivo nuevo completo.
    - `git status` del checkout real **limpio** durante y después del ciclo (R14 hecho evidencia).
    - `git apply --check` a mano sobre el patch volcado de la base: pasa.
    - Log `tests-worktree-ok`/`tests-worktree-error` con `exitCode`/`durationMs` reales; repetir con la suite rota, verificar que el Developer recibió la **cola** con el resumen de fallas.
    - TUI: `/login` → `/ver-propuesta` → `/ver-propuesta <id>` → `/aplicar-propuesta <id>` (eco, cero escrituras verificado con `SELECT` antes/después) → confirmar (aplica) → `git status` sin stagear, `git log` sin commits nuevos.
    - `SELECT` sobre `registro_acciones_empleado` ⇒ fila con `comando='/aplicar-propuesta'`, `propuesta_id` correcto.
    - `/descartar-propuesta <id> <motivo>` sobre otra propuesta ⇒ `estado='descartada'`, motivo guardado, auditoría.
    - Conflicto (R13): modificar a mano un archivo que el patch toca, intentar aplicar ⇒ mensaje de conflicto, estado intacto, cero auditoría.
    - Barrido: worktree con mtime forzado a 3h, reinicio del arnés ⇒ desaparece; con mtime reciente ⇒ sobrevive.
    - Rollback: `HARNESS_ESCRITURA_DELEGADA=off`, mismo PR ⇒ cero worktrees, cero filas, mismo veredicto que `v2.0.0`, `/ayuda` sin los tres comandos.
    - R4: con un worktree abierto, `npm test` reporta el mismo conteo de archivos y tests que sin él.
    - Suite completa: `npm test` y `npm run typecheck` en verde; ningún test invoca el modelo real ni abre un puerto.
    - Evidencia para `docs/progreso/v2.1-escritura-delegada/`: el patch real, el eco del comando, `git status` antes/después, volcado de `propuestas_cambio`/`registro_acciones_empleado`, log estructurado.
    - Commit: no aplica (evidencia va a `docs/progreso/v2.1-escritura-delegada/` al cierre, según `AGENTS.md`).

## Después de la tarea 37

- Pasa al Reviewer (`sdd-verify` + `code-review`) contra este documento, los 4 specs y `design.md`, con atención especial a: RD-12 (máquina de tres estados, no cuatro — el test de la tarea 17 es la barrera), ADR 65 (el caso no transiciona de estado al aplicar — pregunta 1 abierta de `design.md` §15, el Reviewer puede escalarla si el checkpoint no la cerró), y ADR 62 (los diez constructores de argv como la garantía verificable de "nunca commit/push").
- Antes de dar la tarea 37 por completa, el Reviewer/checkpoint debe confirmar las 4 preguntas abiertas de `design.md` §15 (ADR 65, corte de PRs/chain strategy, ADR 69 `/ver-propuesta` privilegiado, `HARNESS_WORKTREE_ROOT`) — ninguna bloquea el código ya escrito, pero sí la lectura final del entregable.
- Si aprueba: checklist de cierre de hito en `AGENTS.md`, tag `v2.1.0`, carpeta `docs/progreso/v2.1-escritura-delegada/`, copia de los ADR 57-70 a `docs/ARC42_Harness_Empresarial.md`.
- Si el Reviewer objeta el relajamiento del invariante de seguridad (delta `delegacion-subagentes`) o RD-13 (sin transacción SQLite↔filesystem), la corrección es **reabrir el checkpoint humano sobre la propuesta/diseño**, no improvisar una mitigación en `sdd-apply` — ambas son decisiones ya fijadas y documentadas (`proposal.md` ADR 57/60, `design.md` §9/§13).

---

**Nota de formato**: la skill `sdd-tasks` sugiere un tope de 530 palabras. Se sigue deliberadamente el formato extenso de este repo (`hito-1.2`, `hito-1.3`, `tui-canal-empleado`, `hito-2.0`), tal como `design.md` de este mismo change documentó en su nota de cierre: `AGENTS.md` exige que el repositorio muestre el proceso de construcción paso a paso, con archivo objetivo, requirement de origen y comando exacto por tarea. El tope genérico de la skill cede ante la convención explícita del proyecto ya establecida por `exploration.md`, `proposal.md`, los 4 `specs/`, `design.md` y `hito-2.0-delegacion-subagentes/tasks.md`.
