> **Nota de proceso (hook de graphify)**: ejecutor sin herramienta de shell disponible (sólo `Read`/`Grep`/`Glob`/`Write`/`Edit`), misma limitación que `proposal.md` y `design.md` de este mismo change. Compensación: se releyeron completos `proposal.md` (341 líneas), `design.md` (267 líneas) y las cinco `specs/*/spec.md`; se verificó puntualmente con `Grep` sobre el código real: `src/build-on-comando-empleado.ts:1721,1756` (el `switch (comando.tipo)` es **exhaustivo** — confirmado por los propios comentarios `:44,66` del archivo, "vuelve a ser exhaustivo"), `src/core/commands/comando-empleado.ts:114-121` (las siete `Forma` vigentes, `"sin_argumentos"` incluida), `:314-317` (`/ayuda` ya usa `"sin_argumentos"`), `:430-433` (★ **hallazgo no documentado por `design.md`**: la rama genérica de `"sin_argumentos"` en `parsearComando` devuelve **hardcodeado** `{ tipo: "logout" }` con el comentario *"Solo /logout llega acá (aparte de /ayuda, ya resuelto arriba)"* — agregar `/estado-bot-prs` con esa misma `Forma` sin tocar esta rama produciría silenciosamente `{ tipo: "logout" }` en vez de `{ tipo: "estado_bot_prs" }`, un bug de ruteo. Se agrega como corrección explícita en la Tarea 3, punto obligatorio) y `src/core/commands/registro-acciones-contract.ts:8-38` (confirmando que `RESULTADO_NO_AUTORIZADO` de `autorizacion-empleado`/ADR 161 **no existe todavía** en `main`, consistente con la dependencia dura). Se recomienda `graphify update .` una vez persistido este archivo.

# Tasks: Comandos de administración de empleados en la TUI — `/crear-empleado`, `/asignar-rol`, `/estado-bot-prs`

**Origen** (no se duplica): [`proposal.md`](proposal.md) (ADR 174-179, R1-R10, RD-80 a RD-84, Success Criteria, checkpoint resuelto) · [`design.md`](design.md) §1-11 (ADR 180-185, resolviendo RD-82/83/84) · specs: [`administracion-empleados-tui`](specs/administracion-empleados-tui/spec.md) (nueva, 7 requirements) · [`comando-empleado-tui`](specs/comando-empleado-tui/spec.md) (delta, 2 MODIFIED + 1 ADDED) · [`autorizacion-empleado`](specs/autorizacion-empleado/spec.md) (delta, 2 MODIFIED) · [`autenticacion-empleado-tui`](specs/autenticacion-empleado-tui/spec.md) (delta, Purpose MODIFIED + 1 ADDED) · [`registro-acciones-empleado`](specs/registro-acciones-empleado/spec.md) (delta, 2 ADDED).

**No es un Hito numerado del Plan** — carpeta descriptiva, rama `hito/<version>-comandos-administracion-empleados` (versión a fijar por el checkpoint).

---

## ★★★ Dependencia dura de secuencia — LÉASE ANTES DE EJECUTAR CUALQUIER TAREA DE LA PR2/PR3

Verificado por `Glob` en `design.md:7` y reverificado acá: `src/core/auth/` hoy (al momento de escribir este documento) tiene `auth-config.ts`, `credenciales-contract.ts`, `login.ts`, `sesion.ts`. **`rol-contract.ts` y `autorizacion-resolucion.ts` NO existen en `main`.** Ambos son artefactos de `autorizacion-empleado` (ADR 157-162), cuyo `design.md` está completo pero **no mergeado**.

**Orden de merge acordado** (tercero de cuatro): `autorizacion-empleado` → `operaciones-negocio-conversacionales` → **`comandos-administracion-empleados` (este change)** → `consultas-negocio-a2a-entrante`.

Las tareas de este documento se dividen en dos pistas:

- **Pista A (tareas 1-4, PR1)** — **NO bloqueada.** No toca ni importa de `rol-contract.ts` ni de `autorizacion-resolucion.ts`. Puede implementarse y mergearse hoy mismo, en paralelo con el diseño/implementación de `autorizacion-empleado`.
- **Pista B (tareas 5-12, PR2 y PR3)** — **BLOQUEADA.** Cada tarea de la Pista B queda marcada `[BLOQUEADA]` explícitamente. **No pueden empezarse** hasta que `autorizacion-empleado` esté mergeado en `main` (o, como mínimo, hasta que la rama de este change incorpore ese merge — ver *Rollout / Branching* más abajo).

**R7 — conflicto mecánico de `DESCRIPTORES` con `operaciones-negocio-conversacionales`**: ese change **quita** tres entradas (`/devolucion`, `/solicitar`, `/cancelar-solicitud`) mientras éste **agrega** tres (`/crear-empleado`, `/asignar-rol`, `/estado-bot-prs`). Este documento **asume que `operaciones-negocio-conversacionales` ya mergeó** (orden fijado arriba): las tareas 2 y 3 parten de **quince** descriptores y el total final de la Pista B es **dieciocho**. **Quien ejecute `sdd-apply` DEBE verificar el largo real de `DESCRIPTORES` en `main` en el momento de la tarea** (`rg -c 'nombre:' src/core/commands/comando-empleado.ts` o el test `toHaveLength` existente) antes de escribir cualquier comentario de conteo — si `operaciones-negocio-conversacionales` no mergeó todavía, el conteo real es 18 → 21, no 15 → 18, y los comentarios de este documento y de `comando-empleado.ts` deben ajustarse en consecuencia, no copiarse a ciegas.

---

## Metodología

**TDD estricto** (`strict_tdd: true`, `npm test` = `vitest run`) en toda tarea con lógica de negocio (1-3, 5-10). **Excepciones explícitas**: tarea 4 y tarea 11 (specs ya escritas por `sdd-spec` + documentación, se commitean sin implementación propia), tarea 12 (verificación manual, sin código de producción).

**Orden de implementación de la Pista B, heredado literal del `Approach` de `proposal.md`** (no es arbitrario — la 1 es prerrequisito de seguridad de la 3): primero el gate de `administrador` (tarea 5), después el escritor de rol (tarea 6), recién ahí los dos comandos que el gate protege (tareas 7-8), al final los criterios de cierre y la documentación (10-12). **El test rojo inicial de todo el change** (tarea 5) es *"un empleado con rol base intenta ejecutar un comando administrativo y lo logra"* — evaluado con un **comando de prueba sintético** (`proposal.md` Approach, punto 1: *"Entregable y testeable solo, contra un comando de prueba, antes de que exista ningún comando administrativo real"*), porque `/asignar-rol` todavía no existe en la tarea 5.

---

## Suggested Work Units (feature-branch-chain)

| PR | Base branch | Tareas | Bloqueo | Contenido |
|---|---|---|---|---|
| **PR1** | `hito/<version>-comandos-administracion-empleados` (forkeada de `main`, hoy) | 1-4 | **Ninguno** | `altaCredencialEmpleado` compartida, campo `requiereAdministrador` (scaffolding, sin consumidor todavía), `/estado-bot-prs` completo (descriptor + fix del bug de ruteo + handler), specs/docs parciales |
| **PR2** | rama de PR1 | 5-6 | **`autorizacion-empleado` mergeado en `main`** | Gate de `administrador` en el dispatcher (probado con comando sintético) + `RolEmpleadoEscritorPort` + wiring de `rolEscritor` |
| **PR3** | rama de PR2 | 7-12 | PR2 mergeada en la cadena | `/asignar-rol`, `/crear-empleado`, comentario de `credenciales-contract.ts`, los Success Criteria de cierre, specs finales, arc42/README, verificación manual |

**Sólo el tracker (`hito/<version>-comandos-administracion-empleados`) mergea a `main`** al final de la cadena, cuando PR3 esté aprobada — criterio `feature-branch-chain` fijado para este change.

**Paso obligatorio entre PR1 y PR2, no negociable**: una vez que `autorizacion-empleado` mergee a `main`, la rama del tracker (base de PR1) debe **actualizarse con `main`** (merge o rebase, a elección del ejecutor) **antes** de forkear la rama de PR2 — es el único modo de que `import { ... } from "../core/auth/rol-contract"` y `"../core/auth/autorizacion-resolucion"` resuelvan a archivos que existen.

---

## Tareas

### PR1 — Pista A: independiente de `autorizacion-empleado` (tareas 1-4)

1. [x] **`src/empleados.ts`** (modificado, ADR 181/RD-82) — extraer `altaCredencialEmpleado(db, {empleadoId, password, ahora})` como función pura-de-efectos-acotados **exportada** (molde exacto de `design.md` §3: valida `ID_REGEX` → valida password no vacía → `insertCredencialEmpleado` con `hashPassword` → captura `CredencialEmpleadoDuplicadaError` → `{ok:false, mensaje}` / `{ok:true}`). **`ID_REGEX` pasa a exportarse** (era privada). `main()`'s rama `"alta"` pasa a **llamar** a `altaCredencialEmpleado` en vez de repetir hash+insert+catch — cero duplicación. **No se comparte** `leerPasswordDeStdin()` ni el `process.exit` (I/O de terminal, no aplica a la TUI). Test primero, en `empleados.test.ts`: alta exitosa; `empleadoId` inválido ⇒ `{ok:false}` con el mismo mensaje que hoy; password vacía ⇒ `{ok:false}`; duplicado ⇒ mismo mensaje que hoy; `main()` modo `"alta"` delega en la función (test de no-regresión, mismo molde que ya usa `empleados.test.ts` para importar sin disparar `main()` vía el guard `isMainModule`). No cubre ningún requirement de spec todavía (la función es infraestructura compartida; la cobertura de spec llega en la tarea 8, cuando `/crear-empleado` la consume). Comando: `npm test -- empleados`. Commit: `refactor(empleados): extrae altaCredencialEmpleado como funcion compartida exportada (comandos-administracion-empleados, tarea 1)`.

2. [x] **`src/core/commands/comando-empleado.ts`** (modificado, ADR 183 parte 1/RD-84) — agrega el campo `readonly requiereAdministrador: boolean` a `DescriptorComando`/`DescriptorInterno`, con **cada descriptor existente declarando `false`** (verificar el conteo real en `main` — ver banner de arriba), y la función de acceso `requiereAdministrador(tipo: ComandoEmpleado["tipo"]): boolean` (molde exacto de `esComandoPrivilegiado`/`esComandoSecreto`: `DESCRIPTORES.find((d) => d.tipo === tipo)?.requiereAdministrador ?? false`). **Sin consumidor todavía** — es scaffolding puro, el dispatcher no la llama hasta la tarea 5. Test primero: cada descriptor existente tiene `requiereAdministrador === false`; `requiereAdministrador("logout")` (o cualquier tipo existente) devuelve `false`; un tipo inexistente devuelve `false` (el `?? false`). No cubre ningún requirement de spec en solitario — es prerrequisito estructural del requirement "El gate de `administrador` vive en un módulo de política compartido" (`administracion-empleados-tui/spec.md`), que se termina de cubrir en la tarea 5. Comando: `npm test -- comando-empleado`. Commit: `feat(core): agrega campo requiereAdministrador a DescriptorComando sin consumidor aun (comandos-administracion-empleados, tarea 2)`.

3. [x] **`src/core/commands/comando-empleado.ts`** + **`src/build-on-comando-empleado.ts`** (modificados, ADR 185) — descriptor `/estado-bot-prs` (`uso: "/estado-bot-prs"`, `privilegiado: true`, `secreto: false`, `requiereAdministrador: false`, `forma: "sin_argumentos"`, tipo `"estado_bot_prs"`, insertado **antes de `/ayuda`**, regla `:134-136`), sin `Forma` nueva (reusa `"sin_argumentos"`). ★ **Corrección obligatoria, hallazgo del proceso de esta tarea**: la rama genérica de `parsearComando` para `forma === "sin_argumentos"` (`:430-433`) devuelve hoy **hardcodeado** `{ tipo: "logout" }` con el comentario *"Solo /logout llega acá (aparte de /ayuda, ya resuelto arriba)"*. Con un segundo comando real usando esa `Forma`, **hay que cambiar esa rama para devolver `{ tipo: descriptor.tipo }`** (genérico) en vez del literal `"logout"` — de lo contrario `/estado-bot-prs` rutea silenciosamente a `logout`. `manejarEstadoBotPrs()` en `build-on-comando-empleado.ts` (molde `design.md` §7): `resolveWebhookConfig()` + `isWebhookEnabled()` + `resolveBoardConfig()` + `isBoardEnabled()` (los cuatro **ya existen**, sin puerto nuevo), respuesta de solo texto (puerto/path si escucha, presencia booleana de `GITHUB_TOKEN`, **nunca el valor**), **sin `registrar()`** (mismo criterio que `/reporte-comisiones`/`/consultar-kpi`, comandos de solo lectura). `case "estado_bot_prs"` en el `switch (comando.tipo)` exhaustivo (`:1721`) — **obligatorio para que el `switch` siga compilando**, no es opcional diferirlo. Depende de la tarea 2 (campo `requiereAdministrador`). Test primero: `parsearComando("/estado-bot-prs")` devuelve `{tipo:"estado_bot_prs"}` (**no** `{tipo:"logout"}` — el test que blinda la corrección del bug de ruteo); `/logout` sigue devolviendo `{tipo:"logout"}` sin regresión; listener habilitado ⇒ respuesta con puerto/path; listener deshabilitado ⇒ `"deshabilitado (sin GITHUB_WEBHOOK_SECRET)"`; `GITHUB_TOKEN` presente/ausente ⇒ `"presente"`/`"ausente"`, **nunca el valor** en el `responseText`; sin sesión ⇒ rechazo por `privilegiado` (mecanismo existente, sin gate de rol); rol base con sesión ⇒ responde igual que un `administrador` (no gateado); **cero** filas nuevas en `registro_acciones_empleado`. Cubre spec `administracion-empleados-tui` requirement "`/estado-bot-prs` es de solo lectura y nunca revela un secreto" (2 escenarios) y, parcialmente, el requirement MODIFIED de `comando-empleado-tui` sobre reconocimiento de comandos (el tercio de `/estado-bot-prs`; los otros dos tercios llegan con las tareas 7-8). Comando: `npm test -- comando-empleado build-on-comando-empleado && npm run typecheck`. Commit: `feat(root): agrega comando /estado-bot-prs de solo lectura y corrige ruteo hardcodeado de sin_argumentos (comandos-administracion-empleados, tarea 3)`.

4. [x] **`openspec/changes/comandos-administracion-empleados/specs/administracion-empleados-tui/spec.md`** (parcial — sólo el requirement "`/estado-bot-prs`...") **+ `specs/comando-empleado-tui/spec.md`** (parcial — la mención de `/estado-bot-prs` dentro del requirement MODIFIED de reconocimiento; **el conteo final de dieciocho queda como aspiracional hasta la PR3**, se anota así en el commit) **+ `README.md`/`docs/ARC42_*.md`** (mención de `/estado-bot-prs` en el Registro de Comandos). TDD exception (specs ya escritas por `sdd-spec`, documentación). **No se commitean todavía** `specs/autorizacion-empleado/spec.md`, `specs/autenticacion-empleado-tui/spec.md` ni `specs/registro-acciones-empleado/spec.md` completos — describen comportamiento de `/crear-empleado`/`/asignar-rol` que no existe hasta la PR3 (llegan en la tarea 11). Commit: `docs: documenta /estado-bot-prs en specs y arc42/README (comandos-administracion-empleados, tarea 4)`.
   ★ **Nota de ejecución (sdd-apply, tarea 4)**: `specs/administracion-empleados-tui/spec.md` y `specs/comando-empleado-tui/spec.md` (delta) YA estaban en el árbol de trabajo como borradores COMPLETOS (los 7 requirements de la primera, el delta íntegro con "dieciocho" de la segunda) al momento de ejecutar esta tarea — no fueron creados por esta tarea, ya existían sin commitear. Se verificó su contenido y YA documentan `/estado-bot-prs` correctamente (mismo `privilegiado`/`requiereAdministrador`/sin-`registrar()` que el código de la tarea 3). **No se recortaron a "solo el requirement de `/estado-bot-prs`"**: hacerlo habría destruido contenido ya escrito de `/crear-empleado`/`/asignar-rol` (PR2/PR3) sin manera de recuperarlo salvo re-generarlo en la tarea 11 — se prefirió no perder ese trabajo en vez de improvisar un recorte no reversible. Decisión de qué hunk de estos archivos entra en el commit de PR1 vs. se difiere queda para quien arme el commit (`git add -p`, tarea humana). `README.md`/`docs/ARC42_Harness_Empresarial.md` SÍ se editaron acá con la mención parcial y honesta (dieciséis, no dieciocho).

**Verificación de cierre de PR1**: `npm test && npm run typecheck` en verde; `git diff --stat main -- src/core/auth/` **vacío** (ninguna tarea de esta PR toca autenticación/autorización); `rg 'requiereAdministrador' src/build-on-comando-empleado.ts` **sin resultados** (el dispatcher todavía no lo consulta — eso es la tarea 5); `/crear-empleado` y `/asignar-rol` siguen sin existir (`rg 'crear_empleado|asignar_rol' src/core/commands/comando-empleado.ts` sin resultados).

### PR2 — Pista B, parte 1: gate + escritor de rol (tareas 5-6) — **[BLOQUEADA]**

5. [x] **`src/core/auth/autorizacion-resolucion.ts`** (modificado, de `autorizacion-empleado`, ADR 183 parte 2/RD-84) + **`src/build-on-comando-empleado.ts`** (modificado, gate genérico, ADR 175/183) — `esAdministrador(rolPort: RolEmpleadoPort, empleadoId: string): boolean` (`rolPort.buscarRol(empleadoId) ?? ROL_EMPLEADO === ROL_ADMINISTRADOR`, nombre **distinto** de `puedeResolverAjeno` a propósito — política distinta aunque hoy coincida en implementación). Paso 6.5 del dispatcher, **después** del gate de sesión existente (`:1714-1718`, sin cambios) y **antes** del `switch`: `if (requiereAdministrador(comando.tipo)) { if (!esAdministrador(rolPort, empleadoId)) { registrar(...RESULTADO_NO_AUTORIZADO...); return sistema(...); } }`, reusando `RESULTADO_NO_AUTORIZADO` de `autorizacion-empleado` (ADR 161, **ya disponible en `main`** porque esta PR está bloqueada hasta que ese change mergee). **Test rojo inicial de todo el change** (Approach punto 1 de `proposal.md`): con un comando de prueba sintético (`requiereAdministrador: true` fabricado en el test, sin tocar `DESCRIPTORES` real todavía), un empleado con rol base es rechazado y uno con `administrador` pasa — **entregable y testeable en solitario, sin que exista ningún comando administrativo real**. Depende de la tarea 2 (campo/función `requiereAdministrador`). Test primero, además del rojo inicial: `esAdministrador` con rol `administrador` explícito ⇒ `true`; con rol `empleado`/ausencia (`undefined ?? ROL_EMPLEADO`) ⇒ `false`; el gate se evalúa **después** de la guarda de sesión (sin sesión, el rechazo es por sesión, no por rol — el rol nunca se consulta). Cubre spec `administracion-empleados-tui` requirement "El gate de `administrador` vive en un módulo de política compartido, no duplicado en el dispatcher" (2 escenarios) y spec `comando-empleado-tui` delta requirement ADDED "El dispatcher evalúa un segundo eje de gateo" (2 escenarios). Comando: `npm test -- autorizacion-resolucion build-on-comando-empleado && npm run typecheck`. Commit: `feat(core): agrega esAdministrador y el gate de segundo eje en el dispatcher, probado con comando sintetico (comandos-administracion-empleados, tarea 5)`.

6. [x] **`src/core/auth/rol-contract.ts`** (modificado, de `autorizacion-empleado`, ADR 180/RD-82) + **`src/build-on-comando-empleado.ts`** (wiring, ADR 180) — `RolEmpleadoEscritorPort { asignarRol(input: {empleadoId, rol, ahora}): void }`, co-ubicado con `RolEmpleadoPort` (lectura, **sin tocar**, ADR 176 pto 1) en el mismo archivo — dos interfaces, un archivo, criterio del ADR 180. Wiring en `Deps`/composición: `const rolEscritor: RolEmpleadoEscritorPort = deps.rolEscritor ?? { asignarRol: (input) => { upsertRolEmpleado(db, input); } }` (molde exacto de `credenciales`/`rolPort` en `:849-865`; `Deps.rolEscritor` **opcional**, no rompe ningún fake existente). `upsertRolEmpleado` (ADR 162) **no cambia de firma**. Depende de que `rol-contract.ts` ya exista (dependencia dura). Test primero: `rol-contract.test.ts` — `RolEmpleadoEscritorPort` es una interfaz de una sola operación (test de forma, molde `*-contract.test.ts`); el adaptador por defecto de `rolEscritor` llama a `upsertRolEmpleado` con los tres campos exactos, verificado con un `db` real de SQLite en memoria. **`RolEmpleadoPort` de lectura no cambia** — `git diff --stat main -- src/core/auth/rol-contract.ts` muestra sólo una interfaz agregada, ninguna línea de la interfaz existente tocada. Cubre spec `autorizacion-empleado` delta requirement MODIFIED "Provisioning del rol por CLI o por `/asignar-rol` bajo gate de administrador" (mitad de infraestructura — el escenario completo de escritura llega con la tarea 7). Comando: `npm test -- rol-contract build-on-comando-empleado && npm run typecheck`. Commit: `feat(core): agrega RolEmpleadoEscritorPort co-ubicado en rol-contract.ts y wiring de rolEscritor (comandos-administracion-empleados, tarea 6)`.
   ★ **Nota de ejecución (sdd-apply, tareas 5-6)**: dos desvíos menores del molde literal de `design.md`, ambos motivados por la exigencia de TDD estricto de `tasks.md` ("test primero", sin consumidor real disponible todavía) y ninguno cambia el contrato observable:
   1. **Tarea 5 — "comando sintético"**: en vez de fabricar un `ComandoEmpleado` con un tipo inventado (imposible sin tocar `DESCRIPTORES`, prohibido por la tarea), se mockeó SOLO la función `requiereAdministrador` de `comando-empleado.js` (vía `vi.mock` + `importOriginal`, todo lo demás del módulo real) para fabricar, únicamente dentro de ese describe, que el tipo YA EXISTENTE `reporte_comisiones` "requiere administrador". Cubre exactamente lo pedido (rol base rechazado / administrador pasa / sin sesión el rol no se consulta) sin tocar `DESCRIPTORES` real ni el resto de las suites del archivo (default del mock `() => false`, idéntico al comportamiento real de hoy).
   2. **Tarea 6 — `rolEscritor` por defecto**: en vez de dejarlo inline (molde literal de `credenciales`/`rolPort`, no testeable sin consumidor), se extrajo a una función exportada `createRolEmpleadoEscritor(db)`, MISMO molde ya establecido en este archivo por `createSolicitudStore`/`createSolicitudA2AEntranteStore` — permite probar el adaptador por defecto contra un `db` real de SQLite en memoria (exigido explícitamente por `tasks.md`) sin esperar a `/asignar-rol` (PR3). El wiring en `Deps`/composición (`deps.rolEscritor ?? createRolEmpleadoEscritor(db)`) sigue siendo la misma costura opcional de un renglón.

**Verificación de cierre de PR2**: `npm test && npm run typecheck` en verde; `git diff --stat main -- src/core/auth/rol-contract.ts` muestra **sólo** la interfaz nueva; `git diff --stat main -- src/core/auth/sesion.ts` **vacío**; ningún `CREATE TABLE`, ninguna migración, ninguna constante `ROLES_*` nueva (`git diff --stat` sobre `src/adapters/memory/migrations/`); `/crear-empleado` y `/asignar-rol` **siguen sin existir** — el gate y el escritor están probados contra comandos sintéticos/directos, no contra comandos reales todavía (eso es exactamente lo que el Approach de `proposal.md` pide: gate y escritor primero, comandos reales después).

### PR3 — Pista B, parte 2: `/asignar-rol`, `/crear-empleado`, cierre (tareas 7-12) — **[BLOQUEADA]**

7. [x] **[BLOQUEADA — depende de las tareas 5 y 6]** **`src/core/commands/comando-empleado.ts`** + **`src/build-on-comando-empleado.ts`** (modificados, ADR 175 pto 4/182, ADR 184) — brazo `{tipo:"asignar_rol"; empleadoId:string; rol:string}`, `Forma` **"id_mas_resto_rol"** (patrón `id_mas_resto`, clave de payload `rol`, molde exacto de `/login`), descriptor `/asignar-rol <empleadoId> <rol>` (`privilegiado:true`, `secreto:false`, `requiereAdministrador:true`, insertado antes de `/ayuda`); rama de `parsearComando` para `"id_mas_resto_rol"` (`ID_REGEX` sobre `empleadoId`, **`rol` sin validar en el parser** — ADR 177 pto 2, se valida contra `ROLES_EMPLEADO` en el handler); `manejarAsignarRol` — valida `rol` contra `ROLES_EMPLEADO`, valida que `empleadoId` tenga credencial (`buscarCredencialEmpleado`, si no existe ⇒ error sin fila huérfana), ★ **chequeo de auto-degradación (ADR 182, prohibición absoluta, sin conteo)**: `if (comando.empleadoId === sesion.empleadoId && comando.rol !== ROL_ADMINISTRADOR) { registrar(...RESULTADO_AUTODEGRADACION_PROHIBIDA...); return sistema(...); }` — **antes** de llamar a `rolEscritor.asignarRol`; éxito ⇒ `rolEscritor.asignarRol({empleadoId, rol, ahora})` + `registrar(...RESULTADO_EXITOSA...)`; `case "asignar_rol"` en el `switch` exhaustivo. `registro-acciones-contract.ts`: + `COMANDO_ASIGNAR_ROL = "/asignar-rol"`, + `RESULTADO_AUTODEGRADACION_PROHIBIDA = "autodegradacion_prohibida"` (molde de las constantes existentes). Depende de las tareas 5 (gate) y 6 (escritor). Test primero: administrador asciende a otro empleado ⇒ `ana` queda `administrador`, escrito por `upsertRolEmpleado`, fila `exitosa`; asignar rol a `empleadoId` sin credencial ⇒ falla, sin fila huérfana en `roles_empleado`; rol base intenta `/asignar-rol ana administrador` ⇒ rechazado por el gate (tarea 5), sin cambio en `roles_empleado`, fila `no_autorizado`; **único administrador intenta degradarse a sí mismo** ⇒ rechazado, rol sigue `administrador`, fila `autodegradacion_prohibida`; **con dos administradores, uno degrada al otro** ⇒ éxito sin restricción; quitarle el rol a un administrador **tiene efecto sin re-login** (sesión sin `expiraEn`, `SESION_TTL_MINUTOS=0`, criterio heredado del ADR 154 pto 1). Cubre spec `administracion-empleados-tui` requirements "`/asignar-rol` escribe rol llamando al mecanismo existente" (2 escenarios), "Un empleado sin rol `administrador` es rechazado..." (parte de `/asignar-rol`), "El último `administrador` no puede degradarse a sí mismo" (2 escenarios); spec `autorizacion-empleado` delta ambos requirements MODIFIED (escenarios de "un administrador existente sí puede asignar rol", "el rol no es parámetro salvo en `/asignar-rol`"); spec `registro-acciones-empleado` delta ambos requirements ADDED (fila exitosa y fila de rechazo por auto-degradación). Comando: `npm test -- comando-empleado build-on-comando-empleado registro-acciones-contract && npm run typecheck`. Commit: `feat(root): agrega /asignar-rol con gate de administrador y proteccion de auto-degradacion (comandos-administracion-empleados, tarea 7)`.

8. [x] **[BLOQUEADA — depende de la tarea 5 y de la tarea 1 (PR1)]** **`src/core/commands/comando-empleado.ts`** + **`src/build-on-comando-empleado.ts`** (modificados, ADR 174, ADR 184) — brazo `{tipo:"crear_empleado"; empleadoId:string; password:string}`, `Forma` **"id_mas_resto_password"** (misma técnica que `"id_mas_resto_rol"`, clave de payload `password`), descriptor `/crear-empleado <empleadoId> <password>` (`privilegiado:true`, **`secreto:true`** — segundo del repo tras `/login`, `requiereAdministrador:true`, antes de `/ayuda`); rama de `parsearComando` para `"id_mas_resto_password"`; `manejarCrearEmpleado` — llama a `altaCredencialEmpleado(db, {empleadoId, password, ahora})` (tarea 1) → éxito: `registrar(...RESULTADO_EXITOSA..., empleadoId objetivo)`; duplicado/inválido: `registrar(...RESULTADO_FALLIDA...)` sin escribir; `case "crear_empleado"` en el `switch`. `registro-acciones-contract.ts`: + `COMANDO_CREAR_EMPLEADO = "/crear-empleado"`. Depende de las tareas 1 y 5. Test primero: administrador da de alta un empleado nuevo ⇒ fila con `password_hash` scrypt, distinto del texto en claro; alta duplicada ⇒ rechazo, `password_hash` de la fila existente sin cambio; rol base ejecuta `/crear-empleado` ⇒ rechazado por el gate (tarea 5), **ninguna** fila en `credenciales_empleado`, fila de auditoría con `resultado` de rechazo por autorización; ★ **invariante de contraseña, extendido del molde `build-on-comando-empleado.test.ts:524`**: la contraseña tipeada **nunca** aparece en ninguna fila de `registro_acciones_empleado` ni en ningún evento de `logTurnEvent` emitido durante el comando — ni completa, ni como prefijo, ni su longitud. Cubre spec `administracion-empleados-tui` requirements "`/crear-empleado` da de alta una credencial..." (2 escenarios), "Un empleado sin rol `administrador` es rechazado..." (parte de `/crear-empleado`); spec `autenticacion-empleado-tui` delta ADDED requirement "Alta de credencial desde la TUI extiende, sin relajar, el invariante de no persistencia en claro" (2 escenarios); spec `registro-acciones-empleado` delta ADDED requirement de fila exitosa con ambos empleados identificables. Comando: `npm test -- comando-empleado build-on-comando-empleado empleados && npm run typecheck`. Commit: `feat(root): agrega /crear-empleado reusando altaCredencialEmpleado bajo gate de administrador (comandos-administracion-empleados, tarea 8)`.

9. [x] **[BLOQUEADA — depende de la tarea 8]** **`src/core/auth/credenciales-contract.ts`** (comentario modificado, ADR 174) — el comentario de `:14-17` (*"la TUI no puede crear credenciales, y eso es una propiedad del diseño, no un olvido"*) se reescribe apuntando a **ADR 174**, dejando explícito que la propiedad fue revertida a pedido del stakeholder y qué invariante sobrevive (no persistencia en claro). **`CredencialesEmpleadoPort` (interfaz) no se toca** — sólo el comentario. TDD exception parcial: no hay comportamiento que testear en el comentario mismo, pero se verifica con un test de "grep de contrato" si el repo ya usa ese patrón (si no, el criterio de cierre es lectura manual). Depende de la tarea 8 (recién ahí la afirmación original es definitivamente falsa). Cubre Success Criterion de `proposal.md`: *"El comentario de `credenciales-contract.ts:14-17` fue actualizado y apunta al ADR 174"*. Commit: `docs(core/auth): actualiza comentario de credenciales-contract.ts para apuntar a ADR 174 (comandos-administracion-empleados, tarea 9)`.

10. [x] **[BLOQUEADA — depende de las tareas 7 y 8]** **Success Criteria de cierre, sin archivo de producción propio** (extensión de los tests ya escritos en 5-8, más aserciones de `git diff` a nivel de suite) — TDD estricto aplica a las aserciones de comportamiento, no a las de `git diff` (esas son estáticas). Cubre, explícitamente, los Success Criteria de `proposal.md` que ninguna tarea anterior deja cerrados en solitario:
   - `git diff` demuestra **cero** migraciones nuevas, **cero** `CREATE TABLE`, **cero** constante de rol nueva — `ROLES_EMPLEADO` sigue con exactamente dos miembros (R4).
   - `git diff` demuestra que `src/core/auth/sesion.ts` **no** fue modificado, que `privilegiado` **no** fue resignificado, y que **ningún** descriptor existente cambió de orden ni de forma (comparar `DESCRIPTORES` antes/después de las tareas 3, 7, 8 — sólo `/crear-empleado`, `/asignar-rol`, `/estado-bot-prs` cambian de posición, y sólo para insertarse antes de `/ayuda`) (R5, ADR 177 pto 5).
   - `git diff` demuestra que `src/adapters/webhooks/`, `src/adapters/board/` y `src/core/config/env.ts` **no** fueron tocados (verificando que el diferido del ADR 178 se respetó pese a agregar `/estado-bot-prs`).
   - `DESCRIPTORES` queda en **dieciocho** entradas (verificar contra el conteo real de `main` — ver banner de arriba), con los tres nuevos antes de `/ayuda`, que sigue último.
   - Todo rechazo por autorización (gate de rol, auto-degradación) deja fila distinguible en `registro_acciones_empleado`, sin ningún método de puerto nuevo.
   Comando: `npm test && npm run typecheck`. Commit: `test: cierra los Success Criteria de git-diff e invariantes negativos (comandos-administracion-empleados, tarea 10)`.

11. [x] **[BLOQUEADA — depende de la tarea 10]** **`specs/administracion-empleados-tui/spec.md`** (commit final, íntegro) **+ `specs/comando-empleado-tui/spec.md`** (delta final, conteo confirmado en dieciocho) **+ `specs/autorizacion-empleado/spec.md`** (delta final) **+ `specs/autenticacion-empleado-tui/spec.md`** (delta final) **+ `specs/registro-acciones-empleado/spec.md`** (delta final) **+ `README.md`/`docs/ARC42_*.md`** (reparto de canales completo — chat vs. TUI; ADR 174 con su motivo; R1 documentado como residual aceptado con **procedimiento de rotación por CLI obligatorio** tras cada alta; ADR 178 y 179 como deuda con nombre, cada uno con su motivo verificado y condición de disparo — mismo tratamiento que `autorizacion-empleado` le dio a su R7). TDD exception (specs + documentación). Commit: `docs: commitea specs finales y documenta reparto de canales, ADR 174/178/179 en arc42 (comandos-administracion-empleados, tarea 11)`.
    ★ **Nota de ejecución (sdd-apply, tarea 11)**: las cinco `specs/*/spec.md` ya estaban completas y correctas desde la tarea 4 (ver su nota de ejecución) — se releyeron íntegras y se verificaron contra el código real de las tareas 7-8 (gate, auto-degradación, invariante de contraseña, gate de `/estado-bot-prs`): coinciden sin necesidad de edición. Se agregó una sección nueva "Administración de empleados desde la TUI" en `README.md` (reemplaza la nota parcial "PR1"), y en `docs/ARC42_Harness_Empresarial.md`: la nota de "Registro de Comandos" se actualizó a estado final (tres comandos, no uno), se agregó **Concepto Transversal 8** (segundo eje de gateo, auto-degradación sin conteo, alta compartida, ADR 174), **Riesgo 5 (R1)** con el procedimiento de rotación por CLI obligatorio, y **Deuda 7 (ADR 178)**/**Deuda 8 (ADR 179)** con su condición de disparo.

12. [x] **[BLOQUEADA — depende de la tarea 11]** **Verificación manual del entregable** — TDD exception (sin código de producción). Cubre, como mínimo:
    - Flujo completo: `administrador` bootstrap por CLI (`empleados:crear -- admin --rol administrador`) → login en la TUI → `/crear-empleado ana <password>` → `/asignar-rol ana administrador` → `ana` puede ejecutar `/aprobar-reembolso` (verificando que el rol asignado por TUI surte el mismo efecto que uno asignado por CLI, sin re-login).
    - Un rol base intentando cada uno de los tres comandos administrativos ⇒ los dos gateados rechazan, `/estado-bot-prs` responde igual que a un administrador.
    - `/estado-bot-prs` con y sin `GITHUB_WEBHOOK_SECRET`/`GITHUB_TOKEN` configurados ⇒ nunca imprime el valor.
    - Rollback del commit de la tarea 7 (o 8) ⇒ el comando desaparece de `DESCRIPTORES` y de `/ayuda`, cae en `ayudaDesconocido`; ningún dato creado antes del rollback queda inválido (credenciales y roles sobreviven, mismos que hubiera escrito el CLI).
    - `npm test`/`npm run typecheck` en verde sobre el estado final de las tres PRs encadenadas.
    Evidencia en `docs/progreso/<version>-comandos-administracion-empleados/`. Commit: `docs: evidencia de verificacion manual del entregable de administracion de empleados (comandos-administracion-empleados, tarea 12)`.
    ★ **Nota de ejecución (tarea 12)**: versión fijada en `v3.7`. Puntos 1-3 y 5 verificados de forma automatizada y reproducible (test de integración de flujo real, tests unitarios existentes, `npm test`/`npm run typecheck`); el punto de rollback (4) verificado con un experimento real de `git reset --hard` sobre una rama descartable, posible recién después de separar los commits `f15f818`/`99ce203` (hallazgo de `code-review`, ver commit `ded56b2`). La sesión TUI interactiva real (Ink, requiere TTY — fuera del alcance de las herramientas del agente) fue corrida a mano por el humano (JimmyFung123) el 2026-09-13, confirmando funcionamiento correcto. Evidencia completa: [`docs/progreso/v3.7-comandos-administracion-empleados/verificacion-manual-tarea-12.md`](../../../docs/progreso/v3.7-comandos-administracion-empleados/verificacion-manual-tarea-12.md).
    ★ **Nota de ejecución (sdd-apply, tarea 12) — PARCIAL, sin marcar `[x]`**: no hay terminal interactiva disponible en este ejecutor para capturar una sesión real de la TUI de Ink (`start-tui.tsx`). En su lugar se escribió y corrió `src/test/integration/comandos-administracion-empleados-flujo.integration.test.ts`: el flujo COMPLETO del primer bullet (bootstrap equivalente al CLI → login → `/crear-empleado` → `/asignar-rol` → `ana` aprueba un reembolso, sin paso adicional) y el segundo bullet (rol base rechazado en los dos comandos gateados, `/estado-bot-prs` sin gate), ambos contra `db` SQLite real, hash `scrypt` real y `VentaStorePort` real — sin ningún doble en el camino crítico; los dos tests pasan (`npm test -- comandos-administracion-empleados-flujo`, verde). El tercer bullet (`/estado-bot-prs` con/sin env vars, nunca imprime el valor) ya estaba cubierto por `build-on-comando-empleado.test.ts` (tarea 3) — no se duplicó. El cuarto bullet (rollback de un commit real) y la captura de la TUI interactiva real **quedan pendientes de verificación manual humana**, después de que se commiteen las tareas 7-11 — no hay ningún commit real todavía sobre el que probar un rollback (`AGENTS.md`: el commit lo ejecuta siempre el humano). `npm test && npm run typecheck` corren en verde sobre el estado completo de las tareas 1-11 de esta rama (quinto bullet, cerrado). `docs/progreso/` NO se crea todavía — `AGENTS.md` lo gatea a la aprobación del Reviewer, que no corrió en esta sesión.

---

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~800 (prod+test+docs+specs, las tres PRs) |
| 400-line budget risk | **High** (PR1 ~240 OK; PR2 ~155 OK; **PR3 ~405, al borde/por encima del presupuesto por sí sola**) |
| Chained PRs recommended | **Yes — y no es opcional**: la dependencia dura sobre `autorizacion-empleado` obliga a partir en al menos dos entregas aunque el conteo de líneas fuera menor |
| Suggested split | PR1 (~240, tareas 1-4, sin bloqueo) → PR2 (~155, tareas 5-6, bloqueada) → PR3 (~405, tareas 7-12, bloqueada, depende de PR2) |
| Delivery strategy | `ask-on-risk` (contexto recibido) |
| Chain strategy | `feature-branch-chain` (contexto recibido) — PR1 targetea el tracker `hito/<version>-comandos-administracion-empleados` (forkeado de `main`); PR2 targetea la rama de PR1; PR3 targetea la rama de PR2; **sólo el tracker mergea a `main`** |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High
```

**Por qué la decisión no es sólo de tamaño**: incluso si el total fuera menor a 400 líneas, la Pista B (tareas 5-12) **no puede empezar** hasta que `autorizacion-empleado` mergee a `main` — el bloqueo es estructural (imports a archivos que no existen), no una preferencia de revisión. El checkpoint tiene que decidir, además del split por tamaño, **cuándo** disparar PR2/PR3 (inmediatamente después del merge de `autorizacion-empleado`, o después de que también mergee `operaciones-negocio-conversacionales` por R7).

### Reconciliación con `design.md` §9

| Archivo | Tareas | Líneas est. (impl+test) | PR |
|---|---|---|---|
| `src/empleados.ts` (extracción `altaCredencialEmpleado`) | 1 | ~65 | PR1 |
| `comando-empleado.ts` (campo `requiereAdministrador` + accessor, sin consumidor) | 2 | ~50 | PR1 |
| `comando-empleado.ts` + `build-on-comando-empleado.ts` (`/estado-bot-prs`, fix de ruteo) | 3 | ~110 | PR1 |
| specs parciales + docs parciales | 4 | ~20 | PR1 |
| **Subtotal PR1** | 1-4 | **~245** | |
| `autorizacion-resolucion.ts` (`esAdministrador`) + gate en dispatcher | 5 | ~95 | PR2 |
| `rol-contract.ts` (`RolEmpleadoEscritorPort`) + wiring `rolEscritor` | 6 | ~55 | PR2 |
| **Subtotal PR2** | 5-6 | **~150** | |
| `/asignar-rol` (descriptor, `Forma`, handler, auto-degradación, constantes) | 7 | ~150 | PR3 |
| `/crear-empleado` (descriptor, `Forma`, handler, constante) | 8 | ~110 | PR3 |
| `credenciales-contract.ts` (comentario) | 9 | ~8 | PR3 |
| Success Criteria de cierre (tests, sin archivo propio) | 10 | ~60 | PR3 |
| Specs finales + arc42/README | 11 | ~65 | PR3 |
| Verificación manual (evidencia, sin código) | 12 | — | PR3 |
| **Subtotal PR3** | 7-12 | **~395** | |
| **Total** | | **~790** | |

---

## Después de la tarea 12

- Pasa al Reviewer (`sdd-verify` + `code-review`) contra este documento, las cinco specs y `design.md`, con atención especial a: **el orden de evaluación del gate (sesión antes que rol, tarea 5)**, **la prohibición absoluta de auto-degradación sin conteo (tarea 7, ADR 182 — verificar que no se coló un `SELECT COUNT`)**, **el invariante de contraseña extendido a `/crear-empleado` (tarea 8, molde `:524`)**, y **que el `switch` exhaustivo de `build-on-comando-empleado.ts` compile en cada PR** (las tareas 3, 7 y 8 agregan `case` en el mismo commit que el brazo de tipo, nunca por separado).
- Antes de abrir PR2: confirmar que `autorizacion-empleado` mergeó a `main` y que la rama del tracker se actualizó con `main` (ver *Rollout / Branching*).
- Antes de abrir PR3: confirmar que PR2 mergeó en la cadena y que `RESULTADO_NO_AUTORIZADO` (ADR 161) sigue existiendo con el mismo nombre.
- Si el checkpoint elige la salida (b) o (c) de R1/RD-81 (contraseña generada, o enmascarado levantando el ADR 21) en vez de la (a) ya asumida por este documento, **la tarea 8 y su cobertura de spec cambian de forma** — no es un ajuste cosmético, re-abrir `sdd-design` antes de ejecutar la tarea 8.
- Si aprueba: checklist de cierre de `AGENTS.md` (Reviewer aprobado, `docs/progreso/<version>-comandos-administracion-empleados/` completo), tag de versión a fijar por el checkpoint.

---

**Nota de formato**: mismo criterio que `definicion-skills/tasks.md`, `comando-reporte-comisiones/tasks.md`, `comando-cancelar-solicitud/tasks.md` y `comando-visibilidad-a2a-entrante/tasks.md` — se sigue el formato extenso ya establecido por `proposal.md`, las cinco `specs/` y `design.md` de este mismo change, en vez del tope de 530 palabras de la skill `sdd-tasks`.
