> **Nota de proceso**: fase corrida **sin `Bash`/`graphify`** (mismo motivo que las tres fases previas de este change: entorno sin esa herramienta en esta sesión) — `Read`/`Grep`/`Glob` directos. Se leyó completo `proposal.md` (ADR 86-92, R1-R12, RD-32 a RD-35), los 2 `specs/` (`servidor-a2a-jsonrpc` 9 requirements/22 escenarios, `solicitud-a2a-entrante` 7 requirements/15 escenarios) y `design.md` completo (1129 líneas, ADR 93-102, §0-15, corte de 6 PRs en §12). Molde de formato: `openspec/changes/hito-2.2-a2a-cliente/tasks.md` completo (23 tareas).
>
> **Datos verificados en la sesión que dio origen a este documento (no de memoria, no del `design.md`)**: el techo real de migraciones en `main` **hoy** es `0009_propuestas_cambio` (`git show main:src/adapters/memory/migrations/index.ts`) — confirma la derivación de `design.md` §7.1/RD-38, que no había podido correr `git show`. Cuando `hito-2.2` (migración `0010_delegaciones_a2a`) mergee a `main`, el techo pasa a `0010` y la migración de este hito será **`0011`** — regla `M+1`, no un número fijo (tarea 6 lo deja explícito). El literal de commit **`Hito 7`** está confirmado libre (`git log --oneline --all | grep -i "Hito 7"` sin resultados) y coincide con el nombre real del Plan ("Hito 7: Comunicación A2A entrante", línea 368): se usa sin ambigüedad en todos los commits de abajo.

# Hito 7 — Comunicación A2A entrante: Servidor A2A (v3.0.0): desglose de tareas

## ❌ PRECONDICIÓN DE APLICACIÓN — GATE **NO** CUMPLIDO

**`sdd-apply` de este documento NO PUEDE ARRANCAR todavía.** `design.md` §0 fijó las condiciones verificables contra `main` — hoy **ninguna se cumple**:

| # | Condición | Comando de verificación | Estado hoy |
|---|---|---|---|
| 1 | El tag `v2.2.0` existe | `git tag -l \| rg -x v2.2.0` | **NO CUMPLE** — `hito-2.2-a2a-cliente` tiene 20/23 tareas commiteadas en su rama, sin Reviewer, sin merge, sin tag |
| 2 | `src/adapters/a2a/` **y** `src/core/agents/a2a-contract.ts` son código en `main` | `git ls-tree main --name-only src/adapters/a2a/` y `git ls-tree main --name-only src/core/agents/a2a-contract.ts`, ambos con salida no vacía | **NO CUMPLE** — los dos viven sólo en `hito/v2.2-a2a-cliente` |
| 3 | La migración `0010_delegaciones_a2a` está en `main` | `git show main:src/adapters/memory/migrations/index.ts \| rg migration0010` | **NO CUMPLE** — hoy `main` llega hasta `migration0009PropuestasCambio` (verificado esta sesión) |

**Consecuencia**: este hito reusa el **100%** de lo que necesita del Hito 6 — `TASK_STATE_*`/`esTaskStateConocido`/`esEstadoTerminal` de `a2a-contract.ts`, `delegarTarea`/`TaskLike`/`extraerEndpointJsonRpc` de `client.ts`, y el número de migración `M+1`. Sin las tres condiciones, este change **no tiene de dónde importar nada de eso**. Es el **tercer hito consecutivo** que arrastra este gate (`hito-2.1` y `hito-2.2` lo incumplieron al arrancar sus respectivos `design.md`).

**Lo que sí queda listo mientras el gate no se cumple**: este documento completo, listo para ejecutarse en cuanto `v2.2.0` esté mergeado. `sdd-apply` revalida las tres condiciones como su primer paso y reporta `blocked` si alguna falla — no procede "parcialmente".

**Spec y diseño de origen** (no se duplican acá): [`proposal.md`](proposal.md) (ADR 86-92, R1-R12), [`specs/servidor-a2a-jsonrpc/spec.md`](specs/servidor-a2a-jsonrpc/spec.md) (9 requirements, 22 escenarios), [`specs/solicitud-a2a-entrante/spec.md`](specs/solicitud-a2a-entrante/spec.md) (7 requirements, 15 escenarios), [`design.md`](design.md) (ADR 93-102, §0-15, corte de PRs en §12, RD-32 a RD-43).

**Entregable funcional a demostrar** (los 17 pasos de `design.md` §11): con `HARNESS_A2A_ENTRANTE_TOKEN` configurado, un agente externo puede leer el Agent Card sin auth, hacer `SendMessage` autenticado y recibir `TASK_STATE_SUBMITTED` de inmediato, poll-ear `GetTask` hasta `TASK_STATE_COMPLETED` con el texto del turno, y el ciclo completo se verifica contra **el propio Cliente A2A del Hito 6, sin modificarlo**. En paralelo: `git diff --stat main -- src/core/` no muestra ningún archivo modificado (sólo uno agregado y un doc-comment) — la verificación mecánica del Escenario 6 del arc42. Sin token, cero puertos abiertos, comportamiento idéntico a `v2.2.0`.

**Metodología**: TDD estricto (`strict_tdd: true`). **Siete excepciones explícitas**, mismo criterio que `hito-2.1`/`hito-2.2`: tarea 2 (`.env.example`, documentación), tarea 5 (`a2a-contract.ts`, sólo doc-comment, cero cambio de comportamiento), tarea 6 (migración `NNNN`, SQL declarativo — su test va en la tarea 7, que **sí** va primero), tarea 16 (`main.ts`, wiring puro de composition root, molde de los bloques 5a/5b ya existentes), tarea 18 (`README.md` + `docs/ARC42_Harness_Empresarial.md`, documentación), tarea 20 (nota de corrección al Plan en `docs/progreso/`, documentación — pedida por ADR 89 pto 6 y por el hallazgo de RD-37), tarea 21 (verificación manual, sin código de producción). **Ninguna otra tarea es excepción**: los tres métodos JSON-RPC, la auth, el tope de turnos en vuelo y el composition root van con test primero.

**Fixture nueva de este change**: dobles planos de `A2ARequest`/`A2AResponse` (molde `webhooks/server.ts:30-44`/`web/http.ts`, ningún test del suite por defecto abre un puerto) y — a diferencia del Hito 6 — el test de integración de este hito **no depende de infraestructura de terceros**: cliente y servidor son ambos nuestros (`delegarTarea` del Hito 6 contra `startA2AServer` en puerto efímero), así que **corre en CI sin degradar a `skip`**.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | **~2 460** (`design.md` §12, prod+test, relación 1:1 con `strict_tdd: true`) |
| 400-line budget risk | **High** |
| Chained PRs recommended | Yes |
| Suggested split | PR1 → PR2 → PR3 → PR4 → PR5 → PR6 → Work Unit 7 (ver `design.md` §12) |
| Delivery strategy | **PRs encadenados** (resuelto por el checkpoint humano — mismo riesgo alto que los Hitos 2.0/2.1/2.2) |
| Chain strategy | **feature-branch-chain** (resuelto por el checkpoint humano, mismo criterio que Hitos 2.0, 2.1 y 2.2) |

```text
Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High
```

**Sin ajuste al corte de `design.md` §12**: sus seis PRs (~270/380/480/430/470/430) suman exactamente los ~2 460 estimados. Este documento **sí** subdivide PR4 y PR5 en tareas más finas de lo que `design.md` §12 esboza (`server.ts` es un solo archivo pero crece en tres capas conceptuales distintas por PR — tipos/auth/body, ruteo/card, parseo/errores en PR4; `construirTask`, los tres métodos, y `startServer`/tope/drenaje/puerto en PR5), porque a esa granularidad el criterio de "cada tarea es una unidad de review de una frase" (regla de la skill `sdd-tasks`) exige más de un checklist item por PR — **sin mover una sola línea entre PRs ni cambiar el total**. Se documenta acá porque es el único ajuste real de esta fase.

### Suggested Work Units

| Unit | Goal | Tasks | Est. lines | Base branch (`feature-branch-chain`) |
|------|------|-------|-----------|-------------|
| 1 | Config del adaptador: rutas, métodos, códigos de error JSON-RPC (con verificación real de los dos A2A-específicos), resolución de env | 1-2 | ~270 | `hito/v3.0-a2a-servidor` |
| 2 | Agent Card puro + prompt sintético del núcleo (aditivos puros, nadie los consume aún) | 3-5 | ~380 | Unit 1 |
| 3 | Persistencia `solicitudes_a2a_entrantes`: migración + repositorio (la tabla existe y se puede escribir/leer a mano por SQL) | 6-7 | ~480 | Unit 2 |
| 4 | `server.ts` parte 1 — **transporte y protocolo**: recorte estructural, auth, tope de body, ruteo, Agent Card servido, sobre JSON-RPC + errores base. Demostrable solo: `curl` del card + `401` + `-32601`, sin ninguna fila | 8-10 | ~430 | Unit 3 |
| 5 | `server.ts` parte 2 — **los tres métodos y el ciclo de vida**: `construirTask`, `SendMessage`/`GetTask`/`CancelTask` reales, tope de turnos en vuelo, drenaje, puerto efectivo, fachada `server-index.ts` | 11-14 | ~470 | Unit 4 |
| 6 | Composition root (`build-on-a2a-entrante.ts`) + wiring en `main.ts` + test de integración contra el Cliente A2A del Hito 6 + README — **ENTREGABLE COMPLETO** | 15-19 | ~430 | Unit 5 |
| 7 | Nota de corrección al Plan (hallazgo RD-37) + verificación manual del entregable | 20-21 | ~40 (evidencia, no código) | Unit 6 |

Con `feature-branch-chain`: PR1 apunta al tracker `hito/v3.0-a2a-servidor`, cada hijo al inmediato anterior; sólo el tracker mergea a `main`. Rollback en orden inverso 7→1 — `design.md` §12: "Los seis PRs son aditivos puros… todo el adaptador puede quedarse mergeado sin daño: sin token, no hace nada." Es el rollback más limpio de todos los hitos del proyecto.

## Tareas

### PR1 — Config del adaptador: rutas, métodos, códigos de error JSON-RPC (tareas 1-2)

1. [x] **`src/adapters/a2a/server-config.ts`** (nuevo, §6.1) — `A2AServerConfig`, `DEFAULT_A2A_ENTRANTE_PORT = 8888`, `DEFAULT_A2A_ENTRANTE_PUBLIC_URL`, `DEFAULT_A2A_ENTRANTE_MAX_BODY_BYTES = 65_536`, `DEFAULT_A2A_ENTRANTE_MAX_EN_VUELO = 4`, `A2A_CLOSE_TIMEOUT_MS = 5_000` (constante, NO env var), `A2A_SERVER_LOG_CORRELATION_ID`, `RUTA_AGENT_CARD`, `RUTA_JSONRPC`, `METODO_SEND_MESSAGE`/`METODO_GET_TASK`/`METODO_CANCEL_TASK`, los cinco códigos base JSON-RPC 2.0 (`JSONRPC_PARSE_ERROR = -32700` etc., ADR 94 pto 8), `resolveA2AServerConfig`, `isA2AServerEnabled`.

   > **★ Verificación obligatoria ANTES de escribir las dos constantes A2A-específicas** (ADR 94 pto 9-10, RD-37 — esta fase no tuvo `WebFetch`): `A2A_ERROR_TASK_NOT_FOUND` y `A2A_ERROR_TASK_NOT_CANCELABLE` se verifican contra `specification/a2a.proto` **y** `docs/specification.md` del tag `v1.0.0` de `a2aproject/A2A` (mismo procedimiento que resolvió RD-24 en el Hito 6: `gh api repos/a2aproject/A2A/contents/specification/a2a.proto?ref=v1.0.0`). **Regla**: si la especificación define códigos propios para "tarea no encontrada"/"tarea no cancelable", se usan esos, literales. Si no, se usan dos valores del rango `-32000..-32099` (reservado para errores de implementación por JSON-RPC 2.0). El hallazgo se anota en `docs/progreso/v3.0-a2a-servidor/` (tarea 20).

   Test primero: `server-config.test.ts` — `resolveA2AServerConfig` con env vacío ⇒ los cinco defaults; `HARNESS_A2A_ENTRANTE_TOKEN` en blanco/sólo espacios ⇒ `isA2AServerEnabled` da `false`; los tres numéricos con `"abc"`/`"0"`/`"-1"`/`"Infinity"` ⇒ siempre default, **nunca lanza** (molde `resolvePositiveNumber`); `publicUrl` normalizada sin `/` final; `RUTA_AGENT_CARD`/`RUTA_JSONRPC` son los strings literales del ADR 96; los cinco códigos base son exactamente los de JSON-RPC 2.0; las dos constantes A2A-específicas están definidas y son números distintos entre sí y de los cinco base. Sin fixture (puro, `env` inyectado). Cubre spec `servidor-a2a-jsonrpc` reqs "El token es el único interruptor" y "Ruteo exclusivo por `method`" (la mitad de configuración). ADR 88 pto 4, 94, 97. Comando: `npm test -- server-config`. Commit: `feat(adapters/a2a): agrega server-config con rutas, metodos y codigos de error JSON-RPC (Hito 7, tarea 1)`.

2. [x] **`.env.example`** (modificado) — **TDD exception** (documentación) — agrega las 5 variables nuevas (`HARNESS_A2A_ENTRANTE_TOKEN`, `HARNESS_A2A_ENTRANTE_PORT`, `HARNESS_A2A_ENTRANTE_PUBLIC_URL`, `HARNESS_A2A_ENTRANTE_MAX_BODY_BYTES`, `HARNESS_A2A_ENTRANTE_MAX_EN_VUELO`), comentadas, **sin valores reales**, con el default de cada una al lado (molde de la tabla `HARNESS_A2A_*` que el Hito 6 ya agregó). Commit: `docs(root): agrega variables HARNESS_A2A_ENTRANTE a .env.example (Hito 7, tarea 2)`.

### PR2 — Agent Card puro + prompt sintético del núcleo (tareas 3-5)

3. [x] **`src/core/agents/a2a-entrante-prompt.ts`** (nuevo, §5.1, ADR 95, **SIN imports**) — `MAX_SOLICITUD_A2A_CHARS = 8_000`, `buildSolicitudA2APrompt(texto): string`, PURA, cuatro secciones (rol, solicitud truncada, limitación de sólo lectura declarada literal, instrucción de honestidad), marca de truncado `[…truncado]` (molde `soporte-prompt.ts`). Test primero: `a2a-entrante-prompt.test.ts` — las cuatro secciones presentes y en orden; **el párrafo de "esta solicitud es de sólo lectura… no podés delegar a otro agente" está, literal**; texto de `8_001` chars ⇒ truncado a `8_000` con la marca; texto vacío ⇒ no lanza (función total); **cero imports** en el archivo fuente (assert leyendo el texto del módulo, molde `a2a-contract.test.ts`/`hitl-contract.test.ts`). Sin fixture (puro). Cubre spec `solicitud-a2a-entrante` req "`CASO_TIPO_A2A_ENTRANTE` y el prompt sintético son aditivos y puros" (escenario "el prompt sintético es puro y testeable sin red ni modelo"). ADR 91 pto 5, 95, 98 pto 6. Comando: `npm test -- a2a-entrante-prompt`. Commit: `feat(core): agrega buildSolicitudA2APrompt puro con tope de 8000 caracteres (Hito 7, tarea 3)`.

4. [x] **`src/adapters/a2a/agent-card.ts`** (nuevo, §6.2, ADR 96) — `construirAgentCard(config: A2AServerConfig): AgentCardJson`, PURA, sin I/O ni reloj. Los siete campos requeridos: `name`, `description`, `version = "3.0.0"`, `capabilities` (las tres en `false`, explícitas), `defaultInputModes`/`defaultOutputModes = ["text/plain"]`, **una** `skill` (`id: "consulta-arnes"` + los strings del ADR 96 pto 3), **una** entrada de `supportedInterfaces` (`protocolBinding: "JSONRPC"`, `url: config.publicUrl + RUTA_JSONRPC`), `securitySchemes` con esquema HTTP bearer. Test primero: `agent-card.test.ts` — el objeto **entero** comparado contra un literal; `capabilities.streaming`/`pushNotifications`/`extendedAgentCard` presentes y en `false` (ninguno ausente — assert de las tres claves, no sólo de sus valores); **exactamente una** skill y **exactamente una** entrada de `supportedInterfaces` con `protocolBinding === "JSONRPC"` y `url` no vacío (el requirement duro que hace que `extraerEndpointJsonRpc` del Cliente A2A del Hito 6 pueda hablarle — se verifica con la misma función real, importada del Hito 6, no reimplementada); `version === "3.0.0"`; **no** emite `preferredTransport` ni `additionalInterfaces` (ADR 96 pto 6). Sin fixture (puro). Cubre spec `servidor-a2a-jsonrpc` req "El Agent Card se publica en la URL bien conocida, público y sin autenticación" (los 3 escenarios). ADR 92, 96. Comando: `npm test -- agent-card`. Commit: `feat(adapters/a2a): agrega construirAgentCard puro con skill unica y supportedInterfaces (Hito 7, tarea 4)`.

5. [x] **`src/core/agents/a2a-contract.ts`** (modificado, sólo doc-comment) — **TDD exception** (comentario, cero cambio de comportamiento, ADR 91 pto 7) — el doc-comment del módulo pasa de *"Vocabulario del Cliente A2A saliente"* a *"Vocabulario del protocolo A2A, en sus dos direcciones"*. Ningún export cambia de firma ni de comportamiento — se confirma reafirmando (no reescribiendo) los tests existentes del módulo, que siguen en verde sin tocarlos. Commit: `docs(core): actualiza doc-comment de a2a-contract para reflejar las dos direcciones del protocolo (Hito 7, tarea 5)`.

### PR3 — Persistencia `solicitudes_a2a_entrantes` (tareas 6-7)

6. [x] **`src/adapters/memory/migrations/0011_solicitudes_a2a_entrantes.ts`** (nueva, §7.1, ADR 89 pto 4) — **TDD exception** (SQL declarativo, se escribe junto a su test en la tarea 7, que **sí** va primero). `NNNN` **no es un número fijo**: es `M + 1` sobre el techo real de `migrations/index.ts` **en `main`** al momento de `sdd-apply` (regla del ADR 63). **Verificado esta sesión**: hoy `main` llega hasta `0009_propuestas_cambio`; cuando `hito-2.2` (que agrega `0010_delegaciones_a2a`) mergee, el techo será `0010` y este archivo será `0011_solicitudes_a2a_entrantes.ts`, exportando `migration0011SolicitudesA2AEntrantes`. `sdd-apply` revalida con `git show main:…` antes de crear el archivo — no confía en este número. DDL exacto del ADR 89 pto 4: tabla `solicitudes_a2a_entrantes` (`id` PK, `a2a_task_id NOT NULL UNIQUE`, `agente_externo_url` nullable — siempre `NULL` en este hito, `origen_transporte NOT NULL`, `caso_id` nullable `REFERENCES casos(id)` — `NULL` sólo en `REJECTED`, `mensaje_recibido NOT NULL`, `estado NOT NULL` sin `CHECK`, `resultado` nullable, `created_at`/`updated_at NOT NULL`) + un solo índice `idx_solicitudes_a2a_entrantes_caso`. Agregada **al final** del array, sin reordenar ninguna entrada previa. Commit: `feat(adapters/memory): agrega migracion NNNN con tabla solicitudes_a2a_entrantes (Hito 7, tarea 6)`.

7. [x] **`src/adapters/memory/repository.ts`** (modificado, §7.2) — `InsertSolicitudA2AEntranteInput`/`insertSolicitudA2AEntrante` (`agente_externo_url`/`resultado` nacen en `NULL` siempre; `casoId` ausente ⇒ `NULL`, fila `REJECTED`), `SolicitudA2AEntranteYaExisteError` (colisión de `a2a_task_id`, molde `VentaAlreadyExistsError`), `SolicitudA2AEntranteRow`, `getSolicitudA2AEntrantePorTaskId` (lookup síncrono, `undefined` si no existe), `ActualizarSolicitudA2AEnCursoInput`/`actualizarSolicitudA2AEnCurso` (**el `UPDATE` condicional** con `WHERE a2a_task_id = @a2aTaskId AND estado IN ('TASK_STATE_SUBMITTED','TASK_STATE_WORKING')`, `COALESCE(@resultado, resultado)`, devuelve `changes > 0`, **no lanza** por `changes === 0` — a diferencia de `actualizarDelegacionA2A`, acá el cero es un desenlace esperado, ADR 94 pto 5), `cancelarSolicitudA2AEntrante` (las cuatro ramas del ADR 94: `SELECT` del estado + `UPDATE` guardado sólo si no es terminal — `"cancelada"`/`"ya-cancelada"`/`"no-cancelable"`/`"no-encontrada"`), `listSolicitudesA2AEntrantesPorCaso`. Test primero: `repository.test.ts` (SQLite `:memory:`) — la migración de la tarea 6 aplica; `insertSolicitudA2AEntrante` deja `agente_externo_url`/`resultado` en **NULL**; colisión de `a2a_task_id` ⇒ `SolicitudA2AEntranteYaExisteError`; `actualizarSolicitudA2AEnCurso` sobre una fila `CANCELED` devuelve `false` y **no** pisa el estado (la guarda del `WHERE`); `getSolicitudA2AEntrantePorTaskId` de un id inexistente ⇒ `undefined`; `cancelarSolicitudA2AEntrante` sobre `WORKING` ⇒ `"cancelada"` + fila `CANCELED`; sobre `CANCELED` ⇒ `"ya-cancelada"`, **`updated_at` intacto**; sobre `COMPLETED`/`FAILED`/`REJECTED` ⇒ `"no-cancelable"`, **la fila no se toca**; sobre un id inexistente ⇒ `"no-encontrada"`; `listSolicitudesA2AEntrantesPorCaso` ordena por `created_at`; **el `estado` persistido es `TASK_STATE_*` crudo**, nunca traducido. **Fixture**: SQLite `:memory:`. Cubre spec `solicitud-a2a-entrante` reqs "Ciclo de vida completo de `solicitudes_a2a_entrantes`" y "`agente_externo_url` es nullable y hoy siempre `NULL`" (los 6 escenarios de ambos). ADR 87 pto 5-7, 89, 94. Comando: `npm test -- repository`. Commit: `feat(adapters/memory): agrega insertSolicitudA2AEntrante actualizarSolicitudA2AEnCurso y cancelarSolicitudA2AEntrante (Hito 7, tarea 7)`.

### PR4 — `server.ts` parte 1: transporte y protocolo (tareas 8-10)

> Demostrable solo, sin ninguna fila creada todavía: `curl` del card, `401` sin auth, `-32601` para un método desconocido. Los tres métodos reales (`SendMessage`/`GetTask`/`CancelTask`) se agregan en PR5 — hasta entonces, el dispatcher los reconoce por nombre pero no ejecuta lógica de negocio (placeholder documentado en la tarea 10, reemplazado en la tarea 12 sin romper ningún test de PR4).

8. [x] **`src/adapters/a2a/server.ts`** (nuevo, §6.3, parte 1a — tipos + auth + body) — `A2ARequest`/`A2AResponse`/`A2AHttpServerLike`/`CreateA2AServerFn` (recorte estructural duplicado a propósito de `web/http.ts`/`webhooks/server.ts:30-51`, ADR 13 — **tercer servidor HTTP de la misma familia**; `A2ARequest.socket?.remoteAddress` es la única pieza nueva respecto de `WebRequest`/`WebhookRequest`, ADR 89 pto 3), `pathFromUrl` (duplicado), `esAutorizado(req, token)` (calcado de `web/server.ts:113-132`: **token `"" ⇒ false` sin comparar nada**, chequeo de longitud **antes** de `timingSafeEqual`), lectura de body con tope (`maxBodyBytes`, `413` + `res.end()` **antes** de `req.destroy()`). Test primero: `server.test.ts` — con dobles planos de `A2ARequest`/`A2AResponse` — `esAutorizado` con token `""` ⇒ `false` sin `timingSafeEqual` invocado (spy); largo de header distinto al del token ⇒ `false` sin `RangeError`; header `Authorization` en array ⇒ usa el primer valor; prefijo `Bearer ` exacto exigido; body sobre `maxBodyBytes` ⇒ `413`, con `end()` llamado **antes** que `destroy()` (orden observado con spies ordenados). **Cero llamadas a ningún callback de negocio en estos casos** (`toHaveBeenCalledTimes(0)`). Sin puerto real. Cubre spec `servidor-a2a-jsonrpc` reqs "Autenticación bearer obligatoria… verificada antes de parsear el body" (los 3 escenarios). ADR 88, 13. Comando: `npm test -- server`. Commit: `feat(adapters/a2a): agrega recorte estructural esAutorizado y lectura de body con tope en server.ts (Hito 7, tarea 8)`.

9. [x] **`src/adapters/a2a/server.ts`** (modificado, parte 1b — ruteo + Agent Card) — `createRequestListener` gana el ruteo por `método+ruta` (orden exhaustivo: `método+ruta → tope de body → AUTH → parseo JSON-RPC → despacho`, tabla de §6.3): `GET RUTA_AGENT_CARD` ⇒ `200` con el card de `construirAgentCard`, **sin exigir auth**; cualquier `método+ruta` no reconocido ⇒ `404` vacío. Test primero: extiende `server.test.ts` — `GET /.well-known/agent-card.json` **sin** `Authorization` ⇒ `200` con el card completo, no `401`; una ruta/método inventado ⇒ `404` vacío; el card servido **evento `a2a-card-servido`** con `origenTransporte`. Cubre spec `servidor-a2a-jsonrpc` req "El Agent Card se publica en la URL bien conocida, público y sin autenticación" (escenario "el card se sirve sin `Authorization`"). ADR 88 pto 5, 92. Comando: `npm test -- server`. Commit: `feat(adapters/a2a): agrega ruteo de metodo y ruta y publicacion sin auth del Agent Card (Hito 7, tarea 9)`.

10. [x] **`src/adapters/a2a/server.ts`** (modificado, parte 1c — sobre JSON-RPC + errores base) — parseo del `POST RUTA_JSONRPC`: `JSON.parse` inválido ⇒ `-32700`, `id: null`; sobre no-objeto / `jsonrpc !== "2.0"` / `method` no-string ⇒ `-32600`, `id: null`; `method` fuera de `{SendMessage, GetTask, CancelTask}` ⇒ `-32601`, con el `id` del request (`a2a-metodo-no-soportado`). **Interino documentado**: los tres métodos conocidos, hasta la tarea 12, responden `-32603` con un mensaje de "handler no configurado" — la tarea 12 reemplaza ese placeholder por el despacho real, sin tocar ninguno de los tests de esta tarea. Test primero: extiende `server.test.ts` — los cinco casos de la tabla de errores base con su `code` exacto y su `id`; `ListTasks`/`SendStreamingMessage` ⇒ `-32601`, **status HTTP `200`**, no `500`; un campo desconocido del sobre **no** hace fallar el parseo. Sin fixture adicional. Cubre spec `servidor-a2a-jsonrpc` req "Ruteo exclusivo por `method`" (los 2 escenarios). ADR 92 pto 1, 94 pto 7-8. Comando: `npm test -- server`. Commit: `feat(adapters/a2a): agrega parseo del sobre JSON-RPC y los cinco errores base (Hito 7, tarea 10)`.

### PR5 — `server.ts` parte 2: los tres métodos y el ciclo de vida (tareas 11-14)

11. [x] **`src/adapters/a2a/server.ts`** (modificado, parte 2a, ADR 93) — `construirTask(vista: SolicitudA2AEntranteVista): TaskJson`, **PURA y TOTAL** — `COMPLETED` con `resultado` ⇒ `artifacts: [{artifactId: \`${id}-0\`, name:"respuesta", parts:[{text:resultado}]}]`, **sin** `status.message`; `FAILED`/`CANCELED`/`REJECTED` ⇒ `status.message` con el texto fijo correspondiente (tabla ADR 93 pto 3), **sin** `artifacts`; `SUBMITTED`/`WORKING` ⇒ ninguno de los dos; `status.timestamp = vista.updatedAt`; `contextId = vista.casoId ?? a2aTaskId` (nunca `null`); `COMPLETED` sin `resultado` (que ningún camino produce) ⇒ **no lanza**, `artifacts` ausente. Test primero: extiende `server.test.ts` — **el test estrella del ADR 93**, las siete combinaciones de estado × presencia de `artifacts`/`status.message`; `status.timestamp === vista.updatedAt` exacto; `artifactId`/`messageId` derivados, sin `randomUUID`; total sin ninguna rama que lance. Sin fixture (puro). Cubre spec `servidor-a2a-jsonrpc` req "El `Task` devuelto es parseable por el Cliente A2A propio" (mitad de forma). ADR 93. Comando: `npm test -- server`. Commit: `feat(adapters/a2a): agrega construirTask puro y total con resultado en artifacts (Hito 7, tarea 11)`.

12. [x] **`src/adapters/a2a/server.ts`** (modificado, parte 2b, ADR 94, ADR 100) — reemplaza el placeholder de la tarea 10 por el despacho real de los tres métodos: `SendMessage` ⇒ `a2aTaskId = newTaskId()`, invoca `deps.onSolicitudA2A({a2aTaskId, texto, origenTransporte, hayCupo})`, responde `Task` vía `construirTask` (`SUBMITTED` o `REJECTED` según el resultado); `GetTask`/`CancelTask` ⇒ **SÍNCRONOS** (`onConsultarTarea`/`onCancelarTarea`, ADR 100 — sin `Promise` en su firma, así que un `await` a un modelo adentro es **error de compilación**, no de criterio); `GetTask` de un id inexistente ⇒ `A2A_ERROR_TASK_NOT_FOUND`; `CancelTask` con las cuatro ramas del ADR 94 (`"cancelada"` ⇒ `result` `CANCELED`; `"ya-cancelada"` ⇒ `result` idempotente sin tocar la fila; `"no-cancelable"` ⇒ `A2A_ERROR_TASK_NOT_CANCELABLE`, fila intacta; `"no-encontrada"` ⇒ `A2A_ERROR_TASK_NOT_FOUND`); `SendMessage`/`GetTask`/`CancelTask` sin `params` bien formados (`message.parts[*].text` / `id` string no vacío) ⇒ `-32602`; un handler que lanza ⇒ `-32603`, **nunca un stacktrace en el cuerpo** (`a2a-handler-fallido`). Test primero: extiende `server.test.ts` — `SendMessage` con doble de `onSolicitudA2A` que resuelve `SUBMITTED` ⇒ `200 {result: Task{state:"TASK_STATE_SUBMITTED"}}` con `id` no vacío; **`GetTask` no ejecuta ningún `await`** (el doble de `onConsultarTarea` es síncrono y el test afirma que `res.end` ya corrió en el mismo tick — verificación mecánica de "`GetTask` no invoca al modelo"); `CancelTask` sobre las cuatro ramas, cada una con su código/resultado exacto y **la fila no se toca** en las ramas `"ya-cancelada"`/`"no-cancelable"` (spy de que no se llamó a ningún método de escritura); `params` mal formados en los tres métodos ⇒ `-32602` sin invocar el callback; un `onSolicitudA2A` que rechaza ⇒ `-32603` sin stacktrace. **Fixture**: los tres callbacks dobles (`onSolicitudA2A`/`onConsultarTarea`/`onCancelarTarea`). Cubre spec `servidor-a2a-jsonrpc` reqs "`SendMessage` responde `TASK_STATE_SUBMITTED`… `GetTask` es una lectura pura" (los 3 escenarios) y "`CancelTask` marca la tarea como cancelada con honestidad declarada" (los 2 escenarios). ADR 87 pto 1 y 6, 92 pto 4, 94, 100. Comando: `npm test -- server`. Commit: `feat(adapters/a2a): agrega despacho real de SendMessage GetTask y CancelTask (Hito 7, tarea 12)`.

13. [x] **`src/adapters/a2a/server.ts`** (modificado, parte 2c, ADR 99, ADR 101) — `startServer(deps, createServer?)`: `const enVuelo = new Set<Promise<void>>()` (a la vez contador del tope y conjunto de drenaje), envuelve `deps.onSolicitudA2A` con `hayCupo: enVuelo.size < deps.config.maxEnVuelo` inyectado como argumento (no como callback — ADR 99 pto 3), registra `resultado.turno` en `enVuelo` sólo cuando `estado === "TASK_STATE_SUBMITTED"` (el tipo de `SolicitudA2AAceptada` lo hace imposible de omitir en la rama `REJECTED`, que no trae `turno`), `.then(olvidar, olvidar)` para desregistrar (molde `web/server.ts:515-523`); `address()` resuelve el **puerto efectivo** dentro del callback de `listen` (ADR 101 — con fallback a `deps.config.port` si el doble de test no implementa `address()`); `close()` drena `enVuelo` con `Promise.allSettled` contra `A2A_CLOSE_TIMEOUT_MS`, **nunca rechaza**, loguea `a2a-servidor-cierre-con-turnos-en-vuelo` si agota el techo. Test primero: extiende `server.test.ts` con reloj falso — `maxEnVuelo: 2`, tres `SendMessage` con turnos que no resuelven ⇒ el tercero recibe `hayCupo: false` y `REJECTED`; al resolver uno, el siguiente vuelve a tener cupo; `close()` con un turno pendiente no resuelve hasta que el turno resuelve; con un turno colgado, resuelve al agotar `A2A_CLOSE_TIMEOUT_MS` y **no rechaza nunca**, ni con un turno que rechaza; **`port` del handle es el de `server.address()` con `listen(0)`, no el `0` configurado** (el test que hace posible el test de integración de la tarea 17). **Fixture**: `CreateA2AServerFn` doble + reloj falso. Cubre spec `servidor-a2a-jsonrpc` reqs "Tope de turnos en vuelo… `SendMessage` responde `TASK_STATE_REJECTED`" (los 2 escenarios) y "Drenaje de turnos en vuelo al cerrar, con techo" (los 2 escenarios). ADR 90 pto 4, 97, 99, 101. Comando: `npm test -- server`. Commit: `feat(adapters/a2a): agrega startServer con tope de turnos en vuelo drenaje y puerto efectivo (Hito 7, tarea 13)`.

14. [x] **`src/adapters/a2a/server-index.ts`** (nuevo, §6.4) — `A2AServerAdapter` (`port`, `publicUrl`, `close()`), `startA2AServer(deps)` — fachada opt-in, molde literal de `startWebhookServer`/`startWebServer`: sin `HARNESS_A2A_ENTRANTE_TOKEN` ⇒ `undefined` (**no** un no-op — para que `main.ts` distinga "no hay nada que cerrar"), loguea `a2a-servidor-deshabilitado`; con token, cablea `resolveA2AServerConfig` + `createRequestListener` + `startServer`; un `listen` que rechaza (`EADDRINUSE`) **propaga** tal cual, `main.ts` decide. Test primero: `server-index.test.ts` — sin token ⇒ `undefined`, cero llamadas a `createServer`; con token ⇒ arma la config completa y llama a `startServer` una vez; `listen` que rechaza ⇒ la promesa de `startA2AServer` rechaza (no la traga). Sin fixture real (dobles). Cubre spec `servidor-a2a-jsonrpc` req "El token es el único interruptor" (los 2 escenarios). ADR 88 pto 4. Comando: `npm test -- server-index`. Commit: `feat(adapters/a2a): agrega fachada startA2AServer opt-in por token (Hito 7, tarea 14)`.

### PR6 — Composition root + wiring en `main.ts` + integración + docs (tareas 15-19)

15. [x] **`src/build-on-a2a-entrante.ts`** (nuevo, §5.2, ADR 91, ADR 98, hermano de `build-on-soporte.ts`) — `BuildOnA2AEntranteDeps` con **los mismos ocho campos exactos** que `BuildOnSoporteDeps` (`db`, `memory`, `hooks`, `agents`, `createKnowledge`, `newId?`, `now?`, `logDeps?` — ni uno más, ADR 98), `A2AEntranteHandlers` (`onSolicitudA2A`/`onConsultarTarea`/`onCancelarTarea`), `buildOnA2AEntrante(deps)`. `CASO_TIPO_A2A_ENTRANTE = "a2a_entrante"` y `CASO_ESTADO_ACTIVO = "activo"` (constantes locales de módulo, sin migración de `casos`). Secuencia de `onSolicitudA2A`: `hayCupo === false` ⇒ `insertSolicitudA2AEntrante(REJECTED, sin caso)` + evento `a2a-solicitud-rechazada-tope`, **sin caso, sin turno, sin modelo**; si hay cupo ⇒ `createCaso` → `insertSolicitudA2AEntrante(SUBMITTED)` → `return {estado:SUBMITTED, turno: ejecutarTurno(...)}` **sin awaitear** `ejecutarTurno`. `ejecutarTurno` **nunca rechaza** (contraste deliberado con `build-on-soporte.ts`, que propaga porque allá espera un navegador): `actualizarSolicitudA2AEnCurso(WORKING)` (si `false`, canceló antes de arrancar, se loguea sin gastar modelo) → `buildSolicitudA2APrompt` → `await handleTurn(casoId, prompt, {...})` (firma idéntica a `buildOnSoporte`) → éxito: `actualizarSolicitudA2AEnCurso(COMPLETED, result.responseText)`, `false` ⇒ `a2a-turno-entrante-descartado`; `catch`: `actualizarSolicitudA2AEnCurso(FAILED)` + evento, con un **segundo `catch`** que evita cualquier `unhandledRejection`. `onConsultarTarea`/`onCancelarTarea` **síncronos** (delegan directo a `getSolicitudA2AEntrantePorTaskId`/`cancelarSolicitudA2AEntrante`). Test primero: `build-on-a2a-entrante.test.ts` (molde `build-on-soporte.test.ts`: `vi.mock` de `handle-turn.js` + `openDatabase(":memory:")`) — `casos.tipo === "a2a_entrante"`; **la fila existe ANTES de que la promesa de `onSolicitudA2A` resuelva** (assert de orden con doble de store); `createKnowledge(casoId)` llamado **una** vez; `handleTurn` recibe el prompt de `buildSolicitudA2APrompt`; `handleTurn` rechaza ⇒ fila `FAILED` **y la promesa `turno` NO rechaza**; `hayCupo === false` ⇒ `createCaso` **cero** llamadas; `CancelTask` mientras `handleTurn` está pendiente ⇒ al resolver, la fila **sigue** `CANCELED`, `resultado` sigue `NULL`, se loguea `a2a-turno-entrante-descartado`; **la firma de `BuildOnA2AEntranteDeps` no incluye ningún puerto de escritura** (`board`, store de `actividades`, `escritura`, `ClienteA2APort`) — assert sobre las claves del tipo/los imports del módulo, molde de la verificación "el núcleo no importa el adaptador" de `v2.2.0`. **Fixture**: `HandleTurnResult` doble + SQLite `:memory:`. Cubre spec `solicitud-a2a-entrante` casi entera: "el mismo Selector de Turno, sin camino paralelo", "cero archivos de `src/core/` modificados", "ciclo de vida completo de la fila", "`CASO_TIPO_A2A_ENTRANTE` aditivo y puro", "el turno entrante es de lectura, invariante verificable por firma", "sin `KeyedQueue`" (todos sus escenarios salvo el de concurrencia, cubierto en la tarea 17). ADR 87, 90 pto 1-3, 91, 94 pto 5-6, 95, 98. Comando: `npm test -- build-on-a2a-entrante`. Commit: `feat(root): agrega buildOnA2AEntrante con dependencias identicas a build-on-soporte (Hito 7, tarea 15)`.

16. [x] **`src/main.ts`** (modificado, §7.3) — **TDD exception** (wiring puro de composition root, molde de los bloques 5a/5b ya existentes) — bloque 5d: `const a2aEntrante = buildOnA2AEntrante({db, memory, hooks, agents, createKnowledge})`, `try { a2aServidor = await startA2AServer({...a2aEntrante, logEvent}) } catch { a2aServidor = undefined; logTurnEvent("a2a-servidor-arranque-fallido", ...) }` (molde `webhooks/index.ts:46-49`, un `EADDRINUSE` no impide que el resto del arnés arranque); en el `finally`, tercera guarda del mismo molde que webhooks/web: `if (a2aServidor !== undefined) { try { await a2aServidor.close() } catch {...} }`, **antes** de `db.close()`. Verificación manual (no automatizada, a confirmar en la tarea 21) de que sin `HARNESS_A2A_ENTRANTE_TOKEN` el bloque no abre puerto y el resto del arranque es idéntico a `v2.2.0`. Commit: `feat(root): conecta HARNESS_A2A_ENTRANTE_TOKEN y cablea el Servidor A2A en main (Hito 7, tarea 16)`.

17. [x] **`src/test/integration/a2a-server.integration.test.ts`** (nuevo, §10.D) — levanta `startA2AServer({config:{token:"test-token", port:0, ...}})` con `createServer` **real** ⇒ puerto efímero leído del handle (ADR 101); lo consulta con **`delegarTarea` de `client.ts` del Hito 6, importada directo**, `destino: {baseUrl: "http://127.0.0.1:<port>", authToken:"test-token"}` — **`DESTINOS_A2A` no se toca** (verificado: `delegarTarea` no usa `input.clave` para nada más que logging); ciclo completo `GET` del card → `SendMessage` → **loop real de `GetTask`** (no con reloj inyectado — el primer test del proyecto que ejercita ese loop contra un socket real) → `TASK_STATE_COMPLETED` con el texto; `handleTurn` sustituido por un doble a nivel del composition root del test (se prueba el protocolo, no el turno); casos adicionales: token equivocado ⇒ `reason:"transporte"`; tope superado ⇒ `reason:"rejected"` **sin una línea nueva del lado cliente**; `method` inválido con `fetch` crudo ⇒ `-32601`. **Sin infraestructura de terceros — corre en CI, no degrada a `skip`** (a diferencia del test de integración del Hito 6). Además: N `SendMessage` concurrentes **más** un turno de actividad de webhook doble sobre el mismo `proyectoId`, con `db` real en `:memory:` ⇒ ninguna fila inconsistente, ninguna fuente bloquea a la otra — **la evidencia del ADR 90 pto 7 / Riesgo 2**. Cubre spec `servidor-a2a-jsonrpc` req "El `Task` devuelto es parseable por el Cliente A2A propio, sin modificarlo" (el escenario completo) y spec `solicitud-a2a-entrante` req "sin `KeyedQueue`" (escenario de concurrencia con webhook). ADR 86 pto 6, 87 pto 4, 90 pto 7. Comando: `npm test -- a2a-server.integration`. Commit: `test(integration): agrega a2a-server.integration.test contra el Cliente A2A del Hito 6 (Hito 7, tarea 17)`.

18. [x] **`README.md`** + **`docs/ARC42_Harness_Empresarial.md`** (ambos modificados) — **TDD exception** (documentación) — `README.md`: sección *"Variables de entorno del servidor A2A entrante"* (tabla de 5 filas, molde de `HARNESS_A2A_*`) + *"Cómo consultar el arnés como agente A2A"* (`curl` del card, `curl` de `SendMessage`, el loop de `GetTask`). `docs/ARC42_Harness_Empresarial.md`: Caja Blanca 3.2 deja de decir "hito posterior"; **Deuda 2 se cierra entera**; **Riesgo 2 se anota como confirmado**, citando la evidencia de la tarea 17 (test de concurrencia). Commit: `docs: documenta el servidor A2A entrante en README y cierra Deuda 2 y Riesgo 2 en el arc42 (Hito 7, tarea 18)`.

19. [ ] **`git diff --stat main -- src/core/` — verificación mecánica del Escenario 6** (sin código nuevo, checkpoint de esta tarea) — confirma que el único cambio dentro de `src/core/` es el archivo nuevo de la tarea 3 (`a2a-entrante-prompt.ts`) y el doc-comment de la tarea 5 (`a2a-contract.ts`) — **ningún archivo existente aparece como `MODIFIED`**. Si aparece alguno más, es señal de que alguna tarea anterior tocó el núcleo por error y debe corregirse antes de continuar. Cubre spec `solicitud-a2a-entrante` req "Cero archivos existentes de `src/core/` modificados en comportamiento" (escenario "`git diff --stat`"). ADR 91. Comando: `git diff --stat main -- src/core/`. Commit: (sin commit propio — se documenta como parte del cierre de PR6, verificado antes de abrir el PR).

### Nota de corrección al Plan (tarea 20) y verificación manual (tarea 21) — Work Unit 7

20. [ ] **`docs/progreso/v3.0-a2a-servidor/`** (nuevo, nota de corrección) — **TDD exception** (documentación, pedida por ADR 89 pto 6 y por el hallazgo de RD-37) — anota, junto al resto de la evidencia de la tarea 21: (a) la corrección al comentario del Plan sobre `agente_externo_url TEXT NOT NULL` — el protocolo A2A v1.0.0 no transporta la identidad del emisor, la columna queda nullable y siempre `NULL` en este hito (ADR 89); (b) **el hallazgo real de los dos códigos de error A2A-específicos** verificados en la tarea 1 (`A2A_ERROR_TASK_NOT_FOUND`/`A2A_ERROR_TASK_NOT_CANCELABLE`) contra `specification/a2a.proto` + `docs/specification.md` del tag `v1.0.0` — mismo tratamiento que RD-24/RD-37. Commit: `docs: documenta correccion sobre agente_externo_url y los codigos de error A2A verificados (Hito 7, tarea 20)`.

21. [ ] **Verificación manual del entregable** — **TDD exception** (sin código de producción) — los 17 pasos de `design.md` §11 con el arnés real levantado: arranque con token ⇒ log `a2a-servidor-escuchando {port:8888}`; card sin auth ⇒ `200` completo; `POST /a2a` sin token ⇒ `401`, `SELECT count(*)` no cambia; `SendMessage` con token ⇒ `SUBMITTED` inmediato (cronometrado), fila ya en base con `agente_externo_url` `NULL`; `GetTask` inmediato ⇒ encontrado; a los pocos segundos ⇒ `COMPLETED` con texto; ciclo completo contra el test de la tarea 17 con el arnés real levantado; `method` inexistente ⇒ `-32601`, no `500`; tope con `MAX_EN_VUELO=1` ⇒ el segundo `SendMessage` `REJECTED` sin `caso_id`; `CancelTask` en vuelo ⇒ `CANCELED`, resultado descartado al terminar el turno; `CancelTask` sobre `COMPLETED` ⇒ error de no cancelable, `resultado` intacto; Riesgo 2: 6 `SendMessage` concurrentes + un webhook real sobre un proyecto ⇒ sin filas inconsistentes; **`git diff --stat main -- src/core/`** (repetido con el código final, no sólo el chequeo de la tarea 19); rollback quitando el token ⇒ idéntico a `v2.2.0`; cierre con turno en vuelo (Ctrl+C durante un `SendMessage`) ⇒ drena sin `unhandledRejection`; `rg` sobre `data/harness.log` por el token ⇒ cero resultados; `npm test`/`npm run typecheck` en verde **con el test de integración corriendo, no `skipped`**. Reunir la evidencia completa (card servido, tráfico de los tres métodos, volcado de `solicitudes_a2a_entrantes` en sus cinco estados, la corrida de concurrencia, y la nota de la tarea 20) en `docs/progreso/v3.0-a2a-servidor/`. Commit: `docs: evidencia de verificacion manual del entregable Servidor A2A entrante (Hito 7, tarea 21)`.

## Después de la tarea 21

- Pasa al Reviewer (`sdd-verify` + `code-review`) contra este documento, los 2 specs y `design.md`, con atención especial a: **ADR 91/Escenario 6** (`git diff --stat main -- src/core/` como la garantía mecánica, no documental, de que no hay camino de código paralelo), **ADR 94/RD-37** (los dos códigos de error verificados en la tarea 1 — el Reviewer confirma que la verificación se hizo contra la fuente real y no se inventó), **ADR 98** (la firma de `BuildOnA2AEntranteDeps` como la garantía de que el turno entrante es de lectura, de la que depende el ADR 90 sin cola), y **ADR 90 pto 7** (la evidencia real de concurrencia de la tarea 17, no sólo la declaración).
- **Antes de dar la tarea 21 por completa**, el Reviewer/checkpoint debe confirmar las preguntas que `design.md` §15 deja genuinamente abiertas: punto 2 (ADR 93 — el resultado va sólo en `artifacts`, no también en `status.message`), punto 3 (ADR 97 — `MAX_EN_VUELO = 4` y el puerto `8888`), punto 4 (ADR 95 — el tope de `8_000` caracteres del texto entrante, no `4_000`), punto 5 (ADR 98 — confirmar que ningún hito futuro puede darle escritura a un turno entrante sin tocar esa interfaz), punto 6 (ADR 101/RD-42 — la asimetría de que sólo este servidor reporta el puerto efectivo, declarada y no reabierta en `web/`/`webhooks/`), y punto 7 (el body se lee dentro de `server.ts`, sin `body.ts` propio, por tener una sola ruta con cuerpo). El punto 1 (RD-37, los códigos de error) queda **resuelto por la tarea 1** de este documento antes de llegar al Reviewer. El punto 8 (corte de PRs y `chain_strategy`) y el punto 9 (RD-36, el gate) ya están resueltos por este documento y por el checkpoint humano — no requieren nueva confirmación. Ninguno de los siete puntos restantes bloquea el código escrito por este documento, pero sí la lectura final del entregable.
- Si aprueba: checklist de cierre de hito en `AGENTS.md`, tag `v3.0.0`, carpeta `docs/progreso/v3.0-a2a-servidor/` completa, copia de los ADR 93-**102** a `docs/ARC42_Harness_Empresarial.md` (ya iniciada en la tarea 18). **Este es el último hito del Plan** — el checklist de cierre incluye confirmar que el objetivo específico 7 del Alcance queda cerrado por completo.
- Si el Reviewer objeta la semántica asíncrona del ADR 87 (el llamador poll-ea sin garantía de reanudación si el proceso muere, RD-39) o la ausencia de cola del ADR 90, la corrección es **reabrir el checkpoint humano sobre el diseño**, no improvisar una mitigación en `sdd-apply` — ambas son decisiones ya fijadas y documentadas (`proposal.md` ADR 87/90, `design.md` §13 RD-39/RD-41).

---

**Nota de formato**: la skill `sdd-tasks` sugiere un tope de 530 palabras. Se sigue deliberadamente el formato extenso de este repo (`hito-1.2`, `hito-1.3`, `tui-canal-empleado`, `hito-2.0`, `hito-2.1`, `hito-2.2`), tal como `design.md` de este mismo change documentó en su nota de cierre: `AGENTS.md` exige que el repositorio muestre el proceso de construcción paso a paso, con archivo objetivo, requirement de origen y comando exacto por tarea. El tope genérico de la skill cede ante la convención explícita del proyecto, ya establecida por `proposal.md`, los 2 `specs/`, `design.md` y `hito-2.2-a2a-cliente/tasks.md`.
