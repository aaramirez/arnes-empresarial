> **Nota de proceso (hook de graphify)**: el hook de este repo exige correr `graphify query`/`explain`/`path` antes de leer código fuente. El ejecutor de esta fase corrió **sin herramienta de shell disponible** (solo `Read`/`Edit`/`Write`/`Grep`/`Glob`), misma limitación ya documentada por `exploration.md`, `proposal.md`, los 6 archivos de `specs/` y `design.md` de este mismo change. Se compensó con lectura directa y completa de `proposal.md` (ADR 42-47), `design.md` (las 852 líneas, ADR 48-56, §1-14, ignorando deliberadamente §15 "trabajo futuro v2.1.0" — fuera de alcance de este hito), los 6 specs (`despacho-delegacion`, `delegacion-subagentes`, `revision-pr-por-roles`, `activity-webhook-turn` delta, `hitl-generico`, `solicitud-interna-hitl`), `openspec/changes/hito-1.3-ventas-comisiones/tasks.md` y `hito-1.2-bot-revision-prs/tasks.md` (plantilla de formato) y `openspec/config.yaml`. Ninguna tarea de este documento reinterpreta el diseño: cada una desglosa un archivo o contrato ya fijado por `design.md`. Se recomienda correr `graphify update .` una vez persistido este archivo.

# Hito 5 — Delegación a subagentes: desglose de tareas

**Spec y diseño de origen** (no se duplican acá): [`proposal.md`](proposal.md) (ADR 42-47), [`specs/despacho-delegacion/spec.md`](specs/despacho-delegacion/spec.md), [`specs/delegacion-subagentes/spec.md`](specs/delegacion-subagentes/spec.md), [`specs/revision-pr-por-roles/spec.md`](specs/revision-pr-por-roles/spec.md), [`specs/activity-webhook-turn/spec.md`](specs/activity-webhook-turn/spec.md) (delta), [`specs/hitl-generico/spec.md`](specs/hitl-generico/spec.md), [`specs/solicitud-interna-hitl/spec.md`](specs/solicitud-interna-hitl/spec.md), [`design.md`](design.md) (ADR 48-56, §1-14; **§15 "trabajo futuro v2.1.0" fuera de alcance, no genera tareas**).

**Entregable funcional a demostrar**: un evento de PR dispara tres invocaciones de subagente en orden Planner → Developer → Reviewer, con tres filas en `delegaciones` del mismo `caso_id`, cada una con `tarea_delegada`/`resultado` completos y sin historial del rol anterior; el veredicto final lo emite solo el Reviewer y transiciona el estado igual que en `v1.2.0`; resolver un destino `a2a` lanza `DelegacionA2ANoImplementadaError`; `/solicitar` crea `caso`+`solicitud_interna` en una transacción, el validador emite dictamen sin transicionar, y `/aprobar-solicitud`/`/rechazar-solicitud` resuelven con confirmación en dos pasos y registro append-only (design.md §10).

**Metodología**: TDD estricto (`openspec/config.yaml: strict_tdd: true`) en toda tarea con lógica de negocio. Excepciones explícitas, marcadas una por una: tarea 3 (migración `0007`, SQL declarativo — se escribe junto a su test en `repository.test.ts`, que sí va primero), tarea 13 (`activity-contract.ts`, solo doc-comment, sin cambio de lógica ni de test), tarea 15 (migración `0008`, SQL declarativo, mismo criterio que la tarea 3), tarea 21 (`registro-acciones-contract.ts`, constantes de datos sin lógica nueva — molde de la tarea 11 de Hito 4 sobre `env.ts`), tarea 24 (verificación manual, sin código de producción).

**Nota de precedencia y decisiones de desglose propias de esta fase** (no reabren el diseño, solo ordenan tareas dentro de los PR que `design.md` §11 ya fijó):

1. **Ubicación de `repository.ts` para `delegaciones`** (tarea 10): `design.md` §11 no lo enumera explícitamente ni en PR1 ni en PR2. Se ubica en **PR2**, no en PR1, aplicando la misma lógica que el propio diseño usa para separar 3a/3b: el núcleo de PR1 (`dispatch-delegation.ts`) se testea con dobles de `DelegacionStorePort`, nunca toca SQLite real — la implementación real del store solo la necesita el wiring de `build-on-activity.ts` (PR2). La migración `0007` sí queda en PR1 porque el diseño la nombra ahí explícitamente.
2. **Split 3a/3b de solicitud interna**: `design.md` §11 dice textualmente *"separar la migración 0008 + core/solicitudes/* (3a) de los tres comandos + dispatcher (3b)"*. Por ese texto literal, `repository.ts` de solicitudes (tarea 19, que es la capa de escritura que consume el dispatcher) se ubica en **3b**, no en 3a — mismo criterio del punto 1.
3. **ADR 52 (son cuatro subagentes, no tres)**: las tareas de `definitions.ts` (5, 6) ya declaran los cuatro roles (`planner`, `developer`, `reviewer`, `validador-solicitudes`). La corrección textual de `proposal.md` ("los tres roles" → "los cuatro roles") queda **para el checkpoint/Reviewer**, no es una tarea de código.
4. **Resuelto por el checkpoint humano** (`design.md` §14): sí corresponde documentar en el `README` el registro de roles y el interruptor `HARNESS_DELEGACION_ROLES` — ver tarea 25. No corresponde abrir ya un change de `v2.1.0` para el trabajo futuro de §15 (no se abre en este hito).

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~1100-1300 (25 tareas: 3 migraciones+repository, núcleo nuevo de ~9 archivos, 2 módulos de composition root, extensión de 2 módulos existentes, más README) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR1 → PR2 → PR3a → PR3b (ver tabla abajo) |
| Delivery strategy | ask-on-risk |
| Chain strategy | **feature-branch-chain** (confirmado por el checkpoint humano) |

Decision needed before apply: No — resuelto
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

**Decisión de `chain_strategy` (confirmada)**: `feature-branch-chain`. Igual que los hitos anteriores, `AGENTS.md` fija una sola rama por hito (`hito/v2.0-delegacion-subagentes`) con un único merge humano a `main` al cierre. El corte de `design.md` §11 es explícitamente secuencial (PR1 es aditivo puro y puede quedar mergeado aunque se reviertan PR2/PR3; PR2 depende de PR1; PR3a depende de PR2 solo porque comparten rama, no por acoplamiento real de código). Cada PR de la cadena apunta al anterior, no directo a `main` — solo el cierre final del hito mergea `hito/v2.0-delegacion-subagentes` a `main`.

### Suggested Work Units

| Unit | Goal | Tasks | Est. lines | Base branch |
|------|------|-------|-----------|-------------|
| 1 | Despachador de Delegación + vocabulario HITL + roles reales (núcleo puro, testeado con dobles) | 1–9 | ~400 | `hito/v2.0-delegacion-subagentes` |
| 2 | Bot de PRs por roles (repository de `delegaciones` + cadena de revisión + wiring + interruptor) | 10–14 | ~250 | Unit 1 |
| 3a | Núcleo de solicitud interna (migración `0008` + contrato + casos de uso puros) | 15–18 | ~200 | Unit 2 |
| 3b | Comandos TUI + repository de solicitudes + wiring del dispatcher | 19–23 | ~250 | Unit 3a |
| 4 | Documentación de roles en README | 25 | ~30 | Unit 3b |
| 5 | Verificación manual end-to-end | 24* | ~0 (evidencia, no código) | Unit 4 |

\* La verificación manual (tarea 24) se ejecuta al final, tras la tarea 25, aunque conserva su numeración original del desglose por PR — ver nota en la tarea 24.

## Tareas

### PR1 — Despachador de Delegación + vocabulario HITL + roles reales (tareas 1-9)

1. [x] **`src/core/hitl/hitl-contract.ts`** (nuevo, §5.1) — **COMPLETADO** (`npm test`: 988/988 verde, `npm run typecheck`: sin errores). Test primero: `hitl-contract.test.ts` — `CASO_ESTADO_PENDIENTE_APROBACION_HUMANA === "pendiente_aprobacion_humana"`, `CASO_ESTADO_RESUELTO === "resuelto"`, `MOTIVO_NO_ENCONTRADA`/`MOTIVO_CAS` con sus literales; el archivo **no importa nada** (mismo test que `knowledge-contract.test.ts`/`activity-contract.test.ts`); las cuatro variantes de `ResolucionHitlResult` (`listado`/`requiere_confirmacion`/`aplicada`/`no_aplicable`) compilan y son exhaustivas en un `switch`. Sin fixture de LLM. Cubre spec `hitl-generico` req. "`hitl-contract.ts` es un módulo de núcleo sin imports". ADR 42, ADR 50. Commit: `feat(core): agrega hitl-contract con vocabulario HITL compartido y ResolucionHitlResult (Hito 5, tarea 1)`.

2. [x] **`src/core/ventas/ventas-contract.ts`** (modificado, §5.1) — **COMPLETADO** (RED→GREEN real: revertida temporalmente la edición de fuente con `git stash push -- ventas-contract.ts` manteniendo los asserts nuevos en el test → `npx vitest run ventas-contract.test.ts` → 1 failed / 23 passed (RED: falla exactamente el assert "no vuelve a declarar el literal"); `git stash pop` para reaplicar el re-export → mismo comando → 24/24 passed (GREEN); `npm test`: 991/991 verde; `npm run typecheck`: sin errores). Test primero: extiende `ventas-contract.test.ts` — `CASO_ESTADO_RESUELTO` importado desde `ventas-contract.ts` resuelve al mismo string que el de `hitl-contract.ts` (mismo valor, sin segunda declaración literal); ningún test existente de `v1.4.0` se modifica. Reemplaza el bloque de comentario líneas 49-63 por el re-export (`export { ... } from "../hitl/hitl-contract.js"`) con el comentario actualizado del §5.1. **También actualiza el header del módulo (línea ~12, ADR 46)**: ya no dice "Este archivo no importa nada" sin matiz — ahora documenta la única excepción (el re-export de HITL), sin usar la palabra `import` en el comentario para no romper el test existente "has no import statements" (usa "excepción"/"re-exporta"/"importa" en su lugar). Sin fixture de LLM. Cubre spec `hitl-generico` reqs. "`ventas-contract.ts` re-exporta sin duplicar..." y "El comentario... se actualiza". ADR 42, ADR 46. Commit: `refactor(core): re-exporta CASO_ESTADO_* desde hitl-contract en ventas-contract (Hito 5, tarea 2)`.

3. [x] **`src/adapters/memory/migrations/0007_delegaciones.ts`** (nuevo, §6.1) + **append** a `migrations/index.ts` (nunca reordenar entradas ya commiteadas) — **TDD exception** (SQL declarativo, se escribe junto a su test). **COMPLETADO** (`npm test`: 996/996 verde [991 baseline + 5 nuevos]; `npm run typecheck`: sin errores). Test primero: agregado caso a `repository.test.ts` (`describe("delegaciones (migración 0007)")`, 5 tests) — tras `runMigrations` (vía `openDatabase`), la tabla `delegaciones` y el índice `idx_delegaciones_caso` existen; correr las migraciones dos veces no falla (`IF NOT EXISTS`); insertar una fila con `sesion_padre_id` y `sesion_subagente_id` ambos `NULL` **funciona** (prueba de que el ADR 48 era necesario, `foreign_keys = ON` no lo bloquea); `caso_id` inexistente falla por FK; `agent_id` ausente falla por `NOT NULL`. Cubre spec `despacho-delegacion` req. "Registro de la delegación antes de invocar" (prerrequisito de esquema). ADR 48. Commit: `feat(adapters/memory): agrega migracion 0007 con tabla delegaciones y FKs nullable (Hito 5, tarea 3)`.

4. [x] **`src/core/agents/definitions.ts`** (modificado, §5.2, parte 1 — campo `description`) — **COMPLETADO** (RED→GREEN real: `npx vitest run src/core/agents/definitions.test.ts` con el nuevo `it` de `description` → 1 failed / 8 passed [RED: `expected undefined to be 'Agente conversacional...'`] → implementado `description` en `AgentDefinition`/`CONVERSATIONAL_AGENT` → mismo comando → 9/9 passed [GREEN]; `npm test`: 998/998 verde; `npm run typecheck`: sin errores). Test primero: extiende `definitions.test.ts` — `AgentDefinition` exige `description: string`; `CONVERSATIONAL_AGENT.description` es el string exacto del diseño; `systemPrompt`/`allowedTools` de `CONVERSATIONAL_AGENT` **no cambian** (incluida la frase "Todavía no tenés delegación a otros agentes"). Sin fixture de LLM. Cubre spec `delegacion-subagentes` req. "`CONVERSATIONAL_AGENT` sin cambios de comportamiento" (parcial). ADR 44 punto 3. Commit: `feat(core): agrega description obligatorio a AgentDefinition y a CONVERSATIONAL_AGENT (Hito 5, tarea 4)`.

    **Ampliación de alcance resuelta por el checkpoint humano (mismo patrón que el fix del header de la tarea 2 — brecha de diseño cerrada dentro de la misma tarea, revelada, no absorbida en silencio)**: `design.md` §5.2 no enumera qué pasa con los archivos de test que ya construyen un `AgentDefinition` a mano (`makeAgent()`/equivalente) al volverse `description` obligatorio. `npm run typecheck` tras el cambio reveló **8 archivos** rotos, no los 6 que el desglose original de la tarea había identificado (`close-turn.test.ts`, `handle-turn.test.ts`, `resolve-turn.test.ts`, `build-on-soporte.test.ts`, `build-on-submit.test.ts`, `close-turn.integration.test.ts`): también rompían `src/build-on-activity.test.ts` (línea 118, mismo patrón `makeAgent(id)`) y `src/core/turn-selector/invoke-model.test.ts` (línea 81, patrón `makeAgent(overrides)` con spread). El fix aplicado a los 8 es idéntico y mecánico: una línea `description: "agente de prueba",` agregada al objeto literal de cada `makeAgent`/equivalente, mismo placeholder en los 8 para simplicidad, sin tocar ninguna otra línea ni lógica de esos archivos. Se documenta acá en vez de abrir tarea aparte porque es la misma naturaleza de ajuste ya autorizado (dobles de test satisfaciendo un tipo, no comportamiento nuevo) — el Reviewer puede objetar si prefiere que estos 2 archivos adicionales pasen por una decisión explícita separada.

5. [x] **`src/core/agents/definitions.ts`** (modificado, §5.2, parte 2 — `SUBAGENT_REGISTRY`) — **COMPLETADO** (RED→GREEN real: `npx vitest run src/core/agents/definitions.test.ts` con los 5 nuevos `it` del describe `SUBAGENT_REGISTRY` → 4 failed / 10 passed [RED: `getSubagentDefinition is not a function`] → implementado `SUBAGENT_REGISTRY`/`getSubagentDefinition`/`listSubagentDefinitions` con los cuatro roles → mismo comando → 14/14 passed [GREEN]; `npm test`: 1003/1003 verde [998 baseline + 5 nuevos]; `npm run typecheck`: sin errores). Test primero: extiende `definitions.test.ts` — los cuatro subagentes (`planner`, `developer`, `reviewer`, `validador-solicitudes`) existen en `SUBAGENT_REGISTRY` con `description` no vacío; `reviewer.allowedTools !== developer.allowedTools`; **ninguno** de los cuatro incluye `Agent`/`Task`/`Write`/`Edit`/`Bash` en `allowedTools`; `validador-solicitudes.allowedTools` es `[]`; `listAgentDefinitions()` (registro de primer nivel) sigue teniendo **exactamente uno** (`AGENT_REGISTRY` intacto — regresión de RD-2). Sin fixture de LLM. Cubre spec `delegacion-subagentes` reqs. "Tres `AgentDefinition` reales..." (parcial), "`allowedTools` acotado por rol" (escenario "El Planner no tiene herramientas de escritura"), "Un solo nivel de profundidad de delegación". ADR 44, ADR 51, ADR 52. Commit: `feat(core): agrega SUBAGENT_REGISTRY con planner/developer/reviewer/validador-solicitudes (Hito 5, tarea 5)`.

    **Nota de alcance declarada (systemPrompt no dictado literalmente por `design.md`)**: `design.md` §5.2 fija `description` y `allowedTools` de los cuatro roles palabra por palabra (transcritos tal cual en la tabla), pero **no** da un `systemPrompt` literal para ninguno de los cuatro — a diferencia de `CONVERSATIONAL_AGENT` (tarea 4), donde el string sí estaba fijado y el test lo verifica con `toBe(...)`. El test de esta tarea (verbatim en el desglose) tampoco exige un `systemPrompt` exacto: solo `description` no vacío y `allowedTools` por rol. Se implementó un `systemPrompt` en segunda persona para cada rol, derivado directamente del `description` ya fijado por el diseño (mismo contenido, sin agregar comportamiento nuevo), en vez de inventar contenido no relacionado. Se agregaron también cuatro constantes de id (`PLANNER_AGENT_ID`/`DEVELOPER_AGENT_ID`/`REVIEWER_AGENT_ID`/`VALIDADOR_SOLICITUDES_AGENT_ID`), no mencionadas explícitamente por `design.md` §5.2/ADR 51 pero consistentes con el patrón ya existente (`CONVERSATIONAL_AGENT_ID`). Ambos puntos se dejan señalados para que el Reviewer objete si esperaba una redacción distinta.

6. [x] **`src/core/agents/subagents.ts`** (nuevo, §5.3, arc42 2.2) — **COMPLETADO** (RED→GREEN real: `npx vitest run src/core/agents/subagents.test.ts` antes de crear `subagents.ts` → `Cannot find module './subagents.js'` [RED, 0 tests] → implementado `subagents.ts` per design.md §5.3 → mismo comando → 7/7 passed [GREEN]; `npm test`: 1010/1010 verde [1003 baseline + 7 nuevos]; `npm run typecheck`: sin errores). Test primero: `subagents.test.ts` — `construirTareaDelegada` es pura (mismo input → mismo output); trunca a `TAREA_DELEGADA_MAX_CHARS` agregando `TAREA_TRUNCADA_SUFIJO` cuando el texto excede el tope; `InsumoDelegado.material` es siempre `string` plano (ningún tipo por el que pueda colarse un historial de sesión). Sin fixture de LLM (`InvocarSubagente` es solo un tipo de puerto acá; se ejercita con doble en la tarea 8). Cubre spec `delegacion-subagentes` req. "`tarea_delegada` contiene solo la tarea, nunca el historial del padre". Commit: `feat(core): agrega construirTareaDelegada puro con tope de tamano (Hito 5, tarea 6)`.

    **Nota de alcance declarada (formato exacto del texto ensamblado no dictado literalmente por `design.md`)**: `design.md` §5.3 fija literalmente `TAREA_DELEGADA_MAX_CHARS = 8_000`, `TAREA_TRUNCADA_SUFIJO`, y la firma completa de `InsumoDelegado`/`construirTareaDelegada`/`InvocacionSubagenteResult`/`InvocarSubagente` — todo transcrito verbatim. No fija, sin embargo, el formato exacto del string que ensambla `construirTareaDelegada` a partir de `rol`+`insumo` (solo dice "ensambla el texto... hay un punto donde mirar"). Se implementó un formato de tres secciones (encabezado `Rol delegado: {id} — {description}`, instrucción, material acotado) — deliberadamente **sin** incluir `rol.systemPrompt` en ningún punto, porque ese texto ya viaja aparte como parte de la `AgentDefinition` propia del rol registrada en `options.agents` (tarea 9); incluirlo acá lo duplicaría en la fila de `delegaciones` sin necesidad y sería más superficie para que un historial se filtre. El truncado sigue el molde exacto de `truncarTexto` de `activity-prompt.ts` (slice + sufijo, sin descontar el largo del sufijo del tope). Se deja señalado para que el Reviewer objete si esperaba un formato distinto.

7. [x] **`src/core/turn-selector/dispatch-delegation.ts`** (nuevo, §5.4, parte 1 — tipos y resolución) — **COMPLETADO** (RED→GREEN real: `npx vitest run src/core/turn-selector/dispatch-delegation.test.ts` antes de crear `dispatch-delegation.ts` → `Cannot find module './dispatch-delegation.js'` [RED, 0 tests] → implementado `DestinoDelegacion`/`resolverDestino`/`DelegacionA2ANoImplementadaError`/`SubagenteDesconocidoError` per design.md §5.4 parte 1 → mismo comando → 5/5 passed [GREEN]; `npm test`: 1015/1015 verde [1010 baseline + 5 nuevos]; `npm run typecheck`: sin errores). Test primero: `dispatch-delegation.test.ts` — `resolverDestino(agentId)` de un rol conocido devuelve `{kind:"in-process", agentId}`; la función es **síncrona** (test explícito, molde de `procesar-devolucion`), cero `await`; `DelegacionA2ANoImplementadaError` y `SubagenteDesconocidoError` son clases de `Error` con nombre propio. Sin fixture de LLM. Cubre spec `despacho-delegacion` req. "`DestinoDelegacion` como unión discriminada resuelta por función pura". ADR 45. Commit: `feat(core): agrega resolverDestino sincrono y errores tipados del despachador (Hito 5, tarea 7)`.

    **Nota de alcance declarada (test adicional no listado verbatim en el desglose)**: el texto verbatim de esta tarea no enumera explícitamente un test para `agentId` desconocido, pero design.md §8 ("`agentId` no está en `SUBAGENT_REGISTRY`" ⇒ `SubagenteDesconocidoError`, "antes de tocar la base") y §9 atribuyen esa responsabilidad a `resolverDestino` — es la única función de este archivo que consulta el registro. Se agregó un tercer test (`resolverDestino("rol-inexistente")` lanza `SubagenteDesconocidoError`) para que la clase de error tenga un punto de uso real, no solo una instanciación aislada en el test de "nombre propio". El Reviewer puede objetar si prefería que ese test quedara reservado para la tarea 8.

8. [x] **`src/core/turn-selector/dispatch-delegation.ts`** (modificado, §5.4, parte 2 — `despacharDelegacion`/`despacharCadena`) — **COMPLETADO** (RED→GREEN real: `npx vitest run src/core/turn-selector/dispatch-delegation.test.ts` antes de implementar `despacharDelegacion`/`despacharCadena` → `TypeError: despacharDelegacion is not a function` / `TypeError: despacharCadena is not a function` en 6 tests [RED, 5 previos de la tarea 7 seguían en verde] → implementado `DelegacionStorePort`/`DespacharDelegacionDeps`/`DelegacionAplicada`/`Eslabon`/`despacharDelegacion`/`despacharCadena` per design.md §5.4 parte 2 → mismo comando → 11/11 passed [GREEN]; `npm test`: 1021/1021 verde [1015 baseline + 6 nuevos]; `npm run typecheck`: sin errores). Test primero: extiende `dispatch-delegation.test.ts` con doble de `InvocarSubagente` y de `DelegacionStorePort` — orden `crear → invocar → completar` (spy ordenado, `callOrder: string[]`, mismo molde de `handle-turn.test.ts`); destino `a2a` lanza `DelegacionA2ANoImplementadaError` **antes** de crear ninguna fila (no importa ningún módulo de `src/adapters/a2a/`, que no existe); falla del invocador deja la fila **sin** `resultado`, propaga sin revertir, el estado canónico no se toca; `despacharCadena` de 3 eslabones encadena `sesionPadreId` (planner sin padre, developer=sesión planner, reviewer=sesión developer) y el `material` del rol N+1 es el `resultado` del rol N (un test adicional confirma que la falla de un eslabón intermedio propaga sin capturar y sin invocar al eslabón siguiente); `parent_tool_use_id` ausente no bloquea el despacho. **Fixture de LLM**: sí — doble de `InvocarSubagente`. Cubre spec `despacho-delegacion` reqs. "El brazo A2A lanza un error tipado, sin adaptador" (**este es el test que documenta el contrato con Hito 6 — ADR 45 punto 3, borrar en Hito 6**), "Registro... antes de invocar, completado después" (ambos escenarios), "Cadena determinista Planner → Developer → Reviewer", "`parent_tool_use_id` se registra como correlación". ADR 45, ADR 48. Commit: `feat(core): agrega despacharDelegacion y despacharCadena con brazo A2A tipado (Hito 5, tarea 8)`.

    **Nota de alcance declarada (`Eslabon` sin firma literal en design.md)**: design.md §5.4 nombra `Eslabon` únicamente como el parámetro de `despacharCadena` (`eslabones: readonly Eslabon[]`) y en la firma de `construirEslabonesRevision` de §5.5 (tarea 11) — nunca declara su forma. Se implementó como `{ agentId: string; insumo: InsumoDelegado }`, reutilizando `InsumoDelegado` (§5.3, ya existente) en vez de duplicar campos `instruccion`/`material`. Para el primer eslabón, `insumo.material` se usa tal cual (p. ej. el prompt de la actividad, que `construirEslabonesRevision` embutirá en la tarea 11); para los siguientes, `despacharCadena` reemplaza `insumo.material` por el `resultado` del eslabón anterior, sin tocar `insumo.instruccion` (fija por rol). El Reviewer puede objetar si esperaba una forma distinta.

    **Nota de alcance declarada (`despacharDelegacion` gana un campo `destino?` no listado en la firma literal de design.md)**: la firma de §5.4 solo declara `casoId`/`agentId`/`insumo`/`sesionPadreId` como input de `despacharDelegacion`. Sin embargo, el propio doc-comment de `resolverDestino` (tarea 7, ya commiteado y aprobado) anticipa explícitamente el mecanismo de esta tarea: *"la rama `a2a`... se ejercita recién en la tarea 8 construyendo el destino a mano y pasándoselo directo a `despacharDelegacion`"*. Como ningún `agentId` real de `SUBAGENT_REGISTRY` resuelve jamás a `a2a` (`resolverDestino` los resuelve todos a `in-process` en este hito), la única forma de ejercitar el brazo `a2a` llamando literalmente a `despacharDelegacion` es aceptando un `destino` ya resuelto por parámetro opcional, que el test construye a mano. Se agregó `input.destino?: DestinoDelegacion`, ausente en producción (donde siempre se resuelve con `resolverDestino(agentId)`). Disclosed para que el Reviewer confirme o proponga una forma distinta (p. ej. una función separada en vez de un campo opcional).

9. [x] **`src/core/turn-selector/invoke-model.ts`** (modificado, §5.8) — **COMPLETADO** (RED→GREEN real: `npx vitest run src/core/turn-selector/invoke-model.test.ts` con los 4 `it` nuevos/modificados de esta tarea [1 modificado: "registers the agent under options.agents[id]..." pasado a `agent.description` verbatim + `subagentes: []` inyectado; 3 nuevos: registro de 5 entradas en `options.agents`, forward verbatim de `description`, no ensanchamiento de `options.allowedTools`; más 2 `it` nuevos del describe `parentToolUseId`] → 4 failed / 14 passed [RED: `expected [...] to have a length of 5 but got 1` en el test de registro múltiple; `expected 'Agente principal del Hito 1...' to be '...'` en el test de forward verbatim y en el de exact-match de Fix 1 (seguía sintetizando vía `toMainThreadAgentDescription`); `expected undefined to be 'tool-use-123'` en el test de captura de `parentToolUseId`] → implementado el cambio exacto de §5.8 (`toMainThreadAgentDescription` eliminada; `toSdkAgentDefinition` usa `agent.description` tal cual; `toQueryOptions` gana `subagentes` con default `listSubagentDefinitions()` y registra el padre + los cuatro roles en `options.agents`, sin tocar la derivación de `options.allowedTools` [solo agente principal]; `invokeModel` gana el mismo parámetro trailing `subagentes` y lo reenvía; `InvokeModelResult` gana `parentToolUseId?: string`, capturado de un mensaje `assistant` con `parent_tool_use_id` no nulo, agregado al objeto de retorno solo cuando está definido [mismo patrón incremental de `resume`/`allowedTools`/`mcpServers`, `exactOptionalPropertyTypes: true`]) → mismo comando → 18/18 passed [GREEN]; `npm test` [con `dist/` limpiado primero — mismo gotcha de doble conteo de Vitest ya documentado]: 1026/1026 verde [1021 baseline + 5 nuevos]; `npm run typecheck`: sin errores, **sin ampliación de alcance** [a diferencia de la tarea 5, ningún archivo de test adicional se rompió — este cambio no toca ninguna firma consumida por otros módulos: `handle-turn.ts:240` sigue pasando 6 argumentos posicionales y el nuevo 7º parámetro de `invokeModel` (`subagentes`) tiene default, así que compila sin tocarlo]). Test primero: extiende `invoke-model.test.ts` — `toMainThreadAgentDescription` ya no existe (ni el símbolo ni su test propio); `toSdkAgentDefinition` usa `agent.description` tal cual (no sintetizado); `toQueryOptions` con `subagentes` (parámetro con default `listSubagentDefinitions()`) registra **5** entradas en `options.agents` (padre + 4 roles), cada una con su propio `description`; `options.agent` sigue siendo el agente del hilo principal; `options.allowedTools` se deriva **solo** del agente principal (un subagente registrado pero no invocado no auto-aprueba nada); `InvokeModelResult.parentToolUseId` es opcional y su ausencia no rompe nada. Actualiza los doc-comments de las líneas 6-9 (el Despachador ya existe; `tool_use` de delegación del modelo queda fuera de alcance con motivo) y 126-132 (`description` es dato de ruteo). **Fixture de LLM**: sí — `queryFn` fake (ya existe en el módulo). Cubre spec `delegacion-subagentes` reqs. "`toMainThreadAgentDescription` se retira..." (ambos escenarios), "`options.agents` registra el padre y los tres roles" (con la corrección del ADR 52, son cuatro), "`CONVERSATIONAL_AGENT` no gana `allowedTools` de delegación". ADR 44, ADR 46, ADR 51. Commit: `feat(core): registra multiples agentes en toQueryOptions y retira toMainThreadAgentDescription (Hito 5, tarea 9)`.

    **Nota de alcance declarada (default de `subagentes` aplica a *todo* llamador de `invokeModel`, no solo al camino de delegación)**: `design.md` §5.8 punto 2 fija el default de `subagentes` en `listSubagentDefinitions()` y dice explícitamente "producción omite" el argumento — no distingue entre el turno conversacional de primer nivel (`handle-turn.ts:240`) y el futuro `invocar` de la tarea 14 (`build-on-activity.ts`, invocado por el Despachador para cada rol). Tomado literalmente, esto significa que **todo** turno — incluida la conversación normal del empleado, no solo las delegaciones — pasa a registrar los cuatro roles en `options.agents` por default. No hay contradicción con `options.allowedTools` (que sigue derivándose solo del agente principal, cubierto por su propio test), pero sí es una decisión de alcance real: el `Agent` tool nunca queda auto-aprobado para `CONVERSATIONAL_AGENT`, así que el modelo no puede invocar ningún rol aunque los vea registrados — la superficie que se ensancha es puramente informativa (`options.agents` como catálogo), no de autorización. Se implementó tal cual el texto literal del diseño, sin agregar un interruptor para excluir el turno conversacional del registro de subagentes (no pedido por ningún test de esta tarea ni por ningún ADR). El Reviewer puede objetar si prefería que el registro de subagentes quedara acotado al camino de delegación (`invocar`, tarea 14) en vez de aplicar a todo llamador de `invokeModel`.

### PR2 — Bot de PRs por roles (tareas 10-14)

10. [x] **`src/adapters/memory/repository.ts`** (modificado, §6.3, funciones de `delegaciones`) — **COMPLETADO** (RED→GREEN real: `npx vitest run src/adapters/memory/repository.test.ts` con los 5 `it` nuevos del describe `insertDelegacion / completarDelegacion / listDelegacionesPorCaso` → `TypeError: insertDelegacion is not a function` en los 4 tests que la usan [RED, 95 previos seguían en verde] → implementadas `insertDelegacion`/`completarDelegacion`/`listDelegacionesPorCaso` per design.md §6.3 → mismo comando → 99/99 passed [GREEN]; `npm test` [con `dist/` limpiado primero — mismo gotcha de doble conteo de Vitest ya documentado]: 1031/1031 verde [1026 baseline + 5 nuevos]; `npm run typecheck`: sin errores). Test primero: extiende `repository.test.ts` — `insertDelegacion` escribe una fila con `sesion_padre_id`/`sesion_subagente_id` `NULL` y `resultado` `NULL`; un segundo test confirma que `sesionPadreId` se persiste cuando el eslabón no es la cabeza de la cadena (triangulación); `completarDelegacion` es **atómica** (una `db.transaction`: `INSERT sesiones_agente` + `UPDATE delegaciones`, molde exacto de `resolverEscalacionTransaccional`, líneas 1403-1455) — un test de camino feliz confirma que ambas escrituras ocurren juntas, y un test de fallo confirma que una sesión huérfana sin su delegación completada **no queda posible**: `delegacionId` inexistente hace que la transacción entera revierta (incluida la fila de `sesiones_agente` recién insertada dentro de la misma transacción) y lanza `DelegacionNotFoundError`; `listDelegacionesPorCaso` devuelve en orden `created_at` (tres filas insertadas fuera de orden, verificadas en orden). Sin fixture de LLM (SQLite `:memory:`). Cubre spec `despacho-delegacion` reqs. "Delegación exitosa deja fila completa", "Falla del subagente deja la fila sin `resultado`". ADR 48. Commit: `feat(adapters/memory): agrega insertDelegacion y completarDelegacion transaccional (Hito 5, tarea 10)`.

    **Nota de alcance declarada (`DelegacionNotFoundError` no dictado literalmente por `design.md`)**: `design.md` §6.3 fija la firma de `completarDelegacion` con retorno `void`, sin mencionar un error de "no encontrado". Sin embargo, el propio texto de la tarea 10 (verbatim) exige la invariante *"una sesión huérfana sin su delegación completada no debe quedar posible"*. Sin una guarda que aborte la transacción cuando `delegacionId` no matchea ninguna fila, un `UPDATE` sin `RETURNING` simplemente afecta 0 filas **sin lanzar** — dejando exactamente la sesión huérfana que la invariante prohíbe (la fila de `sesiones_agente` quedaría insertada y comprometida, sin ninguna fila de `delegaciones` completada que la referencie). Se agregó `DelegacionNotFoundError` (mismo patrón que `CasoNotFoundError`/`ActividadNotFoundError`/`SesionAgenteInvalidCasoError` ya existentes en el archivo) y una guarda `if (changes === 0) throw ...` dentro de la misma `db.transaction`, para que la invariante sea estructuralmente cierta y no solo una expectativa del camino feliz. El Reviewer puede objetar si prefería que esta guarda quedara fuera de alcance de la tarea 10 (p. ej., delegada a un caller que garantice `delegacionId` válido por construcción).

11. [x] **`src/core/activity/cadena-revision.ts`** (nuevo, §5.5) — **COMPLETADO** (RED→GREEN real: `npx vitest run src/core/activity/cadena-revision.test.ts` antes de crear `cadena-revision.ts` → `Cannot find module './cadena-revision.js'` [RED, 0 tests] → implementado `construirEslabonesRevision`/`despacharRevisionPorRoles` per design.md §5.5 → mismo comando → 6/6 passed [GREEN]; `npm test` [con `dist/` limpiado primero — mismo gotcha de doble conteo de Vitest ya documentado]: 1037/1037 verde [1031 baseline + 6 nuevos]; `npm run typecheck`: sin errores). Test primero: `cadena-revision.test.ts` con doble de `DespacharDelegacionDeps` — `construirEslabonesRevision` produce las tres instrucciones en orden fijo Planner → Developer → Reviewer; **solo** la instrucción del Reviewer menciona `VEREDICTO_PREFIX`; `despacharRevisionPorRoles` corre `despacharCadena` y devuelve `{responseText: resultado del último eslabón, agentLabel: REVIEWER_AGENT_ID}`; el `material` del rol N+1 es el `resultado` (texto) del rol N, nunca su sesión — aunque el Planner "alucinara" una línea `VEREDICTO:`, ese texto nunca llega al outcome devuelto. **Fixture de LLM**: sí — doble de despacho (mismo patrón de `InvocarSubagente`). Cubre spec `revision-pr-por-roles` req. "El veredicto final lo emite únicamente el Reviewer"; ejercita en su forma específica del PR la cadena determinista de `despacho-delegacion`. ADR 44, ADR 47. Commit: `feat(core): agrega cadena-revision con la secuencia Planner-Developer-Reviewer del bot de PRs (Hito 5, tarea 11)`.

    **Nota de alcance declarada (nombres de constantes de rol — `ROL_PLANNER`/`ROL_DEVELOPER`/`ROL_REVIEWER` del pseudocódigo de design.md §5.5 vs. las constantes reales)**: el bloque `ts` de §5.5 nombra `ROL_PLANNER`/`ROL_DEVELOPER`/`ROL_REVIEWER` como si fueran constantes nuevas de este módulo. Verificado contra el estado real de `src/core/agents/definitions.ts` (tarea 5, ya commiteada): esas constantes **ya existen** con otro nombre — `PLANNER_AGENT_ID`/`DEVELOPER_AGENT_ID`/`REVIEWER_AGENT_ID` (mismo valor string: `"planner"`/`"developer"`/`"reviewer"`). Se reusaron las constantes reales en vez de declarar un segundo juego duplicado apuntando al mismo string — mismo criterio que la instrucción explícita de la tarea ("confirmá los nombres exactos... no los inventes"). `despacharRevisionPorRoles` devuelve `agentLabel: REVIEWER_AGENT_ID`. El Reviewer puede objetar si prefería que el módulo declarara alias `ROL_*` como re-export de esas constantes para matchear el pseudocódigo literal.

12. [x] **`src/core/activity/run-activity-turn.ts`** (modificado, §5.5, cuatro ediciones exactas: líneas 73, 158, 235, 147). **COMPLETADO** (RED→GREEN real: `npx vitest run src/core/activity/run-activity-turn.test.ts` tras renombrar la clave del doble a `despacharRevision` y agregar el test nuevo → 7 failed / 2 passed [`TypeError: runTurn is not a function`, RED] → renombrado `RunActivityTurnDeps.runTurn` → `despacharRevision` [mismo tipo] en las tres ediciones de código (línea 73 declaración+doc-comment, línea 158 desestructuración, línea 235 la llamada) + doc-comment de la secuencia de nueve pasos → mismo comando → 9/9 passed [GREEN]; `npm test` [con `dist/` limpiado primero — mismo gotcha de doble conteo de Vitest ya documentado]: 1032/1038, **6 failed, todos en `src/build-on-activity.test.ts`** — ver nota de alcance debajo, no es una regresión de este archivo sino la tarea 14 sin cablear todavía; `npm run typecheck`: **3 errores, los tres en `src/build-on-activity.ts:223`** — ver misma nota). Test primero: `run-activity-turn.test.ts` — clave del doble inyectado renombrada de `runTurn` a `despacharRevision` en todo el archivo (sin reescribir los asserts de `estadoAnterior`/`estado`/`veredicto`); test nuevo `"falla del Developer (segundo rol de la cadena) no invoca ningun metodo de store ni de board tras el paso 5"` — el doble de `despacharRevision` rechaza (simulando lo que pasaría si `despacharCadena` fallara en el eslabón del Developer) y se assertan `store.updateActividadEstado`, `board.publicarRevision` y `board.mirrorEstado` como no invocados (el alcance del assert se limita a los efectos que ocurren *después* del paso 5 — `store.findActividadPorReferencia`/`createCasoConActividad` y `board.leerMetadatos` corren *antes* de él en la secuencia real, así que un assert de "cero llamadas a store/board" en términos absolutos habría sido incoherente con el propio flujo que la tarea no toca). Doc-comment de la secuencia de nueve pasos actualizado (el paso 5 pasa a describir la cadena). **Fixture de LLM**: sí — doble (ya existente, solo cambia la clave). Cubre spec `activity-webhook-turn` (delta) reqs. "Parseo de veredicto..." (escenario "El veredicto proviene del Reviewer, no del Planner ni del Developer"), "El paso 5 despacha una delegación por roles..." (ambos escenarios). ADR 47, ADR 54. Commit: `refactor(core): renombra runTurn a despacharRevision en el paso 5 de runActivityTurn (Hito 5, tarea 12)`.

    **Nota de alcance declarada #1 (número de línea del doc-comment de la secuencia — la tabla de `design.md` cita `147`, el archivo real tenía el paso 5 en `143`)**: verificado contra el estado real del archivo antes de editar. La tabla de cuatro ediciones de `design.md` §5.5 cita la línea `147` para "doc de la secuencia" — en el archivo real (post-tarea 11), la línea 147 caía dentro del texto del **paso 7** (`* Propaga si falla.`, continuación de `* 7. store.updateActividadEstado — SIEMPRE...`), no del paso 5. El doc-comment del paso 5 real estaba en las líneas **143-144** (`* 5. runTurn(actividad.casoId, prompt) — propaga si falla, sin llamar a` / `* nada más después.`). Las otras tres líneas citadas (73, 158, 235) sí coincidieron exactamente con el archivo real. Mismo criterio que el drift de línea ya documentado en tareas anteriores (p. ej. tarea 10, `resolverEscalacionTransaccional` líneas 1403-1455): se editó la ubicación real del paso 5 (143-144), no el número 147 literal, porque ahí es donde vive semánticamente el contenido que la tarea pide actualizar.

    **Nota de alcance declarada #2 (hallazgo, NO resuelto — `src/build-on-activity.ts` deja de compilar y 6 tests de `build-on-activity.test.ts` empiezan a fallar)**: exactamente la advertencia que la tarea pedía vigilar. `build-on-activity.ts:223` sigue cableando la clave vieja (`runTurn: (casoId, prompt) => {...}`) porque su wiring real es la tarea 14, no esta. Tras el renombre: (a) `npm run typecheck` reporta 3 errores en esa línea — `TS2353: Object literal may only specify known properties, and 'runTurn' does not exist in type 'RunActivityTurnDeps'` más dos `TS7006` (parámetros `casoId`/`prompt` quedan con tipo implícito `any` porque el compilador ya no puede inferirlos desde una propiedad que no existe); (b) en runtime (vitest no type-checkea), `deps.despacharRevision` queda `undefined` dentro de `runActivityTurn`, así que el paso 5 lanza `TypeError: despacharRevision is not a function` en cuanto se invoca — eso hace fallar 6 tests de `build-on-activity.test.ts` que dependían de que el turno completara el ciclo (`incluye stage en el log...`, `queue.run envuelve el ciclo COMPLETO...`, `createKnowledge(casoId) se invoca UNA VEZ...`, los dos de serialización de `KeyedQueue`, y el de aislamiento de citas). Ninguno de esos 6 tests toca lógica de este archivo — todos viven en `build-on-activity.test.ts`, fuera del alcance de la tarea 12. **No se tocó `build-on-activity.ts`** para no adelantar la tarea 14 (instrucción explícita: ese cableado, incluido el interruptor `HARNESS_DELEGACION_ROLES`, es su responsabilidad). El Reviewer puede confirmar que este es el estado esperado de un hito partido en PRs (`PR2 — tareas 10-14`) con la tarea 14 todavía pendiente, o pedir que la 14 se aplique en la misma tanda para no dejar la suite roja entre commits.

13. [x] **`src/core/activity/activity-contract.ts`** (modificado, línea 24) — **TDD exception** (solo doc-comment, sin cambio de lógica). **COMPLETADO** (`npx vitest run activity-contract.test.ts run-activity-turn.test.ts`: 22/22 verde, ninguno de los dos tests modificado; `npm test` [con `dist/` limpiado primero]: 1032/1038 verde, los 6 fallos son preexistentes de la tarea 12 en `build-on-activity.test.ts`, ningún fallo nuevo; `npm run typecheck`: 3 errores, los mismos tres preexistentes de la tarea 12 en `build-on-activity.ts:223`, ningún error nuevo). Hecho: el comentario de `ACTIVIDAD_TIPOS` pasa a explicar por qué `solicitud_interna` sigue sin ejercitarse sobre `actividades` (`actividades.proyecto_id NOT NULL REFERENCES proyectos(id)`, una solicitud de vacaciones no tiene proyecto — ADR 43); `activity-contract.test.ts` existente sigue en verde sin modificarse. ADR 43, ADR 46. Commit: `docs(core): actualiza comentario de ACTIVIDAD_TIPOS sobre solicitud_interna sin ejercitar (Hito 5, tarea 13)`.

    **Condición 2 del Reviewer de la tarea 12, resuelta en esta misma pasada**: el doc-comment de módulo de `src/core/activity/run-activity-turn.ts` (líneas 6-9) todavía nombraba `runTurn`/`RunActivityTurnDeps.runTurn`, símbolo que la tarea 12 ya había renombrado a `despacharRevision` en el resto del archivo (el propio doc-comment del campo, líneas 67-79, ya estaba correcto — solo el doc-comment superior del módulo había quedado desactualizado). Se actualizó para nombrar `despacharRevision`/`RunActivityTurnDeps.despacharRevision` y describir la forma real del contrato (cadena de delegación Planner → Developer → Reviewer vía `despacharRevisionPorRoles`, o `handleTurn` bajo `HARNESS_DELEGACION_ROLES="off"`), sin tocar ninguna otra línea del archivo. Mismo test (`run-activity-turn.test.ts`, 9/9) sigue en verde sin modificarse.

    **Rechazo del Reviewer sobre la tarea 13 (segunda iteración, resuelto en esta pasada)**: el doc-comment de módulo recién descrito quedó afirmando en presente y sin salvedad que el interruptor `HARNESS_DELEGACION_ROLES="off"` ya cierra el composition root — pero `rg -n "HARNESS_DELEGACION_ROLES"` en todo `src/` confirmó que ese identificador solo aparece en doc-comments, ningún `process.env.HARNESS_DELEGACION_ROLES` real todavía (ese cableado es la tarea 14, sin aplicar). Se agregó la misma salvedad que ya tenía el doc-comment de campo (`RunActivityTurnDeps.despacharRevision`, tarea 12): `(ADR 54, cableado real pendiente en la tarea 14)`, sin tocar ninguna otra línea. `run-activity-turn.test.ts` sigue 9/9 en verde; `npm test` [con `dist/` limpiado primero]: 1032/1038, mismos 6 fallos preexistentes de la tarea 12 en `build-on-activity.test.ts`; `npm run typecheck`: mismos 3 errores preexistentes en `build-on-activity.ts:223`. Ningún fallo ni error nuevo.

14. [x] **`src/build-on-activity.ts`** (modificado, §6.4, ADR 53/54) — **COMPLETADO** (RED→GREEN real: extendido `build-on-activity.test.ts` con `vi.mock("./core/turn-selector/invoke-model.js")` + un nuevo describe dedicado al interruptor [6 tests: 2 vía `it.each` para `HARNESS_DELEGACION_ROLES` ausente/"on", 1 para `"off"`, 1 dedicado a `resumeSessionId: undefined` + `memory.getLatestSesionAgente` no invocado, 1 dedicado a que `createKnowledge` no se construye con el interruptor activo, 1 para `createDelegacionStore` round-trip vía `listDelegacionesPorCaso`] antes de cablear nada en `build-on-activity.ts` → `npx vitest run src/build-on-activity.test.ts` → 6 failed (los mismos 6 preexistentes de la tarea 12, RED, sin ningún test nuevo corriendo todavía porque el archivo no compilaba: `createDelegacionStore`/`invokeModel` no eran símbolos exportados) → cableado el wiring real (`despacharRevision:` reemplaza `runTurn:`, interruptor `HARNESS_DELEGACION_ROLES` vía `process.env` leído solo en el composition root [precedente ADR 17], `createDelegacionStore(db)` envolviendo `insertDelegacion`/`completarDelegacion` de la tarea 10, closure `invocar` cerrando `invokeModel` con `resumeSessionId: undefined` explícito y `caso` con placeholders no leídos nunca por `invokeModel` fuera de `.id`, resolución de `createKnowledge` movida DENTRO de la rama `"off"` del ternario para que la rama de roles nunca la evalúe) → mismo comando → 19/19 passed [GREEN, los 13 tests preexistentes + los 6 nuevos]; `npm test` [con `dist/` limpiado primero — mismo gotcha de doble conteo de Vitest ya documentado]: **1044/1044 verde** [1038 baseline de la tarea 13 (1032 pasando + 6 rojos) + 6 nuevos, los 6 rojos vueltos verdes]; `npm run typecheck`: **0 errores** [los 3 preexistentes de la tarea 12/13 en `build-on-activity.ts:223` — `TS2353` + 2×`TS7006` — desaparecieron, ningún error nuevo]). Test primero: extiende `build-on-activity.test.ts` — el interruptor `HARNESS_DELEGACION_ROLES` bindea el closure correcto en cada valor (`"off"` ⇒ el `handleTurn` único de `v1.4.0`; cualquier otro valor o ausente ⇒ `despacharRevisionPorRoles`); `createDelegacionStore(db)` se construye a partir de las funciones de la tarea 10; el closure `invocar` cierra `invokeModel` con `resumeSessionId: undefined` **explícito** — test dedicado que asserta que esa implementación **no** llama a `memory.getLatestSesionAgente` (ADR 53, cierra RD-5); con el interruptor activo **no** se construye `createKnowledge` (ningún rol tiene el tool de conocimiento en su `allowedTools`). **Fixture de LLM**: sí — doble de `invokeModel`/`queryFn` (`vi.mock` wholesale del módulo, mismo criterio que `handleTurn` ya mockeado en este archivo). Cubre spec `delegacion-subagentes` req. "Invocación in-process contra la `AgentDefinition` propia del rol" (a nivel de wiring real). ADR 53, ADR 54. Commit: `feat(root): conecta el despachador de delegacion en build-on-activity con interruptor de degradacion (Hito 5, tarea 14)`.

    **Cierra la condición 1 del Reviewer de la tarea 12 (nota de alcance declarada #2 de esa tarea)**: la tarea 12 había dejado documentado y sin resolver que `build-on-activity.ts:223` seguía cableando la clave vieja `runTurn:` contra `RunActivityTurnDeps` ya renombrado a `despacharRevision`, con 6 tests de `build-on-activity.test.ts` en rojo y 3 errores de `typecheck` — condición explícita de "el Reviewer puede... pedir que la 14 se aplique... para no dejar la suite roja entre commits". Esta tarea es esa aplicación: el wiring real reemplaza `runTurn:` por `despacharRevision:`, la suite completa vuelve a **1044/1044 verde** (sin fallos preexistentes ni nuevos) y `npm run typecheck` queda en **0 errores** (los tres de `build-on-activity.ts:223` desaparecen sin que aparezca ninguno nuevo). Cierre verificado con la misma secuencia de comandos que documentó el hallazgo (`npx vitest run` del archivo puntual → `npm test` con `dist/` limpio → `npm run typecheck`).

    **Nota de alcance declarada (`AssembledContext.caso` con placeholders para el closure `invocar`)**: la firma de `DespacharDelegacionDeps.invocar` (tarea 8) solo recibe `{ agent, casoId, tareaDelegada }` — sin el resto de un `CasoSnapshot` real (`tipo`/`estado`/`createdAt`/`updatedAt`), que tampoco existe para un subagente (no hay una fila de `casos` propia del rol delegado). El pseudocódigo de `design.md` §6.4 ya anticipa esto con una elipsis literal (`caso: { id: casoId, … }`). Verificado contra `invoke-model.ts`: `invokeModel` solo lee `context.caso.id` (para `hookContext.casoId`) — ningún otro campo de `CasoSnapshot` se usa en ningún punto de ese módulo. Se completaron los campos no leídos con placeholders explícitos (`tipo: "delegacion"`, `estado: "activo"`, `createdAt`/`updatedAt` = `now()`) en vez de inventar un cuarto tipo opcional o relajar `AssembledContext` — el Reviewer puede objetar si prefería una forma distinta (p. ej. una variante de `CasoSnapshot` con campos opcionales solo para este camino).

    **Condición del Reviewer sobre la tarea 14, RESUELTA (loop de rechazo/condición, esta pasada)**: el Reviewer aprobó la tarea CON CONDICIÓN — `tipo: "delegacion"` no era problema (no colisiona con ningún valor real de dominio), pero `estado: "activo"` sí: es el valor literal de `CASO_ESTADO_ACTIVO`, indistinguible de un caso genuinamente activo si algún código futuro llegara a leer `context.caso.estado` (hoy no lo hace — el propio ADR 53/RD-5 existe justo para prevenir esta clase de falla silenciosa). El Reviewer señaló una alternativa mejor y disponible que la nota de arriba no había explorado: el `casoId` que llega a `invocar` NO es sintético — es el `casoId` REAL del caso padre del turno de actividad (`actividad.casoId`, el mismo que `despacharCadena` reutiliza para los tres eslabones Planner/Developer/Reviewer), que YA tiene una fila real en `casos` insertada transaccionalmente por `createCasoConActividad` **antes** de que `runActivityTurn` invoque `despacharRevision` — y por lo tanto antes de que este closure corra. Fix aplicado: el placeholder se reemplazó por una lectura real, `getCasoById(db, casoId)` (ya exportada de `repository.ts:113`, módulo que este archivo ya importa con `db` en scope). Manejo de `undefined`: dado el invariante recién descrito, un `undefined` en ese punto es corrupción de estado, no una entrada externa esperable — se falla RUIDOSAMENTE con `CasoNotFoundError` (reusada de `repository.ts`, mismo criterio que `ActividadTipoEstadoInvalidoError` ya declarado en este archivo), en vez de fabricar un segundo placeholder disfrazado de dato real. Test nuevo en `build-on-activity.test.ts`: inserta un caso real con `tipo`/`estado` reconocibles (`"tipo-reconocible"`/`"estado-reconocible"`, deliberadamente distintos del placeholder viejo) y confirma que esos valores reales — no el placeholder — llegan al `context.caso` que reciben los tres roles vía `invokeModel`; un segundo test nuevo, con `getCasoById` espiado (`vi.spyOn`, sin tocar la fila real por la FK de `delegaciones.caso_id`), confirma que la rama `undefined` lanza `CasoNotFoundError` y que `buildOnActivity` la traga en su límite de nivel superior (nunca rechaza), logueándola como `actividad-turno-fallido`. `npx vitest run src/build-on-activity.test.ts`: 21/21 verde (19 preexistentes + 2 nuevos); `npm test` [con `dist/` limpiado primero]: 1046/1046 verde; `npm run typecheck`: 0 errores. Sin regresión respecto del estado ya aprobado por el Reviewer.

### PR3a — Núcleo de solicitud interna (tareas 15-18)

15. [x] **`src/adapters/memory/migrations/0008_solicitudes_internas.ts`** (nuevo, §6.2) + **append** a `migrations/index.ts` — **TDD exception** (SQL declarativo). **COMPLETADO** (RED→GREEN real: se agregó el describe `"solicitudes_internas (migración 0008)"` a `repository.test.ts` con los 4 casos exigidos por la tarea ANTES de crear la migración → `npx vitest run repository.test.ts -t "solicitudes_internas"` → 4 failed [RED, los 4 por la razón esperada: `no such table: solicitudes_internas` / `expected [...] to include 'solicitudes_internas'`, ninguna tabla ni índice existían todavía] → creada `0008_solicitudes_internas.ts` con el DDL literal de §6.2 y appendeada a `migrations/index.ts` (import + entrada al final del array, sin tocar las siete migraciones existentes) → mismo comando → 4/4 passed [GREEN]). Test primero: extiende `repository.test.ts` — tras `runMigrations` (vía `openDatabase`) existen la tabla `solicitudes_internas` y los dos índices (`idx_solicitudes_caso`, confirmado además `UNIQUE` con `PRAGMA index_list`; `idx_solicitudes_estado`); insertar dos solicitudes con el mismo `caso_id` falla con `/UNIQUE/`; insertar con `solicitante_id`/`resuelta_por` arbitrarios que no existen en ninguna tabla (sin FK, ADR 49) no lanza; insertar sin un `caso_id` existente falla con `/FOREIGN KEY/`. `npx vitest run src/adapters/memory/repository.test.ts src/adapters/memory/migrate.test.ts`: 122/122 verde; `npm test` [con `dist/` limpiado primero]: **1050/1050 verde** [1046 baseline de la tarea 14 + 4 nuevos]; `npm run typecheck`: **0 errores**. Cubre spec `solicitud-interna-hitl` req. "Tabla propia `solicitudes_internas`, sin reusar `actividades`". ADR 43, ADR 49. Commit: `feat(adapters/memory): agrega migracion 0008 con tabla solicitudes_internas (Hito 5, tarea 15)`.

16. [x] **`src/core/solicitudes/solicitudes-contract.ts`** (nuevo, §5.6) — **COMPLETADO** (RED→GREEN real: `solicitudes-contract.test.ts` escrito primero contra un módulo inexistente → `npx vitest run src/core/solicitudes/solicitudes-contract.test.ts` → falló con `Cannot find module './solicitudes-contract.js'` [RED, 0 tests corridos porque el archivo no existía] → creado `solicitudes-contract.ts` con el vocabulario + `SolicitudStorePort` de §5.6 → mismo comando → 17/17 passed [GREEN]). Test primero: `solicitudes-contract.test.ts` — `SOLICITUD_TIPOS` tiene exactamente `vacaciones`/`gasto`; `SOLICITUD_ESTADO_PENDIENTE === CASO_ESTADO_PENDIENTE_APROBACION_HUMANA` (importado, no redeclarado, verificado además que el archivo no vuelve a contener el literal `"pendiente_aprobacion_humana"`); el archivo importa **únicamente** de `../hitl/hitl-contract.js` (núcleo → núcleo, mismo cruce que `resolver-escalacion-reembolso.ts` → `../auth/sesion.js`, verificado con un assert sobre cada línea `import` del source); un doble en memoria de `SolicitudStorePort` ejercita las cinco operaciones (`crearSolicitudConCaso`, `adjuntarDictamen` sin tocar `estado`, `listarSolicitudesPendientes` con/sin filtro por id, `aprobarSolicitud`/`rechazarSolicitud` con CAS que no matchea ⇒ `undefined`). Sin fixture de LLM. `npx vitest run src/core/solicitudes/solicitudes-contract.test.ts`: 17/17 verde; `npm test` [con `dist/` limpiado primero]: **1067/1067 verde** [1050 baseline de la tarea 15 + 17 nuevos]; `npm run typecheck`: **0 errores**. Cubre spec `hitl-generico` (consumidor del vocabulario) + prerrequisito de `solicitud-interna-hitl`. ADR 42, ADR 49. Commit: `feat(core): agrega solicitudes-contract con vocabulario y SolicitudStorePort (Hito 5, tarea 16)`.

    **Desviación declarada respecto del texto de la tarea 16**: `design.md` §5.6 declara la firma de `SolicitudStorePort.crearSolicitudConCaso(input: CrearSolicitudConCasoInput)` y `.aprobarSolicitud/.rechazarSolicitud(input: ResolucionSolicitudInput)` pero **no** desarrolla el cuerpo de esos dos tipos (a diferencia de `adjuntarDictamen`/`listarSolicitudesPendientes`, que sí trae los tipos inline completos). Se completaron ambos por molde directo de los precedentes ya aprobados de `ventas-contract.ts` — `CrearSolicitudConCasoInput` calca la forma de `CrearVentaConCasoInput` (`{ caso, solicitud, timestamp }`, con `solicitud` reflejando las columnas de la migración 0008 que no derivan de otro insumo: `id`, `solicitanteId`, `tipo`, `detalle`, `estado`) y `ResolucionSolicitudInput` calca `ResolucionEscalacionInput` campo por campo (`solicitudId`/`casoId`/`empleadoId`/`accionId`/`ahora`). Ningún campo de `solicitudes_internas` (migración 0008, tarea 15) queda sin cubrir por estos dos tipos. Señalado para que el Reviewer/Spec Author confirme la forma antes de que la tarea 17 (`crear-solicitud-interna.ts`) y la tarea 19 (`repository.ts`) empiecen a consumirla — si la forma no calza, es más barato ajustarla acá que después de dos consumidores.

17. [x] **`src/core/solicitudes/crear-solicitud-interna.ts`** (nuevo, §4.2, §5.6) — **COMPLETADO** (RED→GREEN real: `crear-solicitud-interna.test.ts` escrito primero contra un módulo inexistente → `npx vitest run src/core/solicitudes/crear-solicitud-interna.test.ts` → `Cannot find module './crear-solicitud-interna.js'` [RED, 0 tests corridos] → implementado `crear-solicitud-interna.ts` per design.md §4.2/§5.6 → mismo comando → 4/4 passed [GREEN]; `npm test` [con `dist/` limpiado primero — mismo gotcha de doble conteo de Vitest ya documentado]: **1071/1071 verde** [1067 baseline de la tarea 16 + 4 nuevos]; `npm run typecheck`: **0 errores**). Test primero: `crear-solicitud-interna.test.ts` con dobles de `SolicitudStorePort` + `InvocarSubagente` (validador) — tipo desconocido ⇒ `{resultado:"tipo_desconocido"}`, el store **nunca** recibe un write (los 5 métodos de `SolicitudStorePort` sin invocar, `despacharDeps.invocar` y `despacharDeps.store.crearDelegacion` tampoco); alta válida ⇒ `crearSolicitudConCaso` se llama (transacción interna del store, ya documentada como "1 transacción" en `SolicitudStorePort`, tarea 16), luego delega al `validador-solicitudes` con `material` = tipo+detalle **exclusivamente** (assert explícito: `tareaDelegada` contiene tipo y detalle, **no** contiene el `solicitanteId`; orden `crearSolicitudConCaso` ANTES de `invocar` verificado con `invocationCallOrder`); el dictamen se adjunta vía `adjuntarDictamen` **sin** transicionar el estado (`resultado.solicitud.estado` sigue `SOLICITUD_ESTADO_PENDIENTE`, `aprobarSolicitud`/`rechazarSolicitud` nunca invocados); si la delegación al validador falla (`invocar` rechaza), se **degrada a evento** `solicitud-validacion-fallida` (molde de `manejarSoporte`, ADR 40) — la solicitud ya creada por `crearSolicitudConCaso` queda sin dictamen (`resultado.solicitud.dictamen === undefined`, `adjuntarDictamen` nunca invocado), sin lanzar (`await` no rechaza). **Fixture de LLM**: sí — doble de `InvocarSubagente` (vía `DespacharDelegacionDeps`, mismo criterio que `cadena-revision.test.ts`/`dispatch-delegation.test.ts`). Cubre spec `solicitud-interna-hitl` reqs. "`/solicitar` crea `caso` y `solicitud_interna`..." (ambos escenarios), "El subagente validador emite un dictamen sin transicionar automáticamente" (ambos escenarios). ADR 40, ADR 43. Commit: `feat(core): agrega crearSolicitudInterna con delegacion al validador y degradacion de fallo (Hito 5, tarea 17)`.

    **Desviación declarada (`caso.tipo` de la solicitud — literal, no una constante nueva exportada)**: `design.md` §5.6 no fija el valor de `caso.tipo` para el `caso` que acompaña a una solicitud interna, y `CrearSolicitudConCasoInput.caso.tipo` (tarea 16, ya commiteada) es `string` abierto sin vocabulario propio en `solicitudes-contract.ts`. En vez de agregar un `CASO_TIPO_SOLICITUD_INTERNA` nuevo a `solicitudes-contract.ts` (que hubiese significado reabrir un archivo ya cerrado por la tarea 16 para un símbolo no pedido por ningún test), se usó el literal `"solicitud_interna"` tal cual — es EXACTAMENTE el mismo valor que los dobles de `solicitudes-contract.test.ts` (tarea 16, líneas 218/244/283, ya commiteadas) usan para ese mismo campo, y el mismo string que `ACTIVIDAD_TIPO_SOLICITUD_INTERNA` (`activity-contract.ts`) usa para `actividades.tipo` — dominio distinto (`casos.tipo` acá), sin acoplar los dos módulos entre sí. El Reviewer puede objetar si prefería una constante exportada de `solicitudes-contract.ts` en su lugar.

    **Desviación declarada (forma de `CrearSolicitudInternaDeps` — no fijada literalmente por `design.md`)**: `design.md` §5.6 describe el flujo de `crearSolicitudInterna` (líneas 254-279) pero no da la firma completa de sus `deps`. Se implementó `{ store: SolicitudStorePort; despacharDeps: DespacharDelegacionDeps }`, reusando `DespacharDelegacionDeps` completo (con su propio `newId`/`now`/`logEvent`) en vez de declarar un tercer juego de `newId`/`now`/`logEvent` a nivel de `crearSolicitudInterna` — mismo criterio que `despacharCadena` reusando sus propias `deps` para toda la cadena: un solo reloj y un solo generador de ids para todo el caso de uso, sin diverger entre la creación de `casoId`/`solicitudId` y la delegación. El Reviewer puede objetar si prefería `deps` más chatas y explícitas en vez de reusar la interfaz del Despachador completa.

18. [x] **`src/core/solicitudes/resolver-solicitud-interna.ts`** (nuevo, §5.6) — **COMPLETADO** (RED→GREEN real: `resolver-solicitud-interna.test.ts` escrito primero contra un módulo inexistente → `npx vitest run src/core/solicitudes/resolver-solicitud-interna.test.ts` → `Cannot find module './resolver-solicitud-interna.js'` [RED, 0 tests corridos] → implementado `resolver-solicitud-interna.ts` per design.md §5.6/§7 → mismo comando → 9/9 passed [GREEN]; `npm test` [con `dist/` limpiado primero — mismo gotcha de doble conteo de Vitest ya documentado]: **1080/1080 verde** [1071 baseline de la tarea 17 + 9 nuevos]; `npm run typecheck`: **0 errores**). Test primero: `resolver-solicitud-interna.test.ts` con dobles de `SolicitudStorePort` — función **SÍNCRONA** (test explícito, sin `Promise`/thenable, molde de `resolverEscalacionReembolso`); `confirmado: false` con `solicitudId` presente ⇒ `"requiere_confirmacion"` con el ítem (eco), sin escritura (`aprobarSolicitud`/`rechazarSolicitud` no invocados); `confirmado: true` (aprobar y rechazar, en tests separados) ⇒ delega al `store.aprobarSolicitud`/`rechazarSolicitud` con `{solicitudId, casoId, empleadoId: sesion.empleadoId, accionId: newId(), ahora: now()}` exactos, mapea a `"aplicada"` con `item`/`estadoFinal`; store que devuelve `undefined` (CAS no matcheó) ⇒ `"no_aplicable"` con `motivo: MOTIVO_CAS`, `itemId`+`casoId`, sin lanzar; `solicitudId` ausente sin confirmar ⇒ `"listado"` de pendientes vía `listarSolicitudesPendientes({limite})`, respetando `limiteListado` custom; recibe `sesion: SesionEmpleado`, no `empleadoId` suelto (ADR 37); asserts de `logEvent` para `solicitud-listada`/`solicitud-resolucion-solicitada`/`solicitud-aprobada` con los campos exactos de la tabla de eventos (design.md §7). Sin fixture de LLM (pura, síncrona). Cubre spec `solicitud-interna-hitl` reqs. "`/aprobar-solicitud` y `/rechazar-solicitud` exigen sesión y confirmación en dos pasos" (escenarios de eco/confirmación), "Cualquier empleado autenticado puede resolver". ADR 50. Commit: `feat(core): agrega resolverSolicitudInterna sincrona con confirmacion en dos pasos (Hito 5, tarea 18)`.

    **Desviación declarada (escenario `solicitudId` inexistente ⇒ `no_aplicable`/`no_encontrada`, agregado más allá del texto literal de esta tarea)**: el texto de la tarea 18 agrupa *"`solicitudId` ausente/inexistente sin confirmar ⇒ `'listado'`"* en un solo bullet, lo que leído literalmente podría sugerir que un `solicitudId` que no matchea ninguna fila también cae en `"listado"`. Se implementó, en cambio, la rama `no_aplicable`/`MOTIVO_NO_ENCONTRADA` — **igual que el paso C de `resolverEscalacionReembolso`** (`ventaId` presente pero no encontrado ⇒ `no_aplicable`/`no_encontrada`, nunca `"listado"`) — por tres razones verificadas contra fuente, no solo por precedente: (1) la instrucción explícita y repetida de esta tarea es que `resolverSolicitudInterna` sea "el mismo molde" de `resolverEscalacionReembolso`, y ese molde solo devuelve `"listado"` cuando el id está **ausente**, nunca cuando fue provisto y no matcheó; (2) `hitl-contract.ts` (tarea 1, ya commiteada) declara `MotivoNoAplicableHitl = "no_encontrada" | "cas"` como parte del genérico `ResolucionHitlResult` que esta misma tarea usa — si `solicitud-interna-hitl` nunca ejercitara `MOTIVO_NO_ENCONTRADA`, sería el único motivo del vocabulario HITL sin ningún consumidor real en este hito; (3) `design.md` §7 (tabla de eventos, línea 690) fija `solicitud-resolucion-no-aplicable` con `casoId` **"caso o `tui-comando`"** — la distinción explícita entre dos `casoId` posibles para ese mismo evento solo tiene sentido si existen dos ramas distintas (`no_encontrada` con `casoId: "tui-comando"` cuando no hay caso real que correlacionar, y `cas` con el `casoId` real de la solicitud encontrada), exactamente lo que el código implementado produce. El Reviewer puede objetar si prefería que la tarea 18 se leyera literalmente (ambos casos ⇒ `"listado"`) en vez de priorizar la fidelidad al molde y a `design.md` §1/§7.

### PR3b — Comandos TUI + repository de solicitudes + wiring del dispatcher (tareas 19-23)

19. [x] **`src/adapters/memory/repository.ts`** (modificado, §6.3, funciones de `solicitudes_internas`) — **COMPLETADO** (RED→GREEN real: extendido `repository.test.ts` con los 9 `it` nuevos del describe `crearSolicitudConCaso / adjuntarDictamenSolicitud / listSolicitudesInternas / aprobarSolicitudInterna / rechazarSolicitudInterna` ANTES de crear las cinco funciones → `npx vitest run src/adapters/memory/repository.test.ts` → 9 failed [RED: `TypeError: {crearSolicitudConCaso,adjuntarDictamenSolicitud} is not a function` en los 7 tests que dependen de crear una solicitud primero, mismo error en los 2 tests directos de `adjuntarDictamenSolicitud`/`crearSolicitudConCaso`; los 103 tests preexistentes del archivo seguían en verde] → implementadas `crearSolicitudConCaso`/`adjuntarDictamenSolicitud`/`listSolicitudesInternas`/`aprobarSolicitudInterna`/`rechazarSolicitudInterna` + el privado `resolverSolicitudTransaccional` per design.md §6.3 → mismo comando → 112/112 passed [GREEN, 103 preexistentes + 9 nuevos]; `npm test` [con `dist/` limpiado primero — mismo gotcha de doble conteo de Vitest ya documentado]: **1089/1089 verde** [1080 baseline de la tarea 18 + 9 nuevos]; `npm run typecheck`: **0 errores**). Test primero: extiende `repository.test.ts` — `crearSolicitudConCaso` **atómica** (`createCaso` + `INSERT solicitudes_internas`, molde exacto de `createCasoConActividad`/`createVentaConCaso`; un test de camino feliz confirma `solicitud.casoId === caso.id`, y un segundo test fuerza el fallo del `INSERT` de la solicitud — colisión de `id` reusando una solicitud ya creada, con un `caso.id` NUEVO en el mismo intento — y confirma con `getCasoById` que el caso nuevo **no** quedó persistido, mismo patrón que el test de atomicidad de `createCasoConActividad`); `adjuntarDictamenSolicitud` escribe `dictamen`/`dictaminada_at` **sin** tocar `estado` (assert explícito: `estado` sigue `pendiente_aprobacion_humana` tras adjuntar), `undefined` si la solicitud no existe; `listSolicitudesInternas` filtra por `estado = 'pendiente_aprobacion_humana'` (una solicitud ya aprobada queda excluida del listado) y opcionalmente por `solicitudId`; `aprobarSolicitudInterna`/`rechazarSolicitudInterna` comparten el `resolverSolicitudTransaccional` privado (calco de `resolverEscalacionTransaccional`, líneas 1403-1455) — CAS `UPDATE` + `updateCaso('resuelto')` + `insertAccionEmpleado`, todo en una transacción, verificado leyendo `registro_acciones_empleado` con SQL directo (no solo el valor de retorno); CAS que no matchea (solicitud forzada a `estado = 'aprobada'` antes de la llamada, vía SQL directo) devuelve `undefined`, y una consulta directa post-fallo confirma que **ni** `getCasoById` cambia de `pendiente_aprobacion_humana` **ni** existe la fila `registro_acciones_empleado` con el `accionId` de ese intento (rollback verificable de verdad, no solo `try/catch`). Sin fixture de LLM (SQLite `:memory:`). Cubre spec `solicitud-interna-hitl` req. "Confirmación aplica el CAS y deja registro en la misma transacción". ADR 49. Commit: `feat(adapters/memory): agrega CAS de aprobar/rechazar solicitud interna con registro atomico (Hito 5, tarea 19)`.

    **Desviación declarada (`config` de `resolverSolicitudTransaccional` conserva `estadoOrigen`/`estadoCaso` aunque ambos llamadores actuales usan el mismo valor)**: a diferencia de `resolverEscalacionTransaccional` (tres llamadores — `aprobar`/`rechazar`/`reabrirEscalacionReembolso` — con `estadoOrigen`/`estadoCaso` genuinamente distintos entre sí), `solicitud-interna-hitl` solo tiene dos llamadores (`aprobar`/`rechazarSolicitudInterna`), ambos partiendo del mismo `estadoOrigen` (`pendiente_aprobacion_humana`) y terminando en el mismo `estadoCaso` (`resuelto`) — no hay un equivalente a `reabrirEscalacionReembolso` en el desglose de este hito. Se mantuvo el `config` con los cinco campos completos (`estadoOrigen`/`estadoDestino`/`estadoCaso`/`comando`/`resultado`) en vez de hardcodear `estadoOrigen`/`estadoCaso` directamente en el SQL, por fidelidad literal a la firma que `design.md` §6.3 escribe explícitamente (`resolverSolicitudTransaccional(db, input, config)`) y para dejar la puerta abierta a un futuro `reabrirSolicitud` sin tener que tocar la firma del privado. El Reviewer puede objetar si prefería los dos campos hardcodeados por no tener hoy un segundo valor real que los use (mismo tipo de objeción ya resuelta a favor de la fidelidad literal en la tarea 16).

    **Desviación declarada (`CrearSolicitudConCasoInput`/`ResolucionSolicitudDbInput` del adapter, nombres no fijados literalmente por `design.md`)**: `design.md` §6.3 declara `crearSolicitudConCaso(db, input)` sin nombrar el tipo de `input`, y sí nombra literalmente `ResolucionSolicitudDbInput` para `aprobar/rechazarSolicitudInterna`. Para el primero se declaró `CrearSolicitudConCasoInput` en `repository.ts` — mismo nombre que el tipo ya existente en `solicitudes-contract.ts` (núcleo, tarea 16) pero en un módulo distinto que nunca lo importa (sin colisión real de símbolos, mismo criterio ya usado por `ComisionRow`/`VentaRow` field-for-field mirrors) — en vez de agregarle un sufijo `Db` como si tuviera el mismo patrón de duplicado-con-nombre-distinto que `ResolucionEscalacionInput`/`ResolucionEscalacionDbInput`: la razón de ese sufijo en el molde de ventas es que `CreateVentaConCasoInput` (adapter) YA diverge de forma de `CrearVentaConCasoInput` (núcleo) — usa el `CreateCasoInput` completo para `caso`, no el subconjunto `{id,tipo,estado}` — mientras que la forma que se necesitaba acá coincide campo por campo con la del núcleo (`caso: {id,tipo,estado}`, `solicitud: {id,solicitanteId,tipo,detalle,estado}`, `timestamp`), así que reusar el mismo nombre sin sufijo es más preciso, no menos. El Reviewer puede objetar si prefería el sufijo `Db` de todos modos, por consistencia posicional con `ResolucionSolicitudDbInput`.

20. [x] **`src/core/commands/comando-empleado.ts`** (modificado, §5.7, ADR 56) — **COMPLETADO** (RED→GREEN real: extendido `comando-empleado.test.ts` con los tests de los 7 puntos de esta tarea ANTES de tocar el parser → `npx vitest run src/core/commands/comando-empleado.test.ts` → 15 failed / 38 passed [RED — todos los fallos nuevos son por los tres comandos y la forma `"id_opcional_solicitud"` inexistentes todavía; los 38 tests preexistentes seguían en verde] → agregadas las tres variantes a `ComandoEmpleado`, la forma `"id_opcional_solicitud"` a `Forma`, los tres descriptores nuevos a `DESCRIPTORES` [antes de `/ayuda`, que sigue último] y las dos ramas nuevas de `parsearComando` [`"id_opcional_solicitud"`, calco de `"id_opcional"` con clave `solicitudId`; `"/solicitar"`, calco de `"/login"` con dos argumentos obligatorios `tipoSolicitud`+`detalle`] → mismo comando → 58/58 passed [GREEN, 38 preexistentes + 20 nuevos]). Test primero: extiende `comando-empleado.test.ts` — nuevos descriptores `/solicitar` (`forma: "id_mas_resto"`, `privilegiado: true`), `/aprobar-solicitud` y `/rechazar-solicitud` (`forma: "id_opcional_solicitud"` **nueva**, `privilegiado: true`, devuelven `{tipo, solicitudId}` sin cast — verificado leyendo el source: ningún `as` en las dos ramas nuevas); `/solicitar` sin detalle (y sin ningún argumento) ⇒ resultado de ayuda/argumentos; `esComandoPrivilegiado` es `true` para los tres; `/ayuda` lista **once** comandos; los 8 descriptores existentes no cambian de orden ni de forma (test explícito nuevo: primeros 7 conservan índice y forma, los 3 nuevos ocupan los índices 7-9 en el orden de `design.md` §5.7, `/ayuda` sigue último en el índice 10). Sin fixture de LLM (parser puro, sin imports — el test de la línea 199-204 que asserta "no hay `import`" en el source sigue en verde sin modificarse). Cubre prerrequisito de `solicitud-interna-hitl` ("exigen sesión y confirmación en dos pasos"). ADR 56. `npx vitest run src/core/commands/comando-empleado.test.ts`: 58/58 verde; `npm test` [con `dist/` limpiado primero]: **1109/1109 verde** [1089 baseline de la tarea 19 + 20 nuevos]; `npm run typecheck`: **1 error nuevo, esperado y diferido a las tareas 22-23** (ver desviación declarada abajo). Commit propuesto: `feat(core): agrega comandos /solicitar, /aprobar-solicitud y /rechazar-solicitud al parser (Hito 5, tarea 20)`.

    **Desviación declarada (1 error nuevo de `typecheck`, en un archivo fuera del alcance de esta tarea — mismo patrón que las tareas 12→14)**: agregar tres miembros a la unión discriminada `ComandoEmpleado` (`solicitar`/`aprobar_solicitud`/`rechazar_solicitud`) hace que el `switch (comando.tipo)` de `src/build-on-comando-empleado.ts:455-472` deje de ser exhaustivo — ese archivo no está en el alcance de la tarea 20 (es el de las tareas 22-23) y hoy solo tiene `case` para los 8 tipos de `v1.4.0`. TypeScript ya no puede probar que la función siempre retorna, así que el tipo inferido de `return async (texto, onAgentResolved) => {...}` pasa de `Promise<TuiTurnResult>` a `Promise<TuiTurnResult | undefined>`, lo que rompe la asignación a `SubmitPromptHandler` (`build-on-comando-empleado.ts:423`, `TS2322`). Verificado que es un error genuinamente NUEVO (no preexistente): `git stash` de los dos archivos tocados por esta tarea + `npm run typecheck` → **0 errores**; `git stash pop` + mismo comando → **1 error**, exactamente el descrito. `npm test` con `dist/` limpio en ambos estados confirma que la suite de Vitest **no** se ve afectada (1109/1109 verde con los cambios aplicados) — Vitest transforma con esbuild, sin chequeo de tipos, así que el error de `tsc --noEmit` no bloquea la ejecución de tests. Se dejó el `switch` de `build-on-comando-empleado.ts` intacto (sin agregar un `default` ni casos placeholder) porque escribir esos tres `case` es exactamente el trabajo de las tareas 22 (`/solicitar` + ranura de confirmación ensanchada, ADR 55) y 23 (resolución de `/aprobar-solicitud`/`/rechazar-solicitud`) — anticiparlos acá habría significado tomar decisiones de esas tareas (wiring de `solicitudStore`, `delegacionDeps`, la unión `ConfirmacionPendiente` por `dominio`) sin el contexto ni los tests que esas tareas todavía no escribieron. Mismo criterio ya aceptado por el Reviewer en las tareas 12→14: la tarea 12 dejó `build-on-activity.ts` con `runTurn`/`despacharRevision` desincronizados (3 errores de `typecheck`, documentados como "preexistentes" en la tarea 13) hasta que la tarea 14 cableó el wiring real. El Reviewer puede objetar si prefiere que la tarea 20 agregue un `case` temporal (p. ej. `throw` o `sistema("no implementado")`) para mantener `typecheck` en 0 errores durante el intervalo, en vez de dejar el error documentado y diferido.

21. [x] **`src/core/commands/registro-acciones-contract.ts`** (modificado, §5.7) — **TDD exception** (constantes de datos, sin lógica nueva, molde de la tarea 11 de Hito 4 sobre `env.ts`) — **COMPLETADO** (RED→GREEN real: extendido `registro-acciones-contract.test.ts` con los imports y los 4 asserts nuevos ANTES de tocar el source → `npx vitest run src/core/commands/registro-acciones-contract.test.ts` → 2 failed / 4 passed [RED: `expected undefined to be '/solicitar'` en el describe `vocabulario de comando` y `expected undefined to be 'creada'` en `vocabulario de resultado` — los 4 tests preexistentes de `AccionEmpleado`/`RegistroAccionesEmpleadoPort`/source seguían en verde] → agregadas `COMANDO_SOLICITAR`/`COMANDO_APROBAR_SOLICITUD`/`COMANDO_RECHAZAR_SOLICITUD` después de `COMANDO_REABRIR_REEMBOLSO` y `RESULTADO_CREADA` después de `RESULTADO_REABIERTA` — sin tocar imports, la interfaz `AccionEmpleado` ni `RegistroAccionesEmpleadoPort` → mismo comando → 6/6 passed [GREEN]; `npm test` [con `dist/` limpiado primero]: **1109/1109 verde** [mismo total que la tarea 20 — no se agregaron `it` nuevos, solo asserts dentro de los dos describes existentes]; `npm run typecheck`: **1 error, el mismo ya documentado y diferido por la tarea 20** [`TS2322` en `build-on-comando-empleado.ts:423`, confirmado por inspección de mensaje: mismo archivo, misma línea, mismo texto — esta tarea no lo toca ni lo agrava]). Test primero: extiende `registro-acciones-contract.test.ts` — `COMANDO_SOLICITAR`/`COMANDO_APROBAR_SOLICITUD`/`COMANDO_RECHAZAR_SOLICITUD`/`RESULTADO_CREADA` existen con los strings esperados; `RESULTADO_APROBADA`/`RESULTADO_RECHAZADA`/`RESULTADO_NO_APLICABLE` se **reusan** sin redeclararse. Commit propuesto: `feat(core): agrega constantes de comando y resultado para solicitudes internas (Hito 5, tarea 21)`.

    **Desviación declarada (`RESULTADO_CREADA = "creada"`, string no fijado literalmente por `design.md`)**: a diferencia de los tres comandos nuevos (`COMANDO_SOLICITAR`/`COMANDO_APROBAR_SOLICITUD`/`COMANDO_RECHAZAR_SOLICITUD`), cuyos strings exactos ya quedaron fijados por la tarea 20 en `comando-empleado.ts` (`"/solicitar"`, `"/aprobar-solicitud"`, `"/rechazar-solicitud"`, confirmados por lectura directa de ese archivo antes de escribir esta tarea), `design.md` §5.7 (línea 554) nombra `RESULTADO_CREADA` pero no fija literalmente el valor del string que debe contener. Se eligió `"creada"` por ser el molde exacto ya usado por las ocho constantes `RESULTADO_*` existentes: participio femenino en minúscula, sin guiones, sin prefijo de comando (`"exitosa"`, `"atendida"`, `"fallida"`, `"reembolsada"`, `"escalada"`, `"aprobada"`, `"rechazada"`, `"reabierta"`) — es la desviación más chica posible frente a "no está fijado" y calza semánticamente con el evento que representa (la solicitud fue creada por `/solicitar`, molde exacto de `"reembolsada"` para `/devolucion` o `"reabierta"` para `/reabrir-reembolso`). El Reviewer puede objetar si prefería otro string (p. ej. `"solicitada"`, más simétrico con el verbo del comando `/solicitar` en vez de con el sustantivo "solicitud").

22. [x] **`src/build-on-comando-empleado.ts`** (modificado, §6.4, parte 1 — `ConfirmacionPendiente` + alta de solicitud, ADR 55) — **COMPLETADO** (RED→GREEN real: extendido `build-on-comando-empleado.test.ts` con 5 tests nuevos [describe `/solicitar` × 4 + describe ADR 55 × 1] ANTES de tocar el source → `npx vitest run src/build-on-comando-empleado.test.ts` → 5 failed [`hooks` faltante en `BuildOnComandoEmpleadoDeps`, `case "solicitar"` inexistente en el switch — TS no compilaba/los 5 tests nuevos fallaban] / 21 passed [los 21 preexistentes seguían en verde] → agregados: unión discriminada `ConfirmacionPendiente` (`dominio: "reembolso" | "solicitud"`, ADR 55) con `dominio: "reembolso"` sumado al único constructor y comparador existentes (`manejarEscalacion`, sin tocar ningún otro comportamiento de esa función); `hooks` requerido (sin default) en `BuildOnComandoEmpleadoDeps`; `createSolicitudStore(db)` — factory de nivel de módulo, mismo patrón que `createVentaStore`/`createDelegacionStore`, con función privada `toPortSolicitud` + `SolicitudTipoEstadoInvalidoError` (mismo molde que `toPortActividad`/`ActividadTipoEstadoInvalidoError` de `build-on-activity.ts`); `despacharDeps: DespacharDelegacionDeps` con `invocar` cerrado sobre `invokeModel`, resolviendo el `caso` real vía `getCasoById`/`CasoNotFoundError` (mismo molde que `build-on-activity.ts:291-305`, ADR 53); `manejarSolicitud` (calco de `manejarSoporte` en cuanto a `async`+degradación, no de `manejarEscalacion`, ver desviación de alcance abajo) que llama `crearSolicitudInterna` (tarea 17) y responde `sistema(...)`; `case "solicitar"` en el switch; call site de `main.ts:375-384` actualizado con `hooks` → mismo comando → 26/26 passed [GREEN, 21 preexistentes + 5 nuevos]. `npm test` [con `dist/` limpiado primero]: **1114/1114 verde** [1109 baseline de la tarea 21 + 5 nuevos]. `npm run typecheck`: **1 error, MISMO ya documentado por las tareas 20/21** [`TS2322` en `build-on-comando-empleado.ts`, ahora línea 639 — mismo texto, mismo archivo; se corrió de la línea 423 solo por las ~200 líneas agregadas arriba; la causa raíz (switch no exhaustivo: faltan `aprobar_solicitud`/`rechazar_solicitud`) sigue exactamente igual — esta tarea cierra 1 de los 3 `case` faltantes, así que el error persiste sin agravarse, tal como las tareas 20/21 ya anticipaban ("diferido a las tareas 22-23"); se resuelve recién al cierre de la tarea 23].

    **Desviación declarada 1 — el bullet "armar una confirmación de reembolso y después una de `/solicitar` pisa la ranura única" NO se implementó tal cual, y se documenta por qué**: verificado contra `design.md` §4.2 (diagrama del Entregable B, líneas 254-267) ANTES de escribir el test — `/solicitar` es una acción de **un solo paso** (sesión vigente exigida, sin eco/confirmación), igual que `/devolucion`; nunca construye ni lee `confirmacionPendiente`. La rama `dominio: "solicitud"` de la unión (ADR 55) la escriben `/aprobar-solicitud`/`/rechazar-solicitud` (tarea 23, todavía sin `case` en el switch) — no existe manera de producir un `confirmacionPendiente` real con `dominio: "solicitud"` sin ese caso de uso, así que un test que afirme "una confirmación de `/solicitar` pisa la de reembolso" describiría un comportamiento que el propio diseño nunca pide que exista. Se escribió en su lugar el test que sí representa el comportamiento real: una confirmación de reembolso pendiente **sobrevive intacta** a un `/solicitar` exitoso de por medio (dominios distintos, `/solicitar` no toca la ranura) — cubre el mismo objetivo de fondo del ADR 55 (que el ensanchamiento del tipo no regresione el camino `dominio: "reembolso"`) sin inventar un escritor de `dominio: "solicitud"` que no existe todavía. El test cruzado real ("armar reembolso, después SOLICITUD, la solicitud pisa") queda explícitamente para la tarea 23. Mismo patrón que la tarea 12 diferido a la tarea 14. El Reviewer puede objetar si prefiere que el bullet se marque como no cubierto en vez de reinterpretado.

    **Desviación declarada 2 — `registrar(...)` con `COMANDO_SOLICITAR`/`RESULTADO_CREADA` SÍ se consume en esta tarea, no se difiere**: `design.md` §4.2/§6.4 no menciona `registrar` explícitamente para `/solicitar`, pero tres señales convergen: (a) la tarea 21 (ya commiteada) agregó `COMANDO_SOLICITAR` y `RESULTADO_CREADA` con el comentario `// /solicitar` en el propio `registro-acciones-contract.ts`, sin ningún otro consumidor posible en el resto del código; (b) `crearSolicitudInterna` (tarea 17) nunca toca `registro_acciones_empleado` — su transacción es solo `caso`+`solicitud`; (c) el criterio general ya establecido por `manejarDevolucion` (comando privilegiado exitoso con sesión vigente deja fila, escrita FUERA de la transacción del caso de uso) aplica igual acá. Se registra únicamente el resultado `"creada"` (con o sin dictamen — ambos casos devuelven `resultado: "creada"` desde `crearSolicitudInterna` por ADR 40); `tipo_desconocido` **no** deja fila, mismo criterio "cero escrituras" que `crearSolicitudInterna` ya declara para ese caso (no hay `caso_id` que correlacionar, y `design.md` §7 no define ningún evento de log para él tampoco). El Reviewer puede objetar si prefiere que `tipo_desconocido` sí deje fila con `resultado: "no_aplicable"` (reusado).

    **Desviación declarada 3 — `toPortSolicitud` valida `tipo`/`estado` contra vocabulario cerrado antes de castear, en vez de castear directo**: `repository.ts:1744-1751` documenta que `SolicitudRow` es "field-for-field the same" que `SolicitudInterna`, lo que sugeriría un cast directo. Se prefirió replicar el molde YA aprobado por el Reviewer para el mismo problema estructural (`toPortActividad`/`ActividadTipoEstadoInvalidoError`, `build-on-activity.ts`): las columnas `tipo`/`estado` de `solicitudes_internas` son TEXT sin `CHECK` (mismo criterio que `actividades`), así que una fila corrupta es indistinguible de una válida sin esta validación explícita. Consistencia de patrón dentro del mismo codebase, no una decisión nueva.

    **Desviación declarada 4 — `hooks` pasa a requerido sin default, tocando el call site de `main.ts`**: `BuildOnComandoEmpleadoDeps` no tenía `hooks` (el dispatcher nunca invocaba al modelo antes de esta tarea). Se agregó como campo **requerido** (mismo criterio que `BuildOnActivityDeps.hooks`, sin default silencioso) porque `despacharDeps.invocar` lo necesita para cerrar sobre `invokeModel`; esto obligó a actualizar `main.ts:375-384` para pasarlo (ya estaba en scope como `const { ..., hooks, ... } = startup;`, línea 240) — único call site de producción, verificado por grep antes de tocar el archivo.

    Cubre spec `solicitud-interna-hitl` req. "`/solicitar` crea `caso` y `solicitud_interna`..." (escenarios "Alta válida crea caso y solicitud" [delegado a `crear-solicitud-interna.test.ts`, tarea 17] y "`/solicitar` sin sesión vigente se rechaza" [test nuevo acá]). ADR 55. `npx vitest run src/build-on-comando-empleado.test.ts`: 26/26 verde; `npm test`: 1114/1114 verde; `npm run typecheck`: 1 error, mismo ya documentado, diferido a la tarea 23. Commit propuesto (no ejecutado — el humano lo corre): `feat(root): conecta /solicitar en build-on-comando-empleado con ranura de confirmacion ensanchada (Hito 5, tarea 22)`.

23. [x] **`src/build-on-comando-empleado.ts`** (modificado, §6.4, parte 2 — resolución de solicitud, ADR 36, ADR 55) — **COMPLETADO** (RED→GREEN real: extendido `build-on-comando-empleado.test.ts` con 6 tests nuevos [describe "resolución de solicitudes en dos pasos (Hito 5, tarea 23)" × 6] ANTES de tocar el source → aislado el archivo de producción con `git stash push -- src/build-on-comando-empleado.ts` para correr los tests nuevos contra el estado real dejado por la tarea 22 (sin `case "aprobar_solicitud"`/`"rechazar_solicitud"`) → `npx vitest run src/build-on-comando-empleado.test.ts` → 5 failed [`TypeError: Cannot read properties of undefined (reading 'responseText')` en los 5 tests que ejercitan el switch — sin esos dos `case` el handler retorna `undefined` para esos comandos; el sexto test nuevo ("sin sesión vigente...") pasaba igual en este estado porque la guarda de privilegio del preámbulo (paso 6) intercepta ANTES de llegar al switch, sin importar si el `case` existe] / 27 passed [los 27 preexistentes de la tarea 22 seguían en verde] → `git stash pop` para restaurar la implementación real → agregados: `manejarResolucionSolicitud` [molde EXACTO de `manejarEscalacion`, delegando la lógica pura a `resolverSolicitudInterna` (tarea 18, PURA y SÍNCRONA, sin `await`) y el CAS transaccional a `SolicitudStorePort.aprobarSolicitud`/`rechazarSolicitud` (tarea 19)]; `formatearListadoSolicitudes`/`formatearLineaSolicitud` [molde de `formatearListado`/`formatearLineaEscalacion`]; `formatearEcoSolicitud` [molde de `formatearEco`, ver desviación declarada 1 abajo]; tabla `ACCION_SOLICITUD_COMANDO` [molde reducido de `ACCION_ESCALACION_INFO`, ver desviación declarada 2]; imports nuevos en `build-on-comando-empleado.ts`: `COMANDO_APROBAR_SOLICITUD`/`COMANDO_RECHAZAR_SOLICITUD` (tarea 21, `registro-acciones-contract.js`), `MOTIVO_CAS` (`core/hitl/hitl-contract.js`), y `resolverSolicitudInterna`/`ACCION_APROBAR_SOLICITUD`/`ACCION_RECHAZAR_SOLICITUD` (tarea 18, `resolver-solicitud-interna.js` — antes solo se importaba `type AccionSolicitud` de ese módulo); dos `case` nuevos en el switch (`"aprobar_solicitud"`, `"rechazar_solicitud"`); doc-comment de cabecera del archivo actualizado (ya no dice "tarea 23, todavía sin implementar") → mismo comando → 32/32 passed [GREEN, 27 preexistentes + 5 nuevos]. `npm test` [con `dist/` limpiado primero]: **1120/1120 verde** [1114 baseline de la tarea 22 + 6 nuevos]. `npm run typecheck`: **0 errores** — CIERRA el `TS2322` diferido desde la tarea 20: con los dos `case` nuevos el `switch (comando.tipo)` vuelve a ser exhaustivo, TypeScript vuelve a probar que la función siempre retorna `TuiTurnResult`, y la asignación a `SubmitPromptHandler` vuelve a tipar limpio. Cubre spec `solicitud-interna-hitl` reqs. "`/aprobar-solicitud` y `/rechazar-solicitud` exigen sesión y confirmación en dos pasos" (todos los escenarios: eco sin escrituras, CAS aplicado sin registro extra desde este archivo, CAS perdido con registro fuera de transacción, listado sin id, solicitud inexistente) y "Cualquier empleado autenticado puede resolver" (ningún test condiciona por `empleadoId` distinto al que creó la solicitud). También cubre el test cruzado que la tarea 22 dejó explícitamente diferido ("armar una confirmación de reembolso y después una de `/aprobar-solicitud` pisa la ranura única", ADR 55). Sin fixture de LLM adicional (esta parte solo invoca al `store`, no al modelo — mismo criterio que `manejarEscalacion`). `npx vitest run src/build-on-comando-empleado.test.ts`: 32/32 verde; `npm test`: 1120/1120 verde; `npm run typecheck`: 0 errores. Commit propuesto (no ejecutado — el humano lo corre): `feat(root): conecta /aprobar-solicitud y /rechazar-solicitud en build-on-comando-empleado (Hito 5, tarea 23)`).

    **Desviación declarada 1 — `formatearEcoSolicitud` NO recibe `accion` (a diferencia de `formatearEco`, que sí la recibe)**: `formatearEco` necesita `accion` porque `reabrir` agrega una salvedad dominio-específica (quién rechazó antes y cuántas reaperturas previas tuvo la venta) que `aprobar`/`rechazar` no tienen. En solicitudes internas no existe ningún análogo de `reabrir` — `AccionSolicitud` es solo `"aprobar" | "rechazar"` (`resolver-solicitud-interna.ts`), simétricos en todo lo que el eco necesita mostrar (id, tipo, detalle, caso de la solicitud). Agregar un parámetro `accion` sin ninguna rama que lo use habría sido código muerto — se omitió directamente en vez de calcarlo mecánicamente de `formatearEco`. El Reviewer puede objetar si prefiere mantener la firma simétrica con `formatearEco` de cara a un futuro tercer `AccionSolicitud` que sí necesite una salvedad.

    **Desviación declarada 2 — `ACCION_SOLICITUD_COMANDO` es un `Record<AccionSolicitud, string>` plano, no una tabla `estadoOrigen`/`estadoCaso`/`comando` como `ACCION_ESCALACION_INFO`**: `manejarEscalacion` necesita `estadoOrigen`/`estadoCaso` en `build-on-comando-empleado.ts` porque construye los mensajes de "no está en `estadoOrigen`" y "quedó en `estadoCaso`" con esos valores literales. En solicitudes, esa transición de estado la resuelve enteramente `resolverSolicitudTransaccional` (`repository.ts`, tarea 19, ya con `estadoOrigen`/`estadoDestino`/`estadoCaso` propios por cada llamada a `aprobarSolicitudInterna`/`rechazarSolicitudInterna`) — este archivo nunca necesita conocer esos strings, solo el `comando` para la fila de `registrar(...)` en el motivo `MOTIVO_CAS`. Portar la tabla completa de `ACCION_ESCALACION_INFO` habría dejado dos campos (`estadoOrigen`/`estadoCaso`) sin ningún lector.

    **Desviación declarada 3 — textos de eco/listado/aplicada/no_aplicable sin string literal fijado por `design.md`**: mismo criterio que la tarea 21 con `RESULTADO_CREADA` — `design.md` §6.4 describe el comportamiento ("eco", "listado", "aplica el CAS") pero no fija los strings de respuesta al empleado. Se calcaron los moldes exactos de `formatearEco`/`formatearListado`/los mensajes de `manejarEscalacion` (mismo separador `·`, mismo cierre "repetí el comando para confirmar.", mismo prefijo "Listo:", mismo cierre "no se aplicó nada."), reemplazando vocabulario de venta por vocabulario de solicitud (`solicitud`/`tipo`/`detalle` en vez de `venta`/`vendedor`/`monto`). El Reviewer puede objetar cualquiera de los strings elegidos sin que eso afecte la lógica de negocio (son puramente presentacionales).

### Documentación (tarea 25)

25. [x] **`README.md`** (raíz, modificado). Documenta el registro de roles (`SUBAGENT_REGISTRY`: `planner`/`developer`/`reviewer`/`validador-solicitudes`, con una línea de `allowedTools` por rol) y el interruptor `HARNESS_DELEGACION_ROLES` (`"off"` = comportamiento `v1.4.0`, cualquier otro valor o ausente = delegación por roles activa). Sin test — es documentación, no código de producción; no hay lógica que TDD pueda cubrir. **COMPLETADO** (nueva sección `## Configuración` agregada al README, entre `## Stack técnico` y `## Hoja de ruta`, con dos subsecciones: "Roles de subagentes (`SUBAGENT_REGISTRY`)" [tabla `Rol (id)` / `Responsabilidad` / `allowedTools`, los cuatro roles, datos confirmados contra `src/core/agents/definitions.ts` líneas 152-229 — no copiados a ciegas de `design.md` §5.2] y "Interruptor `HARNESS_DELEGACION_ROLES`" [tabla `Valor` / `Comportamiento`, confirmado contra `src/build-on-activity.ts:312`]). Commit: `docs: documenta SUBAGENT_REGISTRY y el interruptor HARNESS_DELEGACION_ROLES en el README (Hito 5, tarea 25)`.

    **Ampliación de alcance resuelta por el checkpoint humano (brecha de la tarea 25, no del diseño)**: el texto verbatim de esta tarea asume que existe una sección del README "donde ya se documentan variables de entorno/interruptores (ej. `WEB_PORT`/`EMAIL_API_KEY`)". Verificado leyendo el `README.md` completo (86 líneas antes de esta tarea): esa sección **no existe** — `WEB_PORT`/`EMAIL_API_KEY` solo estaban documentados en código fuente y en `openspec/changes/hito-1.3-ventas-comisiones/`, nunca llegaron al README. Se creó la sección `## Configuración` desde cero en vez de insertar en una sección inexistente. Consultado el checkpoint humano sobre cómo encarar la brecha, autorizó explícitamente ampliar el alcance de la tarea en dos puntos adicionales, ambos ya aplicados: (1) el blurb de estado (línea 5) decía literalmente "fase de diseño... el código todavía no se ha escrito", desactualizado desde hace varios hitos — reescrito para reflejar que v1.0.0/v1.1.0/v1.2.0/v1.4.0 están cerrados y tageados (`v1.3.0` sin tag, señalado explícitamente sin especular la causa) y que v2.0.0 está en curso, sin cerrar todavía; (2) la tabla de "Hoja de ruta" ganó una columna `Estado` (✅ cerrado / 🔄 en curso / — sin tag), verificada fila por fila contra `git tag --list` (`v1.0.0`, `v1.1.0`, `v1.2.0`, `v1.4.0` reales) — ninguna fila nueva se agregó a esa tabla, solo se marcaron las siete filas ya existentes. Ningún otro contenido del README (arquitectura, stack técnico, stakeholders) fue tocado. El Reviewer puede objetar el título elegido para la sección (`## Configuración`) o su ubicación si prefería otro criterio.

### Verificación manual (tarea 24)

24. [x] **Verificación manual end-to-end** (sin código de producción, cierre del hito) — **COMPLETADO** (Entregable A real: PR [#3](https://github.com/JimmyFung123/practicaDeDesarrollo/pull/3) en `JimmyFung123/practicaDeDesarrollo`, `HARNESS_DELEGACION_ROLES` sin definir, cadena Planner→Developer→Reviewer real vía `gh webhook forward`, tres filas en `delegaciones` [`caso_id 88294282-02ee-4a9e-a14a-4dccdaa6a5e0`] correctamente encadenadas por `sesion_padre_id`/`sesion_subagente_id`, comentario+label+assignee reales en GitHub equivalentes a `v1.2.0`; aislamiento confirmado leyendo `tarea_delegada` del Developer y del Reviewer — ninguna contiene system prompt ni sesión del rol anterior; `git status` del arnés limpio tras el ciclo. Entregable B real: empleado de prueba `verif-e2e`, `/login` → `/solicitar vacaciones "una semana en marzo"` [validador-solicitudes, cuarto rol ADR 52, dictamen no nulo sin transicionar] → `/aprobar-solicitud` primera vez confirmado CERO cambios en las 3 tablas [comparación de snapshots antes/después] → segunda vez aplica [`solicitud.estado→aprobada`, `caso.estado→resuelto`, fila nueva en `registro_acciones_empleado`]. Rollback real: PR [#4](https://github.com/JimmyFung123/practicaDeDesarrollo/pull/4) con `HARNESS_DELEGACION_ROLES=off`, comportamiento equivalente a `v1.4.0` [un solo agente, un veredicto, sin plan/hallazgos por rol], **0** filas nuevas en `delegaciones` para ese `caso_id` [total de la tabla: 4, las 3 de Entregable A + la de `validador-solicitudes` de Entregable B]. Doc-comments ADR 46 [`ventas-contract.ts:1-17,49-63`, `invoke-model.ts:6-9,99-140`, `activity-contract.ts:24-31`] confirmados actualizados, sin promesas pendientes. Suite completa con `dist/` limpio primero: `npm test` 1120/1120 verde, `npm run typecheck` 0 errores; confirmado por lectura de código que ningún test invoca el modelo real [`invoke-model.test.ts` inyecta `queryFn` fake] ni abre un puerto real. Evidencia completa — capturas de log estructurado real, los `SELECT` reales y las URLs de los dos PRs reales — en `docs/progreso/v2.0-delegacion-subagentes/README.md`.

    **Hallazgo real, no corregido acá (fuera de alcance de esta tarea)**: `npm test` contamina el archivo de producción `data/harness.log` con eventos de fixture (`soporte-caso-creado`, `venta-confirmada`, etc., con `casoId` genéricos tipo `"caso-1"`) — reproducido de forma aislada corriendo solo `src/build-on-soporte.test.ts` (+9 líneas reales). Misma clase de bug que el Reviewer ya encontró y arregló en `v1.2.0` para `build-on-activity.test.ts` (6 tests), pero nunca extendido a `build-on-soporte.test.ts` ni a `build-on-venta.test.ts` (Hito 4) — `build-on-venta.test.ts` tiene **cero** ocurrencias de `logDeps` en todo el archivo. Ver detalle completo, causa raíz y sugerencia de fix en `docs/progreso/v2.0-delegacion-subagentes/README.md`. No invalida ningún resultado de esta verificación (la evidencia real usa `casoId`s UUID, distinguibles de la contaminación), pero sí es un hallazgo real de higiene de test-isolation pendiente de que el Implementer lo resuelva.

    **Desviación declarada — orden de ejecución respecto a la nota de `tasks.md` línea 47**: esa nota dice que la tarea 24 se ejecuta "al final, después de la tarea 25". Esta sesión ejecutó la tarea 24 SIN esperar a la tarea 25 (README raíz), por instrucción explícita de quien encargó esta verificación — la tarea 25 queda pendiente para otra sesión, sin que eso invalide ninguna de las verificaciones de acá (task 24 no depende funcionalmente de que el README ya esté escrito, solo el orden sugerido de la nota de desglose lo asumía así).

    Commit: no aplica (evidencia va a `docs/progreso/v2.0-delegacion-subagentes/README.md`, según lo previsto). Cubre el §10 completo del diseño:
    - **Entregable A**: con `HARNESS_DELEGACION_ROLES` sin definir, reenviar el forwarding de webhooks de Hito 3 sobre un PR chico. Esperado: revisión y transición de estado **equivalente a `v1.2.0`**; `SELECT agent_id, sesion_padre_id, sesion_subagente_id, length(tarea_delegada), length(resultado) FROM delegaciones WHERE caso_id = ? ORDER BY created_at` devuelve **tres** filas en orden `planner, developer, reviewer`, la primera con `sesion_padre_id` `NULL` y las otras dos encadenadas, las tres con `resultado` completado.
    - **Aislamiento, leído, no confiado**: imprimir la columna `tarea_delegada` del Developer y del Reviewer y verificar que no contiene el system prompt ni mensajes de sesión del rol anterior.
    - **Sin efectos sobre el repositorio**: `git status` tras el ciclo completo debe estar limpio (ningún rol escribe).
    - **Entregable B**: `/login` → `/solicitar vacaciones "una semana en marzo"` → la solicitud queda `pendiente_aprobacion_humana` con dictamen adjunto → `/aprobar-solicitud <id>` (eco, sin escribir) → repetir (aplica) → `SELECT` sobre `solicitudes_internas`, `casos` y `registro_acciones_empleado`.
    - **Rollback**: `HARNESS_DELEGACION_ROLES=off`, reenviar el mismo PR, verificar comportamiento de `v1.4.0` y **cero** filas nuevas en `delegaciones`.
    - **Doc-comments (ADR 46)**: confirmar que los cuatro quedaron actualizados y ninguno promete algo pendiente — `ventas-contract.ts:~12` (header del módulo, tarea 2), `ventas-contract.ts:49-63` (tarea 2), `invoke-model.ts:6-9,126-132` (tarea 9), `activity-contract.ts:24` (tarea 13).
    - **Suite completa**: `npm test` y `npm run typecheck` en verde; confirmar que ningún test invoca el modelo real ni abre un puerto.
    - **Evidencia** para `docs/progreso/v2.0-delegacion-subagentes/`: capturas de la TUI, log estructurado, y el volcado real de las tres filas de `delegaciones`.
    - Commit: no aplica (evidencia va a `docs/progreso/v2.0-delegacion-subagentes/` al cierre, según `AGENTS.md`).

## Después de la tarea 24

- Pasa al Reviewer (`sdd-verify` + `code-review`) contra este documento, los 6 specs y `design.md`, con especial atención al ADR 48 (desviación de esquema de `delegaciones`, ya aprobada por el checkpoint), al ADR 52 (son cuatro subagentes — el criterio de éxito de `proposal.md` debe leerse "el padre y los cuatro roles") y a RD-5 (el subagente no debe pasar por `assembleContext`).
- Si aprueba: checklist de cierre de hito en `AGENTS.md`, tag `v2.0.0`, carpeta `docs/progreso/v2.0-delegacion-subagentes/`, copia de los ADR 42-56 a `docs/ARC42_Harness_Empresarial.md`.
- Si el Reviewer objeta el alcance de R3 (delegación decidida por el Despachador, no por el modelo) o la ausencia de `Write`/`Edit` en el Developer, la corrección es **reabrir el checkpoint humano sobre la propuesta/diseño**, no improvisar `tool_use` interceptado ni otorgar herramientas de escritura en `sdd-apply` — ambas son decisiones ya fijadas y documentadas en `proposal.md` (ADR 44) y `design.md` (§5.2, §15).
