> **Nota de proceso (hook de graphify)**: ejecutor sin herramienta de shell disponible (sólo `Read`/`Grep`/`Glob`/`Write`/`Edit`), misma limitación que `proposal.md` y `design.md` de este mismo change. Este documento no reabre ninguna decisión (ADR 125-133, RD-59-62 ya resueltas). Compensación: se releyeron completos `proposal.md`, `design.md` y las dos specs, más `grep` de `SolicitudStorePort`/`createSolicitudStore` sobre `src/` para verificar un acoplamiento de compilación que `design.md` §9 no desglosa por PR (ver "Ajuste sobre `design.md` §9" más abajo). Se recomienda `graphify update .` tras persistir este archivo.
>
> **★ Tercera pasada sobre este mismo `tasks.md` (ADR 144, PR3).** Las tareas 1-13 (PR1+PR2) están **implementadas y commiteadas**; la tarea 14 original (verificación manual) seguía pendiente. El Reviewer encontró que `/cancelar-solicitud` **sin id** no filtraba por dueño (bug funcional por el `LIMIT 20` en SQL, más fuga de `detalle`/`solicitanteId`), el hallazgo volvió al Spec Author, se aprobó **ADR 144** (checkpoint humano ya dado — ver `design.md` §2-bis/§9-bis/§11) y `sdd-spec` ya escribió los dos deltas de spec correspondientes (`specs/cancelacion-solicitud-interna/spec.md`, `specs/solicitud-interna-hitl/spec.md`), leídos completos y sin tocar en esta pasada. Esta pasada agrega las **tareas 15-18** (el PR3 de `design.md` §9-bis, desglose de la tabla A-D de §10) **antes** de la verificación manual, que se renumera como **tarea 19** y se amplía con los dos escenarios nuevos que §10 punto D exige. Ejecutor sin herramienta de shell disponible, mismo criterio que las dos pasadas anteriores: se releyó completo `design.md` (incluidas §2-bis, §3-bis, §5-bis, §6-bis, §9-bis, §10, §11) y las dos specs ya escritas; sin `grep`/`graphify` para verificar acoplamientos de compilación adicionales — no se necesitaron, porque el campo nuevo del puerto es opcional (§2-bis consecuencia 1: "ninguna implementación del puerto rompe por compilación").

# Tasks: `/cancelar-solicitud [solicitudId]` — el solicitante retira su propia solicitud pendiente (v3.3.0)

**Origen** (no se duplica): [`proposal.md`](proposal.md) (ADR 125-128) · [`design.md`](design.md) §2-11 (ADR 129-133, y ADR 144 de la segunda ronda) · specs: [`cancelacion-solicitud-interna`](specs/cancelacion-solicitud-interna/spec.md) (nueva, **5 requirements** — 4 de la primera ronda + el requirement de listado sin id agregado por el ADR 144), [`solicitud-interna-hitl`](specs/solicitud-interna-hitl/spec.md) (delta, 1 requirement ADDED + 2 MODIFIED, ambos MODIFIED ampliados en la segunda ronda).

**No es un Hito numerado del Plan** — los commits usan `(comando-cancelar-solicitud, tarea N)`.

**Metodología**: TDD estricto (`strict_tdd: true`) en toda tarea con lógica de negocio (1, 2, 4-10, 15-17). **Seis excepciones explícitas**: tarea 3 (constantes puras, sin comportamiento propio, ejercitadas indirectamente por la tarea 6), tarea 11 (specs ya escritas por `sdd-spec`, se commitean), tarea 12-13 (`README.md`/`docs/ARC42_*.md`, documentación), tarea 18 (specs de la segunda ronda, ADR 144, ya escritas por `sdd-spec`, se commitean), tarea 19 (verificación manual, sin código de producción — renumerada desde la tarea 14 original).

**Entrega ya decidida**: `chain_strategy: stacked-to-main`. PR1 y PR2 (tareas 1-13) **ya están implementados, commiteados y mergeados**. El **PR3** (tareas 15-18, ADR 144) es un **tercer PR encadenado**, slice único sin chain interno (`design.md` §9-bis), que va **antes** de la verificación manual (tarea 19) — verificar a mano un comportamiento que ya se sabe que va a cambiar es trabajo tirado. El corte de `design.md` §9 (núcleo+adaptador vs. comando+UX+docs) para PR1/PR2 se respetó tal cual se ejecutó; el corte de PR3 sigue la tabla A-D de `design.md` §10, ejecutada en el orden A→B→C→D por dependencia real (el puerto+SQL antes que el caso de uso, el caso de uso antes que el handler, las specs al final junto con el resto del PR).

## Los tres hallazgos de `design.md`, confirmados con tarea propia

| Hallazgo | Dónde | Tarea | Por qué no puede quedar implícito |
|---|---|---|---|
| 1. Ternario de `aplicarCas` (`resolver-solicitud-interna.ts:56-64`) → `switch` con guarda `never` | ADR 129, tabla fila 1 | **Tarea 1** | Sin el `switch`, un cuarto ternario silencioso hace que `cancelar` caiga en `rechazarSolicitud` — corrupción de datos con tipos en verde (R5) |
| 2. Segundo ternario del nombre de evento (`resolver-solicitud-interna.ts:150`) → `Record<AccionSolicitud, string>` | ADR 129, tabla fila 2 | **Tarea 2** | Sin el `Record`, una cancelación exitosa emitiría el evento `solicitud-rechazada` — la traza miente, tipos en verde |
| 3. `SOLICITUD_ESTADOS` (`build-on-comando-empleado.ts:381`) gana `SOLICITUD_ESTADO_CANCELADA` | ADR 133, hallazgo no previsto por la propuesta | **Tarea 5** | Sin esto, la primera cancelación real escribe la transacción entera y **después** explota en `toPortSolicitud` con `SolicitudTipoEstadoInvalidoError` — falla ruidosa pero tardía, con datos ya persistidos |

Ninguno de los tres vive dentro de otra tarea de alcance más amplio: cada uno es la totalidad de su propia tarea, con su propio test rojo-primero y su propio commit.

## Ajuste sobre `design.md` §9 (secuencia, no alcance)

`design.md` §9 no desglosa `build-on-comando-empleado.ts` por PR más allá de nombrar sus piezas. Verificado por `grep` de `SolicitudStorePort`/`createSolicitudStore` sobre `src/`: **dos acoplamientos de compilación reales** que exigen mover código de lo que el resumen de PR2 parecía sugerir hacia PR1, para que **cada commit intermedio siga compilando**:

1. `createSolicitudStore` (`build-on-comando-empleado.ts:426`) tiene **retorno anotado** `: SolicitudStorePort` — ampliar la interfaz (`SolicitudStorePort.cancelarSolicitud`) sin implementar el método en el mismo commit rompe `npm run typecheck` de inmediato. Mismo problema con el fake **no-`Partial`** de `solicitudes-contract.test.ts:148` (`const store: SolicitudStorePort = {...}`). ⇒ la tarea 7 junta contrato + implementación del store en un solo commit.
2. `ACCION_SOLICITUD_COMANDO` (`build-on-comando-empleado.ts:540`) es `Record<AccionSolicitud, string>` — el propio `design.md` (ADR 129) dice *"el compilador exige la clave nueva"* apenas se amplía `AccionSolicitud`. Como esa ampliación ocurre en la tarea 8 (PR1), la tercera clave de `ACCION_SOLICITUD_COMANDO` **debe** ir en el mismo commit, no diferida a PR2.

Consecuencia en líneas: el corte real queda **~PR1 280 / PR2 193** en vez de los ~250/~225 de `design.md` §9 (la suma total no cambia, ~473). Ver reconciliación al final. Ambos PRs siguen cómodos bajo el presupuesto de 400 líneas; el riesgo global sigue siendo `High` porque la suma de los dos supera 400.

### Suggested Work Units

| Unit | Goal | Tasks | Est. líneas | Base branch (`stacked-to-main`) |
|---|---|---|---|---|
| 1 | Núcleo + adaptador: vocabulario, CAS de cancelación, chequeo de dueño, capacidad inalcanzable desde la TUI | 1-8 | ~280 | `main` |
| 2 | Comando, UX, docs: descriptor, mensajes, cableado del dispatcher, specs, README, ARC42 | 9-13 | ~188 | `main` (con Unit 1 ya mergeada) |
| 3 | ★ Segunda ronda (ADR 144): filtro por dueño en el listado sin id — puerto+SQL, caso de uso, handler, specs | 15-18 | ~217 | `hito/v3.3-comando-cancelar-solicitud` recreada desde `main` (con Units 1 y 2 ya mergeadas), `design.md` §9-bis |
| — | Verificación manual final del entregable completo (post-ADR 144) | 19 | — | posterior al merge de Unit 3 |

## Tareas

### PR1 — Núcleo + adaptador (tareas 1-8)

1. [x] **`src/core/solicitudes/resolver-solicitud-interna.ts`** (modificado, refactor puro, ADR 129 — ★ Hallazgo 1) — `aplicarCas` pasa del ternario (`:61-63`) a `switch (accion) { case ACCION_APROBAR_SOLICITUD: ...; case ACCION_RECHAZAR_SOLICITUD: ...; default: { const _exhaustivo: never = accion; throw ...} }`, **sin agregar todavía `ACCION_CANCELAR_SOLICITUD`** — comportamiento idéntico, unión de 2 miembros. Verificación: `npm test -- resolver-solicitud-interna` en verde **sin modificar una sola línea de test**. Comando: `npm run typecheck`. Commit: `refactor(core): convierte aplicarCas de ternario a switch con guarda never (comando-cancelar-solicitud, tarea 1)`.

2. [x] **`src/core/solicitudes/resolver-solicitud-interna.ts`** (modificado, refactor puro, ADR 129 — ★ Hallazgo 2) — el segundo ternario del nombre de evento (`:150`) pasa a `EVENTO_SOLICITUD_APLICADA: Record<AccionSolicitud, string>` con las 2 entradas actuales (`aprobar`→`solicitud-aprobada`, `rechazar`→`solicitud-rechazada`), consumido en el camino feliz como `EVENTO_SOLICITUD_APLICADA[accion]`. Comportamiento idéntico, unión de 2 miembros. Verificación: `npm test -- resolver-solicitud-interna` en verde sin tocar tests. Commit: `refactor(core): convierte el nombre de evento de ternario a Record exhaustivo (comando-cancelar-solicitud, tarea 2)`.

3. [x] **`src/core/commands/registro-acciones-contract.ts`** (modificado, ADR 133 pto 2-3) — `COMANDO_CANCELAR_SOLICITUD = "/cancelar-solicitud"` y `RESULTADO_CANCELADA = "cancelada"`, molde literal de las constantes existentes. TDD exception (constantes puras) — sin comando de test propio, ejercitadas por la tarea 6. Commit: `feat(core): agrega COMANDO_CANCELAR_SOLICITUD y RESULTADO_CANCELADA (comando-cancelar-solicitud, tarea 3)`.

4. [x] **`src/core/solicitudes/solicitudes-contract.ts`** (modificado, sólo el estado, ADR 125) — `SOLICITUD_ESTADO_CANCELADA = "cancelada"` y cuarto miembro de `SolicitudEstado`. **No toca todavía `SolicitudStorePort`** (eso es la tarea 7 — ver acoplamiento de compilación arriba). Test primero: extiende `solicitudes-contract.test.ts` — `SolicitudEstado` acepta los cuatro literales; `SOLICITUD_ESTADO_CANCELADA === "cancelada"`. Cubre spec `solicitud-interna-hitl` delta, requirement "`SolicitudEstado` incorpora `cancelada`", escenario 1. Comando: `npm test -- solicitudes-contract`. Commit: `feat(core): agrega SOLICITUD_ESTADO_CANCELADA como cuarto valor terminal (comando-cancelar-solicitud, tarea 4)`.

5. [x] **`src/build-on-comando-empleado.ts`** (modificado, ADR 133 — ★ Hallazgo 3) — `SOLICITUD_ESTADOS` gana `SOLICITUD_ESTADO_CANCELADA` como cuarto elemento del array `as const` que `toPortSolicitud` valida. Depende de la tarea 4. Test primero (**rojo contra el array sin ampliar**): un row con `estado = "cancelada"` pasado a `toPortSolicitud` **no** lanza `SolicitudTipoEstadoInvalidoError`. Cubre el Success Criterion de `proposal.md` sobre el hallazgo del ADR 133 y la consecuencia declarada en `design.md` §2 (ADR 133). Comando: `npm test -- build-on-comando-empleado`. Commit: `fix(root): agrega cancelada a SOLICITUD_ESTADOS para que toPortSolicitud no explote tras el CAS (comando-cancelar-solicitud, tarea 5)`.

6. [x] **`src/adapters/memory/repository.ts`** (modificado, aditivo, ADR 131) — `cancelarSolicitudInterna`: tercera llamada a `resolverSolicitudTransaccional` con `{estadoOrigen: "pendiente_aprobacion_humana", estadoDestino: "cancelada", estadoCaso: "resuelto", comando: COMANDO_CANCELAR_SOLICITUD, resultado: RESULTADO_CANCELADA}`. **`resolverSolicitudTransaccional` sin una línea de cambio.** Reescribe el comentario de `migrations/0008_solicitudes_internas.ts:28-29` (deja de decir que sólo aprobar/rechazar escriben `resuelta_por`/`resuelta_at`). Depende de la tarea 3. Test primero: sobre `pendiente`, `estado='cancelada'`, caso `resuelto`, **una** fila `/cancelar-solicitud`/`cancelada`, `resuelta_por === solicitanteId`, `resuelta_at === ahora`; sobre una fila ya `aprobada`, devuelve `undefined` y **las tres tablas quedan idénticas** (snapshot antes/después). Cubre spec `cancelacion-solicitud-interna` requirement "Sólo se cancela una solicitud pendiente..." y requirement "Confirmación en dos pasos... aplica el CAS" (la mitad de escritura). Comando: `npm test -- repository`. Commit: `feat(adapters/memory): agrega cancelarSolicitudInterna como tercera llamada al CAS transaccional (comando-cancelar-solicitud, tarea 6)`.

7. [x] **`src/core/solicitudes/solicitudes-contract.ts` + `src/build-on-comando-empleado.ts`** (ambos modificados, UN SOLO COMMIT por acoplamiento de compilación — ver "Ajuste sobre `design.md` §9") — `cancelarSolicitud(input): SolicitudInterna | undefined` en `SolicitudStorePort` + comentario de `resueltaPor` reescrito (ADR 131); `createSolicitudStore` implementa el método delegando en `cancelarSolicitudInterna` (tarea 6). Depende de las tareas 4 y 6. Test primero: `solicitudes-contract.test.ts` — el fake `store: SolicitudStorePort` de `:148` gana `cancelarSolicitud` (el tipo lo exige); `build-on-comando-empleado.test.ts` — `createSolicitudStore(db).cancelarSolicitud(...)` sobre una fila `pendiente` transiciona a `cancelada` sin lanzar (round-trip real con la tarea 5). Comando: `npm test -- solicitudes-contract build-on-comando-empleado && npm run typecheck`. Commit: `feat(core,root): agrega cancelarSolicitud al puerto y a createSolicitudStore en un solo commit por acoplamiento de tipos (comando-cancelar-solicitud, tarea 7)`.

8. [x] **`src/core/solicitudes/resolver-solicitud-interna.ts` + `src/build-on-comando-empleado.ts`** (ambos modificados, UN SOLO COMMIT — ★ el trabajo real, ADR 126/129/130) — `ACCION_CANCELAR_SOLICITUD = "cancelar"` como tercer miembro de `AccionSolicitud`; tercer `case` en el `switch` de la tarea 1 (`store.cancelarSolicitud(input)`); tercera entrada en el `Record` de la tarea 2 (`"solicitud-cancelada"`); **chequeo de dueño** entre `:118` y `:120` (`accion === ACCION_CANCELAR_SOLICITUD && solicitud.solicitanteId !== sesion.empleadoId`), gateado por acción; cuarta variante `{resultado: "no_es_dueno", accion, itemId}` de `ResolverSolicitudResult`, **sin campo `item`** (R6 estructural); `logEvent("tui-comando", "solicitud-cancelacion-no-autorizada", {...})`. **Mismo commit**: tercera clave `[ACCION_CANCELAR_SOLICITUD]: COMANDO_CANCELAR_SOLICITUD` en `ACCION_SOLICITUD_COMANDO` (`build-on-comando-empleado.ts:540`) — el compilador la exige apenas se amplía `AccionSolicitud`. Depende de las tareas 3 y 7. Test primero (R5, **rojo contra el `switch` de 2 casos**): con `accion="cancelar"`, `store.cancelarSolicitud` se llama **una vez** y `aprobarSolicitud`/`rechazarSolicitud` **nunca** (misma aserción, spies sobre los tres), evento `solicitud-cancelada`; (R6) solicitud de `emp-otro`/sesión `emp-yo` ⇒ `resultado === "no_es_dueno"`, `itemId === solicitudId`, `"item" in resultado === false`, cero llamadas CAS, evento `solicitud-cancelacion-no-autorizada` con `casoId === "tui-comando"`; (R1, gate por acción) la MISMA solicitud ajena con `accion:"aprobar"`/`"rechazar"` devuelve `requiere_confirmacion`, no `no_es_dueno` (`describe.each`). Cubre spec `cancelacion-solicitud-interna` requirements "Sólo el propio solicitante puede cancelar" (2 escenarios) y spec `solicitud-interna-hitl` delta MODIFIED "excepto retirar la propia solicitud" (3 escenarios). Comando: `npm test -- resolver-solicitud-interna build-on-comando-empleado && npm run typecheck`. Commit: `feat(core,root): agrega ACCION_CANCELAR_SOLICITUD con chequeo de dueño y variante no_es_dueno (comando-cancelar-solicitud, tarea 8)`.

**Verificación de cierre de PR1**: `npm test && npm run typecheck` en verde; `git diff --stat main -- src/core/hitl/ src/core/propuestas/ src/core/ventas/ src/adapters/tui/` vacío; la capacidad es **inalcanzable desde la TUI** (`parsearComando` no produce `cancelar_solicitud` todavía) — cero exposición al usuario, verificable con `rg 'cancelar_solicitud' src/core/commands/comando-empleado.ts` sin resultados.

### PR2 — Comando, UX y documentación (tareas 9-13)

9. [x] **`src/core/commands/comando-empleado.ts`** (modificado, ADR 127) — brazo `{ tipo: "cancelar_solicitud"; solicitudId?: string }` en `ComandoEmpleado`; descriptor `/cancelar-solicitud [solicitudId]` en `DESCRIPTORES` (antes de `/ayuda`), `privilegiado: true`, `secreto: false`, `forma: "id_opcional_solicitud"` (**reusada, sin `Forma` nueva**); comentario de conteo de descriptores actualizado. **`parsearComando` no gana una línea** — la rama `id_opcional_solicitud` ya existente absorbe el tercer literal. Test primero: `/cancelar-solicitud S-7` ⇒ `{tipo:"cancelar_solicitud", solicitudId:"S-7"}`; sin id ⇒ `{tipo:"cancelar_solicitud"}`; `esComandoPrivilegiado("cancelar_solicitud") === true`; `Forma` conserva **cinco** miembros; `DESCRIPTORES`/`COMANDOS` cuenta actualizada (coordinar el número real con el estado de `comando-reporte-comisiones` en `main` al momento de este commit — R3, orden de merge). Cubre spec `cancelacion-solicitud-interna` requirement "Descriptor... forma existente, privilegiado" (2 escenarios). Comando: `npm test -- comando-empleado`. Commit: `feat(core): agrega descriptor /cancelar-solicitud reusando forma id_opcional_solicitud (comando-cancelar-solicitud, tarea 9)`.

10. [x] **`src/build-on-comando-empleado.ts`** (modificado, ADR 132, ADR 130 pto 5, depende de la tarea 9) — `MENSAJE_SOLICITUD_NO_ENCONTRADA: Record<AccionSolicitud, (id: string) => string>` (nuevo, completo desde el inicio con sus tres claves — `aprobar`/`rechazar` reproducen el literal actual **byte por byte**, `cancelar` con el texto que no sugiere "no existe"); rama `if (resultado.resultado === "no_es_dueno")` en `manejarResolucionSolicitud`, en los dos bloques (eco y confirmado); `case "cancelar_solicitud"` en el `switch (comando.tipo)`, junto a las otras dos ramas de solicitud. `manejarResolucionSolicitud` **no cambia de firma**; su mensaje de éxito ya sirve tal cual con `estadoFinal = "cancelada"`. Test primero: dos pasos end-to-end (eco con detalle propio sin escritura, luego *"Listo: la solicitud S-7 quedó cancelada."*, `confirmacionPendiente === undefined`); un eco pendiente de `/aprobar-solicitud S-7` **no** es consumido por `/cancelar-solicitud S-7`; no-dueño con **las tres garantías juntas** en un solo `it` (respuesta explicativa, `confirmacionPendiente` sin armar, `detalle` ajeno ausente de `responseText`); `/cancelar-solicitud` sobre id inexistente ⇒ texto nuevo, `/aprobar-solicitud` sobre el mismo id ⇒ texto actual sin cambios. Cubre spec `cancelacion-solicitud-interna` requirements "Confirmación en dos pasos" (2 escenarios) y "Sólo el propio solicitante" (el escenario de UX), spec `solicitud-interna-hitl` delta MODIFIED "Comandos privilegiados... ahora tres" (los 4 escenarios). Comando: `npm test -- build-on-comando-empleado`. Commit: `feat(root): cablea /cancelar-solicitud al dispatcher con mensaje por accion y rama no_es_dueno (comando-cancelar-solicitud, tarea 10)`.

11. [x] **`openspec/changes/comando-cancelar-solicitud/specs/cancelacion-solicitud-interna/spec.md`** (nueva) **+ `specs/solicitud-interna-hitl/spec.md`** (delta) — ya escritas por `sdd-spec`, no llevan implementación propia. Se commitean junto al resto del PR2 verificando consistencia final contra el código de las tareas 1-10: el delta **acota sin derogar** el requirement `:72` (R1) — confirmar que `/aprobar-solicitud`/`/rechazar-solicitud` siguen sin chequeo de dueño en el código de la tarea 8. TDD exception (specs, no código). Commit: `docs(specs): agrega cancelacion-solicitud-interna y delta de solicitud-interna-hitl (comando-cancelar-solicitud, tarea 11)`.

12. [x] **`README.md`** (modificado) — TDD exception (documentación) — documenta `/cancelar-solicitud [solicitudId]` en el flujo de solicitud interna: quién puede ejecutarlo (el propio solicitante), la restricción a `pendiente_aprobacion_humana`, y que no hay `/reabrir-solicitud`. Commit: `docs: documenta /cancelar-solicitud en el flujo de solicitud interna en README (comando-cancelar-solicitud, tarea 12)`.

13. [x] **N/A, verificado sin diff** — `docs/ARC42_Harness_Empresarial.md` no describe hoy el flujo de solicitud interna en ningún nivel (Vista de Bloques, Vista de Ejecución ni Conceptos Transversales): cero menciones de `HITL`, `SolicitudEstado`, `pendiente_aprobacion_humana` o `resuelta_por`; la Caja Blanca del Registro de Comandos tampoco lleva un conteo de comandos/descriptores. Los tres puntos originales de esta tarea (conteo, cuarto estado `cancelada`, nota de `resuelta_por`) no tienen dónde aplicarse — es deuda documental previa a este change, no algo que esta tarea deba resolver. Verificado con `grep` sobre todo el archivo antes de descartar la tarea. Sin commit — cero diff.

**Verificación de cierre de PR2**: `npm test && npm run typecheck` en verde; los dos pasos end-to-end sobre SQLite real, más las tres garantías de no-dueño (§6, fila "Integration — no dueño, tres garantías juntas"); `rg 'cancelar_solicitud' src/core/commands/comando-empleado.ts` con resultados (la capacidad ya es alcanzable desde la TUI).

### PR3 — Segunda ronda: filtro por dueño en el listado sin id (ADR 144, tareas 15-18)

Los PRs 1 y 2 **ya están implementados y mergeados** (tareas 1-13 commiteadas). Este PR3 es el **slice único** que `design.md` §9-bis describe (~217 líneas, riesgo `Low`, sin chain interno, base `hito/v3.3-comando-cancelar-solicitud` recreada desde `main`). Desglosa la tabla A-D de `design.md` §10 en cuatro tareas concretas, en el orden de dependencia real: puerto+SQL primero (nadie más puede filtrar si el store no lo soporta), caso de uso segundo (gatea la llamada nueva por acción), handler tercero (consume el caso de uso y cierra la fuga de `detalle` en el formateo), specs al final (documentan lo que las tres anteriores ya implementaron y prueban).

15. [x] **`src/core/solicitudes/solicitudes-contract.ts` + `src/adapters/memory/repository.ts`** (ambos modificados, ADR 144 pto 1 — ★ Alcance A de `design.md` §10) — `listarSolicitudesPendientes` gana un tercer campo **opcional** `solicitanteId?: string` en su objeto de filtro (contrato) y `listSolicitudesInternas` suma la cláusula `AND (@solicitanteId IS NULL OR solicitante_id = @solicitanteId)` + el parámetro (SQL), mismo idioma que `repository.ts:1298`/`:2088`/`:2370`. El predicado `estado = 'pendiente_aprobacion_humana'` **no se toca** (sigue sin envolver en `IS NULL OR`, `idx_solicitudes_estado` se sigue usando — ADR 144 pto 6). Sin dependencias de tareas previas de esta ronda (es la primera). Test primero, **rojo contra el código actual**: (a) con **25** solicitudes pendientes ajenas creadas **antes** que la única propia de E (`ORDER BY created_at`), `listSolicitudesInternas({ solicitanteId: E, limite: 20 })` **devuelve la propia** — hoy el `LIMIT 20` se la come, éste es el test que prueba que el hallazgo del Reviewer era un bug funcional, no cosmética (`design.md` §6-bis fila 1); (b) `solicitudes-contract.test.ts` — el fake de `:187` honra `solicitanteId`; combinado con `{ solicitudId }` no rompe (filtro doble). Cubre spec `cancelacion-solicitud-interna` requirement "El listado sin id... muestra únicamente las solicitudes pendientes del propio solicitante", escenario "Solicitudes ajenas no consumen el tope del listado (el bug del `LIMIT`)". Comando: `npm test -- solicitudes-contract repository && npm run typecheck`. Commit: `feat(core,adapters/memory): agrega solicitanteId opcional al filtro de listarSolicitudesPendientes (comando-cancelar-solicitud, tarea 15)`.

16. [x] **`src/core/solicitudes/resolver-solicitud-interna.ts`** (modificado — ★ el trabajo real de la ronda, ADR 144 ptos 2/3/5, Alcance B de `design.md` §10) — la rama sin id (`:139-144`) gana `const soloPropias = accion === ACCION_CANCELAR_SOLICITUD;` y pasa `soloPropias ? { limite, solicitanteId: sesion.empleadoId } : { limite }` a `store.listarSolicitudesPendientes`, gateado por acción con la misma fórmula del chequeo de dueño (ADR 130 pto 4); el evento `solicitud-listada` suma `...(soloPropias ? { soloPropias: true } : {})` por spread condicional. La rama B (búsqueda por id, `:146`) **no se toca**: sigue llamando `{ solicitudId }` sin `solicitanteId` (ADR 144 pto 3 — es la trampa de coherencia deliberada). Depende de la tarea 15. Test primero: (R1, gate por acción) `describe.each` sobre `["cancelar"]`/`["aprobar", "rechazar"]` — `cancelar` sin id llama con `{ limite, solicitanteId }`; `aprobar`/`rechazar` llaman con **`{ limite }` exacto**, y las aserciones vigentes `:78`/`:89`/`:230` **quedan verdes sin modificarse**; (R8, anti-regresión de la rama B) búsqueda por id de una solicitud **ajena** sigue llamando con `{ solicitudId }` **sin** `solicitanteId`, y el resultado sigue siendo `no_es_dueno`, **no** `no_aplicable/no_encontrada` — candado explícito contra "unificar las dos llamadas" (riesgo R8 de `design.md` §10); evento — rama A de cancelar emite `soloPropias: true`, rama A de aprobar emite el payload **exacto** `{ accion, cantidad }` sin la clave nueva. Cubre spec `cancelacion-solicitud-interna` requirement "El listado sin id..." escenario "El listado sin id devuelve sólo las solicitudes propias" y escenario "La búsqueda por id de una solicitud ajena sigue encontrándola — no se filtra por dueño (ADR 144 pto 3)"; spec `solicitud-interna-hitl` delta, escenario MODIFIED "`/aprobar-solicitud`/`/rechazar-solicitud` sin id siguen listando toda la organización, sin filtro de dueño (ADR 144, no derogado)". Comando: `npm test -- resolver-solicitud-interna && npm run typecheck`. Commit: `feat(core): agrega gate por accion en el listado sin id y soloPropias en el evento (comando-cancelar-solicitud, tarea 16)`.

17. [x] **`src/build-on-comando-empleado.ts`** (modificado, ADR 144 pto 4, Alcance C de `design.md` §10) — `MENSAJE_LISTADO_SOLICITUDES_VACIO: Record<AccionSolicitud, string>` nuevo (`aprobar`/`rechazar` reproducen *"No hay solicitudes para listar."* byte por byte, `cancelar` dice *"No tenés solicitudes pendientes para cancelar."*); `formatearListadoSolicitudes` gana la `accion` como segundo parámetro y la usa **sólo** para el caso vacío — `formatearLineaSolicitud` no cambia (las que se listan ya son propias, nada que ocultar en ellas); actualiza el call site (`:1042`). Depende de la tarea 16. Test primero: (a) end-to-end sin id — E ejecuta `/cancelar-solicitud` sin id con solicitudes ajenas pendientes en la base ⇒ `responseText` **no contiene** el `detalle` ni el `solicitanteId` ajenos (`not.toContain`), espejo en la rama A de la garantía que el ADR 130 dio en la rama B; (b) empleado sin pendientes propias ⇒ *"No tenés solicitudes pendientes para cancelar."*; `/aprobar-solicitud` sin id con la base vacía ⇒ *"No hay solicitudes para listar."* byte por byte, sin cambios. Cubre spec `cancelacion-solicitud-interna` requirement "El listado sin id..." escenarios "Una solicitud ajena pendiente no aparece ni expone su detalle" y "Sin solicitudes propias pendientes, mensaje propio de la acción cancelar". Comando: `npm test -- build-on-comando-empleado && npm run typecheck`. Commit: `feat(root): filtra detalle ajeno del listado sin id y agrega mensaje vacio por accion (comando-cancelar-solicitud, tarea 17)`.

18. [x] **`openspec/changes/comando-cancelar-solicitud/specs/cancelacion-solicitud-interna/spec.md`** (modificado — nuevo requirement + escenario agregado) **+ `specs/solicitud-interna-hitl/spec.md`** (modificado — requirement MODIFIED ampliado) — Alcance D de `design.md` §10, mitad de specs. **Ya escritas por `sdd-spec` en esta misma ronda, no llevan implementación propia**: cubren el requirement "El listado sin id (`/cancelar-solicitud` sin argumento) muestra únicamente las solicitudes pendientes del propio solicitante" (4 escenarios, incluido el del `LIMIT`), el escenario agregado "La búsqueda por id de una solicitud ajena sigue encontrándola" al requirement vigente de dueño, y la ampliación del requirement MODIFIED de `solicitud-interna-hitl` que fija en contrato que `/aprobar-solicitud`/`/rechazar-solicitud` siguen listando todo sin filtro. TDD exception (specs, no código). Se commitean verificando consistencia final contra el código de las tareas 15-17: confirmar que la rama B (búsqueda por id) del código de la tarea 16 sigue sin filtrar por dueño, tal como el escenario del punto 2 de `design.md` §10 exige. Comando: `npm test && npm run typecheck` (suite completa en verde, sin comando de test propio de esta tarea). Commit: `docs(specs): amplia cancelacion-solicitud-interna y solicitud-interna-hitl con el filtro por dueno del listado sin id (comando-cancelar-solicitud, tarea 18)`.

**Verificación de cierre de PR3**: `npm test && npm run typecheck` en verde; `git diff --stat main -- src/core/hitl/ src/core/propuestas/ src/core/ventas/ src/adapters/tui/` vacío (blast radius sigue cero, también en esta ronda — `design.md` §5-bis); `rg 'ACCION_CANCELAR_SOLICITUD' src/core/solicitudes/resolver-solicitud-interna.ts` confirma que la llamada de la rama B (`{ solicitudId }`) sigue sin `solicitanteId` — evidencia manual de que R8 no se coló; ningún archivo de `src/core/commands/comando-empleado.ts`, `registro-acciones-contract.ts` ni `migrations/**` con diff (ADR 144 pto 6, cero migraciones también en esta ronda). *Rollback*: revert del PR3 completo ⇒ vuelve el listado global de `/cancelar-solicitud` sin id, sin datos que reparar (ninguna escritura cambia en esta ronda, `design.md` §9-bis).

### Verificación manual final del entregable completo (tarea 19, renumerada desde la tarea 14 original)

19. [x] **Verificación manual del entregable** — TDD exception (sin código de producción) — corre **después** de mergear el PR3, no antes (`design.md` §9-bis: *"verificar a mano un comportamiento que ya sabemos que vamos a cambiar es trabajo tirado"*). Casos heredados de la tarea 14 original, sin cambios: dueño cancela una solicitud propia pendiente (dos pasos, `estado='cancelada'` confirmado por consulta directa a `data/harness.db`); un tercero intenta cancelar ⇒ rechazo, sin `detalle` ajeno visible; cancelar una solicitud ya `aprobada` ⇒ mismo mensaje que un id inexistente, **cero** filas nuevas; `/aprobar-solicitud`/`/rechazar-solicitud` sobre solicitudes ajenas siguen funcionando sin chequeo de dueño (R1 no derogado); `rg 'MotivoNoAplicableHitl' src/core/hitl/hitl-contract.ts` sin diff; `git diff --stat main -- src/core/hitl/ src/core/propuestas/ src/core/ventas/` vacío; rollback del commit de la tarea 8 ⇒ `resolver-solicitud-interna.ts` vuelve a 2 acciones; `npm test`/`npm run typecheck` en verde. **Dos casos nuevos, sumados por el ADR 144** (`design.md` §10 punto D): (a) *"`/cancelar-solicitud` sin id lista sólo lo mío"* — con solicitudes pendientes de al menos otro empleado además de las propias en `data/harness.db`, `/cancelar-solicitud` sin id devuelve únicamente las del empleado que lo ejecuta, sin `detalle` ni `solicitanteId` ajenos visibles en la respuesta, y con 20+ solicitudes ajenas pendientes creadas antes que la propia, la propia **sigue apareciendo** (verificación manual del daño 1, el bug del `LIMIT`, sobre datos reales); (b) *"`/aprobar-solicitud`/`/rechazar-solicitud` sin id siguen listando todo"* — el mismo estado de la base, corrido con `/aprobar-solicitud` sin id, devuelve el listado global de la organización con `solicitante`/`detalle` completos, exactamente igual que antes del ADR 144 (evidencia de que R1 y `:72` siguen sin derogarse también después de esta ronda). Evidencia en `docs/progreso/v3.3-comando-cancelar-solicitud/`. Commit: `docs: evidencia de verificacion manual del entregable cancelar-solicitud post-adr-144 (comando-cancelar-solicitud, tarea 19)`.

## Review Workload Forecast

### PR1 + PR2 (tareas 1-13) — ★ ya implementado, commiteado y mergeado

| Field | Value |
|---|---|
| Estimated changed lines | ~473 (`design.md` §9, prod+test+docs+spec — sin cambios en el total, sólo en el corte por PR) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR1 (~280, tareas 1-8) → PR2 (~188, tareas 9-13) |
| Delivery strategy | Ya resuelta por el checkpoint — 2 PRs encadenados |
| Chain strategy | **`stacked-to-main`** — decisión ya tomada, cada PR mergea a `main` en orden |

```text
Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High
```

### PR3 (tareas 15-18) — ★ segunda ronda, ADR 144 — slice separado, NO se mezcla con los números de arriba

`design.md` §9-bis. Este PR3 **no** se suma al total de PR1+PR2 anterior: es trabajo nuevo sobre código ya mergeado, con su propio presupuesto y su propio riesgo — mezclarlo inflaría artificialmente el "High" de PR1+PR2 con líneas que ya se revisaron y aprobaron.

| Field | Value |
|---|---|
| Estimated changed lines | ~217 (`design.md` §5-bis: ~42 producción, ~140 tests, ~35 specs) |
| 400-line budget risk | **Low** — holgado bajo el presupuesto, ni siquiera cerca de la mitad |
| Chained PRs recommended | **No** — slice único y coherente, sin chain interno |
| Suggested split | Un solo PR (tareas 15-18) — puerto+SQL → caso de uso → handler → specs, en ese orden de commits |
| Delivery strategy | `stacked-to-main` — tercer PR de la misma cadena, apila sobre PR1+PR2 ya mergeados |
| Chain strategy | **`stacked-to-main`** — mismo patrón, base `hito/v3.3-comando-cancelar-solicitud` recreada desde `main` |
| Decision needed before apply | **Ya resuelta** — ADR 144 aprobado por checkpoint humano (ver nota de proceso al inicio de este documento y `design.md` §11); no hay pregunta bloqueante pendiente para `sdd-apply` |

```text
Decision needed before apply: No (ADR 144 ya aprobado por checkpoint humano)
Chained PRs recommended: No
Chain strategy: stacked-to-main (tercer PR de la cadena existente)
400-line budget risk: Low
```

### Reconciliación con `design.md` §9

| Archivo | Tareas | Líneas est. |
|---|---|---|
| `resolver-solicitud-interna.ts` (switch, Record, chequeo, variante) | 1, 2, 8 | ~8 + ~10 + ~117 = ~135 |
| `registro-acciones-contract.ts` | 3 | ~5 |
| `solicitudes-contract.ts` (estado + puerto) | 4, 7 (mitad) | ~13 + ~21 = ~34 |
| `repository.ts` + comentario `0008` | 6 | ~67 |
| `build-on-comando-empleado.ts`, porción PR1 (`SOLICITUD_ESTADOS`, store, `ACCION_SOLICITUD_COMANDO`) | 5, 7 (mitad), 8 (mitad) | ~19 + ~18 + ~7 = ~44 |
| **Subtotal PR1** | 1-8 | **~285** (redondeado a ~280 en la tabla superior) |
| `comando-empleado.ts` | 9 | ~39 |
| `build-on-comando-empleado.ts`, porción PR2 (mensajes, rama, case) | 10 | ~67 |
| `specs/` (capability nueva + delta) | 11 | ~60 |
| `README.md` + `docs/ARC42_*.md` | 12, 13 | ~22 |
| **Subtotal PR2** | 9-13 | **~188** (~193 con redondeo) |
| **Total** | | **~473** — igual al total de `design.md` §9 |

El total no se movió; el corte por PR sí, ~30 líneas de `build-on-comando-empleado.ts` pasaron de la porción "PR2" implícita de `design.md` §9 a la porción PR1, por el acoplamiento de compilación de `SolicitudStorePort`/`ACCION_SOLICITUD_COMANDO` documentado arriba. Ambos PRs siguen muy por debajo de 400 líneas individualmente.

### Reconciliación con `design.md` §5-bis (PR3, ADR 144)

| Archivo | Tarea | Líneas est. |
|---|---|---|
| `solicitudes-contract.ts` (campo `solicitanteId?`) | 15 | ~8 |
| `repository.ts` (cláusula SQL + parámetro) | 15 | ~8 |
| `solicitudes-contract.test.ts` (fake + filtro combinado) | 15 | ~20 |
| `repository.test.ts` (filtro por solicitante + test del `LIMIT`) | 15 | ~40 |
| **Subtotal tarea 15 (Alcance A)** | 15 | **~76** |
| `resolver-solicitud-interna.ts` (gate por acción, `soloPropias`) | 16 | ~14 |
| `resolver-solicitud-interna.test.ts` (gate, anti-regresión rama B, evento) | 16 | ~45 |
| **Subtotal tarea 16 (Alcance B)** | 16 | **~59** |
| `build-on-comando-empleado.ts` (mensaje vacío, `formatearListadoSolicitudes`) | 17 | ~12 |
| `build-on-comando-empleado.test.ts` (end-to-end sin id, mensaje vacío) | 17 | ~35 |
| **Subtotal tarea 17 (Alcance C)** | 17 | **~47** |
| `specs/cancelacion-solicitud-interna/spec.md` + `specs/solicitud-interna-hitl/spec.md` | 18 | ~35 |
| **Subtotal tarea 18 (Alcance D, mitad specs)** | 18 | **~35** |
| **Total PR3** | 15-18 | **~217** — igual al total de `design.md` §5-bis |

Coincide byte por byte con el estimado de `design.md` §9-bis. `hitl-contract.ts`, `comando-empleado.ts`, `registro-acciones-contract.ts` y `migrations/**` quedan en cero líneas también en esta ronda (ADR 144 pto 6).

## Después de la tarea 19

- Pasa al Reviewer (`sdd-verify` + `code-review`) contra este documento, las 2 specs (ya con el delta del ADR 144) y `design.md`, con atención especial a: **el orden real de commits dentro de PR1** (tareas 4→5→7→8, cada una debe dejar `npm run typecheck` en verde por sí sola — es la garantía que motivó el ajuste de secuencia de este documento), **ADR 126/130** (el chequeo de dueño gateado por acción, R1 no derogado), **R6** (`"item" in resultado === false` como aserción estructural, no de disciplina) y, nuevo en esta ronda, **R8** (`design.md` §10: que nadie haya "unificado" las dos llamadas a `listarSolicitudesPendientes` — el test anti-regresión de la tarea 16 y el escenario dedicado de la tarea 18 son la evidencia a revisar primero).
- Antes de mergear PR1/PR2 (si todavía no mergeados en algún entorno): confirmar con `comando-reporte-comisiones` el orden de merge (R3) — el segundo en mergear rebasa el conteo de descriptores en las tareas 9 y 13. El PR3 no toca esta superficie (`comando-empleado.ts` sin diff en esta ronda), así que R3 no reaparece acá.
- Al cerrar PR3: confirmar que el listado sin id de `/cancelar-solicitud` nunca devolvió una lista vacía a un empleado con solicitudes propias pendientes durante la verificación manual (tarea 19, caso nuevo (a)) — es la evidencia final de que el daño 1 (bug del `LIMIT`) quedó cerrado, no sólo cubierto por tests.
- Si aprueba: checklist de cierre de `AGENTS.md` (Reviewer aprobado, `docs/progreso/v3.3-comando-cancelar-solicitud/` completo con la evidencia post-ADR 144), tag `v3.3.0` (numeración a confirmar por el checkpoint).

---

**Nota de formato**: mismo criterio que `definicion-skills/tasks.md` y `comando-reporte-comisiones/tasks.md` — se sigue el formato extenso ya establecido por `proposal.md`, las 2 `specs/` y `design.md` de este mismo change, en vez del tope de 530 palabras de la skill `sdd-tasks`. Esta tercera pasada (tareas 15-19, PR3/ADR 144) mantiene el mismo criterio de extensión, el mismo molde de commit `<tipo>(<scope>): <descripción> (comando-cancelar-solicitud, tarea N)` y la misma estructura de tabla de Review Workload Forecast, separada en subsecciones para no mezclar el riesgo ya aprobado de PR1+PR2 con el riesgo `Low` nuevo de PR3.
