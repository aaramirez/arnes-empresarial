> **Nota de proceso (hook de graphify)**: ejecutor sin herramienta de shell disponible (solo `Read`/`Grep`/`Glob`/`Write`/`Edit`), misma limitación documentada por `proposal.md` y `design.md` de este change. Este documento no reabre ninguna decisión: cada tarea desglosa una pieza ya fijada por `design.md` §1 (mapa de arquitectura), §9 (capability → módulos) y §10-11 (testing y archivos), y sus ADR 157-162. Se recomienda `graphify update .` tras persistir este archivo.

# Tasks: Autorización de Empleado — el gate de "resolver lo ajeno" y la prohibición de autoaprobación

**Origen** (no se duplica): [`proposal.md`](proposal.md) (ADR 152-156, Success Criteria) · [`design.md`](design.md) §1-13 (ADR 157-162) · specs: [`autorizacion-empleado`](specs/autorizacion-empleado/spec.md) (11 requirements, capability nueva), deltas de [`autenticacion-empleado-tui`](specs/autenticacion-empleado-tui/spec.md), [`reembolso-resolucion-escalacion`](specs/reembolso-resolucion-escalacion/spec.md), [`solicitud-interna-hitl`](specs/solicitud-interna-hitl/spec.md), [`registro-acciones-empleado`](specs/registro-acciones-empleado/spec.md).

**Orden de merge**: primero en la cadena acordada (`autorizacion-empleado` → `operaciones-negocio-conversacionales` → `comandos-administracion-empleados` → `consultas-negocio-a2a-entrante`). Este change **no depende de ningún change no iniciado** (`proposal.md`, *Dependencies*); las tareas de abajo asumen `main` limpio, sin rebasar sobre ningún otro change en curso.

**Metodología**: TDD estricto (`strict_tdd: true`, `npm test` = `vitest run`) en toda tarea con lógica (1.1-1.3, 2.1-2.2, 3.1, 4.1-4.2, 5.1). Excepción única: 5.2 (`docs/ARC42_*.md` + `README.md`, documentación sin código de producción).

## Review Workload Forecast

| Field | Value |
|---|---|
| Estimated changed lines | ~1045 (5 work units: fundamentos, política+gate reembolso, gate+autoaprobación solicitud, auditoría+dispatcher, CLI+docs) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR1 (~245) → PR2 (~170) → PR3 (~155) → PR4 (~225) → PR5 (~250) |
| Delivery strategy | ask-on-risk |
| Chain strategy | feature-branch-chain |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High
```

**Por qué cinco unidades y no una sola rama**: `design.md` §1 ya separa el trabajo en piezas con dependencia estricta de compilación (contrato → política → gate por función → dispatcher → CLI), y `proposal.md` *Approach* fija el mismo orden en cuatro pasos con el corte 1 declarado "entregable sin cambiar comportamiento". Partir el gate de solicitud (Unit 3) del de reembolso (Unit 2) sigue el mismo diff que `design.md` §4 muestra por separado para cada función, y aísla la revisión de ADR 155 (autoaprobación) de la de ADR 153 (gate por rol). Cada unidad es revertible en orden inverso (`proposal.md`, *Rollback Plan*: revertir el gate nunca deja un estado inválido, solo vuelve a ser permisivo; la migración `0013` no se revierte y queda inerte).

### Suggested Work Units (feature-branch-chain)

| Unit | Goal | Tasks | Est. lines | Base branch |
|---|---|---|---|---|
| 1 | Fundamentos sin cambio de comportamiento: contrato de rol, migración `0013`, puerto de lectura/escritura en el adaptador | 1.1-1.3 | ~245 | `hito/v3.5-autorizacion-empleado` (tracker, fijado por el checkpoint) |
| 2 | Política de decisión + gate en `resolverEscalacionReembolso` | 2.1-2.2 | ~170 | Unit 1 |
| 3 | Gate + prohibición de autoaprobación en `resolverSolicitudInterna` | 3.1 | ~155 | Unit 2 |
| 4 | Constantes de auditoría + wiring y 4 ramas nuevas en el dispatcher | 4.1-4.2 | ~225 | Unit 3 |
| 5 | Provisioning por CLI (`--rol`) + documentación de cierre | 5.1-5.2 | ~250 | Unit 4 |

Solo la rama tracker (`hito/v3.5-autorizacion-empleado`) mergea a `main`; cada PR hijo apunta a la rama del PR inmediatamente anterior. Si un diff hijo muestra cambios de la unidad previa, retargetear/rebasar antes de pedir revisión.

## Phase 1: Fundamentos — contrato, migración y puerto (Work Unit 1, PR1)

- [x] 1.1 **`src/core/auth/rol-contract.ts`** (nuevo, sin imports, molde `credenciales-contract.ts`, ADR 157/162) — `ROL_EMPLEADO = "empleado"`, `ROL_ADMINISTRADOR = "administrador"`, `ROLES_EMPLEADO = [ROL_EMPLEADO, ROL_ADMINISTRADOR] as const`, `RolEmpleado`, `RolEmpleadoPort.buscarRol(empleadoId): RolEmpleado | undefined`. Test primero: `rol-contract.test.ts` — `ROLES_EMPLEADO` tiene exactamente 2 miembros; cero imports del archivo (assert sobre el texto, molde `*-contract.test.ts`); el puerto expone únicamente una operación de lectura síncrona. Cubre spec `autorizacion-empleado` req. "Rol de empleado como valor enumerado" y "Puerto de lectura de rol". Comando: `npm test -- rol-contract`. Commit: `feat(core): agrega rol-contract con ROL_EMPLEADO/ROL_ADMINISTRADOR y RolEmpleadoPort (autorizacion-empleado, tarea 1.1)`.

- [x] 1.2 **`src/adapters/memory/migrations/0013_roles_empleado.ts`** (nuevo, ADR 157) + entrada al **final** de `migrations/index.ts` (una línea de import + una entrada, sin editar ni reordenar `0001`-`0012`) — `CREATE TABLE IF NOT EXISTS roles_empleado (empleado_id TEXT PRIMARY KEY, rol TEXT NOT NULL, created_at TEXT NOT NULL, updated_at TEXT NOT NULL)`, sin FK, sin `CHECK`. Test primero: extiende el test de `migrations/index.ts` — las doce migraciones previas no cambian su definición; `0013` es la última entrada; sobre una BD con filas previas de `credenciales_empleado`, tras migrar, `roles_empleado` queda con **cero filas** (verificación directa del default-deny sin backfill, ADR 156/157). Cubre spec req. "Persistencia aditiva del rol, con las filas existentes en rol base". Comando: `npm test -- migrations`. Commit: `feat(db): agrega migracion 0013 tabla roles_empleado aditiva sin backfill (autorizacion-empleado, tarea 1.2)`.

- [x] 1.3 **`src/adapters/memory/repository.ts`** (modificado, molde exacto de `buscarCredencialEmpleado`/`updateCredencialEmpleado`, ADR 162) — `RolEmpleadoRow`, `buscarRolEmpleado(db, empleadoId): RolEmpleadoRow | undefined` (`SELECT ... WHERE empleado_id = ?`), `upsertRolEmpleado(db, {empleadoId, rol, ahora}): RolEmpleadoRow` (`ON CONFLICT(empleado_id) DO UPDATE`, molde ya usado en el archivo; `created_at` solo en el INSERT inicial, `updated_at` se pisa siempre). Test primero: `repository.test.ts` — `buscarRolEmpleado` sobre tabla vacía ⇒ `undefined`; `upsertRolEmpleado` dos veces sobre el mismo `empleadoId` ⇒ `created_at` no cambia, `updated_at` sí (mismo test que ya existe para `updateCredencialEmpleado`). Cubre spec req. "Puerto de lectura de rol" (lado adaptador) y ADR 156 pto 4. Comando: `npm test -- repository`. Commit: `feat(adapters): agrega buscarRolEmpleado y upsertRolEmpleado con ON CONFLICT (autorizacion-empleado, tarea 1.3)`.

## Phase 2: Política de decisión + gate en escalación de reembolso (Work Unit 2, PR2)

- [x] 2.1 **`src/core/auth/autorizacion-resolucion.ts`** (nuevo, importa solo `rol-contract.js`, ADR 158) — `puedeResolverAjeno(rolPort: RolEmpleadoPort, empleadoId: string): boolean` — `rolPort.buscarRol(empleadoId) ?? ROL_EMPLEADO`, compara contra `ROL_ADMINISTRADOR`. Test primero: `autorizacion-resolucion.test.ts` — rol `administrador` explícito ⇒ `true`; rol `empleado` explícito ⇒ `false`; **puerto devuelve `undefined` ⇒ `false`** (el test que hace verificable ADR 154 pto 5 / spec req. "Un `empleado_id` sin fila de rol cae al rol base, nunca al elevado"). Comando: `npm test -- autorizacion-resolucion`. Commit: `feat(core): agrega puedeResolverAjeno como unico evaluador de autorizacion (autorizacion-empleado, tarea 2.1)`.

- [x] 2.2 **`src/core/ventas/resolver-escalacion-reembolso.ts`** (modificado, ADR 153/159) — `ResolverEscalacionDeps` gana `readonly rolPort: RolEmpleadoPort` (requerido, no opcional — inexpresable sin él, mismo criterio ADR 108 de `definicion-skills`); inserción entre `!confirmado` y `resolucionInput`: `if (!puedeResolverAjeno(deps.rolPort, sesion.empleadoId)) { logEvent(...); return { resultado: "no_autorizado", accion, ventaId, casoId: venta.casoId } }`; unión `ResolverEscalacionResult` gana el miembro `no_autorizado`. Test primero: extiende `resolver-escalacion-reembolso.test.ts` — **todos los fixtures existentes inyectan `rolPort`** (dejan de compilar hasta hacerlo, la forma concreta de "el test rojo hoy pasa en verde"); rol base + `confirmado:true` sobre `aprobar`/`rechazar`/`reabrir` ⇒ `no_autorizado`, sin tocar `ventas.estado`/`casos.estado`; rol elevado ⇒ comportamiento idéntico al de antes del change (test de regresión explícito). Cubre spec `autorizacion-empleado` req. "Gate de autorización sobre resolver lo ajeno" y delta `reembolso-resolucion-escalacion` req. "Las tres transiciones de resolución exigen rol elevado". Comando: `npm test -- resolver-escalacion-reembolso`. Commit: `feat(core): agrega gate de rol a resolverEscalacionReembolso via rolPort en deps (autorizacion-empleado, tarea 2.2)`.

## Phase 3: Gate + prohibición de autoaprobación en solicitud interna (Work Unit 3, PR3)

- [x] 3.1 **`src/core/solicitudes/resolver-solicitud-interna.ts`** (modificado, ADR 153/155/159) — `ResolverSolicitudDeps` gana `readonly rolPort: RolEmpleadoPort` (requerido); inserción entre `!confirmado` y `resolucionInput`, dentro de `if (!esAccionAutoservicio(accion))` (no toca ni subsume `esAccionAutoservicio`, ADR 154 pto 4): primero `!puedeResolverAjeno(...)` ⇒ `no_autorizado`, después `solicitud.solicitanteId === sesion.empleadoId` ⇒ `autoaprobacion_prohibida` (orden exacto de RD-78: rol antes que autoaprobación, `design.md` §4); unión `ResolverSolicitudResult` gana ambos miembros con `casoId` (deviación explícita del molde `no_es_dueno`, que no lo lleva). Test primero: extiende `resolver-solicitud-interna.test.ts` — fixtures existentes inyectan `rolPort`; rol base + `aprobar` ajena ⇒ `no_autorizado`; rol elevado + solicitud propia ⇒ `autoaprobacion_prohibida`; rol base + `cancelar` propia ⇒ sigue aplicando sin que el gate la bloquee; rol elevado + ajena ⇒ aplica igual que antes. Cubre spec req. "Gate de autorización... solicitud interna ajena", "El gate de rol convive con el chequeo de autoservicio" y "Prohibición de autoaprobación", más los deltas `solicitud-interna-hitl` (ambos requirements MODIFIED/ADDED). Comando: `npm test -- resolver-solicitud-interna`. Commit: `feat(core): agrega gate de rol y prohibicion de autoaprobacion a resolverSolicitudInterna (autorizacion-empleado, tarea 3.1)`.

## Phase 4: Auditoría del rechazo + wiring del dispatcher (Work Unit 4, PR4)

- [ ] 4.1 **`src/core/commands/registro-acciones-contract.ts`** (modificado, ADR 161) — dos constantes nuevas junto a las existentes: `RESULTADO_NO_AUTORIZADO = "no_autorizado"`, `RESULTADO_AUTOAPROBACION_PROHIBIDA = "autoaprobacion_prohibida"`; actualiza el comentario `:75-76` ("las filas de los TRES comandos de resolución no pasan por acá") para matizar que un **intento rechazado** por rol o autoaprobación sí pasa por este puerto, igual que un intento rechazado por CAS. Sin test propio (constantes + doc-comment); cubierto indirectamente por 4.2. Cubre spec req. "Un intento rechazado... deja fila distinguible" y delta `registro-acciones-empleado` req. "Atomicidad". Comando: `npm run typecheck`. Commit: `feat(core): agrega constantes de resultado no_autorizado y autoaprobacion_prohibida (autorizacion-empleado, tarea 4.1)`.

- [ ] 4.2 **`src/build-on-comando-empleado.ts`** (modificado, ADR 161/162) — wiring: `rolPort` como costura opcional en `BuildOnComandoEmpleadoDeps`, default `deps.rolPort ?? { buscarRol: (id) => { const row = buscarRolEmpleado(db, id); return row ? (row.rol as RolEmpleado) : undefined } }` (mismo molde que `credenciales`; **`src/main.ts` no cambia** — verificado en `design.md` §7, `credenciales`/`registro`/`reporteStore` tampoco se pasan explícitamente desde `main.ts`); los 3 call sites de `resolverEscalacionReembolso` y los 3 de `resolverSolicitudInterna` agregan `rolPort` al objeto `deps` inline; 2 ramas nuevas en `manejarEscalacion` (`no_autorizado` → `registrar(...)` + mensaje "no estás autorizado... se requiere rol elevado") y 2 ramas nuevas en `manejarResolucionSolicitud` (`no_autorizado` y `autoaprobacion_prohibida`, cada una con su `registrar(...)` y mensaje distinguible), reusando el helper `registrar()` existente — **sin puerto de escritura nuevo**. Test primero: extiende `build-on-comando-empleado.test.ts` — rol base confirma `/aprobar-reembolso` ⇒ mensaje de no autorizado + fila en `registro_acciones_empleado` con `resultado: "no_autorizado"`; rol elevado + solicitud propia confirma `/aprobar-solicitud` ⇒ fila `"autoaprobacion_prohibida"`; **test de regresión explícito** (Success Criteria de `proposal.md`): `git diff` de `src/core/auth/sesion.ts` y `src/core/commands/comando-empleado.ts` vacío, y los quince comandos de solo lectura `privilegiado: true` siguen respondiendo con cualquier sesión vigente, sin nueva condición de rol. Cubre spec req. "Invariantes negativos de superficie" y "Un intento rechazado... deja fila distinguible". Comando: `npm test -- build-on-comando-empleado`. Commit: `feat(commands): conecta rolPort y agrega ramas no_autorizado/autoaprobacion_prohibida al dispatcher (autorizacion-empleado, tarea 4.2)`.

## Phase 5: Provisioning por CLI y cierre (Work Unit 5, PR5)

- [ ] 5.1 **`src/empleados.ts`** (modificado, ADR 160) — `parseArgsEmpleado` gana el flag `--rol <valor>` (valor validado contra `ROLES_EMPLEADO`), tercer modo `"asignar-rol"`, incompatible con `--rotar` (error explícito "`--rol` y `--rotar` no se combinan"); preserva la regla dura "flag desconocida ⇒ error, nunca cae al default", verificada antes de mirar nada más; `main()` gana una rama para `modo === "asignar-rol"` que **no** pide contraseña, verifica que el `empleadoId` ya tenga credencial (`undefined` ⇒ error, exit 1, mismo molde que `rotar`), y hace `upsertRolEmpleado`. Test primero: extiende `empleados.test.ts` — `--rol administrador` solo ⇒ `modo: "asignar-rol"`; `--rol` sin valor o con valor inválido ⇒ `ok:false`; `--rol` + `--rotar` juntos ⇒ `ok:false`; flag desconocida sigue rechazando primero; `empleadoId` inexistente con `--rol` ⇒ error, no crea credencial ni fila de rol huérfana. Cubre spec req. "Provisioning del rol exclusivamente por CLI" y RD-79. Comando: `npm test -- empleados`. Commit: `feat(cli): agrega modo asignar-rol a empleados.ts vía flag --rol (autorizacion-empleado, tarea 5.1)`.

- [ ] 5.2 **`docs/ARC42_Harness_Empresarial.md`** + **`README.md`** (modificados) — **TDD exception** (documentación) — arc42: diferencia autenticación (`/login`, *quién sos*) de autorización (rol, *qué podés*); cierra **R10** con nombre y evidencia (test de la tarea 2.2/3.1); documenta **R7** como deuda declarada con su condición de disparo (`vendedores` y `credenciales_empleado` sin ligadura — Aclaración 2 pto 2); nota de despliegue con el comando exacto `npm run empleados:crear -- <empleadoId> --rol administrador` y el `empleadoId` concreto del primer `administrador` resuelto por el checkpoint (ADR 156 pto 3) — **completar el `empleadoId` real en la carpeta de progreso antes de mergear la rama tracker**. README: sección breve sobre los dos roles y el comando de asignación. Comando: `npm run typecheck && npm test`. Commit: `docs: documenta autenticacion vs autorizacion, cierra R10 y declara R7 (autorizacion-empleado, tarea 5.2)`.

## Después de la tarea 5.2

- Pasa al Reviewer (`sdd-verify` + `code-review`) contra este documento, las specs y `design.md`, con atención especial a: **ADR 154 pto 5** (ausencia de fila de rol nunca autoriza — verificable en la tarea 2.1), **ADR 156** (default deny sin backfill — verificable en la tarea 1.2 contra una BD con filas previas), **ADR 159/RD-78** (orden exacto rol → autoaprobación en la tarea 3.1), y el **test de regresión de `sesion.ts`/`comando-empleado.ts`** de la tarea 4.2 (invariante más citado del Success Criteria).
- **Antes de mergear la rama tracker a `main`**: confirmar que la nota de despliegue (tarea 5.2) nombra un `empleadoId` real y ejecutar el comando de asignación en el entorno correspondiente — el sistema queda cerrado (ADR 156 pto 2) hasta que eso ocurra.
- Si aprueba: checklist de cierre de `AGENTS.md` (Reviewer aprobado, carpeta de progreso `docs/progreso/<version>-autorizacion-empleado/` con evidencia, tag). Versión y numeración a confirmar por el checkpoint (`proposal.md`, sección final).

---

**Nota de formato**: la skill `sdd-tasks` sugiere un tope de 530 palabras; se sigue el formato extenso ya establecido por `proposal.md`, los 5 `specs/` y `design.md` de este mismo change, y los precedentes `tui-canal-empleado/tasks.md` y `definicion-skills/tasks.md` — archivo objetivo, requirement de origen, ADR, comando exacto y mensaje de commit por tarea, tal como `AGENTS.md` exige para el proceso de construcción paso a paso.
