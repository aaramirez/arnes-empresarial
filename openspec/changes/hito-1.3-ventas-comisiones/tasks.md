> **Nota de proceso (hook de graphify)**: el hook de este repo exige correr `graphify query`/`explain`/`path` antes de leer código fuente. El ejecutor de esta fase corrió **sin herramienta de shell disponible** (solo `Read`/`Edit`/`Write`/`Grep`/`Glob`), igual que las cuatro fases previas de este hito (`sdd-explore`, `sdd-propose`, `sdd-spec`, `sdd-design`). Se compensó con lectura directa y completa de `proposal.md`, los 4 specs (`venta-confirmacion`, `reembolso-evaluacion`, `soporte-web-turno`, `reporte-comisiones-mensual`), `design.md` (las 2178 líneas, incluidos ADR 12-20, §3-13), `openspec/changes/hito-1.2-bot-revision-prs/tasks.md` (plantilla de formato y granularidad) y `openspec/config.yaml`. Ninguna tarea de este documento reinterpreta el diseño: cada una desglosa un archivo o contrato ya fijado por `design.md`. Se recomienda correr `graphify update .` una vez persistido este archivo, y que `sdd-apply` corra `graphify explain`/`query` antes de escribir cada módulo.

# Hito 4 — Confirmación de venta y comisiones: desglose de tareas

**Spec y diseño de origen** (no se duplican acá): [`proposal.md`](proposal.md) (ADR 7-11), [`specs/venta-confirmacion/spec.md`](specs/venta-confirmacion/spec.md), [`specs/reembolso-evaluacion/spec.md`](specs/reembolso-evaluacion/spec.md), [`specs/soporte-web-turno/spec.md`](specs/soporte-web-turno/spec.md), [`specs/reporte-comisiones-mensual/spec.md`](specs/reporte-comisiones-mensual/spec.md), [`design.md`](design.md) (ADR 12-20, §3-13).

**Entregable funcional a demostrar**: `POST /ventas` autenticado crea vendedor/caso/venta y notifica el link; el cliente confirma en la página pública y **una transacción** calcula y persiste la comisión; `POST /devolucion` auto-aprueba o escala a un humano según el umbral; `POST /soporte` es el único camino que invoca al modelo; `npm run reporte:mensual` imprime el comparativo por vendedor y los reembolsos pendientes (§12 del diseño, 20 pasos).

**Metodología**: TDD estricto (`openspec/config.yaml: strict_tdd: true`) en toda tarea con lógica de negocio (1-10, 12, 14-27). Excepciones explícitas, marcadas una por una: tarea 11 (`env.ts`, module doc de registro de nombres de variable, sin lógica nueva — molde de la tarea 6 de Hito 3), tarea 13 (`http.ts`, solo tipos estructurales — se ejercita vía `server.test.ts` de la tarea 17), tarea 28 (`main.ts`, wiring puro cubierto por la red de regresión de `build-on-venta.test.ts`/`build-on-soporte.test.ts`/`build-on-submit.test.ts`/`build-on-activity.test.ts`, molde de la tarea 22 de Hito 3), tarea 29 (`turn-logger.ts`, solo module doc — el contrato de `logTurnEvent` no cambia, molde de la tarea 23 de Hito 3), tarea 30 parte b (`package.json`, una línea de script), tarea 31 (verificación manual, sin código de producción).

**Nota de precedencia**: nombres de archivo, firmas, constantes, SQL literal, tabla de respuestas HTTP y eventos de esta lista provienen literal de `design.md` — no se reinterpretan. Las desviaciones del plan (`expires_at` — ADR 10 de la propuesta; ausencia de `createKeyedQueue()` — ADR 15/§8; alcance de R3) están fijadas por la propuesta/diseño y se implementan tal cual — no se reabren acá.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~6000-6500 (31 tareas: núcleo nuevo de 10 archivos, 2 adaptadores nuevos completos, migración+repository con 10 funciones nuevas, 2 hermanos de composition root, entrypoint CLI) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | 8 unidades de trabajo (ver tabla abajo) |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending — ver nota AGENTS.md abajo |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

**Nota para la decisión de `chain_strategy`**: igual que Hito 3, `AGENTS.md` fija una sola rama por hito (`hito/v1.3-ventas-comisiones`) con un único merge humano a `main` al cierre — eso favorece **`feature-branch-chain`** sobre `stacked-to-main`. Precisión propia de este hito: las Unidades 3 (Web), 4 (Notificaciones) y 5 (Memoria) son funcionalmente independientes entre sí — las tres dependen solo de la Unidad 1 (Núcleo), no unas de otras; un equipo que prefiera paralelizar puede ramificarlas directamente desde la Unidad 1 en vez de encadenarlas entre sí. La Unidad 6 (composition root) depende de que 3, 4 y 5 existan. La Unidad 7 (CLI) depende solo de 1 y 5, y puede correr en paralelo a la 6. El orden secuencial de abajo es la recomendación por defecto para un solo chain lineal simple. El humano confirma `chain_strategy` antes de `sdd-apply`.

### Suggested Work Units

| Unit | Goal | Tasks | Est. lines | Base branch |
|------|------|-------|-----------|-------------|
| 1 | Núcleo de ventas completo (contrato, config, funciones puras, casos de uso, reporte, prompt de soporte) + `env.ts` doc | 1–11 | ~2200 | `hito/v1.3-ventas-comisiones` |
| 2 | *(fusionada en Unit 1 — `env.ts` es module doc de una línea)* | — | — | — |
| 3 | Adaptador Web completo | 12–18 | ~1900 | Unit 1 |
| 4 | Adaptador de Notificaciones completo | 19–21 | ~650 | Unit 1 (o Unit 3 si se encadena lineal) |
| 5 | Migración + repository (CAS + lecturas de agregación) | 22–25 | ~900 | Unit 1 (o Unit 4 si se encadena lineal) |
| 6 | Composition root (`build-on-venta.ts` + `build-on-soporte.ts` + `main.ts` + doc de `turn-logger.ts`) | 26–29 | ~800 | Unit 5 (necesita 3, 4 y 5 ya mergeadas) |
| 7 | Entrypoint CLI del reporte mensual | 30 | ~150 | Unit 5 (necesita `reporte.ts` de Unit 1 y las lecturas de Unit 5) |
| 8 | Verificación manual end-to-end | 31 | ~0 (evidencia, no código) | Unit 6 y Unit 7 |

## Tareas

### Núcleo — contrato y configuración (tareas 1-2)

1. **`src/core/ventas/ventas-contract.ts`** (nuevo, §3.1). Test primero: `ventas-contract.test.ts` asserts `VENTA_ESTADOS` tiene exactamente los 5 valores canónicos en el orden del diseño (incluido `reembolso_pendiente`, ADR 11), `CASO_TIPO_VENTA`/`CASO_TIPO_SOPORTE`/`CASO_ESTADO_PENDIENTE_APROBACION_HUMANA` son los strings exactos, y que el archivo **no importa nada** (mismo test que `knowledge-contract.test.ts`/`activity-contract.test.ts`). Hecho: constantes + `Venta`/`Comision`/`VentaPublica`, `VentaStorePort` (6 métodos), `VentaNotifierPort` (`notificarLinkConfirmacion`), tipos de input/output exportados, test verde. Cubre spec `venta-confirmacion` req. 4-5 y `reembolso-evaluacion` req. 3-4 (vocabulario compartido). Commit: `feat(core): agrega contrato de ventas con VentaStorePort y VentaNotifierPort (Hito 4, tarea 1)`.

2. **`src/core/ventas/ventas-config.ts`** (nuevo, ADR 17, §3.2). Test primero: `ventas-config.test.ts` — defaults con env vacío (`comisionPorcentaje=0.1`, `reembolsoUmbral=500`, `tokenTtlHoras=72`); `COMISION_PORCENTAJE` en `0`, `1.5`, `"abc"` → `ok:false`; `1` exacto → válido; **dos variables inválidas devuelven DOS errores** en el mismo array; `VENTA_TOKEN_TTL_HORAS=0` → válido (`undefined` de `expires_at`), `=-1` → error; la función **no** importa `env.js` ni tiene default `= process.env`. Cubre spec `venta-confirmacion` req. 6 ("Porcentaje de comisión inválido"). Commit: `feat(core): agrega resolveVentasConfig puro con validacion de reglas de negocio (Hito 4, tarea 2)`.

### Núcleo — funciones puras del dominio (tareas 3-5)

3. **`src/core/ventas/comision.ts`** (nuevo, ADR 16, §3.3). Test primero: `comision.test.ts` — `calcularComision(1000, 0.1) === 100` **exacto** (no `100.00000000000001`); redondeo del `.005` hacia arriba; monto con 3+ decimales de entrada; `porcentaje = 1` → comisión = monto; `periodoDeConfirmacion` sobre un ISO de fin de mes en UTC **no** cambia de mes por zona horaria (caso borde 31/01 22:00 UTC). Cubre spec `venta-confirmacion` req. 5 (escenario "Confirmación exitosa calcula y persiste la comisión"). Commit: `feat(core): agrega calcularComision y periodoDeConfirmacion con redondeo a centavos (Hito 4, tarea 3)`.

4. **`src/core/ventas/reembolso.ts`** (nuevo, §3.3). Test primero: `reembolso.test.ts` — `monto < umbral → auto_aprobado`; **`monto === umbral → escalado`** (la celda del borde, `>=` no `>`); `monto > umbral → escalado`. Cubre spec `reembolso-evaluacion` req. "Auto-aprobación por debajo del umbral" y "Escalación humana por encima". Commit: `feat(core): agrega evaluarReembolso con borde en monto>=umbral (Hito 4, tarea 4)`.

5. **`src/core/ventas/token-confirmacion.ts`** (nuevo, §3.3). Test primero: `token-confirmacion.test.ts` — `undefined → inexistente`; estado ≠ `pendiente_confirmacion` → `estado_invalido` (para los 4 otros estados); `expiresAt` en el pasado → `vencido`; `expiresAt === ahora` exacto → `vencido`; `expiresAt` ausente → **válido**; **`Date.now()` nunca aparece en el módulo** (`ahora` siempre inyectado); `calcularExpiresAt(x, 0) === undefined`; `calcularExpiresAt(createdAt, 72)` suma 72h en ISO-8601 UTC. Cubre spec `venta-confirmacion` req. "Validación del token..." (escenario "Token vencido rechazado sin efecto"). Commit: `feat(core): agrega validarTokenConfirmacion con ahora inyectado y calcularExpiresAt (Hito 4, tarea 5)`.

### Núcleo — casos de uso / orquestadores (tareas 6-8)

6. **`src/core/ventas/registrar-venta.ts`** (nuevo, §3.4). Test primero: `registrar-venta.test.ts` con dobles de `VentaStorePort`/`VentaNotifierPort` — store que lanza → **propaga y el notifier NO se llama**; notifier que devuelve `enviado:false` → resultado con `notificado:false`, **sin excepción**; el link se arma como `{baseUrlPublica}/confirmar/{token}`; `newToken`/`newId`/`now` inyectados → salida determinista; `expiresAt` presente con TTL>0 y ausente con TTL=0; el `await notifier...` ocurre **después** de que `store.crearVentaConCaso` resolvió. Cubre spec `venta-confirmacion` req. 2-3 (escenarios "Alta válida...", "Falla el proveedor de email", "Sin `EMAIL_API_KEY`"). Commit: `feat(core): agrega registrarVenta que orquesta alta, transaccion y notificacion best-effort (Hito 4, tarea 6)`.

7. **`src/core/ventas/confirmar-venta.ts`** (nuevo, §3.4). Test primero: `confirmar-venta.test.ts` — token inválido → `no_aplicable` y **el store nunca recibe un write**; confirmar → comisión con monto y período esperados; rechazar → **`confirmarVentaConComision` nunca se llama**; store que devuelve `undefined` (carrera/CAS no matcheó) → `no_aplicable`, sin excepción; **el mismo `ahora` llega a la validación, `confirmed_at` y `periodo`**; función **SÍNCRONA** (no devuelve promesa, per §3.4 nota). Cubre spec `venta-confirmacion` req. 4-5 (escenarios "Rechazo no genera comisión", "Reintento sobre token ya procesado"). Commit: `feat(core): agrega resolverDecisionVenta sincrona con CAS delegado al store (Hito 4, tarea 7)`.

8. **`src/core/ventas/procesar-devolucion.ts`** (nuevo, §3.4, ADR 19). Test primero: `procesar-devolucion.test.ts` — venta inexistente → `no_aplicable`; venta en cada uno de los 4 estados ≠ `confirmada` → `no_aplicable` sin efecto; bajo umbral → `store.aprobarReembolso`; sobre/igual → `store.escalarReembolso` con el `casoId` de la venta; **ningún camino llama a nada que toque `comisiones`**; **`expires_at` vencido NO bloquea la devolución** (ADR 19 punto 2, se pasa `undefined`/venta directo sin pasar por `validarTokenConfirmacion`). Cubre spec `reembolso-evaluacion` req. "Devolución requiere una venta confirmada", "Evaluación no llama al modelo". Commit: `feat(core): agrega procesarDevolucion con token de confirmacion como credencial (Hito 4, tarea 8)`.

### Núcleo — reporte y prompt de soporte (tareas 9-10)

9. **`src/core/ventas/reporte.ts`** (nuevo, §3.5). Test primero: `reporte.test.ts` — dos vendedores en el mismo período agrupan por separado; comisiones de otro período se filtran; orden `totalComisionado` DESC con desempate `vendedorId` ASC; `totalComisionado` redondeado (suma de floats, mismo criterio ADR 16); `reembolsosPendientes` pasa **sin filtrar por período**; período sin comisiones → línea explícita "sin comisiones en el periodo"; `formatearReporteMensual` afirmado como string completo (determinismo) incluyendo la nota de que la escalación se resuelve fuera de banda (ADR 11 punto 5). Cubre spec `reporte-comisiones-mensual` req. "Agregación pura...", "Listado de reembolsos pendientes de aprobación". Commit: `feat(core): agrega agruparReporteMensual y formatearReporteMensual puras (Hito 4, tarea 9)`.

10. **`src/core/ventas/soporte-prompt.ts`** (nuevo, §3.6). Test primero: `soporte-prompt.test.ts` — pura y determinista; trunca la consulta a `MAX_SOPORTE_CONSULTA_CHARS` con marca; **incluye la limitación literal** de que el agente no puede confirmar/cancelar/reembolsar nada; **no incluye `clienteId`**. Cubre spec `soporte-web-turno` req. "Reuso de `CONVERSATIONAL_AGENT` con prompt sintético". Commit: `feat(core): agrega buildSoportePrompt con limitacion declarada de acciones (Hito 4, tarea 10)`.

### Configuración compartida (tarea 11)

11. **`src/core/config/env.ts`** (modificado) — **TDD exception**: se registran en el módulo doc los nombres `WEB_PORT`, `WEB_PUBLIC_URL`, `VENTAS_API_TOKEN`, `WEB_MAX_BODY_BYTES`, `COMISION_PORCENTAJE`, `REEMBOLSO_UMBRAL`, `VENTA_TOKEN_TTL_HORAS`, `EMAIL_API_KEY`, `EMAIL_FROM`, `EMAIL_API_URL`, `EMAIL_TIMEOUT_MS`. Sin lógica de resolución nueva acá (vive en `resolveWebConfig`/`resolveNotificacionesConfig`/`resolveVentasConfig`, que leen `process.env` directamente — mismo criterio de la tarea 6 de Hito 3). Hecho: el archivo sigue siendo el primer import de `main.ts`; `npm run typecheck` verde; tests existentes de `env.ts` sin modificar. Commit: `chore(core): registra variables WEB_*/VENTAS_*/COMISION_*/REEMBOLSO_*/EMAIL_* en env.ts (Hito 4, tarea 11)`.

### Adaptador Web — config y tipos (tareas 12-13)

12. **`src/adapters/web/config.ts`** (nuevo, §4.1). Test primero: `config.test.ts` — defaults con env vacío (`port=0`, `publicUrl=DEFAULT_WEB_PUBLIC_URL`, `ventasApiToken=""`, `maxBodyBytes=DEFAULT_WEB_MAX_BODY_BYTES`); `WEB_PORT` ausente/`"0"`/`"abc"`/negativo → `0` (SIN caer a un default de puerto, a diferencia de `WEBHOOK_PORT` — el gate ES el puerto); `publicUrl` con `/` final se normaliza; `isWebEnabled(config) === config.port > 0`. Cubre spec `venta-confirmacion` req. "Listener web opt-in por configuración". Commit: `feat(adapters/web): agrega resolveWebConfig con puerto como unico gate (Hito 4, tarea 12)`.

13. **`src/adapters/web/http.ts`** (nuevo, ADR 13) — **TDD exception**: recortes estructurales de `node:http` (`WebRequest`, `WebResponse`, `WebHttpServerLike`, `CreateWebServerFn`), duplicados a propósito de `webhooks/server.ts:30-55` (sin `import` cruzado entre adaptadores). Solo tipos, sin lógica ejecutable — se ejercita indirectamente vía `server.test.ts` (tarea 17). Hecho: `npm run typecheck` verde; module doc cita ADR 13 y el disparador de reconsideración (tercer adaptador HTTP entrante). Commit: `feat(adapters/web): agrega tipos estructurales de http duplicados de webhooks (Hito 4, tarea 13)`.

### Adaptador Web — entrada no confiable (tareas 14-15)

14. **`src/adapters/web/body.ts`** (nuevo, §4.3). Test primero: `body.test.ts` — acumulación con tope, corte en `maxBodyBytes` **antes** de guardar el chunk (resultado discriminado `{ok:false, motivo:"tamano"}`, sin escribir la respuesta); `req.on("error")` → `{ok:false, motivo:"error-transporte"}`; `parseJsonBody` con JSON roto → `{ok:false}`; `parseFormBody` decodifica `application/x-www-form-urlencoded`. Cubre spec `venta-confirmacion` (guarda de body previa a auth/parse, §4.3). Commit: `feat(adapters/web): agrega leerBody con tope de tamano y parseo JSON/form (Hito 4, tarea 14)`.

15. **`src/adapters/web/payloads.ts`** (nuevo, §4.3). Test primero: `payloads.test.ts` — `parseAltaVentaPayload`: los 6 campos requeridos, uno ausente por vez → `motivo` distinto; `monto` como string → **rechazado, no coercido**; `monto` `0`/negativo/`NaN`/`Infinity` → rechazado; strings sobre el tope (256) → rechazados; `clienteEmail` sin regex de validación (solo no vacío); `parseDevolucionPayload`: `motivo` opcional recortado a 500 chars; `parseSoportePayload`: `consulta` no vacía; `parseDecisionForm`: `decision` distinta de `confirmar`/`rechazar` → rechazada. Cubre spec `venta-confirmacion` req. 2 y `reembolso-evaluacion` (payload de devolución). Commit: `feat(adapters/web): agrega parsers de payload con type guards a mano (Hito 4, tarea 15)`.

### Adaptador Web — render (tarea 16)

16. **`src/adapters/web/render.ts`** (nuevo, ADR 20). Test primero: `render.test.ts` — **`plan_nuevo` con `<script>` sale escapado** (XSS almacenado); el `token` va con `encodeURIComponent` en el `action`, no con `escapeHtml`; `renderConfirmacionHtml` **no contiene** `caso_id` ni `cliente_id` en el HTML de salida (recibe solo `VentaPublica`); `renderLinkInvalidoHtml()` es idéntico llamado N veces y no toma parámetros; `renderResultadoHtml` no expone montos de comisión. Cubre spec `venta-confirmacion` (página pública) y R6 (respuesta indistinguible). Commit: `feat(adapters/web): agrega render.ts con escapeHtml obligatorio y pagina generica sin parametros (Hito 4, tarea 16)`.

### Adaptador Web — server y fachada (tareas 17-18)

17. **`src/adapters/web/server.ts`** (nuevo, §4.4 — el corazón del adaptador). Test primero: `server.test.ts` con `req`/`res` dobles planos, **sin abrir ningún puerto real** — orden de checks `método+ruta → tope de body → auth (solo /ventas) → parseo → handler`, sin reordenar; `404` en ruta desconocida; `401` sin token y **con `VENTAS_API_TOKEN` vacío**, sin llamar al handler; **header de largo distinto → `401`, NO `RangeError`** (chequeo de longitud antes de `timingSafeEqual`, misma trampa de `signature.ts` de Hito 3); `413` + `destroy()` al pasar el tope, **sin llamar al handler**; `400` con JSON roto; `201` con payload válido; **token vencido, inexistente y ya-confirmado producen respuestas byte a byte idénticas** (las 4 filas en negrita de la tabla de respuestas — requisito de indistinguibilidad, R6); extracción del token con `try/catch` sobre `decodeURIComponent` cayendo en la misma página genérica; `/soporte` responde `504` si el handler tarda más que `SOPORTE_TIMEOUT_MS` (con reloj falso), `502` si el handler rechaza; `close()` espera al turno de soporte en vuelo vía `Promise.allSettled` y resuelve igual al vencer `WEB_CLOSE_TIMEOUT_MS`, logueando `web-cierre-con-turnos-en-vuelo`; headers `Content-Type`/`Cache-Control: no-store`/`Referrer-Policy: no-referrer`/`X-Request-Id` en toda respuesta HTML. Cubre los 24 escenarios de §7.3 en su capa de transporte. Commit: `feat(adapters/web): agrega listener HTTP con tabla de respuestas exhaustiva y drenaje de soporte (Hito 4, tarea 17)`.

18. **`src/adapters/web/index.ts`** (nuevo, §4.6). Test primero: `index.test.ts` — sin `WEB_PORT` → devuelve `undefined`, `createServer` **nunca se llama**, loguea `web-deshabilitado`; con `WEB_PORT` → `WebAdapter` con el `port` configurado; `listen` que rechaza (`EADDRINUSE` simulado) se propaga para que el composition root decida. Cubre spec `venta-confirmacion` req. "Listener web opt-in por configuración". Commit: `feat(adapters/web): agrega fachada startWebServer opt-in por WEB_PORT (Hito 4, tarea 18)`.

### Adaptador de Notificaciones (tareas 19-21)

19. **`src/adapters/notificaciones/config.ts`** (nuevo, §5.1). Test primero: `config.test.ts` — defaults con env vacío (`apiKey=""`, `from=DEFAULT_EMAIL_FROM`, `apiUrl=DEFAULT_EMAIL_API_URL`, `requestTimeoutMs=DEFAULT_EMAIL_TIMEOUT_MS`); overrides por cada var; numérico inválido/≤0 cae a default; `isNotificacionesEnabled` refleja `apiKey.trim() !== ""`. Commit: `feat(adapters/notificaciones): agrega resolveNotificacionesConfig con gate por apiKey (Hito 4, tarea 19)`.

20. **`src/adapters/notificaciones/email-client.ts`** (nuevo, §5.2). Test primero: `email-client.test.ts` con `FetchFn` inyectado — URL/método/headers exactos (`Authorization: Bearer <apiKey>`, `Content-Type: application/json`); cuerpo `{from, to, subject, html}`; **la API key nunca aparece en el mensaje de error**; `!response.ok` → `EmailApiError("http", status)` con el cuerpo del proveedor recortado; `AbortSignal.timeout` → `reason:"timeout"`; throw de red → `reason:"network"`; sin reintentos ni backoff; no parsea la respuesta. Commit: `feat(adapters/notificaciones): agrega enviarEmail sobre fetch inyectable copiado de github-client (Hito 4, tarea 20)`.

21. **`src/adapters/notificaciones/index.ts`** (nuevo, §5.3). Test primero: `index.test.ts` — sin `EMAIL_API_KEY` → `createNoopNotificador`, `fetchFn` **nunca** se llama, loguea `email-omitido` **con el link completo**, devuelve `{enviado:false, motivo:"sin-api-key"}`; `fetchFn` que rechaza en `createNotificadorAdapter` → **resuelve** con `enviado:false`, **nunca lanza**; `clienteEmail` vacío → `{enviado:false, motivo:"sin-destinatario"}` sin llamar a `fetchFn`; **`clienteEmail` nunca aparece en ningún log** (PII). Cubre spec `venta-confirmacion` req. 3 (escenarios "Falla el proveedor de email", "Sin `EMAIL_API_KEY`"). Commit: `feat(adapters/notificaciones): agrega createNotificadorAdapter que implementa VentaNotifierPort (Hito 4, tarea 21)`.

### Memoria — migración (tarea 22)

22. **`src/adapters/memory/migrations/0004_vendedores_ventas_comisiones.ts`** (nuevo) + **append** a `migrations/index.ts` (nunca reordenar entradas ya commiteadas). SQL literal del plan (§6.1) + **una** desviación: columna `expires_at TEXT` nullable en `ventas` (ADR 10 de la propuesta — **requiere aprobación explícita del checkpoint**). Tablas `vendedores`, `ventas` (FK a `vendedores`/`casos`, `estado` TEXT abierto sin CHECK, `token_confirmacion UNIQUE`), `comisiones` (FK a `ventas`/`vendedores`) + `idx_ventas_vendedor` + `idx_comisiones_periodo`. Test primero: agrega caso al test de migraciones existente — tras `runMigrations`, las tres tablas y los dos índices existen; correr las migraciones dos veces no falla (`IF NOT EXISTS`); insertar una `venta` con `vendedor_id`/`caso_id` inexistentes falla por FK; insertar dos `ventas` con el mismo `token_confirmacion` falla por `UNIQUE`. Commit: `feat(adapters/memory): agrega migracion 0004 con vendedores/ventas/comisiones y expires_at (Hito 4, tarea 22)`.

### Memoria — repository: alta y lectura (tarea 23)

23. **`src/adapters/memory/repository.ts`** — `upsertVendedor`, `createVentaConCaso`, `findVentaByToken`, `getVentaById` + errores `VentaNotFoundError`/`VentaAlreadyExistsError`/`VentaInvalidReferenceError`/`VentaTokenDuplicadoError`. Test primero: agrega casos a `repository.test.ts` — `upsertVendedor` idempotente (`ON CONFLICT DO UPDATE`); `createVentaConCaso` **atómica** vía `db.transaction(fn)()`: si el `INSERT` de `ventas` falla, **no queda `vendedor` nuevo ni `caso` huérfano (molde de `createCasoConActividad`); `planAnterior`/`expiresAt` ausentes bindean `null` explícito; `findVentaByToken` indexada por el `UNIQUE`, `undefined` si no existe; FK rota → `VentaInvalidReferenceError`. Cubre spec `venta-confirmacion` req. 2 (escenario "Alta válida crea vendedor, caso y venta"). Commit: `feat(adapters/memory): agrega upsertVendedor y createVentaConCaso transaccional (Hito 4, tarea 23)`.

### Memoria — repository: compare-and-swap (tarea 24)

24. **`src/adapters/memory/repository.ts`** — `confirmarVentaConComision`, `rechazarVenta`, `aprobarReembolso`, `escalarReembolso` (ADR 15, **el corazón del hito**). Test primero: agrega casos a `repository.test.ts` — `confirmarVentaConComision` con el `WHERE estado='pendiente_confirmacion' AND (expires_at IS NULL OR expires_at > @ahora)` en el `UPDATE ... RETURNING`; **dos llamadas seguidas sobre el mismo token: la segunda devuelve `undefined` y `SELECT count(*) FROM comisiones` es 1** (cierra R4 sin `createKeyedQueue()`); token vencido → `undefined` sin escribir nada; `rechazarVenta` nunca toca `comisiones`; `escalarReembolso` deja venta y `caso` coherentes en la MISMA transacción (`updateCaso` reusado sin cambios) y **si la venta no está `confirmada` el `caso` NO se toca**; `aprobarReembolso`/`escalarReembolso` **no modifican `comisiones`** (spec "Comisión ya pagada permanece..."). Cubre spec `venta-confirmacion` req. 5 (escenarios "Confirmación exitosa...", "Reintento sobre token ya procesado") y `reembolso-evaluacion` req. completo. Commit: `feat(adapters/memory): agrega CAS de confirmacion/rechazo/reembolso con UPDATE...RETURNING (Hito 4, tarea 24)`.

### Memoria — repository: lecturas de agregación (tarea 25)

25. **`src/adapters/memory/repository.ts`** — `listComisionesPorPeriodo`, `listVentasEnReembolsoPendiente` (ADR 9 de la propuesta). Test primero: agrega casos a `repository.test.ts` — `listComisionesPorPeriodo` hace `JOIN` con `ventas`/`vendedores`, filtra por `periodo`, incluye `ventas.estado` actual; `listVentasEnReembolsoPendiente` filtra `estado='reembolso_pendiente'` **sin** filtro de período. Ningún `GROUP BY` en el SQL — la agregación vive en `agruparReporteMensual` (tarea 9), pura. Cubre spec `reporte-comisiones-mensual` req. "Agregación pura...", "Listado de reembolsos pendientes". Commit: `feat(adapters/memory): agrega lecturas de agregacion para el reporte mensual (Hito 4, tarea 25)`.

### Composition root (tareas 26-29)

26. **`src/build-on-venta.ts`** (nuevo — módulo hermano DETERMINISTA, ADR 7 punto 4, ADR 12, §6.3). Exporta `createVentaStore(db): VentaStorePort` y `buildOnVenta(deps): VentaHandlers` (`onAltaVenta`/`onConsultaVenta`/`onDecisionVenta`/`onDevolucion`). Test primero: `build-on-venta.test.ts` — **test estructural: el módulo NO importa el SDK ni `handleTurn`** (assert sobre el código fuente o sobre que ningún doble de `queryFn` se invoca en ningún camino); `onConsultaVenta` de una venta vencida → `undefined` (aplica `validarTokenConfirmacion` antes de proyectar a `VentaPublica`, para que el `GET` no sea un oráculo que el `POST` no es); los 4 handlers propagan el error del store (sin `try/catch` que trague — a diferencia de `buildOnActivity`); **dos `onDecisionVenta` concurrentes sobre el mismo token → exactamente una comisión** (test de cierre de R4/§8, sobre `:memory:` real, sin `await` entre las dos invocaciones). Cubre §7.3 escenarios 2, 7, 9, 10, 12-15. Commit: `feat(root): agrega buildOnVenta que conecta el adaptador web con el nucleo sin SDK (Hito 4, tarea 26)`.

27. **`src/build-on-soporte.ts`** (nuevo — módulo hermano CON `handleTurn`, ADR 7 punto 2, ADR 12, §6.4). Exporta `buildOnSoporte(deps): (input) => Promise<SoporteResult>`. Test primero: `build-on-soporte.test.ts` con dobles de `handleTurn`/`memory`/`hooks`/`agents`/`createKnowledge` — crea el `caso` con `tipo:"soporte"` **antes** de llamar a `handleTurn` (orden afirmado); `createKnowledge` se invoca **una vez, con el `casoId` del turno recién creado**; `TurnFailedError` **propaga** (a diferencia de `buildOnActivity`, que traga); reusa `CONVERSATIONAL_AGENT` sin definir un segundo `AgentDefinition`, sin tocar `definitions.ts`; el spread condicional de `logDeps` bajo `exactOptionalPropertyTypes`. Cubre spec `soporte-web-turno` completo (los 3 requisitos). Commit: `feat(root): agrega buildOnSoporte que envuelve handleTurn con caso de soporte previo (Hito 4, tarea 27)`.

28. **`src/main.ts`** — los 4 cambios exactos de §6.5, ninguno toca el camino de la TUI ni el de los webhooks: **(a)** `resolveVentasConfig(process.env)` dentro del `try` existente de `startHarness()`, `throw HarnessBootstrapError` con todos los errores si `!ok` — el proceso muere **antes** de que exista `startWebServer`; **(b)** wiring de la tercera fuente: `notifier = createNotificadorAdapter(...)`, `webConfig = resolveWebConfig()`, `ventaHandlers = buildOnVenta({...})`, `onSoporte = buildOnSoporte({...})` (misma fábrica `createKnowledge` que la TUI y los webhooks ya comparten — R1 de Hito 3 no reintroducido), `web = await startWebServer({...ventaHandlers, onSoporte, logEvent})` con su propio `try/catch` que loguea `web-arranque-fallido` y continúa con `web = undefined`; **(c)** el `finally` gana `await web.close()` (si `web !== undefined`, con su propio `try/catch`) **antes** del cierre de webhooks y de `db.close()` (orden: web → webhooks → db); **(d)** `import "./core/config/env.js"` sigue siendo el primer import. Test primero: dado que `main.ts` no es importable en test (corre `bootstrapHarness()`/top-level `await`, igual que Hitos 1-3), la cobertura de lógica ya está en `build-on-venta.test.ts` (tarea 26) y `build-on-soporte.test.ts` (tarea 27, sin tocar) — y `build-on-submit.test.ts`/`build-on-activity.test.ts` se corren **sin modificar** como red de regresión: si el wiring de `main.ts` rompiera el camino de la TUI o de los webhooks, esos archivos lo detectan. Hecho: `npm run typecheck` verde; `npm test` sin regresiones. Commit: `feat(root): conecta tercera fuente de eventos (web+notificaciones) y cierre ordenado en main.ts (Hito 4, tarea 28)`.

29. **`src/core/logging/turn-logger.ts`** — **TDD exception**: solo se actualiza el module doc listando los eventos nuevos de §9.2 (`web-escuchando`, `venta-creada`, `venta-confirmada`, `comision-calculada`, `reembolso-aprobado`, `reembolso-escalado`, `soporte-caso-creado`, `email-enviado`/`email-omitido`, etc.) y la nota de correlación por `requestId` (§9.1, análogo del `deliveryId` de Hito 3). El contrato de `logTurnEvent(correlationId, event, fields, deps)` **no cambia** — misma corrección que la propuesta ya documentó para su propia tabla (*Modified* sin código). Hecho: `logTurnEvent` compila y sus tests existentes pasan sin tocarlos. Commit: `docs(core): documenta eventos de ventas/notificaciones/soporte en turn-logger (Hito 4, tarea 29)`.

### Entrypoint CLI del reporte mensual (tarea 30)

30. **`src/reporte-mensual.ts`** (nuevo, §6.6, ADR 9 de la propuesta) + **`package.json`** gana la línea `"reporte:mensual": "tsx src/reporte-mensual.ts"` — **TDD exception para la línea de `package.json`** (metadato, sin lógica, molde de la tarea 7 de Hito 3). `reporte-mensual.ts` es solo I/O y wiring: `--periodo YYYY-MM` de `process.argv` o el mes corriente (UTC), `openDatabase("data/harness.db")`, `listComisionesPorPeriodo` + `listVentasEnReembolsoPendiente` (tarea 25), `agruparReporteMensual` + `formatearReporteMensual` (tarea 9, ambas puras y ya testeadas), `process.stdout.write`, `db.close()` en `finally`. Test: si el parseo de `--periodo` se extrae a una función pura, esa función se testea (`reporte-mensual.test.ts` — formato inválido → mensaje de uso); el `main` de I/O no se testea, mismo criterio que `main.ts`. La cobertura real de la lógica ya está en `reporte.test.ts` (tarea 9) y en las lecturas de la tarea 25. Cubre spec `reporte-comisiones-mensual` req. "Comando bajo demanda, sin scheduler ni registro", "Ejecutar o no ejecutar el comando no afecta el resto del sistema". Commit: `feat(root): agrega entrypoint CLI reporte-mensual con script npm (Hito 4, tarea 30)`.

### Verificación manual (tarea 31)

31. **Verificación manual end-to-end** (sin código de producción, cierre del hito — equivalente a la tarea 24 de Hito 3). Cubre el §12 completo del diseño:
    - **Preparación** (§12.1): Node 20+, cuenta y API key de un proveedor de email transaccional (Resend — **el checkpoint humano debe confirmar que hay uno disponible**, R7), túnel genérico (`cloudflared tunnel` o `ngrok`, ninguno dependencia del proyecto ni de CI) para `WEB_PUBLIC_URL`, `.env` completo (`WEB_PORT`, `VENTAS_API_TOKEN`, `COMISION_PORCENTAJE`, `REEMBOLSO_UMBRAL`, `VENTA_TOKEN_TTL_HORAS`, `EMAIL_API_KEY`, `EMAIL_FROM`), un cliente HTTP para simular al vendedor.
    - **Guion de 20 pasos** (§12.2): (1) `npm run dev` sin `WEB_PORT` → TUI/webhooks normales, sin puerto abierto; (2) `COMISION_PORCENTAJE=1.5` → el proceso no arranca; (3) `.env` completo → `web-escuchando`; (4) `POST /ventas` sin `Authorization` → `401`, cero filas; (5) alta válida → `201`, filas nuevas, email real, cero eventos de modelo; (6) abrir el link → HTML sin `caso_id`/`cliente_id`; (7) confirmar → `confirmada` + comisión `monto×0.1` en el período de `confirmed_at`; (8) reenviar el formulario (doble click) → página genérica, `comisiones` sigue en 1; (9) token con `expires_at` vencido → página **byte a byte idéntica** al paso 8; (10) `POST /devolucion` bajo umbral → `reembolsada`, `comisiones` intacta; (11) venta de `monto=1000`, confirmar, devolución → `reembolso_pendiente` + `caso.pendiente_aprobacion_humana`; (12) buscar endpoint de aprobación → no existe ninguno (R3); (13) `POST /soporte` → respuesta del agente en el cuerpo, `caso` tipo `soporte`; (14) escribir en la TUI durante el turno de soporte → dos `casoId` sin citas cruzadas; (15) `npm run reporte:mensual` → comparativo + reembolsos pendientes; (16) reporte con período sin comisiones → línea explícita, sección de reembolsos igual visible; (17) `EMAIL_API_KEY` vacía → `201` con `notificado:false`, link en el log; (18) proveedor caído → `201`, venta persistida, `email-fallido`; (19) Ctrl+C durante un turno de soporte → drenaje ~5s, sin `SQLITE_MISUSE`; (20) `npm test` y `npm run typecheck` en verde.
    - **Evidencia** para `docs/progreso/v1.3-ventas-comisiones/` (§12.3): capturas de las páginas de confirmación/resultado, el email recibido, salida de `reporte:mensual`, extracto de `data/harness.log` (grep por `requestId` y su `casoId` puente), `SELECT` de `vendedores`/`ventas`/`comisiones` antes/después de los pasos 7, 8 y 11, `npm test`/`npm run typecheck` en verde, y **el registro explícito de las desviaciones aprobadas en el checkpoint**: `expires_at` (ADR 10 de la propuesta), la ausencia de la cola por `vendedor_id`/`venta_id` (§8, ADR 15), y el alcance de R3.
    - Commit: no aplica (evidencia va a `docs/progreso/v1.3-ventas-comisiones/` al cierre, según `AGENTS.md`).

## Después de la tarea 31

- Pasa al Reviewer (`sdd-verify` + `code-review`) contra este documento, los 4 specs y `design.md`, con especial atención al ADR 15 (CAS sin cola), R3 (escalación sin cierre de producto) y R6 (respuestas indistinguibles).
- Si aprueba: checklist de cierre de hito en `AGENTS.md`, tag `v1.3.0`, carpeta `docs/progreso/v1.3-ventas-comisiones/`, copia de los ADR 12-20 a `docs/ARC42_Harness_Empresarial.md` junto con los ADR 7-11 de la propuesta.
- Si el Reviewer objeta el alcance de R3 (escalación sin camino de cierre) o el ADR 10 (`expires_at` fuera del SQL literal del plan), la corrección es **reabrir el checkpoint humano sobre la propuesta**, no improvisar un endpoint de aprobación o revertir la columna en `sdd-apply` — ambas son decisiones ya fijadas y documentadas en `proposal.md`.
