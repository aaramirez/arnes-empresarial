> **Nota de proceso**: se corrió `graphify query "cliente A2A dispatch-delegation-a2a a2a-contract delegaciones_a2a ventas-config registrar-venta"` (`graphify-out/graph.json` existe, 545 nodos encontrados) antes de leer cualquier archivo, confirmando el vecindario real de módulos (`ventas-contract.ts`, `dispatch-delegation.ts`, `registrar-venta.ts`, `migrations/index.ts`, `build-on-venta.ts`, `main.ts`) antes de abrirlos. Se leyó completo: `design.md` (1227 líneas, ADR 76-84, §0-15), los 4 `specs/` (`cliente-a2a-jsonrpc`, `delegacion-a2a-saliente` nuevos; `despacho-delegacion` delta MODIFIED, `venta-confirmacion` delta ADDED) y el bloque de riesgos R1-R13 de `proposal.md`. Molde de formato: `openspec/changes/hito-2.1-escritura-delegada/tasks.md` completo (203 líneas) y el cierre de `openspec/changes/hito-2.0-delegacion-subagentes/tasks.md` para el patrón de numeración de "Hito" en los commits.
>
> **Verificación de esta fase, no de memoria**: `git log --oneline --all | grep -i "Hito "` confirma que `hito-2.0-delegacion-subagentes` usa el literal `Hito 5` y `hito-2.1-escritura-delegada` usa `Hito 5.1` en sus 47 commits reales. Para este change el literal correcto es **`Hito 6`** — siguiente en la secuencia interna del repo y coincide además con el número real del Plan de Implementación ("Hito 6: Comunicación A2A saliente", citado también por `design.md` línea 951 y 5.5). Sin ambigüedad esta vez.
>
> **Corrección menor a `design.md` §0, verificada esta sesión, sin efecto sobre el diseño**: el techo real de migraciones en `main` hoy es `0008_solicitudes_internas` (`git show main:src/adapters/memory/migrations/index.ts`), no `0006` como estimó `design.md`. No cambia ninguna conclusión: la rama `hito/v2.1-escritura-delegada` agrega exactamente **una** migración (`0009_propuestas_cambio`) sobre el techo de `main`, sea cual sea — verificado comparando `migrations/index.ts` de `main` contra el de esa rama, que difieren en una sola entrada. El resultado de `design.md` §7.1 (`NNNN = 0010` tras el merge) sigue siendo correcto.

> **★ ENMIENDA POSTERIOR — ADR 85 (productor real para `"kpi-incidente"`, cierra RD-26).** El checkpoint humano respondió `design.md` §15 punto 2 con un **sí**: se agrega el comando TUI privilegiado `/consultar-kpi <consulta>`, **síncrono y bloqueante**, primer llamador de producción del mecanismo central del ADR 74. Cambios de este documento: **PR5 nuevo (tareas 19-21)**; las dos tareas de documentación/verificación manual **se renumeran 19→22 y 20→23** y pasan a **Work Unit 6** (nada estaba aplicado — las 20 casillas seguían sin marcar, así que la renumeración no invalida ningún commit existente); **Review Workload Forecast recalculado** a ~1.975 líneas; ítem de RD-26 marcado **resuelto**. **Verificado esta sesión contra el código real** (`src/core/commands/comando-empleado.ts` 415 líneas y `src/build-on-comando-empleado.ts` 1313 líneas, leídos completos, no de memoria): `build-on-comando-empleado.ts` **no aparecía en ningún PR del corte original** — el PR4 toca sólo `ventas-*`/`build-on-venta.ts`/`main.ts` —, de ahí que sea un PR propio y no un anexo del PR4.

# Hito 6 — Comunicación A2A saliente: Cliente A2A (v2.2.0): desglose de tareas

## ✅ PRECONDICIÓN DE APLICACIÓN — GATE CUMPLIDO (actualizado tras el merge de `hito-2.1`)

**`sdd-apply` de este documento YA PUEDE ARRANCAR.** `design.md` §0 fijó tres condiciones verificables contra `main` — las tres se cumplen ahora, verificado después del merge de PR #6 (`hito/v2.1-escritura-delegada` → `main`, commit `6442002`):

| # | Condición | Estado verificado en esta sesión |
|---|---|---|
| 1 | El tag `v2.1.0` existe | **Cumple** — `git tag -l` lo incluye |
| 2 | `src/adapters/git/` es código en `main` | **Cumple** — `git ls-tree main --name-only src/adapters/git/` devuelve los 10 archivos del adaptador |
| 3 | La migración `0009_propuestas_cambio` está en `main` | **Cumple** — `migrations/index.ts` de `main` llega hasta `migration0009PropuestasCambio` |

**Consecuencia**: la rama de implementación (`hito/v2.2-a2a-cliente`) ya se puede crear desde `main` (`AGENTS.md`: "se crea desde main recién cuando el checkpoint humano aprueba... nunca antes"). Ningún Implementer debe reinventar `resolveGitConfig`/`WorktreePort`/`propuestas_cambio` dentro de este change — es infraestructura de `hito-2.1`, ya real en `main`, no de `v2.2.0`.

**Regla de citación mientras el gate no se cumpla**: todo objeto de `hito-2.1` citado abajo (la migración `0009` como referencia de numeración, la convención de `resolveNumeroValidado`/`resolveGitConfig`) es referencia a la **especificación** de `hito-2.1`, no a código confirmado en `main`. `sdd-apply` revalida cada uno como su primer paso, incluido el número real de la migración `NNNN` de este change (§7.1 de `design.md`: `M + 1` sobre el techo real de `main` al momento de aplicar, no un número fijo).

**Spec y diseño de origen** (no se duplican acá): [`proposal.md`](proposal.md) (ADR 71-75, R1-R13), [`specs/cliente-a2a-jsonrpc/spec.md`](specs/cliente-a2a-jsonrpc/spec.md), [`specs/delegacion-a2a-saliente/spec.md`](specs/delegacion-a2a-saliente/spec.md), [`specs/despacho-delegacion/spec.md`](specs/despacho-delegacion/spec.md) (delta MODIFIED), [`specs/venta-confirmacion/spec.md`](specs/venta-confirmacion/spec.md) (delta ADDED), [`design.md`](design.md) (ADR 76-84, §0-15, corte de PRs en §12; **§5.7 y la enmienda del ADR 85**). El **ADR 85** vive en [`proposal.md`](proposal.md), sección *"Enmienda posterior al diseño"*, y sus requirements en los **tres últimos** de [`specs/delegacion-a2a-saliente/spec.md`](specs/delegacion-a2a-saliente/spec.md) — **no** hay un quinto archivo de spec: el comando va en la capability de su dominio, mismo precedente con que `hito-2.1` puso `/ver-propuesta` dentro de `propuesta-cambio-hitl`.

**Entregable funcional a demostrar**: los pasos de `design.md` §11 — **14 originales más los 7b-7d que agregó el ADR 85** — con `HARNESS_A2A_SALIENTE=on` y un sample de `a2a-samples` levantado. Son **las dos disciplinas de espera del mismo Cliente A2A, demostradas una al lado de la otra**: (a) una venta que alcanza `VENTA_GRANDE_UMBRAL` responde `201` **de inmediato** (no bloquea hasta 2 minutos) y dispara en background una consulta hacia `"riesgo-credito"` cuya fila en `delegaciones_a2a` termina en `TASK_STATE_COMPLETED` con el endpoint efectivo del Agent Card; (b) **un empleado autenticado ejecuta `/consultar-kpi <consulta>` en la TUI y ESPERA** la respuesta del agente de KPIs, que llega como texto del turno — el camino síncrono del ADR 74 y el Escenario 4 del arc42 con un llamador real (ADR 85). Además: el mismo mecanismo corre sin cambiar código contra los dos destinos, ninguna credencial aparece en ningún log ni en ningún mensaje de la TUI, y `HARNESS_A2A_SALIENTE=off` reproduce `v2.1.0` exacto (cero filas, cero `fetch`, y `/consultar-kpi` respondiendo "desactivado").

**Metodología**: TDD estricto (`strict_tdd: true`). Excepciones explícitas, mismo criterio que `hito-2.1-escritura-delegada/tasks.md`: tarea 3 (`.env.example`, documentación), tarea 8 (`README.md`, documentación), tarea 9 (migración `NNNN`, SQL declarativo, se escribe junto a su test en `repository.test.ts` de la tarea 10, que **sí** va primero), tarea 17 (`main.ts`, wiring puro de composition root, molde de `createNotificadorAdapter`/`barrerHuerfanos` ya existente), tarea 18 (`openspec/config.yaml`, config declarativa sin lógica), **tarea 21** (`main.ts` + una línea de `README.md`, wiring puro otra vez — ADR 85), **tarea 22** (nota de corrección al Plan en `docs/progreso/`, documentación — pedida explícitamente por ADR 84 pto 4), **tarea 23** (verificación manual, sin código de producción). **Las tareas 19 y 20 (ADR 85) NO son excepción**: parser y handler van con test primero, como el resto.

**Fixture nueva de este change**: `FetchFn` fake (unit, mismo molde que `email-client.test.ts`/`github-client.test.ts`) con reloj (`ahoraMs`) y sueño (`dormir`) inyectados para testear el loop de polling sin dormir de verdad (ADR 81), y — segunda vez en el repo tras `hito-2.1` — **integración real contra la red** en `src/test/integration/` (tarea 7), gateada por **sondeo** del Agent Card (top-level `await`, no por bandera de entorno — spec `cliente-a2a-jsonrpc`).

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | **~1.975** (era ~1.700; **+275 por el ADR 85** — recalibrado abajo) |
| 400-line budget risk | **High** (sin cambio: ya lo era con 1.700, y el PR nuevo entra por debajo del presupuesto sin empujar a ningún otro) |
| Chained PRs recommended | Yes |
| Suggested split | PR1 → PR2 → PR3 → PR4 → **PR5** → Work Unit 6 (ver `design.md` §12) |
| Delivery strategy | **ask-on-risk → PRs encadenados** (confirmado por el checkpoint humano) |
| Chain strategy | **feature-branch-chain** (confirmado por el checkpoint humano, mismo criterio que hito 2.0 y 2.1) |

**Recalibración del estimado de `design.md` §12**: su prosa afirma "el total real es ~1.500 líneas de diff", pero su propia tabla de 4 PRs suma **380 + 490 + 480 + 350 = 1.700**, no 1.500 — inconsistencia interna del documento, no una decisión de diseño, así que no se reabre: se documenta y se usa la tabla (más granular, y la que de hecho fija el corte de PRs) como base de esta fase. Con `strict_tdd: true` y la relación test:producción ≈ 1:1 que ya viene sosteniendo el repo desde `hito-2.0`, el desglose tarea por tarea de abajo confirma el **~1.700** de la tabla.

**Recalibración por el ADR 85** (tarea por tarea, misma relación 1:1): tarea 19 (parser + descriptor + constante) ~30 prod + ~60 test = **~90**; tarea 20 (handler, `catch` por motivo, deps, caso nuevo, `case` del switch) ~65 prod + ~110 test = **~175**; tarea 21 (`main.ts`, wiring puro, TDD exception) ~10 prod = **~10**. **Total PR5 ≈ 275**, cómodamente bajo el presupuesto de 400. **Total del change: 1.700 + 275 = ~1.975.** El riesgo de presupuesto **sigue siendo High** y la recomendación de PRs encadenados no cambia — lo que cambia es que ahora son **cinco** cortes en vez de cuatro, y ninguno de los cuatro originales se mueve una línea.

Decision needed before apply: **No** — `delivery_strategy`/`chain_strategy` ya resueltas por el checkpoint humano (PRs encadenados, `feature-branch-chain`), y el corte nuevo respeta las dos. De las 9 preguntas de `design.md` §15, la **número 2 (RD-26) quedó respondida** por el ADR 85; las 8 restantes siguen abiertas y ninguna bloquea `sdd-apply` — ver "Después de la tarea 23".

### Suggested Work Units

| Unit | Goal | Tasks | Est. lines | Base branch (si `feature-branch-chain`) |
|------|------|-------|-----------|-------------|
| 1 | Contrato del núcleo A2A + config del adaptador (aditivo puro, nadie lo consume) | 1–3 | ~370 | `hito/v2.2-a2a-cliente` |
| 2 | Cliente A2A HTTP real + fachada + primer test de integración contra la red | 4–8 | ~490 | Unit 1 |
| 3 | Persistencia `delegaciones_a2a` + despacho externo real + deuda del ADR 45 saldada | 9–13 | ~480 | Unit 2 |
| 4 | Enganche **no bloqueante** de ventas + interruptor + composition root | 14–18 | ~350 | Unit 3 |
| **5** (**ADR 85**) | `/consultar-kpi`: productor real de `"kpi-incidente"` y primer llamador del camino **SÍNCRONO** del ADR 74 — **ENTREGABLE COMPLETO**, RD-26 cerrado | **19–21** | **~275** | Unit 4 |
| 6 | Documentación de la corrección al Plan + verificación manual | **22–23** | ~30 (evidencia, no código) | Unit 5 |

Con `feature-branch-chain`: PR1 apunta al tracker `hito/v2.2-a2a-cliente`, cada hijo al inmediato anterior. Con `stacked-to-main`: cada PR mergea directo a `main` en orden. Rollback en orden inverso 6→1 (`design.md` §12: "los PR 1, 2 y 5 son aditivos puros y pueden quedarse mergeados sin efecto observable" — el 5 con el interruptor apagado sólo agrega un comando que responde "desactivado").

## Tareas

### PR1 — Contrato del núcleo A2A + config del adaptador (tareas 1-3)

1. [ ] **`src/core/agents/a2a-contract.ts`** (nuevo, §5.1, **SIN imports**) — `TASK_STATE_*` (las ocho constantes crudas), `TASK_STATES_CONOCIDOS`, `TaskState`, `esTaskStateConocido`, `esEstadoTerminal`, `MotivoDelegacionA2ANoCompletada`, `motivoDeEstadoTerminal`, `DESTINOS_A2A`, `DestinoA2AClave`, `DestinoA2ADesconocidoError`, `ResultadoA2AOk`/`ResultadoA2AFallo`/`ResultadoA2A`, `ClienteA2APort`, `DelegacionA2ANoCompletadaError`. Test primero: `a2a-contract.test.ts` — **el test estrella**: `esEstadoTerminal` exhaustivo sobre los ocho valores (`COMPLETED`/`FAILED`/`CANCELED`/`REJECTED` terminales; `SUBMITTED`/`WORKING` no; `INPUT_REQUIRED`/`AUTH_REQUIRED` terminales de fracaso); `esTaskStateConocido` **rechaza `TASK_STATE_UNSPECIFIED`** y un string inventado (ADR 84 — el hallazgo del noveno valor real de la spec v1.0.0); `motivoDeEstadoTerminal` total sobre los cinco estados terminales de fracaso; `DESTINOS_A2A` tiene **exactamente dos** elementos (`"riesgo-credito"`, `"kpi-incidente"`); `DestinoA2ADesconocidoError` con una clave fuera del registro; **cero imports** en el archivo fuente (assert leyendo el texto del módulo, molde de `hitl-contract.test.ts`). Sin fixture (puro). Cubre spec `delegacion-a2a-saliente` reqs "Registro estático de dos claves de destino..." y "`TASK_STATE_*` persistido crudo, con predicado puro de terminalidad testeado sobre los ocho valores" (los dos escenarios de terminalidad completos). ADR 71 pto 5, 72, 73 pto 2, 75 pto 1, 77, 84. Commit: `feat(core): agrega a2a-contract con vocabulario TASK_STATE y ClienteA2APort (Hito 6, tarea 1)`.

2. [ ] **`src/adapters/a2a/config.ts`** (nuevo, §6.1) — `DestinoA2AConfig`, `A2AConfig`, `DEFAULT_A2A_REQUEST_TIMEOUT_MS`/`DEFAULT_A2A_POLL_INTERVAL_MS`/`DEFAULT_A2A_TASK_TIMEOUT_MS`, `claveAVariableEntorno`, `resolveA2AConfig`, `isA2ASalienteEnabled`. Test primero: `config.test.ts` — `claveAVariableEntorno` para las 2 claves × 2 campos (`"riesgo-credito"` → `HARNESS_A2A_ENDPOINT_RIESGO_CREDITO`, los guiones se vuelven `_`); `resolveA2AConfig` con env vacío / valores válidos / `"abc"`/`"0"`/`"-1"`/`"Infinity"`/`"1e400"`/`"   "` ⇒ **siempre** defaults y **nunca lanza** (molde `resolveGitConfig`/`resolvePositiveNumber` de `hito-2.1`); un destino configurado y el otro no ⇒ `undefined` sólo en ese; `isA2ASalienteEnabled` con `on`/`ON`/` on `/`off`/`true`/`1`/ausente ⇒ sólo `"on"` exacto (tras trim+lowercase) da `true` (ADR 82 pto 1, interruptor **opt-in**, al revés que `HARNESS_DELEGACION_ROLES`). Sin fixture (puro, `env` inyectado). Cubre spec `delegacion-a2a-saliente` reqs "Registro estático..." (la mitad de resolución por env) y "Interruptor `HARNESS_A2A_SALIENTE`". ADR 72 pto 1-3, 82. Commit: `feat(adapters/a2a): agrega config con registro de destinos e interruptor opt-in (Hito 6, tarea 2)`.

3. [ ] **`.env.example`** (modificado) — **TDD exception** (documentación) — agrega las 8 variables nuevas (`HARNESS_A2A_SALIENTE`, `HARNESS_A2A_ENDPOINT_RIESGO_CREDITO`, `HARNESS_A2A_ENDPOINT_KPI_INCIDENTE`, `HARNESS_A2A_TOKEN_RIESGO_CREDITO`, `HARNESS_A2A_TOKEN_KPI_INCIDENTE`, `HARNESS_A2A_REQUEST_TIMEOUT_MS`, `HARNESS_A2A_POLL_INTERVAL_MS`, `HARNESS_A2A_TASK_TIMEOUT_MS`) y `VENTA_GRANDE_UMBRAL`, comentadas y **sin valores reales**, con el default de cada una a un lado. Commit: `docs(root): agrega variables HARNESS_A2A y VENTA_GRANDE_UMBRAL a .env.example (Hito 6, tarea 3)`.

### PR2 — Cliente A2A HTTP real + fachada + primer test de integración (tareas 4-8)

4. [ ] **`src/adapters/a2a/client.ts`** (nuevo, §6.2, parte 1 — Agent Card + `SendMessage`) — `FetchFn`/`FetchResponseLike`, `ERROR_BODY_MAX_CHARS = 500` (duplicación deliberada, 7ª/8ª instancia — mismo criterio que `git/config.ts:49-56`), los tipos propios y cerrados del sobre JSON-RPC, `A2AClientDeps`, la resolución del Agent Card (`GET <base>/.well-known/agent-card.json`) y el envío de `SendMessage`.

   > **★ RD-24 resuelto antes de implementar, no durante** (verificado esta sesión contra fuente normativa, no contra el Plan ni contra `main` del repo del protocolo que puede ir adelante del release): `preferredTransport`/`additionalInterfaces` **no existen** en la spec real. Verificado contra `specification/a2a.proto` (mensaje `AgentCard`) del tag **`v1.0.0`** de `a2aproject/A2A` (`gh api repos/a2aproject/A2A/contents/specification/a2a.proto?ref=v1.0.0`) y contra los ejemplos JSON de `docs/specification.md` del mismo tag (líneas 1014-1018, 2117-2120). El campo real es `supportedInterfaces` — array ORDENADO por preferencia ("the first entry is preferred"), cada entrada `{url, protocolBinding, protocolVersion, tenant?}` (JSON camelCase de los proto `protocol_binding`/`protocol_version`), con `protocolBinding` tomando `"JSONRPC"`/`"GRPC"`/`"HTTP+JSON"`/`"WEBSOCKET"`. El validador: primera entrada de `supportedInterfaces` con `protocolBinding === "JSONRPC"` y `url` no vacío; ausente/vacío/sin entrada `"JSONRPC"` ⇒ `reason: "protocolo"`, sin `SendMessage`. Corrección aplicada también a `design.md` §6.2 y RD-24. Ver `docs/progreso/v2.2-a2a-cliente/` (tarea 22) para la nota formal de corrección al cierre del hito.

   Test primero: `client.test.ts` (con `FetchFn` fake) — card válido con `supportedInterfaces` conteniendo una entrada `"JSONRPC"` ⇒ su `url` es el endpoint de los tres POST; **dos delegaciones consecutivas hacen DOS `GET`** (sin caché — escenario literal del spec); red caída ⇒ `reason: "transporte"` **sin ningún POST**; card sin `supportedInterfaces` / con `supportedInterfaces` pero ninguna entrada `"JSONRPC"` ⇒ `reason: "protocolo"` **sin `SendMessage`**; una entrada `"JSONRPC"` que NO es la primera del array pero sí está presente ⇒ igual se usa (el criterio es "existe una entrada JSONRPC", no "es la preferida" — este cliente sólo habla JSON-RPC); `method` del sobre es exactamente `"SendMessage"`, sin prefijo `a2a/`. **Fixture**: `FetchFn` fake. Cubre spec `cliente-a2a-jsonrpc` reqs "El Agent Card se busca en la URL bien conocida..." y "Los tres métodos JSON-RPC son los nombres reales de v1.0.0...". ADR 71 pto 6, 72 pto 5. Commit: `feat(adapters/a2a): agrega resolucion de Agent Card y envio de SendMessage (Hito 6, tarea 4)`.

5. [ ] **`src/adapters/a2a/client.ts`** (modificado, §6.2, parte 2 — loop de `GetTask`, `CancelTask`, clasificación completa, truncado y extracción del resultado) — el loop con `deadline = ahoraMs() + taskTimeoutMs` calculado una vez, `dormir(pollIntervalMs)` entre consultas, `CancelTask` best-effort al agotar el deadline, clasificación `transporte`/`protocolo` sobre el `status.state` (validado contra los ocho conocidos), truncado del cuerpo de error a 500 chars, exclusión del header `Authorization` de cualquier mensaje, y extracción del texto de resultado (`artifacts[*].parts[*].text` → fallback al último `Message` → `""`). Test primero: extiende `client.test.ts` — con `ahoraMs`/`dormir` inyectados y **cero tiempo real**: el intervalo entre `GetTask` es exactamente `pollIntervalMs`; terminal en la 3ª consulta ⇒ **no hay 4ª**; nunca terminal ⇒ `CancelTask` una vez y `reason: "timeout"` con el **último estado conocido**; un `GetTask` que falla por transporte **no corta** el loop y sigue hasta el deadline; `!response.ok` ⇒ `transporte`; sobre con `error` ⇒ `protocolo`; `status.state` desconocido **y** `TASK_STATE_UNSPECIFIED` ⇒ `protocolo` **sin excepción** (ADR 84, comparten veredicto con un string inventado); campos extra desconocidos junto a un `state` válido ⇒ se ignoran y sigue; cuerpo de error > 500 chars ⇒ truncado, < 500 ⇒ completo; con `authToken` configurado, el header sale en el request **y** el valor no aparece en ningún mensaje ni `logEvent`, para las 6 formas de falla; extracción de resultado con las tres formas (artifacts, fallback a `Message`, vacío ⇒ igual éxito). **Fixture**: `FetchFn` fake + reloj/sueño fake + `logEvent` spy. Cubre spec `cliente-a2a-jsonrpc` reqs "`SendMessage` inicia... loop... timeout total", "`CancelTask` es best-effort...", "Clasificación de fallas...", "El cuerpo de un error HTTP se trunca...". ADR 81, 84, 73 pto 2 (fracaso inmediato de `INPUT_REQUIRED`/`AUTH_REQUIRED` es responsabilidad del núcleo, no de este archivo — ver tarea 12). Commit: `feat(adapters/a2a): agrega loop de polling con reloj inyectable y clasificacion de fallas (Hito 6, tarea 5)`.

6. [ ] **`src/adapters/a2a/index.ts`** (nuevo, §6.3) — `createA2AAdapter(deps)` como única fachada exportada, con defaults reales (`fetch`, `Date.now`, `setTimeout` envuelto, `randomUUID`) y `baseUrlDe` **síncrono, sin I/O** (lectura del registro ya resuelto por `resolveA2AConfig`). **Sin `createNoopA2AAdapter`** — la degradación de este hito es *no cablear el adaptador*, no cablear uno que no hace nada (a diferencia de `notificaciones/`). Test primero: `index.test.ts` — **R8**: ninguna función exportada del módulo acepta una URL como parámetro (assert sobre las firmas/valores del módulo, no un grep); `createA2AAdapter` cierra `config.ts` + `client.ts` sobre `ClienteA2APort` y usa los defaults inyectables cuando no se pasan explícitos; `baseUrlDe` no dispara ningún `fetch`. Cubre spec `cliente-a2a-jsonrpc` req "No existe una firma pública por la que... inyectar una URL de destino arbitraria" y `delegacion-a2a-saliente` req "El puerto `ClienteA2APort` es inyectable...". ADR 75, 80 pto 2. Commit: `feat(adapters/a2a): agrega fachada createA2AAdapter (Hito 6, tarea 6)`.

7. [ ] **`src/test/integration/a2a-client.integration.test.ts`** (nuevo, §10.C) — molde de `run-tests.integration.test.ts` (`describe.skipIf`, degrada a *skip* y no a falla), con la diferencia técnica documentada en `design.md`: el sondeo del Agent Card es asíncrono (`fetch`), así que el gate usa **top-level `await`** en el propio archivo de test (`sondearAgentCard`, timeout de 2 s), no la variante síncrona de `execFileSync` que usa el molde. **Verificación previa** (R9, antes de dar esta tarea por completa): confirmar que el repositorio `a2a-samples` existe hoy con ese nombre e implementa v1.0.0; si ninguno califica, el fallback documentado es un servidor A2A mínimo propio, sólo para este test, no productivo. Verifica contra un agente real: `GET .well-known/agent-card.json` devuelve `url` + transporte JSON-RPC; `SendMessage` devuelve `task.id` y un `status.state` de los ocho conocidos; el loop de `GetTask` llega a `TASK_STATE_COMPLETED` con texto no vacío; **el mismo test, apuntado a `HARNESS_A2A_ENDPOINT_KPI_INCIDENTE`, pasa sin una línea de código distinta** (RD-26 — es la evidencia de que `"kpi-incidente"` es alcanzable aunque este hito no le dé productor de producción). Cubre spec `cliente-a2a-jsonrpc` req "Único test con red real, gateado por sondeo del Agent Card" (los 3 escenarios). ADR 75 pto 2, 71 pto 7. Commit: `test(integration): agrega a2a-client.integration.test contra un agente A2A real (Hito 6, tarea 7)`.

8. [ ] **`README.md`** (modificado) — **TDD exception** (documentación) — sección nueva con la tabla de variables `HARNESS_A2A_*` + `VENTA_GRANDE_UMBRAL` (mismo formato que la tabla existente de `HARNESS_GIT_*`/`HARNESS_WORKTREE_*`) y el procedimiento para levantar a mano un sample de `a2a-samples` contra el que correr la tarea 7. Commit: `docs: documenta variables HARNESS_A2A y procedimiento de sample a2a-samples en el README (Hito 6, tarea 8)`.

### PR3 — Persistencia `delegaciones_a2a` + despacho externo real + deuda del ADR 45 saldada (tareas 9-13)

9. [ ] **`src/adapters/memory/migrations/NNNN_delegaciones_a2a.ts`** (nueva, §7.1) — **TDD exception** (SQL declarativo, se escribe junto a su test en la tarea 10, que sí va primero). `NNNN` **no es un número fijo**: es `M + 1` sobre el techo real del array `migrations` en `main` al momento de `sdd-apply` (regla del ADR 63, no el número — hoy, con el gate cumplido, `M = 9` y el archivo es `0010_delegaciones_a2a.ts` exportando `migration0010DelegacionesA2A`). DDL exacto de `design.md` §7.1: tabla `delegaciones_a2a` (`id`, `caso_id NOT NULL REFERENCES casos(id)`, `destino_clave NOT NULL`, `agente_externo_url NOT NULL`, `tarea_delegada NOT NULL`, `a2a_task_id` nullable, `estado NOT NULL` sin `CHECK`, `resultado` nullable, `created_at`/`updated_at NOT NULL`) + un solo índice `idx_delegaciones_a2a_caso`. Agregada **al final** del array, sin reordenar ninguna entrada previa. Commit: `feat(adapters/memory): agrega migracion NNNN con tabla delegaciones_a2a (Hito 6, tarea 9)`.

10. [ ] **`src/adapters/memory/repository.ts`** (modificado, §7.2) — `InsertDelegacionA2AInput`/`insertDelegacionA2A`, `DelegacionA2ANotFoundError`, `ActualizarDelegacionA2AInput`/`actualizarDelegacionA2A` (`UPDATE` simple, **sin transacción** — a diferencia de `completarDelegacion`, acá no hay una segunda fila que insertar en el mismo acto; `COALESCE(@campo, campo)` para los tres opcionales), `DelegacionA2ARow`, `listDelegacionesA2APorCaso`. Test primero: `repository.test.ts` (SQLite `:memory:`) — la migración de la tarea 9 aplica; `insertDelegacionA2A` deja `a2a_task_id`/`resultado` en **NULL**; `actualizarDelegacionA2A` con un campo `undefined` **no pisa** lo ya escrito (`COALESCE`); `changes === 0` ⇒ `DelegacionA2ANotFoundError` (mismo criterio que `completarDelegacion`); `listDelegacionesA2APorCaso` ordena por `created_at`; **el `estado` persistido es `TASK_STATE_*` crudo**, nunca traducido a minúscula-con-guion. **Fixture**: SQLite `:memory:`. Cubre spec `delegacion-a2a-saliente` reqs "Registro de la delegación externa antes de invocar, completado después..." y "`TASK_STATE_*` persistido crudo..." (escenario "vocabulario persistido no se traduce"). ADR 80. Commit: `feat(adapters/memory): agrega insertDelegacionA2A actualizarDelegacionA2A y listDelegacionesA2APorCaso (Hito 6, tarea 10)`.

11. [ ] **`src/core/agents/subagents.ts`** (modificado, §5.4 — una palabra) — `truncarTareaDelegada` pasa de privada a **exportada**. Cero cambio de comportamiento, cero call sites tocados (`TAREA_DELEGADA_MAX_CHARS` y `TAREA_TRUNCADA_SUFIJO` ya eran públicos). Test primero: extiende `subagents.test.ts` con un `import { truncarTareaDelegada }` directo (RED confirmado: hoy el import falla porque la función no se exporta) y un test que reafirma el truncado exacto ya cubierto por los tests existentes del módulo, ahora ejercitado desde fuera. Sin fixture (puro). Cubre spec `delegacion-a2a-saliente` req "La tarea delegada reusa el mismo tope de caracteres... sin depender de una `AgentDefinition`" (prerrequisito del consumidor de la tarea 12). ADR 79 pto 3. Commit: `feat(core): exporta truncarTareaDelegada para reuso en delegacion A2A (Hito 6, tarea 11)`.

12. [ ] **`src/core/turn-selector/dispatch-delegation-a2a.ts`** (nuevo, §5.2 — la función **hermana**) — `DestinoA2A` (= `Extract<DestinoDelegacion, {kind:"a2a"}>`), `resolverDestinoA2A`, `construirTareaDelegadaA2A` (encabezado = **clave de destino**, no nombre del Agent Card — ADR 79 pto 1, la fila se crea antes de conocer el card), `DelegacionA2AStorePort`, `DespacharDelegacionA2ADeps`, `DelegacionA2AAplicada`, `despacharDelegacionA2A` (secuencia `baseUrlDe → construirTareaDelegadaA2A → crearDelegacionA2A → delegar → actualizarDelegacionA2A → resultado o throw`). Test primero: `dispatch-delegation-a2a.test.ts` — `resolverDestinoA2A` con las dos claves válidas y con `""`/`"riesgo_credito"`/`"otro"` ⇒ `DestinoA2ADesconocidoError`; `construirTareaDelegadaA2A` con encabezado de **clave** (no de card), truncado exacto a `TAREA_DELEGADA_MAX_CHARS` con `TAREA_TRUNCADA_SUFIJO`, sin ninguna `AgentDefinition` involucrada; orden `baseUrlDe → crear → delegar → actualizar` (spy ordenado); destino sin `baseUrlDe` (no configurado) ⇒ **`crearDelegacionA2A` NO se llama**, sin fila huérfana (§9 tabla, ADR 80 pto 2); éxito ⇒ fila `COMPLETED` + `a2aTaskId` + `resultado` + endpoint efectivo; fallo ⇒ fila con el **último estado conocido** y `DelegacionA2ANoCompletadaError` con ese `reason`, lanzada **después** de persistir (ADR 77 pto 3); un puerto que **rechaza** pese al contrato ⇒ traducido a `{ok:false, reason:"transporte"}` por el `try/catch` del núcleo (ADR 77 pto 4, red de seguridad); el módulo **no importa** `src/adapters/a2a/` (assert sobre el fuente). **Fixture**: `ClienteA2APort` fake + `DelegacionA2AStorePort` fake. Cubre spec `delegacion-a2a-saliente` casi en su totalidad: "puerto inyectable...", "Registro... antes de invocar, completado después...", "El Despachador bloquea hasta desenlace terminal o timeout...", "Desenlaces explícitos y exhaustivos...", "La tarea delegada reusa el mismo tope...". ADR 74, 77, 78 pto 3-4, 79, 80 pto 2-4. Commit: `feat(core): agrega despacharDelegacionA2A como funcion hermana del Despachador (Hito 6, tarea 12)`.

13. [ ] **`src/core/turn-selector/dispatch-delegation.ts`** (modificado, §5.3 — sólo **borrados** y un angostamiento) — borra `class DelegacionA2ANoImplementadaError` completa y el `if (destino.kind === "a2a") { throw ... }`; redefine la rama `a2a` de `DestinoDelegacion` a `{ readonly kind: "a2a"; readonly clave: DestinoA2AClave }` (**se va** `endpoint: string` — R8, ninguna URL en un tipo público exportado — y se va `agentId`); angosta `input.destino?: Extract<DestinoDelegacion, {kind:"in-process"}>` (pasar un destino `a2a` acá se vuelve **un error de compilación**, no una convención — ADR 78 pto 2); reescribe los tres doc-comments que hoy prometen esta deuda (líneas 15-24, 41-46, 192-199 según `design.md`), **en el mismo commit** (ADR 78 pto 5). `despacharCadena`, `resolverDestino`, `DelegacionStorePort`, `DespacharDelegacionDeps`, `DelegacionAplicada` y `Eslabon` no cambian ni una línea de cuerpo. Test primero: `dispatch-delegation.test.ts` (modificado) — **se borra** el test heredado de `hito-2.0` que assertea `DelegacionA2ANoImplementadaError` (spec `despacho-delegacion` delta, escenario "El test... está borrado"); test nuevo de una línea: `resolverDestino` sigue devolviendo `in-process` para todo id registrado; test que `DelegacionA2ANoImplementadaError` no está entre los exports del módulo (assert sobre el fuente); nota en el propio test de que pasar `{kind:"a2a", clave:...}` a `despacharDelegacion.input.destino` es un error de **tipo**, verificable por `tsc`/`npm run typecheck`, no por un `it` en runtime — se documenta así porque un test en runtime no puede demostrar un rechazo de compilación. Cubre spec `despacho-delegacion` (delta) completa — los 4 escenarios. ADR 78 pto 1-2, 45 pto 3 de `v2.0.0` (saldada, no reabierta). Commit: `fix(core): borra DelegacionA2ANoImplementadaError y angosta input.destino a in-process (Hito 6, tarea 13)`.

### PR4 — Enganche no bloqueante de ventas + interruptor + composition root (tareas 14-18)

14. [x] **`src/core/ventas/ventas-config.ts`** (modificado, §5.5) — `VentasConfig.ventaGrandeUmbral` + `DEFAULT_VENTA_GRANDE_UMBRAL = 5_000` + la llamada a `resolveNumeroValidado("VENTA_GRANDE_UMBRAL", ...)` dentro de `resolveVentasConfig`, junto a las otras tres. Test primero: extiende `ventas-config.test.ts` — `VENTA_GRANDE_UMBRAL` válido se resuelve; ausente ⇒ default `5_000`; `"0"`/`"-5"`/`"abc"` ⇒ error de arranque **acumulado junto a `COMISION_PORCENTAJE`/`REEMBOLSO_UMBRAL`** cuando también fallan (mismo criterio de acumulación que ya prueba el archivo). Sin fixture (puro, `env` inyectado). Cubre spec `venta-confirmacion` (delta) req "`ventas-config.ts` valida un umbral de venta grande al arranque, mismo criterio que `reembolsoUmbral`" (los 3 escenarios). ADR 83. Commit: `feat(core): agrega ventaGrandeUmbral validado a VentasConfig (Hito 6, tarea 14)`.

15. [ ] **`src/core/ventas/ventas-contract.ts`** + **`src/core/ventas/registrar-venta.ts`** (ambos modificados, §5.6 — un solo bloque conceptual) — `ConsultaRiesgoCreditoPort` (declarado en `ventas-contract.ts` para no romper la regla de imports de `registrar-venta.ts`: "únicamente los otros módulos de `src/core/ventas/`"; **nunca rechaza**, misma disciplina que `VentaNotifierPort`); `RegistrarVentaDeps.riesgoCredito?` opcional; el bloque "2b" entre el paso 2 (persistencia) y el paso 3 (link) que dispara `void riesgoCredito.consultar(...).catch(() => logEvent(...))` — **sin `await`**, molde literal de `main.ts:412-418` — sólo cuando `riesgoCredito !== undefined && input.monto >= config.ventaGrandeUmbral`. **`clienteEmail` NUNCA viaja en el insumo** (no se persiste — ADR 18 pto 4). Test primero: extiende `registrar-venta.test.ts` — `monto < umbral` ⇒ **cero llamadas** a `consultar`; `monto === umbral` ⇒ **sí** dispara (`>=`, no `>`); `riesgoCredito === undefined` ⇒ comportamiento **idéntico a `v2.1.0`**; la llamada ocurre **antes** de `notificarLinkConfirmacion` (spy ordenado); un `consultar` que **nunca resuelve** ⇒ `registrarVenta` **resuelve igual** (la prueba de que no bloquea — es el test central de ADR 76/R3); un `consultar` que **rechaza** ⇒ `registrarVenta` resuelve igual y loguea `a2a-riesgo-credito-contrato-violado`; `clienteEmail` **no aparece** en el input de `consultar`. **Fixture**: puerto fake + promesa colgada (para probar el no-bloqueo). Cubre spec `venta-confirmacion` (delta) req "`registrarVenta` delega una verificación de riesgo/crédito por A2A cuando el monto alcanza el umbral..." (los 3 escenarios) — **nota**: el spec de esta capability deja explícitamente abierto qué pasa si la delegación falla (tres alternativas a-b-c); esta tarea implementa la alternativa **(b)** ya cerrada por `design.md` como **ADR 76** (informativa, no bloqueante, nunca cancela la venta) — no se reabre acá. ADR 76, 82 pto 3. Commit: `feat(core): dispara consulta de riesgo credito no bloqueante en registrarVenta (Hito 6, tarea 15)`.

16. [ ] **`src/build-on-venta.ts`** (modificado, §7.3) — `createConsultaRiesgoCredito(deps)` (envuelve `despacharDelegacionA2A` en un `try/catch` **TOTAL**, molde exacto de `createNotificadorAdapter` — traduce cualquier `DelegacionA2ANoCompletadaError` o throw inesperado, **incluido uno síncrono**, a un evento y devuelve sin propagar; **nunca rechaza, nunca lanza**), `createDelegacionA2AStore(db)` (molde de `createDelegacionStore`), `BuildOnVentaDeps.riesgoCredito?` opcional, y el insumo fijo **en código** (`instruccion` + `material` con `ventaId`/`clienteId`/`planAnterior?`/`planNuevo`/`monto` — nunca del modelo, nunca de un prompt). Test primero: `build-on-venta.test.ts` — `createConsultaRiesgoCredito` **nunca rechaza** aunque `despacharDelegacionA2A` tire (incluido un `throw` síncrono, no sólo una promesa rechazada); con `riesgoCredito` ausente el handler resultante es exactamente el de `v2.1.0`. **Fixture**: puertos fake. Cubre spec `venta-confirmacion` (delta) — la mitad de composition root del mecanismo no bloqueante. ADR 76 pto 4-5. Commit: `feat(root): agrega createConsultaRiesgoCredito con try/catch total en build-on-venta (Hito 6, tarea 16)`.

17. [ ] **`src/main.ts`** (modificado, §7.3) — **TDD exception** (wiring puro de composition root, molde de `createNotificadorAdapter`/el bloque 5b de `barrerHuerfanos` ya existente) — bloque 5b-bis: `const riesgoCredito = isA2ASalienteEnabled() ? createConsultaRiesgoCredito({ db, cliente: createA2AAdapter({...}), logEvent }) : undefined;` seguido del spread condicional hacia `buildOnVenta` (`exactOptionalPropertyTypes`). Verificación manual (no automatizada) de que `HARNESS_A2A_SALIENTE` ausente deja `riesgoCredito === undefined` y por lo tanto `registrarVenta` ni siquiera evalúa el umbral — cero filas, cero `fetch` (ADR 82 pto 3), a confirmar en la tarea 20. Commit: `feat(root): conecta HARNESS_A2A_SALIENTE y cablea el Cliente A2A en main (Hito 6, tarea 17)`.

18. [ ] **`openspec/config.yaml`** (modificado, §7.4) — **TDD exception** (config declarativa, sin lógica) — saca A2A de la lista "Planned (not yet installed)" (línea 8 hoy). `testing.layers.integration` **ya está en `true`** (lo puso `v2.1.0`) — **no se toca**. Commit: `chore(config): remueve A2A de dependencias planificadas en openspec/config.yaml (Hito 6, tarea 18)`.

### PR5 — `/consultar-kpi`: productor real de `"kpi-incidente"` y primer llamador del camino SÍNCRONO (tareas 19-21, **ADR 85**)

> **Por qué es un PR propio y no parte del PR4** — verificado esta sesión, no asumido: `build-on-comando-empleado.ts` **no aparece en ningún PR del corte original**; el PR4 toca exclusivamente el eje de ventas (`ventas-config.ts`, `ventas-contract.ts`, `registrar-venta.ts`, `build-on-venta.ts`, `main.ts`, `openspec/config.yaml`). Sumar el canal TUI ahí mezclaría dos superficies humanas distintas en un mismo diff y llevaría el PR4 de ~350 a ~625 líneas, por encima del presupuesto. Como PR propio, PR5 tiene una unidad de review de una frase y es aditivo puro. Ver `design.md` §5.7 y §12.

19. [ ] **`src/core/commands/comando-empleado.ts`** + **`src/core/commands/registro-acciones-contract.ts`** (ambos modificados, `design.md` §5.7.1 y §5.7.2 — un solo bloque conceptual) — brazo nuevo `{ tipo: "consultar_kpi"; consulta: string }` en la unión `ComandoEmpleado`; descriptor nuevo en `DESCRIPTORES` (`nombre: "/consultar-kpi"`, `uso: "/consultar-kpi <consulta>"`, `privilegiado: true` — mismo criterio ya escrito para `/ver-propuesta` en las líneas 193-198, no una decisión nueva —, `secreto: false`, `forma: "id_mas_resto"` — **ninguna `Forma` nueva** —, `tipo: "consultar_kpi"`), colocado **ANTES de `/ayuda`**, que sigue último (regla textual del doc-comment del array, líneas 92-97); rama de parseo propia calcada de la de `/soporte` (líneas 356-361), **★ obligatoriamente ANTES del bloque final de `/devolucion` (líneas 408-413)**: la cadena `id_mas_resto` tiene a `/devolucion` como **fallthrough sin `if` propio**, así que un descriptor nuevo sin su rama **compila igual** y se parsea en silencio como `{ tipo: "devolucion", token: <primera palabra> }` — es el bug de mayor riesgo real de esta enmienda; los tres doc-comments que dicen "CATORCE"/"catorce descriptores" (líneas 21, 93, 233) pasan a **QUINCE** + "uno de Hito 6 (ADR 85)", en el mismo commit; y `COMANDO_CONSULTAR_KPI = "/consultar-kpi"` en `registro-acciones-contract.ts` (**una constante**, cero vocabulario de `resultado` nuevo: se reusan `RESULTADO_ATENDIDA`/`RESULTADO_FALLIDA`, ya comentados como de `/soporte`). **Sin migración** — verificado: `registro_acciones_empleado.comando` es `TEXT NOT NULL` **sin `CHECK`** (`migrations/0005_registro_acciones_empleado.ts:45-53`) y `caso_id TEXT REFERENCES casos(id)` ya existe. Test primero: extiende `comando-empleado.test.ts` — `/consultar-kpi hola que tal` ⇒ `{tipo:"consultar_kpi", consulta:"hola que tal"}`, el resto **entero** con espacios internos y recorte de bordes, **★ y explícitamente NO un `{tipo:"devolucion"}`** (el test que atrapa el fallthrough); `/consultar-kpi` pelado y `/consultar-kpi` + sólo espacios ⇒ `{tipo:"ayuda", motivo:"argumentos", comando:"/consultar-kpi"}`; `esComandoPrivilegiado("consultar_kpi") === true`; `COMANDOS` tiene **quince** descriptores con `/ayuda` último; `formatearAyuda()` incluye la línea nueva (los tests existentes que cuentan descriptores o comparan el texto de ayuda **se actualizan en este mismo commit**). Sin fixture (puro). Cubre spec `delegacion-a2a-saliente` req "`/consultar-kpi <consulta>` es un comando TUI privilegiado, de un solo paso, con la consulta obligatoria" (los 4 escenarios). ADR 85 ptos 3-5, 9. Commit: `feat(core): agrega el comando privilegiado /consultar-kpi al parser de la TUI (Hito 6, tarea 19)`.

20. [ ] **`src/build-on-comando-empleado.ts`** (modificado, `design.md` §5.7.3) — `BuildOnComandoEmpleadoDeps.clienteA2A?: ClienteA2APort` y `delegacionA2AStore?: DelegacionA2AStorePort` (costuras nuevas, molde de `aplicarPatch`/`propuestaStore`; default del store: `createDelegacionA2AStore(db)` de la tarea 16 — **el módulo NO lee `HARNESS_A2A_SALIENTE` por su cuenta**, el interruptor se consulta una sola vez en `main.ts`, tarea 21); consts de módulo `CASO_TIPO_CONSULTA_KPI = "consulta_kpi"` (precedente `CASO_TIPO_SOLICITUD_INTERNA`, `crear-solicitud-interna.ts:56`) y `CASO_ESTADO_ACTIVO = "activo"` (duplicación deliberada con el mismo doc-comment que `build-on-soporte.ts:41-48` ya lleva); función pura de módulo `mensajeDeMotivoA2A(reason)` con `switch` **exhaustivo** sobre los ocho motivos (un motivo nuevo del vocabulario sería un error de **compilación**, no un mensaje genérico; **nunca** incluye el `detalle` crudo del adaptador); `manejarConsultarKpi` con la secuencia a-e de §5.7.3 (`clienteA2A === undefined` ⇒ mensaje de desactivado sin caso ni fila → `createCaso` nuevo por invocación, **obligatorio** porque `delegaciones_a2a.caso_id` es `NOT NULL REFERENCES casos(id)` → `await despacharDelegacionA2A(...)` con `resolverDestinoA2A(DESTINO_A2A_KPI_INCIDENTE)` y el insumo **fijo en código** → éxito: `registrar(RESULTADO_ATENDIDA)` y devolver el texto → `catch` **explícito**: `DelegacionA2ANoCompletadaError` ⇒ `mensajeDeMotivoA2A(error.reason)`, cualquier otro throw ⇒ `toErrorMessage`, en ambos `RESULTADO_FALLIDA` + evento); `agentLabel: "sistema"`; `case "consultar_kpi"` en el `switch` de ruteo (línea 1282), entre `"ver_propuesta"` y `"aplicar_propuesta"` — el `switch` **vuelve a ser exhaustivo**, mismo patrón de cierre de `TS2322` que las tareas 23 y 32 de los hitos anteriores. Test primero: extiende `build-on-comando-empleado.test.ts` — **sin sesión** ⇒ mensaje de la guarda del preámbulo y **cero** llamadas al puerto y **cero** `createCaso`; `clienteA2A` ausente ⇒ mensaje de desactivado, **cero** caso, **cero** fila; éxito ⇒ el `responseText` **es** el texto del agente externo y hay fila `atendida` en `registro_acciones_empleado`; **el turno ESPERA** (un puerto que resuelve tarde ⇒ la promesa del handler no resuelve antes — el espejo exacto del test de "no bloquea" de la tarea 15, y la prueba de que este camino usa la disciplina síncrona del ADR 74); **los OCHO motivos** ⇒ ocho mensajes **distintos entre sí** (assert de unicidad sobre el conjunto, no ocho asserts sueltos) y `RESULTADO_FALLIDA` en los ocho; un throw NO tipado ⇒ igual responde; `createCaso` que tira ⇒ igual responde **sin** intentar delegar; con `authToken` configurado el valor **no aparece** en ningún `responseText` ni `logEvent`. **Fixture**: `ClienteA2APort` fake + `DelegacionA2AStorePort` fake + SQLite `:memory:`. Cubre spec `delegacion-a2a-saliente` reqs "`/consultar-kpi` es el productor real del destino `\"kpi-incidente\"` y **espera** el resultado" (los 4 escenarios) y "Cada motivo de fracaso se traduce a un mensaje legible y distinto" (los 5 escenarios). ADR 85 ptos 1-2, 6-8, 10; ADR 74 pto 6 y ADR 40 (el `catch` del borde de la TUI). Commit: `feat(root): agrega manejarConsultarKpi con delegacion A2A sincrona a kpi-incidente (Hito 6, tarea 20)`.

21. [ ] **`src/main.ts`** (modificado, `design.md` §7.3 enmendado) + **`README.md`** (una línea) — **TDD exception** (wiring puro de composition root) — el bloque 5b-bis de la tarea 17 se **hoistea**: `const clienteA2A = isA2ASalienteEnabled() ? createA2AAdapter({ logEvent }) : undefined;` primero, y `riesgoCredito` pasa a derivarse de `clienteA2A !== undefined` en vez de volver a leer el interruptor — **una sola lectura de `HARNESS_A2A_SALIENTE` y una sola instancia de adaptador para los DOS consumidores** (`design.md` §5.7.4 tensión 1: se rechaza deliberadamente la salida fácil de leer el interruptor dentro de `build-on-comando-empleado.ts` como ese archivo ya hace con `resolveGitConfig()`, porque `git` no tiene interruptor y A2A sí, y duplicar su lectura pondría el rollback a `v2.1.0` en dos lugares). Después, el spread condicional `...(clienteA2A !== undefined ? { clienteA2A } : {})` en la llamada a `buildOnComandoEmpleado` (`exactOptionalPropertyTypes`). **Ubicación verificada**: `createNotificadorAdapter` en la línea 329, `buildOnVenta` en la 335 y `buildOnComandoEmpleado` en la 377, así que el bloque hoisteado va antes de las tres. En el `README.md`, una línea para `/consultar-kpi` en la lista de comandos de la TUI, junto a la tabla `HARNESS_A2A_*` de la tarea 8. Verificación manual (a confirmar en la tarea 23): con `HARNESS_A2A_SALIENTE` ausente, `/consultar-kpi` responde "desactivado" y no crea ni caso ni fila. Cubre spec `delegacion-a2a-saliente` req del interruptor (escenario "con el interruptor apagado, el comando responde sin delegar"). ADR 82 (aplicado, no reabierto), ADR 85 pto 10. Commit: `feat(root): hoistea el cliente A2A en main y lo cablea al canal TUI (Hito 6, tarea 21)`.

### Documentación de la corrección al Plan (tarea 22) y verificación manual (tarea 23) — Work Unit 6

22. [ ] **`docs/progreso/v2.2-a2a-cliente/`** (nuevo, nota de corrección) — **TDD exception** (documentación, pedida explícitamente por ADR 84 pto 4) — anota, junto al resto de la evidencia de la tarea 20, la corrección al Plan de Implementación verificada en esta fase de diseño: los tres métodos JSON-RPC reales son `SendMessage`/`GetTask`/`CancelTask` (PascalCase, **sin** prefijo `a2a/` — el Plan los cita mal) y el enum `TaskState` de la especificación v1.0.0 real tiene **nueve** valores, no ocho (`TASK_STATE_UNSPECIFIED` existe — ADR 84/RD-27), tratado en este change como estado desconocido ⇒ `protocolo`. Commit: `docs: documenta correccion al Plan sobre metodos A2A y noveno TaskState (Hito 6, tarea 22)`.

23. [x] **Verificación manual del entregable** — cerrada por decisión del checkpoint humano (`docs/progreso/v2.2-a2a-cliente/verificacion-manual-tarea-23.md` §8): dos bugs reales encontrados y corregidos con TDD (header `A2A-Version` faltante, desempaquetado de `result.task`/`result.message` en `SendMessage` — ver §2, §7 del mismo doc); paso 9 (timeout+`CancelTask` de punta a punta) aceptado con cobertura de test unitario, no demostrable contra el sample de referencia sin `configuration.return_immediately` (deuda no bloqueante, documentada); pasos 7b-7d pendientes de sesión interactiva humana por Ink/raw-mode, con los comandos exactos documentados en §4. — **TDD exception** (sin código de producción) — los 14 pasos de `design.md` §11 contra un sample real de `a2a-samples`: `POST /ventas` bajo el umbral ⇒ cero filas; sobre el umbral ⇒ `201` inmediato (cronometrado) + fila `TASK_STATE_SUBMITTED`; segundos después, fila `TASK_STATE_COMPLETED` con `agente_externo_url` = endpoint del card (no la base); la venta sigue su curso sin gatearse; tráfico JSON-RPC capturado confirma los métodos sin prefijo; el mismo test de integración pasa contra `"kpi-incidente"` sin cambiar código; falla a mitad de poll ⇒ último estado + evento, venta intacta; timeout bajo (`HARNESS_A2A_TASK_TIMEOUT_MS=3000`) ⇒ `CancelTask` intentado + `reason="timeout"`; destino no configurado ⇒ evento `a2a-destino-no-configurado`, cero filas; `HARNESS_A2A_SALIENTE=off` ⇒ idéntico a `v2.1.0`; `rg` sobre `data/harness.log` por el valor del token ⇒ cero resultados; `npm test`/`npm run typecheck` en verde con el sample apagado (el archivo de integración aparece *skipped*). **Más los pasos 7b-7d que el ADR 85 agregó a `design.md` §11**: en la TUI, `/login` y después `/consultar-kpi <consulta>` contra el sample de KPIs ⇒ el turno **tarda** lo que tarde el agente (cronometrado — a diferencia del `201` de ventas, acá el tiempo SÍ se percibe: **es la prueba de que este camino bloquea y el de ventas no**), responde con el texto del agente externo y deja una fila `TASK_STATE_COMPLETED` con `destino_clave='kpi-incidente'` más una fila `atendida` en `registro_acciones_empleado`; `/consultar-kpi` **sin `/login`** ⇒ mensaje de sesión requerida con cero filas y cero casos; `/consultar-kpi` con el sample **apagado** ⇒ mensaje legible de transporte, sin stacktrace y sin cuelgue. Reunir la evidencia completa (tráfico de los dos agentes, volcado de `delegaciones_a2a`, log estructurado, medición del `201`, y **la captura del turno de `/consultar-kpi` de punta a punta** — que es la evidencia del Escenario 4 del arc42 con un llamador real) en `docs/progreso/v2.2-a2a-cliente/`, junto a la nota de la tarea 22. Commit: `docs: evidencia de verificacion manual del entregable A2A saliente (Hito 6, tarea 23)`.

## Después de la tarea 23

- Pasa al Reviewer (`sdd-verify` + `code-review`) contra este documento, los 4 specs y `design.md`, con atención especial a: **ADR 76/RD-21** (la consulta de riesgo queda "iniciada", no "resuelta" — el Reviewer puede escalar si la lectura débil del spec no convence), **ADR 78** (el angostamiento por tipo de `input.destino` como la garantía verificable, no documental, de que un destino `a2a` no llega a `despacharDelegacion`), **ADR 84/RD-27** (el noveno `TaskState` tratado como desconocido, con el test exhaustivo de la tarea 1 como barrera), y **ADR 85/RD-30** (la espera de hasta 2 min visible en la TUI, sin barra de progreso — declarada y aceptada, sin perilla nueva).
- **RESUELTO — ya no es un pendiente**: ~~`"kpi-incidente"` sin productor de producción (RD-26)~~. El **ADR 85** lo cierra con `/consultar-kpi <consulta>` (PR5, tareas 19-21), que además aporta el **primer llamador real del camino síncrono del ADR 74** — un gap que RD-26 no había nombrado. La evidencia de RD-26 (test de integración parametrizado + §11 paso 7) sigue en pie y ahora se complementa con los pasos 7b-7d.
- Antes de dar la tarea 23 por completa, el Reviewer/checkpoint debe confirmar las **8 preguntas que quedan abiertas** de `design.md` §15 (RD-21 timeout dedicado para el camino de ventas sí/no, el número de `VENTA_GRANDE_UMBRAL`, ADR 78, ADR 79, ADR 84/RD-27, RD-24 verificación del Agent Card, el corte — ahora de **5** PRs — y su `chain_strategy`, RD-19 el gate de secuencia). La novena (RD-26) ya fue respondida y formalizada como ADR 85. Ninguna bloquea el código escrito por este documento, pero sí la lectura final del entregable.
- Si aprueba: checklist de cierre de hito en `AGENTS.md`, tag `v2.2.0`, carpeta `docs/progreso/v2.2-a2a-cliente/` completa, copia de los ADR 76-**85** a `docs/ARC42_Harness_Empresarial.md`.
- Si el Reviewer objeta el criterio no bloqueante de ADR 76 o la clasificación gruesa de `reason="transporte"` para "destino no configurado" (RD-22), la corrección es **reabrir el checkpoint humano sobre el diseño**, no improvisar una mitigación en `sdd-apply` — ambas son decisiones ya fijadas y documentadas (`design.md` ADR 76, §13 RD-22).

---

**Nota de formato**: la skill `sdd-tasks` sugiere un tope de 530 palabras. Se sigue deliberadamente el formato extenso de este repo (`hito-1.2`, `hito-1.3`, `tui-canal-empleado`, `hito-2.0`, `hito-2.1`), tal como `design.md` de este mismo change documentó en su nota de cierre: `AGENTS.md` exige que el repositorio muestre el proceso de construcción paso a paso, con archivo objetivo, requirement de origen y comando exacto por tarea. El tope genérico de la skill cede ante la convención explícita del proyecto ya establecida por `exploration.md`, `proposal.md`, los 4 `specs/`, `design.md` y `hito-2.1-escritura-delegada/tasks.md`.
