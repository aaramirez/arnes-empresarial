> **Nota de proceso (hook de graphify)**: el hook de este repo exige correr `graphify query`/`explain` antes de leer código fuente. El ejecutor de esta fase corrió **sin herramienta de shell disponible** (solo `Read`/`Edit`/`Write`/`Grep`/`Glob`), igual que las 3 fases anteriores de este hito (`sdd-propose`, `sdd-spec`, `sdd-design`). Se compensó con lectura directa y completa de `proposal.md`, `design.md` (las 1730 líneas), los 3 specs (`activity-webhook-turn`, `activity-board-mirror`, `knowledge-query`), `openspec/changes/hito-1.1-consulta-conocimiento/tasks.md` (plantilla de formato) y `AGENTS.md`. Ninguna afirmación de este documento reinterpreta el diseño: cada tarea desglosa un archivo o contrato ya fijado por `design.md`.

# Hito 3 — Bot de revisión de PRs: desglose de tareas

**Spec y diseño de origen** (no se duplican acá): [`proposal.md`](proposal.md) (ADR 5, ADR 6), [`specs/activity-webhook-turn/spec.md`](specs/activity-webhook-turn/spec.md), [`specs/activity-board-mirror/spec.md`](specs/activity-board-mirror/spec.md), [`specs/knowledge-query/spec.md`](specs/knowledge-query/spec.md), [`design.md`](design.md) (ADR 7-10).

**Entregable funcional a demostrar**: un PR real en un repo de prueba dispara el webhook, se verifica la firma, se crean `proyecto`/`actividad`/`caso`, el agente publica una revisión como comentario, el label del Issue refleja `actividades.estado`, y al resolverse las observaciones el label y el `assignee` se actualizan solos — sin intervención manual (§12 del diseño).

**Metodología**: TDD estricto (`openspec/config.yaml: strict_tdd: true`) en toda tarea con lógica de negocio (1-5, 8-16, 18-22). Excepciones explícitas, marcadas una por una: tarea 6 (`env.ts`, registro de nombres de variable sin lógica nueva), tarea 7 (`package.json`, un número en `engines`), tarea 17 (`.gitignore`, líneas de patrón), tarea 23 (`turn-logger.ts`, solo module doc — el contrato de `logTurnEvent` no cambia), tarea 24 (verificación manual, sin código de producción).

**Nota de precedencia**: nombres de archivo, firmas, constantes, tablas de eventos y argv de esta lista provienen literal de `design.md` — no se reinterpretan. La decisión del checkpoint humano sobre R10 (dos espacios de id de correlación: `deliveryId` para eventos de transporte antes de que exista `caso`, `casoId` desde `actividad-creada` en adelante, con `deliveryId` como puente en los campos) está fijada por el diseño y se implementa tal cual en las tareas de logging — no se reabre.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~4500-5000 (24 tareas: núcleo nuevo completo, 2 adaptadores nuevos completos, migración+repository, composition root) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | 6 unidades de trabajo (ver tabla abajo) |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending — ver nota AGENTS.md abajo |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

**Nota para la decisión de `chain_strategy`**: igual que Hito 2, `AGENTS.md` fija una sola rama por hito (`hito/v1.2-bot-revision-prs`) con un único merge humano a `main` al cierre. Esto favorece **`feature-branch-chain`** (PRs de revisión encadenados contra la rama del hito) sobre `stacked-to-main`. Precisión propia de este hito: las Unidades 2 (Webhooks), 3 (Tablero) y 4 (Memoria) son funcionalmente independientes entre sí — todas dependen solo de la Unidad 1 (Núcleo), no unas de otras. El orden secuencial de abajo es la recomendación por defecto para mantener un solo chain lineal simple; un equipo que prefiera paralelizar puede ramificar 2/3/4 directamente desde la Unidad 1 en vez de encadenarlas entre sí. La Unidad 5 (composition root) sí depende de que 2, 3 y 4 existan. Queda como recomendación — el humano confirma `chain_strategy` antes de `sdd-apply`.

### Suggested Work Units

| Unit | Goal | Tasks | Est. lines | Base branch |
|------|------|-------|-----------|-------------|
| 1 | Núcleo: contrato de actividad, cola, transición, prompt, orquestador + config compartida | 1–7 | ~1300 | `hito/v1.2-bot-revision-prs` |
| 2 | Adaptador de Webhooks completo | 8–12 | ~1200 | Unit 1 |
| 3 | Adaptador de Tablero completo | 13–16 | ~1000 | Unit 2 (o Unit 1 si se paraleliza) |
| 4 | Migración + repository + WAL | 17–20 | ~700 | Unit 3 (o Unit 1 si se paraleliza) |
| 5 | Composition root (`build-on-activity.ts` + `main.ts` + doc de logging) | 21–23 | ~470 | Unit 4 |
| 6 | Verificación manual end-to-end | 24 | ~0 (evidencia, no código) | Unit 5 |

## Tareas

### Núcleo (tareas 1-7)

1. **`src/core/activity/activity-contract.ts`** (nuevo). Test primero: `activity-contract.test.ts` asserts `ACTIVIDAD_ESTADOS` tiene exactamente los 4 valores canónicos en el orden del diseño, `ACTIVIDAD_TIPOS` los 3 valores, `VEREDICTOS` los 3 valores (sin `pendiente_revision`), `VEREDICTO_PREFIX === "VEREDICTO:"`, y que el archivo **no importa nada** (mismo test que `knowledge-contract.test.ts` de Hito 2). Hecho: constantes + `ActividadTipo`/`ActividadEstado`/`Veredicto`, `IncomingActivityEvent`, `Actividad`, `ActivityStorePort`, `ActivityBoardPort`, `PullRequestMetadata` exportados, test verde. Commit: `feat(core): agrega contrato de actividad con constantes, ActivityStorePort y ActivityBoardPort (Hito 3, tarea 1)`.

2. **`src/core/concurrency/keyed-queue.ts`** (nuevo, ADR 8). Test primero: `keyed-queue.test.ts` con promesas controladas (deferred), sin I/O — las 4 garantías de §3.3: orden por clave, aislamiento entre claves distintas, un rechazo no contagia a la cadena ni produce `unhandledRejection`, `size === 0` tras asentarse todo (incluye el caso borde de encolar una tarea nueva mientras la anterior termina — chequeo de identidad, R6). Cubre spec `activity-webhook-turn`: "Dos eventos casi simultáneos del mismo proyecto" y "La cola no filtra memoria". Hecho: `createKeyedQueue()` sin imports. Commit: `feat(core): agrega KeyedQueue con serializacion por clave (Hito 3, tarea 2)`.

3. **`src/core/activity/transicion-estado.ts`** (nuevo). Test primero: `transicion-estado.test.ts` — matriz completa de 12 celdas de `transicionarEstado` (tabla §3.2: `resuelto` solo alcanzable desde `observado`, `aprobado`/`observado` válidos desde cualquier estado); `parseVeredicto`: última línea `VEREDICTO:` gana a una anterior, tolera `**`/backticks/punto final/mayúsculas/acentos, matchea sinónimos (`lgtm`, `approved`, `changes_requested`, `resolved`), y **sin línea reconocible cae a `observado`, nunca a `aprobado`**. Cubre spec: "Veredicto válido transiciona estado" y "Veredicto no parseable cae a estado seguro" (R5). Commit: `feat(core): agrega parseVeredicto y transicionarEstado (Hito 3, tarea 3)`.

4. **`src/core/activity/activity-prompt.ts`** (nuevo). Test primero: `activity-prompt.test.ts` — función pura y determinista; incluye `VEREDICTO_PREFIX` y los tres valores de `VEREDICTOS` enumerados; trunca título/cuerpo/comentario/archivos con marca (`[…truncado]`, `(… y N archivos más)`) usando las constantes exactas (`MAX_PROMPT_TITULO_CHARS`, `MAX_PROMPT_CUERPO_CHARS`, `MAX_PROMPT_COMENTARIO_CHARS`, `MAX_PROMPT_ARCHIVOS`); sin `metadatos` usa los fallbacks del evento y lo declara explícitamente; con `comentarioDisparador` cambia el framing a "verificá si esto resuelve tus observaciones"; incluye la limitación declarada de R4 y **nunca** da a entender que hay un diff disponible. Commit: `feat(core): agrega buildActivityPrompt con topes de truncado (Hito 3, tarea 4)`.

5. **`src/core/activity/run-activity-turn.ts`** (nuevo). Test primero: `run-activity-turn.test.ts` con dobles planos de `ActivityStorePort`/`ActivityBoardPort`/`runTurn` — crea `caso`+`actividad` cuando `findActividadPorReferencia` devuelve `undefined`, reusa cuando existe (mismo `casoId`); `store` que lanza → propaga y `runTurn` **no** se llama (spec: "Fallo de persistencia propaga"); `runTurn` que rechaza → propaga y `updateActividadEstado` **no** se llama; orden estricto: `updateActividadEstado` ANTES de `publicarRevision`/`mirrorEstado`; un `board` cuyos métodos "rechazaran" (doble mal comportado) igual resuelve el orquestador sin romperlo; `leerMetadatos` → `undefined` degrada sin error. Commit: `feat(core): agrega runActivityTurn que envuelve handleTurn sin modificarlo (Hito 3, tarea 5)`.

6. **`src/core/config/env.ts`** (modificado) — **TDD exception**: se agregan `GITHUB_WEBHOOK_SECRET`, `GITHUB_TOKEN`, `WEBHOOK_PORT`, `WEBHOOK_PATH`, `WEBHOOK_MAX_BODY_BYTES`, `GITHUB_API_BASE_URL`, `BOARD_TIMEOUT_MS` al punto único de carga de `.env` (mismo mecanismo que ya usan las vars de `adapters/knowledge/config.ts`). Sin lógica de resolución nueva acá — eso vive en `resolveWebhookConfig`/`resolveBoardConfig` (tareas 8 y 13), que leen `process.env` directamente. Hecho: el archivo sigue siendo el primer import de `main.ts`; `npm run typecheck` verde; los tests existentes de `env.ts` (si los hay) siguen en verde sin modificarlos. Commit: `chore(core): registra variables GITHUB_*/WEBHOOK_* en el punto unico de carga de env (Hito 3, tarea 6)`.

7. **`package.json`** — sube `"engines": { "node": ">=20" }` (ADR 7). **TDD exception**: cambio de metadato, sin lógica ni dependencia nueva — mismo criterio que la tarea 3 de Hito 2 con `zod`. Hecho: `npm install` sin diff de `node_modules`, `npm run typecheck` verde en Node 20+. Commit: `chore(root): sube engines.node a >=20 para fetch global no experimental (Hito 3, tarea 7)`.

### Adaptador de Webhooks (tareas 8-12)

8. **`src/adapters/webhooks/config.ts`**. Test primero: `config.test.ts` — defaults con env vacío (`secret === ""`), override por cada var (`GITHUB_WEBHOOK_SECRET`, `WEBHOOK_PORT`, `WEBHOOK_PATH`, `WEBHOOK_MAX_BODY_BYTES`), numérico ausente/vacío/no numérico/≤0 cae a default, `path` sin `/` inicial se normaliza, `isWebhookEnabled` refleja `secret.trim() !== ""`. Hecho: `resolveWebhookConfig(env = process.env)` pura, `import "../../core/config/env.js"` de efecto lateral presente, nunca lanza. Cubre spec: "Sin secreto configurado". Commit: `feat(adapters/webhooks): agrega resolveWebhookConfig con defaults y overrides por env (Hito 3, tarea 8)`.

9. **`src/adapters/webhooks/signature.ts`**. Test primero: `signature.test.ts` — firma calculada con el mismo secreto verifica; un byte distinto → `false`; header ausente/vacío/array → `false`; **header de largo distinto → `false` y NO lanza `RangeError`** (chequeo de longitud antes de `timingSafeEqual`); prefijo `sha256=` ausente → `false`. Cubre spec: "Firma inválida no crea filas", ADR 9. Commit: `feat(adapters/webhooks): agrega verificacion HMAC-SHA256 en tiempo constante (Hito 3, tarea 9)`.

10. **`src/adapters/webhooks/github-mapper.ts`**. Test primero: `github-mapper.test.ts` con fixtures de payload reales (versionados en el repo) de `pull_request` (`opened`/`synchronize`/`reopened`) e `issue_comment.created`; **issue sin `pull_request` → `undefined`** (spec: "Comentario en un issue que no es PR se ignora"); acción no soportada → `undefined`; evento desconocido → `undefined`; payload deforme → `undefined`; mapeo campo por campo contra la tabla §4.3 (`responsableId` = autor del PR, no quien comenta). Cubre spec: "PR abierto dispara turno". Commit: `feat(adapters/webhooks): agrega mapGithubEvent con traduccion de payload a IncomingActivityEvent (Hito 3, tarea 10)`.

11. **`src/adapters/webhooks/server.ts`**. Test primero: `server.test.ts` con `req`/`res` dobles planos (`WebhookRequest`/`WebhookResponse` estructurales — **sin abrir ningún puerto real**): `404` fuera del path y con método distinto de `POST`; `413` + `req.destroy()` al superar `maxBodyBytes`, **sin llamar a la verificación de firma** (ADR 9); `401` con firma inválida y **`onEvent` no invocado**, cero filas; `400` con `JSON.parse` roto tras firma válida; `400` en `"error"` del request; `202` + evento válido invoca `onEvent`; `202` con `mapGithubEvent` → `undefined` sin invocar `onEvent`; **`res.end()` ocurre ANTES de que la promesa de `onEvent` se asiente** (ADR 10, ack primero); `close()` espera a los `onEvent` en vuelo vía `Promise.allSettled` y resuelve igual si excede `SERVER_CLOSE_TIMEOUT_MS` (5000 ms), logueando `webhook-cierre-con-turnos-en-vuelo`. Commit: `feat(adapters/webhooks): agrega listener HTTP con tope de body y ack 202 antes del turno (Hito 3, tarea 11)`.

12. **`src/adapters/webhooks/index.ts`**. Test primero: `index.test.ts` — sin secreto configurado → devuelve `undefined`, `createServer` **nunca se llama**, se loguea `webhook-deshabilitado` (spec: "Sin secreto configurado"); con secreto → devuelve `WebhookAdapter` con el `port`/`path` configurados; `listen` que rechaza (`EADDRINUSE` simulado vía `createServer` inyectado) se propaga para que el composition root decida. Commit: `feat(adapters/webhooks): agrega fachada startWebhookServer opt-in (Hito 3, tarea 12)`.

### Adaptador de Tablero (tareas 13-16)

13. **`src/adapters/board/config.ts`**. Test primero: `config.test.ts` — defaults con env vacío (`token === ""`), overrides por `GITHUB_TOKEN`/`GITHUB_API_BASE_URL`/`BOARD_TIMEOUT_MS`, numérico inválido/≤0 cae a default, `isBoardEnabled` refleja `token.trim() !== ""`. Commit: `feat(adapters/board): agrega resolveBoardConfig con defaults y overrides por env (Hito 3, tarea 13)`.

14. **`src/adapters/board/labels.ts`**. Test primero: `labels.test.ts` — los 4 mapeos exactos de `ESTADO_LABELS` (`pendiente_revision→necesita-revision`, `observado→observaciones-pendientes`, `resuelto→resuelto`, `aprobado→aprobado`); `mergeLabels` quita **todos** los `MANAGED_LABELS` presentes y agrega el del estado nuevo, **preservando** labels ajenos (`bug`, `docs`) en su orden relativo; idempotente al aplicarse dos veces. Cubre spec `activity-board-mirror`: "Estado aprobado refleja label correspondiente". Commit: `feat(adapters/board): agrega mapeo estado-label y mergeLabels (Hito 3, tarea 14)`.

15. **`src/adapters/board/github-client.ts`**. Antes de fijar el test: correr `npm run typecheck` apenas escrito el archivo para confirmar que `fetch` global y `AbortSignal.timeout()` tipan sin fricción bajo `@types/node@^20` (R15 del diseño — riesgo no verificado en vivo por falta de shell en las fases previas). Test primero: `github-client.test.ts` con `FetchFn` inyectado — URL/método/headers exactos por operación (`Authorization: Bearer <token>`, `Accept: application/vnd.github+json`, `X-GitHub-Api-Version: 2022-11-28`, `Content-Type` solo con body); **el token nunca aparece en el mensaje de error**; `!response.ok` → `GithubApiError("http", status)`; abort (`AbortSignal.timeout`) → `reason: "timeout"`; throw de red → `reason: "network"`; `splitProyectoId` con formato sin exactamente una `/` lanza `GithubApiError("parse")`. Commit: `feat(adapters/board): agrega cliente REST sobre fetch inyectable con clasificacion de errores (Hito 3, tarea 15)`.

16. **`src/adapters/board/index.ts`**. Test primero: `index.test.ts` — sin `GITHUB_TOKEN` → `createNoopBoardAdapter`, `fetchFn` **nunca** se llama, `leerMetadatos` → `undefined` (spec: "Turno completa sin token de GitHub"); `fetchFn` que rechaza en cualquiera de los tres métodos → **los tres resuelven igual** y loguean, sin excepción cruzando la frontera (spec: "Falla de red al espejar estado", "Falla al publicar comentario de revisión"); `mirrorEstado` hace GET de labels actuales + PATCH con `mergeLabels(...)`, y **omite** `assignees` cuando no hay `responsableId` (nunca desasigna a todos); si el GET de labels falla, igual hace el PATCH con `mergeLabels([], estado)` y loguea `tablero-labels-no-leidos`; `publicarRevision` trunca a `MAX_COMMENT_CHARS`; `leerMetadatos` nunca pide el diff (`Accept: ...diff` ausente — R4). Cubre spec: "Metadatos disponibles para el prompt", "Resolución de observaciones actualiza el tablero solo". Commit: `feat(adapters/board): agrega createBoardAdapter que implementa ActivityBoardPort (Hito 3, tarea 16)`.

### Memoria: migración + repository + WAL (tareas 17-20)

17. **`.gitignore`** — agrega `*.db-wal`, `*.db-shm` (ADR 8/§8; `*.db` ya presente no matchea esos sufijos). **TDD exception**: patrón de config de git, sin lógica. Hecho: crear un archivo `data/harness.db-wal` de prueba y confirmar que `git status` no lo lista; revertir el archivo de prueba antes de commitear. Commit: `chore(root): ignora archivos WAL/SHM de SQLite (Hito 3, tarea 17)`.

18. **`src/adapters/memory/db.ts`** — agrega `db.pragma("journal_mode = WAL")` inmediatamente después de `db.pragma("foreign_keys = ON")`. Test primero: agrega caso a `db.test.ts` — `db.pragma("journal_mode")` devuelve `"wal"` al abrir una base de **archivo temporal**; abrir con `:memory:` sigue funcionando exactamente igual que antes (SQLite ignora WAL en memoria — regresión explícita, ningún test existente cambia de comportamiento). Commit: `feat(adapters/memory): habilita journal_mode WAL para concurrencia (Hito 3, tarea 18)`.

19. **`src/adapters/memory/migrations/0003_proyectos_responsables_actividades.ts`** (nuevo) + **append** a `migrations/index.ts` (nunca reordenar entradas ya commiteadas). SQL literal de §6.1: tablas `proyectos`, `responsables`, `actividades` (FK a `proyectos`/`responsables`/`casos`, `estado` como TEXT abierto sin CHECK) + `idx_actividades_proyecto`. Test primero: agrega caso al test de migraciones existente — tras `runMigrations`, las tres tablas y el índice existen; correr las migraciones dos veces no falla (`IF NOT EXISTS`); insertar una `actividad` con `proyecto_id`/`caso_id` inexistentes falla por FK (con `foreign_keys = ON` ya activo desde Hito 1). Commit: `feat(adapters/memory): agrega migracion 0003 con tablas proyectos/responsables/actividades (Hito 3, tarea 19)`.

20. **`src/adapters/memory/repository.ts`** — funciones nuevas: `upsertProyecto`, `getProyectoById`, `upsertResponsable`, `createActividad`, `getActividadById`, `findActividadPorReferencia`, `updateActividad`, `createCasoConActividad`; errores `ActividadNotFoundError`, `ActividadAlreadyExistsError`, `ActividadInvalidReferenceError`. Test primero: agrega casos a `repository.test.ts` — `upsertProyecto`/`upsertResponsable` idempotentes (`ON CONFLICT DO UPDATE`, un `nombre` ausente no borra el existente vía `COALESCE`); `createCasoConActividad` **atómica**: si `createActividad` falla dentro de la transacción, **no queda `proyecto` ni `caso` huérfano** (usa `db.transaction(fn)()`) — es lo que hace verdadera la afirmación "firma inválida no crea ninguna fila" combinada con la tarea 9; `findActividadPorReferencia` devuelve la más reciente y desempata por `rowid` cuando dos filas comparten `created_at`; `updateActividad` con `responsableId: null` desasigna, `undefined`/ausente preserva (`COALESCE`); FK rota → `ActividadInvalidReferenceError`. Cubre spec `activity-webhook-turn`: "Creación transaccional de caso y actividad", "Fallo de persistencia propaga". Commit: `feat(adapters/memory): agrega CRUD de proyectos/responsables/actividades y creacion transaccional (Hito 3, tarea 20)`.

### Composition root (tareas 21-23)

21. **`src/build-on-activity.ts`** (nuevo — módulo hermano de `build-on-submit.ts`, ADR 5). Exporta `buildOnActivity(deps): ActivityEventHandler` y `createActivityStore(db): ActivityStorePort`. Test primero: `build-on-activity.test.ts` con dobles de `runActivityTurn`/`handleTurn`/`store`/`board`/`queue` — la promesa devuelta por el handler **nunca rechaza** aunque `runActivityTurn` lance (se loguea `actividad-turno-fallido`, nunca dentro de `runActivityTurn` mismo); `queue.run` se invoca con `evento.proyectoId` envolviendo el ciclo **completo** (no solo la escritura); `createKnowledge(casoId)` se invoca **una vez por turno**, con el `casoId` que `runActivityTurn` acaba de crear o reusar — regresión de R1; dos eventos del mismo `proyectoId` se serializan (no corren concurrentes), de `proyectoId` distintos sí. **Test dedicado de aislamiento de citas** (spec `knowledge-query`, "Dos turnos concurrentes no cruzan citas"): dos `casoId` distintos, cada uno con su doble de `createKnowledge`/`handleTurn` registrando labels distintos, se afirma que el resultado grabado de uno **no** contiene nodos del otro. Commit: `feat(root): agrega buildOnActivity que conecta webhooks con el nucleo (Hito 3, tarea 21)`.

22. **`src/main.ts`** — los 3 cambios exactos de §6.3, ninguno toca el camino de la TUI: **(a)** `startHarness()` devuelve `createKnowledge: (casoId) => KnowledgeAdapter` en vez de una instancia `knowledge` compartida; el call site de la TUI pasa a `buildOnSubmit(caso.id, memory, hooks, agents, undefined, createKnowledge(caso.id))` — la TUI sigue con una sola instancia para toda su corrida, pero ahora es suya, no compartida; **(b)** wiring de la segunda fuente: `board = createBoardAdapter(...)`, `onActivity = buildOnActivity({ db, memory, hooks, agents, board, queue: createKeyedQueue(), createKnowledge })`, `webhook = await startWebhookServer({ onEvent: onActivity, logEvent })` **fuera** del `try` de `startHarness()`, con su propio `try/catch` que loguea `webhook-arranque-fallido` y continúa con `webhook = undefined` si falla (p. ej. `EADDRINUSE`); **(c)** el `finally` cierra `await webhook.close()` (si `webhook !== undefined`, con su propio `try/catch`) **antes** de `db.close()` (ADR 10). Test primero: dado que `main.ts` no es importable en test (corre `bootstrapHarness()`/`openDatabase()`/`await` top-level, igual que en Hito 1/2), la cobertura de lógica ya está en `build-on-activity.test.ts` (tarea 21, sin tocar) y en `build-on-submit.test.ts`, que se corre **sin modificar** como red de regresión de R2 — si el wiring de `main.ts` rompiera algo del camino de la TUI, ese archivo lo detecta. Este paso deja `main.ts` consistente con contratos ya probados; su verificación de comportamiento real ocurre en la tarea 24. Hecho: `import "./core/config/env.js"` sigue siendo el primer import; `npm run typecheck` verde; `npm test` sin regresiones. Commit: `feat(root): conecta segunda fuente de turnos y cierre ordenado en main.ts (Hito 3, tarea 22)`.

23. **`src/core/logging/turn-logger.ts`** — **TDD exception**: solo se actualiza el module doc listando los eventos/consumidores nuevos de la tabla §9.2 (`webhook-recibido`, `webhook-firma-invalida`, `actividad-creada`, `tablero-actualizado`, etc.). El contrato de `logTurnEvent(correlationId, event, fields, deps)` **no cambia** — el diseño lo confirma explícitamente ("no gana código; a lo sumo una línea en su module doc"), corrigiendo a la propuesta que lo listaba como *Modified* con código. Hecho: `logTurnEvent` compila y sus tests existentes pasan sin tocarlos. Commit: `docs(core): documenta eventos de actividad y tablero en turn-logger (Hito 3, tarea 23)`.

### Verificación manual (tarea 24)

24. **Verificación manual end-to-end** (sin código de producción, cierre del hito — equivalente a la tarea 12 de Hito 2). Cubre el §12 completo del diseño:
    - **Preparación**: repo de prueba en GitHub con permisos de admin, PAT (fine-grained: Issues r/w, Pull requests r/w, Contents read), labels `necesita-revision`/`observaciones-pendientes`/`resuelto`/`aprobado` (opcional, mejora la demo), `.env` con `GITHUB_WEBHOOK_SECRET`/`GITHUB_TOKEN`/`WEBHOOK_PORT=8787`, Node 20+ (`node -v`, ADR 7).
    - **Exponer el endpoint**: `gh webhook forward --repo=<owner>/<repo> --events=pull_request,issue_comment --url=http://localhost:8787/webhooks/github --secret="$GITHUB_WEBHOOK_SECRET"` (recomendada — la firma se ejercita de verdad), o alternativa `smee.io` (canal + `npx smee-client`). Ninguna de las dos entra en CI (R8).
    - **Guion de 10 pasos** (§12.3): (1) `npm run dev` sin secreto → TUI normal, sin puerto abierto, `webhook-deshabilitado` en el log; (2) con secreto + forwarding → `webhook-escuchando`; (3) abrir un PR real → comentario del agente + label correcto + filas nuevas en `proyectos`/`actividades`/`casos`; (4) prompt en la TUI mientras corre el turno del PR → dos `casoId` distintos sin citas cruzadas (R1); (5) comentario que resuelve observaciones → `actividad-reusada`, label → `resuelto`, `assignee` = autor, sin intervención manual; (6) `curl` con firma inválida → `401`, `webhook-firma-invalida`, conteo de `actividades` sin cambio; (7) repetir paso 3 con `GITHUB_TOKEN` vacío → turno completa, estado persiste, `tablero-deshabilitado`, cero excepciones no capturadas; (8) PRs en dos repos distintos casi a la vez → aislamiento por clave de la cola; (9) Ctrl+C durante un turno de webhook → cierre ordenado en ~5s, sin `SQLITE_MISUSE`; (10) `PRAGMA journal_mode` → `wal`, archivos `-wal`/`-shm` presentes, `git status` limpio.
    - **Evidencia** para `docs/progreso/v1.2-bot-revision-prs/`: capturas del PR con comentario y label en cada estado, extracto de `data/harness.log` (grep por un `deliveryId` y por su `casoId` puente), salida de `npm test` y `npm run typecheck` en verde, `SELECT` de las tres tablas nuevas.
    - Commit: no aplica (evidencia va a `docs/progreso/v1.2-bot-revision-prs/` al cierre, según `AGENTS.md`).

## Después de la tarea 24

- Pasa al Reviewer (`sdd-verify` + `code-review`) contra este documento, los 3 specs y `design.md`, con especial atención a R10 (logging con dos espacios de id) y R4 (limitación de "sin diff" declarada en el prompt).
- Si aprueba: checklist de cierre de hito en `AGENTS.md`, tag `v1.2.0`, carpeta `docs/progreso/v1.2-bot-revision-prs/`.
- Si el Reviewer exige la letra literal del spec sobre `casoId` en R10, la corrección es **enmendar el spec**, no el diseño ni las tareas — ver §11, R10 del diseño.
