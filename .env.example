# Copy to .env and fill in your key. Loaded once at startup by src/core/config/env.ts
# — see that file for the ordering constraint (must load before the Claude Agent SDK is imported).
ANTHROPIC_API_KEY=
