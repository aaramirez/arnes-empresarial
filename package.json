{
  "name": "arnes-empresarial",
  "version": "0.1.0",
  "private": true,
  "description": "Harness empresarial de agentes de IA — MVP conversacional (Hito 1).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "dev": "tsx src/adapters/tui/main.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^4.0.0"
  }
}
