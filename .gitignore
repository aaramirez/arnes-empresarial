node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
Thumbs.db
*.db
# Archivos WAL/SHM de SQLite (modo WAL habilitado en Hito 3, tarea 18)
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
graphify-out/
.atl/

# Worktrees aislados del Developer con escritura delegada (Hito 5.1) —
# se crean y se destruyen en runtime, nunca deben versionarse.
.harness/

# Artefacto de Windows/Git Bash: a veces un proceso hijo (tsc/npm/vitest)
# crea un archivo literal "nul" en vez de escribir al dispositivo nulo real.
nul
